{"commands":["bash -lc 'set -euo pipefail; aws s3 cp s3://nwac-prod-artifacts/db/prod-verify-admin-users.sql /tmp/prod-verify-admin-users.sql --region ca-central-1; SECRET=$(aws secretsmanager get-secret-value --secret-id nwac-prod-db-credentials --query SecretString --output text --region ca-central-1); export SECRET; USER=$(python3 -c \"import json,os; print(json.loads(os.environ[\\\"SECRET\\\"])[\\\"username\\\"])\" ); PASS=$(python3 -c \"import json,os; print(json.loads(os.environ[\\\"SECRET\\\"])[\\\"password\\\"])\" ); HOST=nwac-prod-db.cluster-c3g4iamg8j38.ca-central-1.rds.amazonaws.com; DB=iset_intake; PORT=3306; command -v mysql >/dev/null 2>&1 || (sudo dnf install -y mariadb105 >/dev/null 2>&1 || sudo yum install -y mariadb >/dev/null 2>&1); MYSQL_PWD=$PASS mysql -h $HOST -u $USER -P $PORT $DB < /tmp/prod-verify-admin-users.sql'"]}
