# Test Environment Deployment Notes

Status: current TEST deployment notes. Prefer `deployment-quick-guide.md` for the shortest operator commands.
Last reviewed: 2026-09-02 after admin-only release `20260902-feedback-198-withdrawal-r1` completed and normal TEST admin routing was restored.

For the shortest operator commands, start with `docs/ops/deployments/deployment-quick-guide.md`.

## One-command deploy (recommended)

Run the PATH orchestrator from the WSL admin repo:

```bash
cd /home/bill/ISET/admin-dashboard
npm run path:deploy -- --env test --skip-data --release-id <release-id> --skip-qualification --yes
```

This recommended command is app/schema-only. Runtime configuration and allowlisted data promotion are separate scope; include `--dataset intake-release --workflow-id 21` only when Bill explicitly confirms that the TEST run should promote workflow authoring plus the global published intake runtime row.

The TEST app rollout is WSL-native. It builds/packages `/home/bill/ISET/admin-dashboard`, `/home/bill/ISET/ISET-intake`, and `/home/bill/ISET/shared`, uploads the artifacts with the WSL AWS CLI, then runs the in-place SSM update commands. Do not use stale `X:\ISET` or `/mnt/x/ISET` guidance for TEST.

Current TEST topology: TEST is cost-pruned to one steady-state `nwac-test-asg` app instance in `ca-central-1d` and one NAT gateway. The ALB remains on two public subnets. `path:deploy` discovers the healthy ASG host(s) at runtime, and target-group smoke should be read as "all registered targets are healthy"; today that normally means one admin target and one portal target.

Important: the deploy scripts package the current WSL working tree, not just the Git index. If you only want to release a subset of local edits, isolate unrelated local changes before running `path:deploy`.

Important coupling rule: do not assume `admin-only` just because the user-facing behavior is in the admin console. The admin backend stages sibling `../shared` during the TEST admin deploy, and some admin runtime paths also import sibling `../ISET-intake` modules from the deployed portal tree. If the changed code path touches either of those sibling locations, deploy the coupled surface as well instead of using `--skip-portal`. Fresh ASG replacement makes this coupling sharper: a new TEST app instance may not yet have `/opt/nwac/portal` dependencies, and admin notification startup can fail through `../shared/events/notificationDispatcher.js -> /opt/nwac/portal/sesMailer.js` with a missing `@aws-sdk/client-ses` unless the portal runtime has also been installed.

Important maintenance rule: TEST should rehearse PROD maintenance behavior. If a TEST deploy can restart the admin or portal app, make either surface unavailable, or expose transient `502 Bad Gateway` responses, set a scoped maintenance warning before the deploy or put the affected surface behind the ALB fixed-response maintenance page. TEST can remain less strict than PROD about approval flags, but it should not intentionally show raw gateway errors while down. TEST maintenance messages must use the user-facing name `Test and Training environment` and explicitly state that Production is not affected.

Important idle-protection rule: the TEST public portal must remain behind its portal-scoped ALB fixed-response protection page whenever active portal testing is not underway. A portal deploy or acceptance run may temporarily restore normal forwarding, but the operator must set the portal protection again before closing the testing thread. Do not finish an all-surface maintenance sequence by clearing both surfaces and leaving `nwac-public-test.awentech.ca` open; keep the TEST admin console independently forwarded. Use `node scripts/path-maintenance-fallback.js status|set --env test --surfaces portal`.

Important release-note rule: before any TEST app deploy with user-visible changes, create the newest release package in `docs/meta/next-release-notes-log.md` draft sections, not only the dated working-log entries. The first English and French package groups must describe the release being deployed and the generated `src/generated/publicReleaseNotes.js` must show that package first. If the generated release ID is current but the first package title is older, stop before `path:deploy`; the landing page will otherwise show stale release notes.

What it does:
- Verifies the TEST AWS identity/profile before doing anything
- Plans/applies canonical shared-schema migrations through SSM on a TEST app host
- Optionally promotes an allowlisted config dataset from DEV only when `--dataset` is explicitly included
- Runs WSL-native admin and portal TEST app deploy steps from `scripts/path-deploy.js`
- Verifies TEST health through the ALB target groups (`nwac-test-admin-tg`, `nwac-test-portal-tg`)
- Writes a release manifest under `tmp/path-deploy/test/`
- Current dependency-install safeguard: the TEST admin and portal deploy steps clear the deployed `node_modules` tree before running remote `npm ci/install`, use isolated `/tmp/npm-cache-*-deploy` caches, and start the PM2 process when it is missing instead of assuming an already-bootstrapped instance. This mirrors the existing PROD bootstrap posture and avoids stale-filesystem/cache and fresh-ASG-instance failures on rerun.

Current SES safety guard:
- Before the PROD-data migration rehearsal, TEST SES was checked in `ca-central-1` and was still sandboxed (`ProductionAccessEnabled=false`), but the TEST account has several verified recipient identities.
- The deployed TEST app env includes an explicit AWS access key for IAM user `SES_backend`, so SES safety cannot be inferred from the EC2 instance role alone.
- Inline IAM policy `DenySesSendDuringProdDataRehearsal` is now attached to both IAM user `SES_backend` and role `nwac-test-app-role`, denying `ses:Send*` on `*`. IAM simulation confirmed `explicitDeny` for `ses:SendEmail`, `ses:SendRawEmail`, templated send, and bulk send actions.
- DEV local `.env` now uses separate IAM user `SES_backend_dev` with SES `SendEmail` / `SendRawEmail` and scoped DEV Cognito permissions. Do not remove the TEST deny from `SES_backend` when DEV email delivery needs to work; rotate or update the DEV-specific credential instead.
- Leave this deny in place while TEST may contain copied PROD data. Remove it only deliberately when email delivery testing is required, then restore it before any further PROD-data rehearsal.

For a non-destructive preflight first:

```bash
npm run path:deploy:plan -- --env test --skip-data
```

## Recent deploy evidence

- 2026-09-02 UTC: Admin-only feedback `#198` release `20260902-feedback-198-withdrawal-r1` deployed clean detached admin commit `d96c8894813f71174472425995ccb2834dccb23d`; portal `9bd0f3bc75076dc9793eaeb490d645144c090a7f` and shared `f81519d74ab0553b19713cff33961386dd0887da` were recorded but not deployed. Schema, data, TEST refresh, portal, shared runtime, and PROD were explicitly outside scope. The exact candidate passed the production build, 103 frontend suites / 663 tests, 69 backend/tooling suites / 817 tests, quiet lint, privacy route-scope smoke, the complete assessment-workflow browser suite, and targeted withdrawal route/policy tests. Immutable artifact `s3://nwac-test-artifacts/releases/20260902-feedback-198-withdrawal-r1/admin/admin-1f8c61286cac8b39d1ca89f510656b0fded023f2b16b19f259f677b207fad818.zip` has SHA-256 `1f8c61286cac8b39d1ca89f510656b0fded023f2b16b19f259f677b207fad818`; the successful/`UNQUALIFIED` manifest is `/tmp/path-feedback-198-release.b9Uabf/admin-dashboard/tmp/path-deploy/test/20260902-feedback-198-withdrawal-r1--2026-09-02T22-34-45-670Z.json`. A later targeted TEST fixture attempt is explicitly excluded from acceptance evidence: it stopped at the EI-verification precondition before reaching the withdrawal path. Its schema preflight completed, all fixture cleanup counters and remote residue checks returned zero, and the temporary runner transfer was removed. No replacement qualification gate or `GO` is claimed. Final target smoke passed on `i-0b91c2f03fbd20f0d` for admin `:5001` and portal `:5000`; admin forwarding is normal and the public portal remains behind idle protection.

- 2026-08-26 UTC: Clean release `20260826-signing-lineage-r2-copyfix` proved the narrow S3 compatibility-promotion correction through the supported TEST orchestrator. The first attempt stopped before application cutover after prepare SSM command `da153d17-6815-4e68-8143-0fac19997c29` timed out; read-only console evidence showed existing instance `i-0db0637d66d66de8c` had exhausted its root disk, causing prepare and automatic recovery to fail and preventing normal SSM/cloud-init work. Under the active fallback, the instance was terminated with ASG desired capacity preserved. Replacement `i-0b91c2f03fbd20f0d` booted from the unchanged stable aliases, passed local and target baseline health, and had 2.9 GB free before retry. The exact clean candidate—admin `b274d61a27c8ead2f90085c7b8b5dda717d7edd9`, portal `9bd0f3bc75076dc9793eaeb490d645144c090a7f`, shared `f81519d74ab0553b19713cff33961386dd0887da`—then completed production builds, `1,405` admin tests, `394` portal tests, lint/privacy checks, both bounded migration revalidations, atomic prepare/cutover/postflight, target smoke, and alias promotion with preserved SHA metadata and no tagging API. Successful manifest: `/tmp/path-r2-copyfix.4rKtlh/admin-dashboard/tmp/path-deploy/test/20260826-signing-lineage-r2-copyfix--2026-08-26T04-31-50-268Z.json`, SHA-256 `94b5bca7c69f8d89dc6c0342a2f3a3ec4ad6a9b28daa3b072d9a0be2e8054d6a`, status `successful`/`UNQUALIFIED`. Admin is forwarded normally and the public portal is back behind idle protection. Operational follow-up: the completed run left the fresh host at 87% root use (about 1.1 GB free), with retained transaction backups/caches the leading suspected contributor, and the remote transaction state remained `cutover-complete` although the manifest recorded state `accepted`. Do not run another atomic TEST deployment on that host until disk retention is addressed or the host is replaced; do not reinterpret either issue as permission to use the retired qualification harness.

- 2026-08-24 UTC: Stacked release `20260824-path-maintenance-r1` deployed the current dirty admin and portal working trees plus canonical document-lifecycle migration `20260824_0001_add_document_lifecycle_audit.sql` to TEST; shared remained unchanged at `f81519d74ab0553b19713cff33961386dd0887da`. The initial guarded migration attempt failed before creating either lifecycle table because MySQL rejected a check constraint that referenced nullable foreign-key column `document_id` while that foreign key used `ON DELETE SET NULL`. Under the active all-surface fallback, metadata-only SSM command `467d2810-38dd-402f-bde2-95b3b175d192` proved zero partial lifecycle objects, the exact error and failed checksum were retained in `iset_migration`, and the migration was corrected by removing only that incompatible identity check. The corrected checksum `dd9253890e6b398d28706345a1c9cd3a3c902cef65d1b46d24eaa6f04f251c3a` applied successfully; DDL command `38ed04e1-4473-41b0-bc6a-432fe9d7a227` confirmed both tables and their foreign keys/state checks, and ledger command `63517784-4d4a-474f-9330-c4d41abfe253` confirmed the current checksum successful while retaining the obsolete failed attempt as audit history. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260824-175911.zip` installed through SSM command `1ad87403-76f4-45fe-b240-e70b8dff334a`; portal artifact `s3://nwac-test-artifacts/portal/portal-20260824-180103.zip` installed through `9376b4b6-483e-4779-81ad-87f0f8b3519f`. Normal-routing smoke found both target groups healthy on `i-0db0637d66d66de8c`. Full read-only runtime postflight passed exact release/tree provenance, local readiness, runtime configuration, PM2 status, zero canonical migration backlog/current-checksum failures, zero active announcements or delivery/provider blockers, and schema-safe live metrics under commands `dd95fe29-5844-4f4e-845b-60b7649b69b0`, `3a818d07-42dc-4f1d-8913-aebb587f7fec`, and `322ec577-8450-4b6a-a667-0dfcb3d20881`. Final state leaves admin forwarding normally and restores the public portal's portal-scoped fixed-response idle protection. No TEST database refresh, data/workflow/runtime promotion, external-provider call, Terraform action, or PROD mutation ran. Because the deployment intentionally used the retired-harness path, its source trees are faithfully recorded as dirty and the manifest is operational evidence rather than a qualification `GO`: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260824-path-maintenance-r1--2026-08-24T21-55-14-167Z.json`.

- 2026-08-01 UTC: Release `20260801-returned-assessment-edit` deployed exact admin `6bd87e02f7d7456bfa5b5441884597a5b3b9efc1`, unchanged portal `4ac580deb5dbacc00811748fe40623c7eaa08f69`, and unchanged shared `e8dc303d8ecc057e52509b00854106f230823d96` to TEST after all 16 DEV checks returned `GO` under evidence `0fe242d34e20bd7a32dd127df542ac9fa24fd4ef3134ba004333e0d221f8a76e`. The admin artifact was `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260801-112624.zip`; the unchanged portal was subsequently deployed as `s3://nwac-test-artifacts/portal/portal-20260801-113711.zip` because the first acceptance run correctly detected that its prior release stamp named an older admin source fingerprint. The complete deployed acceptance gate was rerun from the beginning and all 11 checks passed under evidence `f51d72c13f3c0c9c0de66029460f36de5b2d8405547074454351c47f0078a9a5`, including the targeted dual-role returned-assessment edit/save journey, exact provenance, rollback readiness, target health, role workflows, intake completion, applicant scope, live privacy denials, payment rollback, maintenance cleanup, and source stability. Both target groups ended healthy; admin forwarding remained normal and `nwac-public-test.awentech.ca` was returned to its portal-scoped fixed-response protection page. No schema, data, workflow, runtime-config, database refresh, provider or PROD operation ran. Manifests: `/home/bill/ISET/release-20260801-returned-assessment-edit/admin-dashboard/tmp/path-deploy/test/20260801-returned-assessment-edit--2026-08-01T15-24-03-205Z.json` and `/home/bill/ISET/release-20260801-returned-assessment-edit/admin-dashboard/tmp/path-deploy/test/20260801-returned-assessment-edit--2026-08-01T15-36-47-620Z.json`.

- 2026-08-01 UTC: Release `20260801-assessment-document-lineage-r2` deployed admin `aa2817711154f07ba051dc0af2d520383e066a12` with the unchanged portal/shared sources to TEST after all 16 DEV gates returned `GO`. Deployed acceptance passed all 11 required gates under evidence `fc1cb076aaa608e72134f36b58d4a41819f28e8b64c0b202393700af76d7dfc8`, including exact provenance, rollback readiness, target health, assessment workflows, privacy denials, payment rollback, source stability, and zero fixture residue. Both target groups ended healthy; after testing, admin forwarding remained normal and `nwac-public-test.awentech.ca` was returned to its portal-scoped fixed-response protection page. No schema, data, workflow, runtime-config, database refresh, provider, or PROD operation ran during TEST deployment. Manifest: `/home/bill/ISET/release-20260801-assessment-document-lineage-r2/admin-dashboard/tmp/path-deploy/test/20260801-assessment-document-lineage-r2--2026-08-01T10-55-31-735Z.json`.

- 2026-07-31/08-01 UTC: Release `20260731-assessment-document-lineage` deployed clean admin `80482e4`, unchanged portal `4ac580d`, and unchanged shared `e8dc303` to TEST with no schema, data, workflow, runtime-config, database-refresh, email, AI, Sage/Intacct, or PROD operation. DEV evidence `2a61dd5a42cc0c6778fdde21e4a4a7cb4884a4b523a74d2e11dd4218f4f633e2` passed all 16 mandatory checks, including rollback-only real-MySQL repeat-application document isolation with zero residue. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260731-210125.zip` retained `admin-dashboard-20260730-171853.zip`; manifest: `/home/bill/ISET/release-20260731-assessment-document-lineage/admin-dashboard/tmp/path-deploy/test/20260731-assessment-document-lineage--2026-08-01T00-59-05-743Z.json`. Initial deployed acceptance correctly returned NO-GO because the unchanged portal provenance still named the preceding admin source fingerprint; no functional gate failed. The unchanged portal was therefore redeployed solely to synchronize exact-candidate provenance: artifact `s3://nwac-test-artifacts/portal/portal-20260731-211220.zip`, retained `portal-20260730-172032.zip`, manifest `/home/bill/ISET/release-20260731-assessment-document-lineage/admin-dashboard/tmp/path-deploy/test/20260731-assessment-document-lineage--2026-08-01T01-11-56-992Z.json`. Final TEST evidence `305dba4bdad6b8962184fe8f991c8d8b385a03053c82fe2bea2ad00ec197e3ed` passed all 11 deployed gates: exact provenance, rollback readiness, target health, runtime postflight, staff assessment workflows, intake completion, applicant scope, live privacy denials, rollback-only payments, maintenance cleanup, and source stability. Both target groups ended healthy on `i-0db0637d66d66de8c`; `service.announcement` had zero rows; admin forwarding was normal; and `nwac-public-test.awentech.ca` was returned to its portal-scoped maintenance page.

- 2026-07-28 UTC: Release `20260728-regional-snapshot-data-quality-funding` deployed exact admin `a252416`, portal `4ac580d`, and shared `e8dc303` to TEST as admin + portal only, with zero schema/data/workflow/runtime/provider scope. DEV evidence `5f44fb97d4435dd67839221bed02d02fc284ce9e01950402f7c30d1b13197f55` passed all 16 checks. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260728-182310.zip` retained `admin-dashboard-20260727-190220.zip`; portal artifact `s3://nwac-test-artifacts/portal/portal-20260728-182451.zip` retained `portal-20260727-190359.zip`. Both target groups returned healthy under normal forwarding, and deployed acceptance evidence `cb781fce6bb0d364fd9c664e5c2c1cc31aba1b5a48c9c49e31d67a44e5283075` passed provenance, rollback, runtime, role, intake, applicant-scope, privacy-denial, payment-rollback, maintenance, and zero-residue checks. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260728-regional-snapshot-data-quality-funding--2026-07-28T22-20-26-148Z.json`.

- 2026-07-27 UTC: Release `20260727-regional-snapshot-financial-overview` deployed exact admin `a0455e1`, portal `4b8135d`, and shared `e8dc303` to TEST and passed the complete deployed acceptance gate under evidence `aa8e3d6710b829905fae4356f47d4034ad8013ce84187cfd122c36d92dd56d8f`. During the later PROD bootstrap recovery, fresh TEST rehearsal found the bootstrap still depended on NodeSource and stale 2025 artifact defaults. The corrected bootstrap now installs SHA-256-verified Node `20.20.2`, uses `bootstrap/admin-dashboard-latest.zip` and `bootstrap/portal-latest.zip`, and restores the admin-bundled shared runtime. Refresh `a14cf57b-d4eb-45b8-85fd-a26901412f77` created `i-0db0637d66d66de8c`; SSM command `1484e3dc-b7c0-487d-a1bb-3dcc235bd7c5` proved exact July 27 provenance, both local readiness checks, Node 20, and both PM2 services online with zero restarts. Final TEST smoke reported both target groups healthy and maintenance routing/warning were cleared.

- 2026-07-20 UTC: Release `20260719-rm-cross-region-reassignment-r3` deployed clean admin `c33c376`, portal `4b8135d`, and shared `e8dc303` to TEST with no schema, data, workflow, runtime-config, database refresh, or provider operation. `r3` differs from previously accepted `r2` only by corrected landing-page release-note fallback content. DEV evidence `a1066eeabf7824d8bb438cfd8753bdd5095fc90442015d47eb1c86a7739899f4` and deployed TEST evidence `0ba9213e745ed2acd815703a3209f1b449a6b27e5c67dc2e55b06fc5d1eee471` returned `GO`; all target health, runtime, role, intake, applicant ownership, privacy denial, rollback, cleanup, and source-stability checks passed. Targeted SSM command `79b9287a-46b7-4283-999a-f12fcbde0b5c` executed the deployed policy and confirmed active cross-region RM targets are allowed while inactive targets and Coordinator assignment are denied. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260719-rm-cross-region-reassignment-r3--2026-07-20T04-03-37-669Z.json`.

- 2026-07-13/14 UTC: Committed admin `f91762a`, portal `e445d20`, and shared `e8dc303` were qualified and deployed to TEST as release `20260714-client-monthly-attendance-prod`; only workflow `54` authoring configuration was synced (3 steps, 18 components, 2 routes, 2 route options). DEV evidence `ad5c54ac4c00517e5b42776e69abe12d2256ade18c54809c09e89e5a28301a1c` passed all 16 mandatory checks. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260713-213143.zip` retained `admin-dashboard-20260713-203301.zip`; portal artifact `s3://nwac-test-artifacts/portal/portal-20260713-213326.zip` retained `portal-20260713-203443.zip`. Deployed TEST evidence `1d17381034197d42d91f0fa135e972ac4ddce9429e5011f077b9da8c1efb3445` passed all 11 mandatory checks with normal forwarding and zero cleanup residue. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260714-client-monthly-attendance-prod--2026-07-14T01-27-43-440Z.json`.

- 2026-07-13: Admin-only TEST recovery release `20260713-admin-schema-readiness-hotfix` restored authenticated admin APIs after confirming TEST had the same canonical `staff_profiles` shape as PROD and therefore the same R4b `last_seen_at` assertion failure. The run followed the TEST-specific warning/fallback/in-place SSM path and used `npm run build:test`; schema, data, TEST DB refresh, shared, portal, runtime configuration, and PROD were excluded. Admission passed 338 tests, quiet lint, and 72 privacy-route checks. Artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260713-070255.zip` installed on `i-052566d75e0214d00` through SSM command `7d99ab9f-29fb-4b08-b08a-c0daa21c9d48`. Final target-group smoke was healthy; source/readiness/log command `2f93ac7b-3475-4ac7-a480-36c2052e7ec7` confirmed release id, `buildTarget=test`, the shared runtime-column contract, local readiness, and no recent schema errors; SQL command `277d928c-aa97-429f-863b-9acc4c5191a8` proved the exact five-column query. Fallback returned to normal forwarding and maintenance command `180d5a8b-d310-4e08-9169-5d17f47580fe` confirmed zero announcement rows. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260713-admin-schema-readiness-hotfix--2026-07-13T11-00-34-994Z.json`.

- 2026-07-12: Coordinated engineering-audit release `20260712-engineering-audit-test` deployed shared runtime into both app packages, admin, portal, and five canonical migrations with exact checksums. Scope excluded TEST DB refresh, allowlisted data/workflow/runtime promotion, real Finance email/Intacct calls, historical repair, Terraform, and PROD. Preflight found `finance/email.routing.enabled=true`; Bill explicitly approved returning only that flag to `false`, preserving all 14 configured regions and their hash. Main manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260712-engineering-audit-test--2026-07-12T13-18-30-212Z.json`; admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260712-092413.zip`; initial portal artifact `s3://nwac-test-artifacts/portal/portal-20260712-092552.zip`.
- The initial rollout exposed a portal readiness typo: `/readyz` checked `input_json_state.payload` while canonical TEST schema uses `input_payload`. Portal commit `efabf21` fixed the contract and portal-only manifest `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260712-engineering-audit-test--2026-07-12T13-31-47-857Z.json` deployed artifact `s3://nwac-test-artifacts/portal/portal-20260712-093213.zip`. A final provenance fix in clean pushed commit `357e2ad` excluded generated build output from the dirty marker; portal-only manifest `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260712-engineering-audit-test--2026-07-12T13-55-55-671Z.json` deployed `s3://nwac-test-artifacts/portal/portal-20260712-095621.zip`. SSM command `6dc86fdc-ac81-48ea-a0d4-86401713399f` confirmed `gitDirty=false`, commit `357e2ad9`, release ID, and portal readiness.
- Acceptance passed: intake completion/retry and cleanup command `e0581ff5-bc06-4c8f-8d30-8b249aeb52bb`; rollback-only payment smoke `f0610331-bf50-4657-a7eb-dda97374dd18`; controlled real-MySQL import/delivery/allocation/AI/Hub acceptance `ca8bb235-1747-467d-b19a-cbc5e19b54d4`; exact migration/config/backlog postflight `41c6bade-58ac-409e-9576-5c0c703cebe6`; final app readiness `3ec21463-88e4-42b4-9aa8-a9219908cc72`. Focused UI/provider suites passed 20 tests, the Intacct local drift guard passed 18/18, all controlled fixtures cleaned to zero, the preserved historical packet `29` was unchanged, both target groups ended healthy on `i-052566d75e0214d00`, all ALB rules forward normally, `service.announcement` has zero rows, finance routing is false, and PROD was untouched.

- 2026-07-08: Full admin + portal TEST release `20260708-critical-feedback-reporting-test` deployed the manual backload approval-date fix, Critical bug/CR System Administrator notification path, and Regional Snapshot `Approved Applications` label cleanup. Scope was app/schema-only with `--skip-data --skip-smoke`; one canonical migration ran successfully (`20260708_0001_seed_admin_feedback_critical_notification.sql`), no TEST DB refresh, runtime config promotion, allowlisted data, Terraform, arbitrary SQL repair, or PROD change was run. Sequence used the TEST maintenance rehearsal path: all-surface `Test and Training environment` warning, wait, all-surface ALB fallback, deploy, fallback clear, normal-routing smoke, deployed-source marker check, warning clear, and maintenance-state cleanup. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260708-200052.zip` installed on TEST host `i-052566d75e0214d00` through SSM command `44c6fbe0-7ee4-40ad-8565-ead448e7112d`; portal artifact `s3://nwac-test-artifacts/portal/portal-20260708-200424.zip` installed on the same host through SSM command `181d3224-90d5-4302-b526-f106a4ee1ac7`. Final normal-routing smoke reported `nwac-test-admin-tg` healthy on `:5001` and `nwac-test-portal-tg` healthy on `:5000`. SQL-over-SSM command `303ac304-9527-45dd-bf81-77ec3270c55c` confirmed the migration was recorded successful at `2026-07-09 00:00:47` UTC and exactly one enabled `admin_feedback_critical` System Administrator email setting exists. Deployed-source SSM command `d3c6bce6-04d5-4ccf-86a2-92f5ee536fa3` confirmed release id in admin/portal bundles, `manualBackloadReviewedAtValue`, `emitAdminFeedbackCriticalNotification`, shared `admin_feedback_critical`, the `Approved Applications` bundle label, and `nwac-admin`/`nwac-portal` PM2 processes. Fallback status returned to normal forwarding and SQL-over-SSM command `f4ed1f76-a555-49ea-8c22-00f7172b0fd0` confirmed `service_announcement_rows = 0`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260708-critical-feedback-reporting-test--2026-07-09T00-00-05-703Z.json`.

- 2026-07-08: Full admin + portal TEST release `20260708-admin-user-ei-notification-fix` deployed the staff setup invite hardening, post-submission EI correction fix, and shared applicant-name notification resolver. Scope was app/schema-only with `--skip-data`; schema pending count was `0`, no TEST DB refresh, runtime config, allowlisted data, Terraform, or PROD data change was run. Sequence used the TEST maintenance rehearsal path: all-surface `Test and Training environment` warning, wait, all-surface ALB fallback, deploy, fallback clear, normal-routing smoke, deployed-source marker check, warning clear, and maintenance-state cleanup. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260708-055531.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260708-055906.zip` installed on TEST host `i-052566d75e0214d00`; final smoke reported `nwac-test-admin-tg` healthy on `:5001` and `nwac-test-portal-tg` healthy on `:5000`. Deployed-source SSM command `87e733cd-c2cc-44c6-9883-94326353c8a4` confirmed release id `20260708-admin-user-ei-notification-fix`, `MessageAction: 'SUPPRESS'`, `ei_eligibility_dependency_blocked`, and shared `GENERIC_APPLICANT_NAME_VALUES` markers in `/opt/nwac/shared` and the staged admin shared copy. Fallback status returned to normal forwarding and SQL-over-SSM command `cb6dde94-2bf5-482b-ac71-a2b7064c821a` confirmed `service_announcement_rows = 0`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260708-admin-user-ei-notification-fix--2026-07-08T09-55-04-552Z.json`.

- 2026-07-05: Admin + portal TEST recovery/release `20260705-two-step-review-test-notification-fix` fixed two-step review notification routing and hardened the TEST deploy path. Scope: app-only, no schema, data, runtime-config promotion, Terraform, PROD change, or feedback-log mutation. Initial admin redeploy under fallback stalled on old host `i-0a8be782ed8604211` (`c073d95e-919f-4127-bbbe-0ff95cb0d1ce`) and left the admin target unhealthy; Codex kept ALB fallback active, terminated the old ASG instance with desired capacity preserved (`13067ccb-49e8-641c-165c-9793ac41cab2`), and recovered on replacement host `i-052566d75e0214d00`. Fresh-host evidence showed admin needed the coupled portal runtime because shared notification code imports the portal SES mailer. Portal artifact `s3://nwac-test-artifacts/portal/portal-20260705-111455.zip` installed the portal runtime; final admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260705-112410.zip` installed through SSM command `46b9775b-08a7-4b25-9fae-2a38a7f6c9f3`. Normal-routing target smoke reported admin `:5001` and portal `:5000` healthy on `i-052566d75e0214d00`; deployed-source command `548c6282-f419-45a7-85c8-70efabc34e11` confirmed the final intervention decision-status marker and release marker; full live TEST two-step smoke `8428df04-2235-46e6-9533-7187a7260ac3` passed with synthetic cleanup counts at zero. Fallback status returned to normal forwarding and SQL-over-SSM command `da868131-5108-46e0-8e79-20855faf821f` confirmed `service_announcement_rows = 0`. Manifests include `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260705-two-step-review-test-notification-fix--2026-07-05T15-24-09-990Z.json`.

- 2026-07-05: Full admin + portal TEST release `20260705-secure-message-batch` deployed the secure-message withdrawal and applicant-name notification fixes with `--skip-schema --skip-data --skip-shared --skip-smoke`; no schema, data, runtime-config, shared, Terraform, or PROD change was run. Sequence used a Test and Training all-surface warning, wait, admin+portal ALB fallback, deploy, fallback clear, normal-routing smoke, warning clear, deployed-source check, and maintenance-state checks. TEST AWS identity was `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260705-091238.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260705-091716.zip` installed on single TEST host `i-0a8be782ed8604211` through SSM commands `5aad611d-a69e-41b0-b965-5b5522cfd85d` and `f736a730-a903-4842-82d4-001750bb2433`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `:5001` and `nwac-test-portal-tg` healthy on `:5000`. Deployed-source SSM command `ce4cbac4-f71b-46ac-b7d7-db16774efdb0` confirmed release id and the admin withdrawal/recipient-confirmation plus portal applicant-name markers. Fallback status returned to normal forwarding and SQL-over-SSM command `0a6e957f-3248-4f28-8ecb-33810bf6afc9` confirmed `service_announcement_rows = 0`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260705-secure-message-batch--2026-07-05T13-12-38-829Z.json`.

- 2026-07-05: Admin-only TEST release `20260705-two-step-review-prevention-test` deployed the two-step review prevention fixes with `--skip-schema --skip-data --skip-portal --skip-shared`; no schema, data, portal, shared, Terraform, runtime-config promotion, or PROD change was run. Sequence used a Test and Training admin warning, admin ALB fallback, deploy, fallback clear, normal-routing admin smoke, warning clear, deployed-source check, and maintenance-state checks. TEST AWS identity was `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260705-073634.zip` installed on single TEST host `i-0a8be782ed8604211` through SSM command `ef03cd15-af30-41fa-a818-bd8275ad0226`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`. Deployed-source SSM command `1eb9b59e-6bc0-485f-b391-555d27cd8990` confirmed release id `20260705-two-step-review-prevention-test`, the generated intervention assessment PDF `interventionIds` path, and the proposal submitted-timestamp preservation marker `ELSE iset_intervention_proposal.submitted_at`. Fallback status returned to normal forwarding and SQL-over-SSM command `7ae38aa8-f14c-4c4c-806c-ab6896263f7e` confirmed `service_announcement_rows = 0`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260705-two-step-review-prevention-test--2026-07-05T11-36-34-263Z.json`.
- 2026-06-26: Admin-only TEST release `20260626-rm-two-step-role-matrix-test` used the runbook sequence `admin warning -> wait -> admin ALB fallback -> deploy -> clear fallback -> normal-routing admin target smoke -> deployed-source/live-route checks -> clear warning`. Flags: `--skip-schema --skip-data --skip-portal --skip-shared --release-id 20260626-rm-two-step-role-matrix-test`; TEST DB refresh was not requested, schema/data/runtime/config promotion was skipped, and PROD was not affected. TEST AWS identity was `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260626-182057.zip` installed on single TEST host `i-0a8be782ed8604211`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`. Deployed-source SSM command `40e4740e-ac1f-4527-bdbd-d72508ee9db2` confirmed release id `20260626-rm-two-step-role-matrix-test`, current public release notes, the role matrix allowing only ISET Coordinator/Regional Manager workflow starts across application assessment, intervention proposal, and intervention revision, stale `Case Manager` denial, and the deployed intervention route preflight guards. Real TEST Cognito role hydration SSM command `1c5b14b3-11d6-4b9b-a5f5-4a1822576fec` confirmed dedicated TEST staff users for ISET Coordinator, Regional Manager, NWAC Administrator, and System Administrator. Live deployed API checks then verified RM application-assessment submit, RM new-intervention submit, and RM intervention-revision submit create the correct `rm_review` workflows; NWAC/System Administrator submit-start attempts return `403 review_workflow_transition_forbidden`; revision submit generated submitted v2 and redline v2 assessment PDFs. Disposable verification cases, application locks, documents, workflow rows, local DB rows, and uploaded TEST S3 objects were removed.
- 2026-06-24: Admin-only TEST release `20260624-rm-draft-edit-hotfix` deployed from isolated worktree `/home/bill/ISET/admin-dashboard-rm-draft-edit-hotfix` so unrelated dirty files in the main checkout were not packaged. Scope: admin only, `--skip-schema --skip-data --skip-portal`, prebuilt with `PATH_DEPLOY_ENV=test PATH_RELEASE_ID=20260624-rm-draft-edit-hotfix npm run build:test`, then deployed with `--skip-build --skip-smoke`. Sequence used admin warning, wait, admin ALB fallback, deploy attempt, SSM recovery, fallback clear, normal-routing smoke, and warning clear. First TEST admin SSM install command `7f2a3784-17d9-4b4f-857c-4b4786395f1b` stayed silent and was cancelled under the active fallback; the admin target temporarily reported `Target.Timeout` while the cancelled child work settled. SSM echo command `ae576b31-6cb1-458b-bca4-2062cce18605` later succeeded, target health returned healthy, and deployed-source check `214e6453-979f-47f8-9a83-d6ce0347a0a8` confirmed release id `20260624-rm-draft-edit-hotfix`, `buildTarget: test`, the public release-notes entry, and `canEditDraftAssessmentAsRegionalManager` in deployed `CoordinatorAssessmentWidget.js`. Final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`; ALB fallback status returned to normal forwarding for admin and portal.
- 2026-06-22: Full admin + portal TEST release `20260622-path-bugfix-patch` used the runbook sequence `warning -> wait -> ALB fallback -> deploy --skip-smoke -> clear fallback -> normal-routing smoke -> deployed-source check -> clear warning`. Flags: `--skip-data --release-id 20260622-path-bugfix-patch`; TEST DB refresh was not requested, schema pending count was `0`, and no dataset/runtime/config/intake promotion ran. TEST AWS identity was `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`. The first admin SSM install command `4a6e0432-e6b8-4f3f-8d3c-66959c29e8a7` stalled without output under the fallback and was cancelled; SSM echo check `550f18fe-6888-4892-9374-28c10cf84696` then passed and the retry completed. Final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`. Deployed-source SSM check `b87d32f3-f8b7-4cec-97b7-dabb269610b8` confirmed the admin/portal signing idempotency guard, admin terminal-reminder guard, and release id in both bundles. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260622-path-bugfix-patch--2026-06-23T00-08-36-902Z.json`.
- 2026-06-20: Full admin + portal TEST release `20260620-rm-two-step-review-rollout` used the runbook sequence `warning -> wait -> ALB fallback -> deploy --skip-smoke -> clear fallback -> normal-routing smoke -> activation SQL -> clear warning`. Flags: `--skip-data --release-id 20260620-rm-two-step-review-rollout`; TEST DB refresh was not requested, schema pending count was `4`, and no dataset/runtime/intake data promotion ran. TEST AWS identity was `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260620-100326.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260620-101127.zip` installed on single TEST host `i-0a8be782ed8604211`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`. One-off SQL `sql/ops/20260620_rm_two_step_review_activation.sql` enabled `feature_flags/workflow.two_step_rm_review.enabled` for application assessments, intervention proposals, and intervention revisions and verified the two-step notification settings. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260620-rm-two-step-review-rollout--2026-06-20T14-01-37-533Z.json`.
- 2026-06-18: Full admin + portal TEST release `20260618-test-financial-overview-editable` used the runbook sequence `warning -> wait -> ALB fallback -> deploy --skip-smoke -> clear fallback -> normal-routing smoke -> clear warning`. Flags: `--skip-data --release-id 20260618-test-financial-overview-editable`; TEST DB refresh was not requested, schema pending count was `0`, and no dataset/runtime/config/intake data promotion ran. TEST AWS identity was `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260618-164719.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260618-165540.zip` installed on the cost-pruned single TEST app host `i-0a8be782ed8604211`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`. ALB fallback returned to normal forwarding for both hostnames and SQL-over-SSM reported `service_announcement_rows = 0`. Deployed-source SSM check `e70de8bb-6d78-4742-9a70-3a182c481fe7` confirmed `fundingOverviewMode` in `/opt/nwac/admin-dashboard/isetadminserver.js`, `finalizeFundingOverviewSigningSubmission` in `/opt/nwac/portal/server.js`, shared `buildFinancialOverviewInitialValues`, shared `FINANCIAL_OVERVIEW_SUBMISSION_KEYS`, intake-style `per month` field copy in `/opt/nwac/shared/financialOverview.js`, and release id `20260618-test-financial-overview-editable` in both admin and portal build info. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260618-test-financial-overview-editable--2026-06-18T20-46-46-070Z.json`.
- 2026-06-13: Corrective admin-only TEST redeploy for release notes on release `20260612-212548` used the runbook sequence `admin warning -> wait -> admin ALB fallback -> admin-only deploy -> clear fallback -> normal-routing admin smoke -> clear warning`. Flags: `--skip-schema --skip-data --skip-portal --skip-shared --release-id 20260612-212548`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260612-210450.zip` installed on the cost-pruned single TEST app host `i-0a8be782ed8604211`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`; maintenance fallback returned to normal forwarding and `service.announcement` rows returned to 0. Deployed bundle check on `/opt/nwac/admin-dashboard/build/static/js/main.95d54324.js` confirmed `Release 20260612-212548` appears before `Release 20260610-prod-modify-component-editor` and includes `System Administrators can reopen a closed action plan`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260612-212548--2026-06-13T01-04-50-045Z.json`.
- 2026-06-12: Full admin + portal TEST release `20260612-212548` used the runbook sequence `warning -> wait -> ALB fallback -> deploy -> clear fallback -> normal-routing smoke -> clear warning`. Flags: `--dataset intake-release --workflow-id 21`; TEST DB refresh was not requested and schema pending count was 0. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260612-173320.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260612-173654.zip` installed on the cost-pruned single TEST app host `i-0a8be782ed8604211`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260612-212548--2026-06-12T21-32-40-718Z.json`.
- 2026-06-10: Admin-only TEST release `20260610-test-application-workspace-review` used the runbook sequence `warning -> wait -> ALB fallback -> admin-only deploy -> clear fallback -> normal-routing smoke -> clear warning`. Flags: `--skip-schema --skip-data --skip-portal --skip-shared`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260610-144955.zip` installed on the cost-pruned single TEST app host `i-0a8be782ed8604211`; final normal-routing smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260610-test-application-workspace-review--2026-06-10T18-49-55-076Z.json`.
- 2026-06-08 post-prune verification: `npm run path:deploy:smoke -- --env test --json` reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`. This is the expected one-host TEST shape.
- 2026-06-08 topology note: after TEST cost-pruning, current target-group smoke is expected to show one registered admin target and one registered portal target when the ASG is at its normal size.
- 2026-06-07: Admin-only TEST release `20260607-test-tutorials-training-shorts` deployed from isolated worktree `/home/bill/ISET/admin-dashboard-test-deploy-tutorials-20260607` so unrelated dirty files in the main checkout were not packaged. Sequence: `warning -> wait -> ALB fallback -> admin-only deploy -> clear fallback -> normal-routing smoke -> clear warning`. Flags: `--skip-schema --skip-data --skip-portal --skip-shared`. Normal-routing smoke after fallback clear reported both `nwac-test-admin-tg` targets healthy on port `5001` under the pre-prune two-instance TEST shape. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260607-test-tutorials-training-shorts--2026-06-07T16-54-17-110Z.json`.

If you only need the legacy component rollout primitives:

Do not call the legacy PowerShell component deploy scripts directly from WSL. Windows `npm.cmd` is not reliable from `\\wsl.localhost\...`, and the supported TEST component path is now the Node orchestrator with `--skip-admin` / `--skip-portal` flags as needed.

Use that split form when you have already determined the coupling explicitly:
- admin-only is acceptable only when the changed runtime path stays inside `admin-dashboard` plus any sibling `shared` code staged by the admin artifact
- include the portal deploy whenever the admin runtime path imports `../ISET-intake/*` modules on the server

For a planned admin-only TEST deploy that may briefly interrupt the admin console, use:

```bash
cd /home/bill/ISET/admin-dashboard
npm run path:maintenance -- set --env test --surfaces admin --start-in 5m --expected-duration 5m --title "Test and Training maintenance" --message "The Test and Training environment is temporarily unavailable for maintenance. Production is not affected."
npm run path:deploy -- --env test --skip-schema --skip-data --skip-portal --skip-shared --release-id <release-id> --skip-qualification --yes
npm run path:maintenance -- clear --env test --surfaces admin
```

Recommended TEST message text: `The Test and Training environment is temporarily unavailable for maintenance. Production is not affected.`

## Feature-Flagged Portal Runtime Changes

For portal-only changes guarded by a runtime flag, use a portal-only app rollout first, then flip the TEST runtime row after the deploy is healthy.

Deploy the portal code:

```bash
cd /home/bill/ISET/admin-dashboard
npm run path:deploy -- --env test --skip-schema --skip-data --skip-admin --release-id intake-draft-autosave-test --skip-qualification --yes
```

Enable the flag after smoke passes:

```bash
cd /home/bill/ISET/admin-dashboard
bash scripts/run-test-sql-via-ssm.sh --sql "INSERT INTO iset_runtime_config (scope, k, v) VALUES ('runtime', 'intake.draft_autosave', CAST('{\"enabled\": true}' AS JSON)) ON DUPLICATE KEY UPDATE v = VALUES(v), updated_at = CURRENT_TIMESTAMP;"
```

Rollback is the same helper with `{\"enabled\": false}`.

## Manual fall-back (legacy process)

These steps are kept for reference in case you ever need to perform the deployment by hand.

1. Build with test variables  
   `npm run build:test`
2. Package the artefact (build/, isetadminserver.js, package.json, package-lock.json, .env.test) using `Compress-Archive`
3. Upload the zip to `s3://nwac-test-artifacts/admin-dashboard/` (or presign it)
4. Craft an SSM `AWS-RunShellScript` payload that downloads the zip, copies the files into place, installs dependencies, and restarts PM2
5. Execute `aws ssm send-command` against the current healthy ASG/SSM app host(s) and poll until the status is `Success`
6. Perform a quick smoke test and clean up the temporary zip once verified

> **Reminder:** Avoid the legacy `deploy.ps1` workflow; use `path:deploy` for the supported PATH flow, or the component scripts/manual SSM path only as a lower-level fallback.
