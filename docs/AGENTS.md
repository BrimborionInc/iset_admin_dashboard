# STOP: NEVER GUESS DATABASE SCHEMA OR SQL

This is the first and non-negotiable instruction for every agent working in this repository.

## STOP AGAIN: CHECK THE FINISHED SQL AGAINST LIVE SCHEMA BEFORE EXECUTION

- **TESTS, SMOKES, FIXTURE SEEDERS, AND CLEANUP SCRIPTS ARE NOT EXEMPT.** Any release or workflow harness that issues SQL must prove the exact live target identity and DDL first, then route every non-metadata statement through a fail-closed per-statement identifier check. A preflight failure must close the connection without attempting fixture cleanup or any other non-metadata SQL.
- **PREFLIGHT MUST PRECEDE EVERY FIXTURE EFFECT.** A deployed acceptance harness must finish its schema-only preflight before it creates Cognito users, uploads fixture objects, or runs ordinary database reads/cleanup. Cleanup must resolve its current relationship rows again inside the cleanup transaction; a stale pre-transaction ID inventory is not proof that a dependent row has not appeared. When a remote runner loads deployed environment files, discover and pin the AWS identity after that same loading sequence instead of assuming the EC2 instance-profile role is the active credential.
- **FIXTURES MUST PROVE THE WHOLE RELATIONSHIP CHAIN.** When product code enforces a submission/participant/application/case/version relationship, the acceptance fixture must seed that exact live-DDL-proven chain. Resolve environment-owned references such as region and budget pot as one compatible live set; never select the first row from one table and assume a related row exists.
- **OUTPUT ALIASES AND METADATA PROBES ARE NOT EXEMPT.** Never invent an unverified alias in an identity or DDL query; it may be a reserved word on the live engine. Prefer native result labels. After a metadata-probe syntax failure, prove that no write ran and remain in metadata-only discovery until the exact target and required DDL are established.
- Retrieving live DDL is not enough. Immediately before executing every non-metadata SQL statement, compare every identifier in the finished SQL text against the live metadata captured for that exact target environment.
- Check one table at a time and explicitly confirm that every selected, filtered, joined, ordered, inserted, or updated column belongs to that table. Never carry a similarly named column from another table into the statement.
- If even one identifier, function, enum value, collation, or relationship cannot be pointed to in the current live metadata output, do not run the statement. Return to metadata-only discovery.
- For PROD repair work, put reviewed non-metadata SQL in an artifact under `sql/ops/`; do not improvise multi-table inventory or mutation SQL directly in a shell command.

- Never compose or execute SQL that references a table, column, index, constraint, relationship, enum, collation, function, or database object that has not been verified against the exact target environment in the current task. This prohibition includes read-only `SELECT` statements and applies to DEV, TEST, and PROD.
- Before any SQL, verify the target account/environment, database, host, and current user. Discover object names through live metadata. Before any mutation, inspect the full live DDL (`SHOW CREATE TABLE` plus any required `SHOW FULL COLUMNS`/index/constraint metadata) for every table touched and every joined or compared field.
- Code, ORM models, migrations, docs, earlier repairs, remembered names, and plausible naming conventions are not proof of the deployed schema. Do not turn a failed guessed query into a revised guess: stop ordinary work and return to schema-discovery queries only.
- The same proof rule applies to application code that reads database result objects. Every `row.field`-style access must be backed by a live column on the exact queried table or by an explicit alias in the executed `SELECT`; a similarly named field on a related table, model, fixture, or payload is not proof. Regression fixtures for database-backed routes must mirror the proven result shape and deliberately omit tempting nonexistent fields.
- PROD requires an additional fail-closed sequence: verified schema evidence, read-only inventory/preview, explicit identifiers and guards, reviewed apply and recovery artifacts, appropriate lock/warning/snapshot controls, and independent post-apply verification. User urgency or authorization never waives schema proof.
- If this rule is breached, stop immediately. Do not retry the mutation. Prove whether anything changed, roll back or recover if needed, clear temporary locks/warnings/procedures, tell Bill plainly, and restart from live schema discovery only after the failure has been reviewed.

# Admin Dashboard Assistant Context

This file is the required project entry point for AI coding agents. Read it first, follow the linked reading order, and do not make code, database, deployment, or documentation changes until you have applied the relevant project guidance below.

Purpose: persistent context for future threads.

This file is a fast onboarding and handoff document for assistants and developers working in the admin dashboard repo. It should help a new thread start quickly, avoid repeated mistakes, and find the right code/docs/data locations with minimal back-and-forth.

Audience: assistants and developers.
Last Updated: 2026-08-06

## Project memory layer

- This repository uses `docs/` as persistent project memory for future short task-based AI threads.
- The root `AGENTS.md` exists only to point agents here; `docs/AGENTS.md` is the canonical entry point.
- Code, schema, migrations, config, tests, and live database evidence are the source of truth. Docs are operational guidance and must be verified before being treated as proof.
- Maintain the memory layer as part of normal work: update affected docs when code, schema, architecture, workflows, operations, or active project state changes.
- When the user corrects or refines Codex's operating behavior and the correction should persist, update the relevant project-memory doc immediately before continuing the original task. Acknowledging the correction in chat is not enough.
- Prefer concise, current, task-useful docs. Revise, merge, archive, or delete stale/conflicting documentation instead of preserving old wording because it might be useful.
- Keep improving the quality of the persistent knowledge base as you work. When you encounter guidance that survived from the Windows-mounted checkout era, update it to the current WSL/Linux `/home/bill/ISET` reality or mark it as historical instead of carrying stale Windows paths, commands, or assumptions forward.
- Follow `docs/meta/standing-directive.md` for the durable maintenance contract and `docs/meta/documentation-audit-2026-04-29.md` for the current documentation cleanup inventory.

## Source-control baseline

- Read `docs/guides/source-control-and-github-operations.md` before any push, branch reconciliation, canonical-branch change, workflow change, or GitHub automation work. Treat every push as a potentially operational event: inspect the full pushed range and all active workflows, integrations, and webhooks before changing `main`.
- The canonical GitHub branch is `main` in each PATH repository: `admin-dashboard`, `ISET-intake`, and `shared`.
- Use temporary local branches and Git worktrees to isolate qualification, hotfix, and release candidates, but do not leave deployed commits stranded there. After a release is verified, fast-forward or merge the exact deployed commit into `main`, push `main`, then delete the obsolete remote release branch.
- A local release worktree may remain detached only when its ignored `tmp/` files are still needed as deployment evidence. It is an evidence snapshot, not a competing source branch.
- The historical admin `backup-main` branch was the working mainline during an older repository transition. It was superseded by canonical `main` during the 2026-08-02 reconciliation and must not be recreated or used for new work.
- No Git push, pull request, merge, branch operation, or path filter may automatically deploy PATH code, mutate a PATH database, or apply Terraform to TEST or PROD. PROD changes must use the established runbooks, explicit environment authorization, and their fail-closed verification gates.

## Working relationship (design dialog)

- Treat implementation as a design dialog with the user, not literal instruction execution.
- When the user is talking through bugs, change requests, design choices, priorities, or process questions, answer in natural conversational English by default. Prefer coherent paragraphs over many one-line bullets, avoid unnecessary headings and nested lists, and give the user one clear next point to respond to when the conversation is still exploratory.
- Bill expects full-powered analysis, investigation, and engineering judgment from Codex, but concise chat responses by default. Do the deep work in code, tools, and reasoning; surface only the answer, decision, evidence, risk, and next action that Bill actually needs unless he asks for more detail.
- Reasoning-mode escalation rule (2026-07-10): Bill should not have to judge the appropriate Codex reasoning level before describing a task. The available levels are `Light`, `Medium`, `High`, `Extra High`, and `Ultra`. When the active level appears materially too low for the request, pause before substantive investigation or mutation, recommend one of those exact higher levels, and give one short reason so Bill can switch modes and resubmit/continue. Escalate for genuinely complex or consequential work such as ambiguous cross-system architecture, difficult multi-cause diagnosis, security/privacy/data-integrity analysis, broad refactors, or production release decisions. Do not invent unsupported level names, interrupt routine work when extra reasoning would offer only marginal benefit, or ask Bill to change a mode that is already sufficient.
- Engineering-audit continuation shorthand (2026-07-10): while `docs/planning/engineering-audit-register.md` has an active review cycle, Bill may say only `next`. Treat that as authorization to read the register, select and execute the next incomplete audit lane or recorded next step, update the persistent register, and report completion or a necessary change of direction. Do not make Bill restate or paste the lane prompt. Still pause before work if a higher reasoning level is materially warranted, if a business ambiguity changes scope, or if progress would require implementation, environment mutation, or other authority beyond the audit's read-only boundary.
- Treat running updates as the place for useful execution detail. Final responses should match Bill's scanning style: briefly state the outcome, material risk or caveat, verification result, and any decision he must make; do not recap the full investigation or implementation unless he asks.
- When Bill asks for workflow/process review, keep chat answers concise, focused on the direct question and any issues or decisions. Do not provide long code summaries unless he explicitly asks for implementation detail.
- Do not turn exploratory design into a branching checklist in the linear chat. If several concerns or unknowns exist, group them, make a recommendation, and advance one live decision at a time. If one point needs deeper discussion, pause the rest, carry the unresolved context yourself, and reintroduce the next relevant point when it is actually needed.
- When guiding Bill through a live external UI or tool, use the screen state he provides and give one concrete next action. Do not rely on brittle numbered navigation paths after the UI has diverged; name the visible control to use next and explain any mismatch with prior guidance briefly.
- The user may be wrong or operating from incomplete context; surface contradictions and risks early.
- If requested changes could break behavior, conflict with data reality, or create regressions, pause and discuss tradeoffs before coding.
- Challenge weak assumptions with concrete evidence (code paths, payloads, schema, runtime config), then agree on the target behavior.
- Own technical decisions once requirements are clear, but do not silently make high-risk assumptions.
- During live repair/release-management work, Codex is responsible for maintaining the overall thread state, current objective, critical path, completed/pending repairs, and owner-notification follow-ups. Do not assume Bill is tracking the full workstream. If Bill asks a useful but distracting question, answer briefly and then steer back to the agreed repair plan instead of drifting into a rabbit hole.
- Production incident stakeholder-message rule (2026-07-22): do not tell Bill an issue is fixed, and do not draft stakeholder email saying it is fixed, from an inferred error message, a database-only repair, or a plausible code-path diagnosis. First obtain the exact reported error text/screenshot or equivalent request evidence, identify the actual failing guard, and verify the affected user journey through the real deployed UI/API or a faithful authenticated reproduction after the repair. If that end-to-end proof is unavailable, say the repair is provisional and do not put Bill in the position of announcing success to staff. A successful data update proves only the data update, not the user workflow.
- Feedback investigation-first rule (2026-07-31): do not jump from a staff report to a code change, data repair, or speculative root cause. First establish the reporter's exact workflow and observable sequence, ask the reporter the smallest decisive question when retained system evidence cannot distinguish plausible click paths, reproduce the reported behaviour where possible, and only then decide whether the cause is product behaviour, a failed request, data damage, user misunderstanding, or another condition. Restore points and database snapshots can establish persisted state, but they do not prove which controls the user clicked. Treat implementation as the outcome of a completed diagnosis, not as the default response to a report.
- Plain-language environment rule (2026-07-27): ordinary PATH users may not know what `production` or `PROD` means. In emails, notices, status updates, and other non-technical communication, say `the live PATH system` or `the PATH system you normally use` instead. Reserve `production` / `PROD` for technical and operational audiences, or explain it in plain language when the term must be included.
- Do not be merely reactive to Bill's live prompts during code, data, or release work. Bill is the business-domain authority, but Codex owns engineering judgment, code/data decisions, release management, sequencing, and quality control. Correct Bill directly when a requested path is unsafe, incomplete, out of order, or distracts from the critical repair objective.
- In discovery/design conversations, Codex leads the design process: provide concrete recommendations, challenge contradictions directly, and drive toward a robust design before implementation planning.
- Treat the user as business-domain authority and Codex as design authority; use constructive, direct challenge ("confrontational perfectionism") to remove ambiguity and weak decisions early.
- During interviews/discovery, avoid list-style question dumps. Ask one question at a time and track answers across the conversation.
- Keep questioning intentional: ask only when a material assumption is required or intent is unclear enough to risk rework.
- Continue the interview until requirements are sufficient to move to the next phase; do not ask questions for their own sake.
- Codex owns code and data decisions. Do not expect Bill to answer implementation-level questions about code paths, payloads, schema, or persistence mechanics.
- Codex must inspect the codebase and database directly and make defensible data-handling decisions from evidence.
- Before presenting an edge case as a material product/reporting decision, verify from the current
  schema, code paths, or environment data that the scenario is possible and realistic. If a
  purely hypothetical case is still useful, label it explicitly and do not make the user resolve
  it as though it were a current system condition.
- For nForm extraction work, Codex manages the workstream end to end: maintain the plan, choose and sequence implementation slices, track completed and outstanding work, verify results, and persist a resumable checkpoint after every material session. Bill is not keeping a parallel engineering progress record and must not be expected to reconstruct one. Codex leads the product architecture and engineering, makes and defends technical recommendations, and challenges overbroad or overly narrow boundaries. Bill is the business-objective authority; ask him in plain language whenever business intent, user experience, priority, or a consequential product tradeoff is genuinely uncertain, but do not ask him to resolve discoverable implementation details.
- nForm extraction must not damage or alter the operational PATH implementation. Treat the admin, public-portal, shared, database, infrastructure, deployment, and Git-history surfaces of PATH as read-only reference material for nForm work. Do not modify, delete, reorganize, migrate, or repoint PATH as part of extraction. A future PATH-on-nForm migration is a separate project requiring Bill's explicit authorization and its own migration, rehearsal, rollback, and release controls.
- Interview focus with Bill should be UI/UX behavior, workflow expectations, and business intent.
- It is expected to challenge current code/data design when flaws are found; use confrontational perfectionism to reach a robust design before refactor planning.
- Do not ask "preference boundary" questions when a clear, defensible recommendation already exists.
- In those cases, state the recommendation, apply it as the default, and only ask for confirmation or an explicit override.
- Do not ask questions for the sake of visible collaboration; minimize user questions.
- Ask only when requirements are genuinely ambiguous or when a real dilemma remains after code/data introspection.

## How to use this file

- Use this as a starting context map, then verify details in code before making claims.
- Treat linked docs as the source of truth for deeper implementation details.
- Keep this file focused on practical orientation (what matters, where to look, what commonly breaks).
- Treat this file as the entry point for standing directives. If a durable workflow/style/how-to guide lives somewhere else in `docs/`, add or maintain an explicit pointer here so a new thread can find it from this file first.
- Keep entry-point guidance high signal. If a detail belongs only to one subsystem or one historical thread, link to the canonical doc instead of expanding this file.

## Thread-start checklist

- Clarify requirements if business behavior is ambiguous.
- If the user references a previous chat, prior note, or missing historical context, search `docs/meta/codex-thread-index.md` first and then open the linked canonical doc(s).
- Confirm real behavior from code/API payloads before changing UI.
- For dashboard/widget work, read `docs/guides/configurable-dashboard-notes.md` first.
- For document checklist configuration, runtime checklist JSON, or checklist-required/missing-document bugs, read `docs/planning/document-checklist-config-widget.md` first.
- For homepage Metrics or Items work, read `docs/dashboards/admin-home-metrics-widget.md`.
- For coordinator-facing PATH help-panel or AI-context work, use `docs/training/TRAINING_MODULES_September_2025_extracted.md` as the baseline for staff workflow expectations and write guidance as a job aid, not a product tour.
- For dashboard help-panel, inline guidance, or AI-support updates, design top-down from the user's job, decision points, and likely failure modes. Write job aids for staff, not transcripts of development conversations: do not carry screenshots, debugging paths, internal rationale, or Bill/Codex design discussion into help text unless it represents a durable user question and is rewritten in user-facing workflow language.
- For PATH finance/reporting wording, distinguish PATH-side program/staff review, export, handoff, and operational follow-up from external Finance/Sage work. Do not frame PATH as the day-to-day system used by NWAC Finance staff unless Bill explicitly confirms that operating model has changed.
- For the Regional Manager two-step review change across application assessments, new intervention proposals, and intervention amendments, read `docs/planning/rm-two-step-review-workflow.md` before changing approval queues, decision guards, review statuses, generated PDFs, notifications, deployment activation, or user guidance. Release `20260620-rm-two-step-review-rollout` deployed and activated the feature in TEST and PROD on 2026-06-20; DEV/TEST/PROD have the runtime toggle enabled for all three workflow types. The required notification configuration is documented in `docs/planning/rm-two-step-review-workflow.md`: enable/normalize the two-step review bell alerts, disable legacy `assessment_submitted` rows for admin roles and Regional Manager, and keep final-decision `rm_review_submitted_to_nwac` rows enabled for current admin support/decision routing. Staff-facing draft guidance lives in `docs/guides/rm-two-step-review-user-guide.md`. In staff-facing workflow copy, say `Decision Maker` for the final-decision actor; do not use `NWAC` as shorthand for the approver because coordinators and Regional Managers are also NWAC staff. For business workflow, UX, queue design, and release acceptance, the key roles are ISET Coordinator, Regional Manager, and NWAC Administrator; System Administrator is technical support/superuser behavior only. Current approval policy: only Shelley Stacey (`sstacey@nwac.ca`) can approve funding of `$20,000` or above.
- For the ongoing workstream to give Regional Managers and Program Administrators controlled ways to correct records, reverse mistakes, retract dependency-free entries, and make limited post-submission corrections, read `docs/planning/staff-record-correction-controls-proposal-email.md`. The proposal has been sent for client review and is awaiting Émilie/client-PM feedback. Related bug fixes and one-off data repairs are implementation evidence, not authorization for broader permissions and not completion of the workstream; preserve that distinction until the agreed role/action/state/dependency/audit matrix is recorded.
- Current post-assessment correction policy (2026-08-06): when an error discovered after assessment submission requires the assessment body or approved funding details to change, the normal path is for the Regional Manager to return the assessment to the submitter for correction so the review workflow records the return, edit, resubmission, and new decision. If a final-decision record has no supported return path, a System Administrator may exceptionally repair it, but the recovery must fail closed: either place it directly at `returned_to_submitter`, or mark a temporary `rm_review` recovery with `requiresSubmitterCorrectionReturn=true` and prove both UI and backend prohibit `rm_submit_to_nwac`. Never reopen a decided assessment to an ordinary dual-action RM review state and depend on email instructions. Treat a missing post-decision return transition as a workflow gap; do not solve it by granting coordinators direct editing of finally decided assessments.
- For major workflow releases or repairs, read `docs/ops/deployments/major-workflow-release-management.md` before coding or deploying. Major workflow changes must be controlled by a business-state release contract covering roles, states, queues, editability, generated artifacts, notifications, data repair, feedback reports, and owner communication; do not treat individual bug reports as the whole release plan.
- For Sage Intacct or Intacct mock-service work, read `docs/planning/intacct-interface-fidelity-audit.md` and `docs/planning/intacct-mock-dashboard-design.md` before changing sender routes, payloads, response parsing, dashboard reconciliation simulation, or mock-service endpoints. `npm run audit:intacct-contract` is a local PATH/mock drift guard only; Sage-fidelity claims still require current official Sage docs or approved sandbox evidence.
- For admin `Ask the AI` chatbot quality, retrieval, or knowledge-base work, read `docs/planning/admin-ai-chatbot-knowledge-base-transformation.md`, `docs/planning/admin-ai-chatbot-coverage-register.md`, `docs/testing/admin-ai-chatbot-evals.md`, and `docs/features/admin-ai-guidance.md` before changing prompts, guidance rows, evals, or model/runtime behavior.
- For browser-level workflow smokes, especially approval/assessment/letter workflows, read `docs/testing/browser-workflow-smoke-automation.md` before inventing one-off manual browser testing. Prefer DB/API fixture automation plus authenticated browser assertions and cleanup evidence.
- Current aggregate-test rule (2026-07-10): `npm test` / `npm run test:all` in both admin and `../ISET-intake` are deterministic non-watch aggregate commands. Admin runs the CRA `src/` suites plus the supported backend/authorization/validation/tooling Jest config under `tests/`; portal runs the CRACO `src/` suites plus all Node tests under `auth/`, `notifications/`, and `routes/`. Use `test:frontend:watch` when interactive frontend watch mode is intended. Do not add maintained test-named files outside these aggregate phases.
- Complete-stack test rule (2026-07-12): both servers support an explicit fail-closed `NODE_ENV=test` + `PATH_APP_FACTORY_MODE=1` import boundary backed by repository-local injected dependencies. Use it only in automated tests, set dependencies before requiring the server, and keep listeners, migrations, workers, pollers, pruners, and cleanup jobs suppressed in that mode. Normal direct process startup must retain those runtime jobs. Full-stack composition tests sample real auth/parser/route order and the portal completion transaction; extracted router/service tests still own deeper failure injection.
- Current release-qualification rule (updated 2026-07-28): `docs/ops/deployments/release-qualification-runbook.md` is the authoritative PROD-admission gate and `docs/testing/release-coverage-inventory.json` is its executable change/dependency map. A TEST run requires unexpired exact-source `DEV GO` evidence; a PROD run requires unexpired exact-source `TEST GO` evidence. Required checks cannot be skipped: local qualification includes both aggregates/lint/builds, compiled browser journeys, real DEV MySQL readiness/request/write contracts, privacy/payment/external-substitute checks; TEST acceptance adds deployed provenance, rollback artifacts, on-instance schema/config/process/worker proof, role/applicant/cross-app journeys, strict denials, cleanup, and maintenance recovery. A green unit suite or health endpoint alone never qualifies a release. `path:deploy` enforces the evidence before mutation and embeds provenance in artifacts. `--skip-build` additionally requires an exact checksummed build manifest. The reduced PROD operator can stage scoped immutable `releases/*` artifacts and descriptors as well as compatibility artifacts; the live launch template still consumes the compatibility keys. `--compatibility-only` remains an explicit recovery path, not the default.
- Caller-boundary regression rule (2026-08-05): a unit/service test that calls the repaired function with the correct value does not prove the production route, job, or event handler derives and supplies that value. For a caller-wiring defect, add an executable test through the real caller boundary using a live-schema-faithful result shape, and add a real DEV/TEST journey when the defect affects a participant-visible workflow or durable artifact.
- Object-store environment-parity rule (2026-08-07): provider support for version-aware operations does not prove that the target bucket has object versioning enabled. Upload, rollback, retry, and compensation tests must cover both a non-null version ID and the exact live unversioned `HEAD` shape. In an unversioned bucket, accept or delete only an acknowledged request-owned key created with `If-None-Match: *` whose signed checksum metadata and size are reverified; never make a versioned-only mock the sole release proof for a workflow that uploads files in TEST.
- Current PROD bootstrap guard (2026-07-28): the canonical `s3://nwac-prod-artifacts/bootstrap/app-bootstrap.sh` is still the old NodeSource version because the reduced operator/instance roles cannot replace it. Release `20260728-regional-snapshot-data-quality-funding` also proved that the current AMI's installed PipeWire/WirePlumber stack conflicts with the hardened script's legacy PulseAudio/session-manager package transaction. Do not start a PROD ASG refresh unless all surfaces are behind fallback and the documented SSM recovery in `docs/ops/deployments/prod-deployment-guide.md` is ready, or until the canonical object and current-AMI package handling have both been corrected and freshly rehearsed.
- Default testing and release-management behavior (2026-07-13): Bill should not have to request tests or remind Codex about release management. For every code/config/schema change, Codex must resolve the affected coverage domains, run proportionate focused tests while developing, run the relevant aggregate/build/browser/real-DB gates before calling the work complete, and keep release scope, evidence, cleanup, and environment readiness current. When work becomes a release candidate, Codex must run DEV qualification automatically, retain/report GO or NO-GO evidence, and prepare the exact TEST approval packet. A request to deploy to TEST authorizes planning and presentation of the rehearsal but not TEST mutation until Bill explicitly approves it; a request to deploy to PROD likewise requires current TEST GO evidence and Bill's explicit PROD approval. Do not wait for Bill to supply special testing prompts, enumerate obvious checks, or notice that evidence has expired or source has drifted.
- Public-portal intake completion integrity (2026-07-10): before any core write, `../ISET-intake` validates the merged completion payload against required values on the applicable path in the currently published workflow. Client resolution/creation plus submission/application/case persistence is one transaction, and only a coherent already-completed result can be replayed idempotently. Read `../ISET-intake/docs/portal/intake/submission-flow.md` before changing this boundary; app-code work does not authorize workflow publishing, runtime/schema/data mutation, or historical repair.
- For dashboard/widget spot checks, always include a runaway request/render-loop check. Open the changed dashboard with DevTools or browser automation network capture and verify that steady-state API calls settle after initial load and intentional polling only. This is a recurring PATH failure mode, especially when inline objects/arrays/functions such as sort descriptors, filters, payloads, or palette items are passed into hook dependencies and cause `useEffect`/`useCallback` refetch loops with aborted requests.
- Keep doc updates in the same change when behavior or structure changes.
- For local DEV dashboard/API work, source-level tests are not enough. After changing a widget's API call shape or the backend route that serves it, smoke the actual local endpoint shape the UI will call before saying the work is done. For protected APIs, an unauthenticated `401 Missing bearer token` is acceptable evidence that the route is up and not crashing; use an authenticated browser/API check when validating returned data or UI behavior.
- Before any AWS operation, confirm the target environment account and identity after sourcing that environment's config or using that environment's explicit profile. DEV and TEST live in a separate AWS account from PROD, and PATH uses different IAM identities for different environments and surfaces. The shell's implicit/default `aws sts get-caller-identity` is not evidence of the app/test identity. Do not request or mutate IAM/Cognito/SES/S3 permissions until the sourced/profiled identity, account, pool/bucket/resource ARN, and environment are all verified. For live AWS infrastructure or IAM adjustments, Bill prefers explicit AWS CLI changes under the verified profile/account/resource while keeping Terraform desired state aligned; do not default to Terraform apply unless Bill asks for it in the current thread. See `docs/ops/agent-operational-access.md`.
- For PROD SQL specifically, apply the schema-proof rule in `docs/ops/agent-operational-access.md`: verify the live table shape for every selected/joined column before running the query, and stop ordinary investigation if a guessed column/table query fails.
- If blocked by tooling, permissions, or environment access, call it out immediately.
- For TEST/PROD deploys, apply the deployment runbook as an execution checklist, not as background reading. PROD deploys require explicit Bill approval in the current thread before running the deploy, even when a fix is prepared and tested. Before `path:deploy`, state the maintenance sequence being used. Default deploy scope is the current app build plus planned schema migrations, not DEV database state. Deployment does not include runtime configuration, allowlisted data promotion, arbitrary SQL data fixes, or full database restores unless Bill explicitly confirms that exact data scope in the current thread; use `--skip-data` or omit `--dataset` for app/schema-only releases, and do not include `--dataset intake-release` as boilerplate. Do not copy "whatever runtime config is in DEV" to PROD as release hygiene; any PROD runtime config change must be a named, reviewed operation for specific keys/rows. PROD app deploys must use clean packaged source trees unless Bill explicitly approves a dirty-source emergency exception with `--allow-dirty --dirty-reason`. If any PROD data/runtime mutation is proposed, first state the exact dataset or SQL, target tables/keys, source environment, reason an app/schema-only deploy is insufficient, restore/rollback path, and verification plan. If the deploy will restart admin/portal processes, refresh ASG instances, reinstall dependencies, change target routing, or otherwise risk a raw `502 Bad Gateway`, set the scoped in-app warning, wait through the warning window when practical, then enable the ALB fixed-response maintenance fallback before starting the deploy. Clear fallback only after smoke is green, then clear the warning. For PROD bug/CR releases, the deploy is not complete until the affected live feedback reports have current status, status history, and internal notes after targeted recheck.
- Canonical migration failure rule (updated 2026-07-13): a failed migration is a failed command, not a successful result with `haltedOnFailure=true`. The shared runner records the failed attempt and throws `schema_migration_apply_failed`; the schema CLI exits nonzero; and both `path:deploy` and TEST DB refresh defensively reject any failure payload before later steps or a successful manifest. Long-lived ledgers can retain failed attempts for obsolete checksums after a corrected canonical checksum succeeds; expose those as historical audit rows, but block release on every pending migration or failed attempt for the current canonical filename/checksum. Preserve both the fail-fast apply contract and this current-versus-historical distinction.
- Canonical schema ownership rule (2026-07-11): after a migration filename succeeds in a durable environment, its current checksum must match a recorded successful checksum or local/remote plan/apply fails with `schema_migration_checksum_drift`; corrections use a new forward filename. Admin, portal, and shared request paths must use read-only schema probes, never CREATE/ALTER repair. Add dependencies through `sql/migrations`, update `/readyz`, and treat a missing table/column/required enum as `schema_not_ready`. PROD normal smoke uses `/readyz`; `/healthz` is only a shallow process probe.
- Release-scope ownership rule (2026-07-05): Codex owns deciding which code/config files belong in a TEST or PROD release. Do not ask Bill implementation-level release-composition questions; inspect the source trees, diffs, generated artifacts, deployment manifests, and runtime-config boundaries, then choose defensibly. Pause only for true business/data/runtime ambiguity, stated in plain English with concrete consequences. Example: if the current DEV-published intake runtime schema appears to be an experimental legal-aid application, ask before promoting that named runtime config to PROD. Never promote DEV runtime config, published intake schemas, allowlisted data, or arbitrary SQL as deployment boilerplate.
- Deployment-trigger reconciliation rule (2026-07-10): an unqualified request to deploy means all outstanding coherent, tested, release-ready scope across threads, repositories, and backlog work, not only the last change discussed. Before mutation, inspect and state the included/excluded release manifest across admin, portal, shared, migrations, runtime/data operations, and feedback items. Do not include unfinished/experimental work. If the readiness or business intent of a prepared change remains genuinely ambiguous after source/project-memory inspection, pause and ask Bill about that concrete ambiguity; asking first is correct and silence is never approval to omit or risk a change.
- Deployment data/config boundary (2026-07-10): “all outstanding scope” means code, planned schema, docs, and deliberately prepared release artifacts; it never means copying DEV applicant, user, client, case, document, message, payment, audit, identity-link, or other operational/business data into TEST or PROD. It also never means blanket-copying DEV configuration. A backlog change that genuinely requires configuration must name and target only the required keys/rows and pass the normal explicit config approval/verification gate.
- Environment-purpose boundary (2026-07-10): DEV is also used to test other nForm applications, including legal-aid experiments. TEST and PROD are dedicated to NWAC's ISET support process. Any published intake/runtime/config payload that is not proven to be the intended ISET workflow is a hard stop. A prior accidental publication of an experimental legal-aid intake to PROD caused a significant SLA and reputational breach; never infer that the current DEV publish belongs in TEST/PROD.
- Expert-challenge rule (2026-07-10): Codex owns technical diligence and must challenge unsafe, contradictory, destructive, anomalous, or quality-reducing instructions in order to protect product integrity, privacy, availability, security, and NWAC contractual obligations. A proceed/deploy instruction does not override a material safety ambiguity. Catastrophic requests such as deleting the codebase, erasing history/data, exposing secrets, or disabling safeguards require refusal and strong independent confirmation through extended dialog, especially when the request may indicate an unauthorized actor.
- Plan-driven maintenance rule (2026-07-10): finalize maintenance and smoke surfaces from the completed deploy plan. If its `App deploy` result has `shared=true`, use all-surface warning/fallback and all-surface normal-routing smoke even when one app artifact is skipped; use a single-surface sequence only when `shared=false` and the rollout is genuinely isolated.

## For future Codex threads

Treat this file as the current project context for this repo. If the user references prior-thread work, notes, or "something we already figured out", check [codex-thread-index.md](./meta/codex-thread-index.md) before searching blindly. As you work, keep the project memory layer current: update [AGENTS.md](./AGENTS.md) with durable context pointers, guardrails, and architecture notes that would help future chats; update [codex-thread-index.md](./meta/codex-thread-index.md) whenever you add or materially revise a durable handoff/how-to note that a future thread may need to rediscover and the exact task-history title is known; update affected live docs under [`docs/`](./) when behavior changes; and record notable shipped changes in [changelog.md](./meta/changelog.md) and [next-release-notes-log.md](./meta/next-release-notes-log.md). The goal is that a new thread can recover the current state of the system from the repo docs without depending on prior chat history.

- Standing operational rule for PROD bug/change triage: if a thread investigates, fixes, deploys, or otherwise materially resolves a PROD item that came through the in-app feedback system, update the live PROD feedback log before closing the thread. That means keeping `admin_feedback_report.status`, `admin_feedback_status_history`, and `admin_feedback_note` in sync with the real outcome instead of leaving the resolution only in chat, code comments, or repo docs.
- Current user-requested bug/CR triage workflow (2026-04-17): when Bill asks Codex to triage bugs or change requests, the expected outcome is not just queue cleanup. Codex should review the live queue, inspect each item's available context, add internal notes, change statuses where warranted, and then present Bill with a prioritized analysis so implementation planning can happen against the real queue state.
- Current user-requested analysis boundary (2026-06-22): do not treat every question or clarification about a PROD feedback report as a request to change code, docs, runtime data, or feedback-log status/notes. When Bill asks for analysis, explanation, or a staff-facing answer, provide the analysis first and wait for explicit direction before making nonessential changes. Update the live feedback log only when the thread is actually triaging/materially resolving a report or Bill asks for the report record to be updated.
- Current user-requested bug/CR validation standard (2026-05-25): take a holistic workflow view before telling staff a report is fixed or marking it `resolved`. For workflow or client-facing-output issues, verify the complete affected flow and every generated/sent artifact in the packet, not only the specific symptom named in the report. If adjacent artifacts or downstream states are not verified, keep the item open and record the remaining verification work in the feedback notes.
- Current user-requested feedback closure rule (2026-07-30, superseding staff-confirmation-only holds): once the actual PROD fix or data repair is complete and an independent technical verification proves the reported condition and its known downstream artifacts are corrected, close the feedback report. Do not leave an otherwise fixed report open solely to wait for reporter/staff confirmation; the repair note and audit trail should state any normal next workflow action. Keep a report open only when concrete engineering, data, policy, privacy, deployment, or technical validation work remains.
- Current user-requested bug/CR release-planning standard (updated 2026-06-14): do not assume each fixed report should be deployed to PROD immediately. Unless Bill explicitly approves an emergency hotfix, keep prepared fixes batched into the next planned PROD maintenance release. Use `planned` only when a fix/change is complete and tested but not yet deployed to PROD. Use `in_progress` when work is underway but incomplete, including items awaiting user/staff feedback or targeted live artifact/workflow recheck. Only after PROD delivery and targeted live recheck should affected reports move to `resolved`.

## Core conventions

- Prefer Cloudscape components over native HTML. Use `Link` from `@cloudscape-design/components` instead of raw `<a>` unless there is no Cloudscape equivalent.
- For Cloudscape `Input` / `Textarea` / `Autosuggest`, set `spellcheck` intentionally instead of relying on browser defaults when field semantics are clear: enable it for narrative staff-entered prose (notes, summaries, messages, rationale, letters, justifications) and disable it for names, institutions, communities, codes, IDs, emails, addresses, references, and numeric/currency fields.
- Cloudscape `Input` does not provide built-in currency formatting. For money-entry fields, use the shared helper in `src/utils/currencyFormat.js` (`getCurrencyInputDisplayValue`) with focus/blur state so the form keeps the raw value while the blurred field shows formatted currency. Do not hand-roll one-off currency formatting per widget or modal.
- Do not assume parity with the public portal. Verify the full chain:
  schema -> runtime config JSON -> API payload -> renderer/template.
- Current intake-conditionality caveat: the public portal runtime now supports checkbox-array condition operators (`contains`, `notContains`, `containsAny`, `notContainsAny`, `containsAll`) and auto-skips steps whose authored components all hide. DEV workflow `21` still uses Step 19 `requested-supports` for transportation, childcare, other-support follow-ups, and support-specific uploads, but as of 2026-05-12 Household Income, base Household Expenses, and amount-driven financial evidence uploads are no longer gated by `Living allowance`; those financial questions now appear regardless of selected support type. DEV workflow `21` also currently splits Step 19 into two variants after Step `93`, routing `dependent-children = 0` applicants to a no-childcare copy of `Financial Supports Requested`. Admin Workflow Preview, the intake-step editor, and Manual Intake now share those operators and the same whole-step skip behavior for renderable manual-intake content, but Manual Intake still intentionally skips portal-only upload/signature steps. See `docs/planning/step19-checkbox-conditionality-followup.md` before editing those rules.
- Current intake publish parity status (2026-05-12): workflow `21` authoring rows were republished through `scripts/publish-workflow.js` after the financial-information conditionality update; treat the step library, workflow library, and `iset_runtime_config(scope='publish', k='workflow.schema.intake')` row as back in sync for DEV workflow `21`. Remaining admin-path caveat: Manual Intake still intentionally skips portal-only upload/signature steps, so parity claims there are limited to renderable manual-intake content.
- Current intake promotion guardrail (2026-06-16): `intake-release` and standalone `intake-runtime-publish` now require `--workflow-id` and parse `iset_runtime_config(scope='publish', k='workflow.schema.intake')`; plan/bundle/apply abort if the runtime payload's declared workflow id does not match. Before any TEST/PROD intake runtime promotion, confirm `summary.runtimePublish.runtime.workflowId` in the plan/manifest matches the workflow being promoted.
- Current intake runtime incident status (2026-06-16): release `20260612-212548` accidentally promoted the DEV Legal Aid Demo runtime row to TEST/PROD (`workflowId=53`, 13 steps, checksum `6126d57f8fd66f712672b83c2c0d5672fb3f410e20d3148488b64a23d4ed08c9`). Bill republished PROD workflow `21`; Codex verified current PROD is back to ISET (`workflowId=21`, 26 steps, no Legal Aid strings) and its stable schema equals the May 21 ISET portal schema in `../ISET-intake` commit `107593e`. TEST was still on Legal Aid and was repaired on 2026-06-16 with `npm run data:sync:apply -- --dataset intake-release --workflow-id 21 --target-env test`; verify future smokes against workflow id/step titles, not only health checks.
- Current intake DOB validation rollout caution (2026-05-20): the DOB/date validation fix is an app-code/runtime-renderer change and should not require republishing workflow `21` in TEST/PROD. A DEV republish during the fix validated cleanly afterward, but it also proved publish is not a harmless timestamp refresh because it can re-materialize authoring-state changes into `iset_runtime_config(scope='publish', k='workflow.schema.intake')` and `../ISET-intake/src/intakeFormSchema.json`. For TEST/PROD, default to deploying code only; if runtime intake JSON must be touched, use the established direct published-JSON/step-library patch pattern or run an explicit full parity check before promoting a freshly republished artifact.
- Current applicant-upload chooser rule (2026-04-21): the legacy public portal renderer in `../ISET-intake/src/renderer/renderers.js` now keeps one visible `Upload` button per `file-upload` component and, on devices likely to support direct camera capture, opens a small chooser offering `Take photo` (`accept="image/*" capture="environment"`) or `Choose file` (the authored broad accept list). Do not collapse mixed document/image uploads to `accept="image/*"` globally; that would break PDF/Word upload paths. Admin Workflow Preview and the intake-step editor now use a static `FileUploadPreview` to explain that mobile-only chooser behavior without pretending the desktop preview can actually invoke the camera. The repeatable authoring/runtime copy update for workflow `21` lives in `scripts/apply-intake-upload-camera-chooser.js`.
- Current public-intake persistence caveat (2026-04-11): the legacy deployed portal in `../ISET-intake` still uses two server-side stores. Step navigation (`Next`/`Back`) writes transient working state to `input_json_state` via `/api/intake-step/:stepId/update` and `/api/intake-json`; that row is DB-backed but TTL-pruned (default ~30 minutes) and is cleared on logout, explicit save-and-finish-later, submission, or expiry. Durable recovery in `iset_application_draft_dynamic` historically happened only when the applicant explicitly chose `Save and finish later` (`POST /api/draft`). The newer rebuild in `../iset-public-portal` already persists `iset_application_draft_dynamic` on each step save (`PUT /api/intake/draft`), but do not assume that behavior exists in the live legacy portal unless the thread is explicitly targeting the rebuild repo. The legacy portal now also has a separate rollout-safe endpoint `POST /api/draft/autosave`, gated by runtime config key `iset_runtime_config(scope='runtime', k='intake.draft_autosave')`; deploy code first with the flag off, then enable it deliberately after validation. Current legacy resume semantics: autosave writes after successful step transitions, authenticated landing-page start now routes through the dashboard, and direct wizard re-entry without `?step=` now honors the saved draft `stepCursor` when the wizard restores from durable draft. For legacy autosave work, keep `input_json_state` as the transient working store and add durable draft saves around successful step transitions instead of replacing the transient layer outright; review draft retention/privacy copy, dashboard resume semantics, uploads/doc refs, event volume, and one-draft-per-user overwrite behavior before implementation.
- Current Application Overview status-edit rule (2026-04-14): the manual status selector in `src/widgets/ApplicationOverviewWidget.js` is available to `System Administrator` and `NWAC Administrator` users. Other roles still see a read-only status badge there and rely on their existing role-gated quick actions / assessment flows instead of freeform status selection.
- Current ISET Applications table rule (updated 2026-06-10): `/api/applications` is application-based, not one-row-per-case. The Active view excludes closed/archived application lifecycle rows, while All Applications and Closed can show multiple historical applications for the same client/case. The application-case workspace accepts `?applicationId=` so opening a historical row loads that specific application on the shared case. Manage ISET Applications work-queue deep links use `/case-assignment-dashboard?bucket=...`; the dashboard passes that bucket to `/api/applications` so filtering happens before server sorting/pagination. Do not reintroduce local-only current-page filtering for `status`/bucket shortcuts. The old `/api/case-assignment/unassigned-applications` route is retired with `410`; use `/api/applications?bucket=new-submissions`.
- Current Regional Manager case-workspace access rule (2026-04-14): the `/api/cases/:id/workspace` family plus action-plan/intervention access validators now allow a Regional Manager to open or mutate a case when it is assigned directly to that manager, even if the file's region falls outside the manager's resolved `regionIds`. When the file is not directly assigned, the existing unassigned / `portfolio_region_id` / owner-region scope checks still apply.
- When adding/changing UI fields, confirm the backend actually returns the data.
- Fix root causes instead of layering workarounds.
- Current route-access source-of-truth rule (2026-05-25): when a dashboard is governed by the runtime Access Control matrix, the backend APIs that power that dashboard must either enforce the same route-matrix permission or a stricter documented object-scope rule. Do not add hardcoded role checks that can drift from the Access Control dashboard.
- Current application SLA rule (2026-04-11): due/overdue displays now derive the active SLA stage from application status + assignment state + `assessment_esdc_eligibility`, using `Assignment -> EI Status Verification -> Assessment -> Program decision`. The frontend source of truth is `src/utils/applicationSla.js`; backend counting/helpers live in `isetadminserver.js` (`getApplicationSlaStageKey`, `computeApplicationSlaTiming`). The due-date baseline is still submission/creation time, not a dedicated per-stage timestamp model. Staff-facing UI labels now prefer `Workflow timing targets`, `Timeline status`, and `Timeline target`; internal config/storage names remain `sla_*`.

## Auth model

- Admin sign-in now uses real Cognito/IAM only. Do not reintroduce simulated-user flows, IAM on/off toggles, dev-bypass headers, or header-driven role impersonation in admin code.
- The admin-dashboard backend is staff-only. Applicant-pool tokens must be rejected by this service; do not treat shared Cognito verification as permission to expose admin API routes to applicants.
- Staff/admin region scope and staff identity IDs must resolve from `staff_profiles` and `staff_region` on the server. Do not make staff authorization or admin-user management depend on Cognito `custom:region_id` or `custom:user_id`; the admin auth middleware must ignore those legacy Cognito custom claims instead of backfilling or authorizing from them.
- Critical identity-domain rule (2026-04-23 follow-on audit): in the admin backend, `req.auth.userId` is currently overwritten with `staff_profiles.id` during staff-profile hydration. Do not treat `req.auth.userId`, `req.staffProfile.id`, or similar staff-profile IDs as a local `user.id` when writing `*_user_id` / `actor_user_id` / `created_by_user_id` / `verified_by_user_id` columns or when comparing against rows in the shared `user` table. Use an explicit resolver to map the authenticated staff subject to the correct local `user.id` first.
- Related backend guardrail (2026-04-23 follow-on audit): avoid helpers that simply accept or reuse numeric IDs from auth/request payloads for shared `user`-table columns. In particular, treat caller-supplied `actorUserId`, `requesterUserId`, `verifiedByUserId`, `programApprovedByUserId`, and `financeApprovedByUserId` as untrusted input unless the route explicitly proves they refer to the authenticated actor or an allowed delegated identity.
- Current auth/session-audit rule (2026-05-26): `user_session_audit` is operational evidence, not cosmetic telemetry. Before relying on it in support/security investigations or shipping auth changes, verify that the deployed writer matches the live table shape and that rows are being written or updated in TEST. Do not swallow schema/write failures silently; log non-PII warnings and add focused tests for the exact deployed columns. The portal runtime owns automatic pruning for this table; default retention is 90 days with bounded daily deletes unless operations deliberately changes the env knobs.
- Current object-scope security rule (2026-04-25): staff authentication is not enough for client-data routes. Document, payment-packet, secure-message, note, reminder, event, signing, and generated-file routes must prove object-level scope before returning client data or issuing presigned file URLs. For supporting documents, never grant access from `iset_document.id` alone; resolve the document through case/application/action-plan/intervention/client/payment context first. Applicant document list/checklist calls must include scoped context (`caseId`, `applicationId`, or `interventionId`) for non-admin users. Document mutations must validate both the existing document and the destination context. See `docs/planning/privacy-security-systematic-review-2026-04-25.md`.
- Current secure-message guardrail (updated 2026-04-27): case-thread reads must validate case access and must not create or trust nonparticipant `message_item` rows. Treat `message_item` as sender/recipient mailbox state only until the case-thread model replaces it. DEV now has typed actor-domain fields and FKs on `messages`, plus `message_item` FKs; `messages.sender_id` and `messages.recipient_id` are physically retired in DEV by `20260427_0009`. New message writes must populate typed actor fields and must not add new recipient-authority behavior. Public portal applicant message reads/replies must require typed `applicant_user` participation plus resolved case/application scope. Admin mailbox-state mutations must use typed `sender_user_id` / `recipient_user_id`, not legacy `sender_id` / `recipient_id`, as the participant authority. Admin UI message direction should prefer typed actor fields; public-portal replies should let the backend derive the recipient from `reply_to` rather than sending legacy `recipient_id`. Attachment presign/adoption must validate the message's case context before producing download URLs or creating `iset_document` rows. Do not revive broad `/api/admin/messages` list/create behavior; case secure messaging must flow through `/api/cases/:id/messages`.
- Current privacy ERM cleanup plan (2026-04-26): live PROD means the deeper secure-message/entity-model cleanup must be handled as a DEV-first privacy hardening and migration release, not casual cleanup. Use `docs/planning/privacy-erm-cleanup-grand-release-plan.md` as the controlling plan and `docs/planning/privacy-erm-cleanup-progress.md` as the running execution log before changing secure messaging, staff/user identity relationships, case/application/client links, document/message attachment scope, or legacy experiment tables. Do not make PROD schema/data changes for this cleanup outside the planned, rehearsed grand cleanup release.
- Current remaining relationship FK rule (updated 2026-04-27): DEV migration `20260427_0018_harden_remaining_relationship_fks.sql` constrains client account events, input-state client links, case-assessment budget-pot links, case-reminder action-plan links, and staff-profile regions after preserving/deleting orphan DEV client-account events. Before TEST/PROD, run the client-account-event orphan preview and quarantine/archive or deterministically repair missing-client rows before adding the FK.
- Current workflow-ID rule (updated 2026-04-27): `workflow_id` on submissions, input state, and dynamic drafts stores runtime string keys such as `iset-v1`, not numeric `workflow.id` values. Do not coerce those fields into numeric workflow FKs; classify or redesign them with an explicit workflow key model first.
- Current legacy document experiment rule (updated 2026-04-27): DEV migration `20260427_0019_retire_zzz_legacy_documents_table.sql` drops `zzz_legacy_documents` only when empty. If TEST/PROD has rows, quarantine/archive their metadata and object keys through a reviewed script before retiring the table; do not reintroduce it as a second document store.
- Current privacy ERM smoke rule (updated 2026-04-27): run `npm run smoke:privacy-erm` after applying/refining the cleanup migrations. It is read-only and checks DB integrity/constraints for the cleaned model; it does not replace route-level authorization denial tests.
- Current route-scope smoke rule (updated 2026-04-27): run `npm run smoke:privacy-routes` after changing document/message/signing/event/finance evidence access routes. It is a static guard-marker tripwire only. Still add or run live wrong-user/wrong-case denial tests when practical.
- Current finance allocation evidence rule (updated 2026-04-27): `/api/allocations/evidence/*` routes must not presign or delete arbitrary object keys. Uploads are finance-role only, create actor-owned pending-upload rows with `document_type = finance_allocation_evidence`, and use `allocations/<localUserId>` object prefixes. Presign/delete must prove the key is referenced by `budget_allocation` or `budget_pot` evidence metadata, or is an unexpired pending upload owned by the current local staff user.
- Current application/case route-scope rule (updated 2026-04-27): application locks, application details/versions, escalations, case watches, case detail/save, case assignment, conflict actions, and ILMP/ready-to-close actions must validate owning case/application scope before reading or mutating data. Do not treat a pessimistic lock, watched-case row, role queue filter, or raw application/case ID as access authority.
- Current workflow authoring/debug rule (updated 2026-04-27): workflow/component authoring and audit/render endpoints are step-editor-only surfaces. Legacy blockstep and raw Nunjucks generator/render endpoints must remain behind `ENABLE_UNSAFE_ADMIN_DEBUG_ROUTES=true` plus System Administrator access.
- Current notes/reminders/events guardrail (2026-04-25): case notes, reminder descriptions/metadata, and case timeline events are client data. Reminder list/detail/mutation routes must validate either scoped case/application/action-plan/intervention context or admin-only global-reminder access; case-note and case-event routes must validate case access before returning narrative data.
- Current payment access rule (updated 2026-05-11): finance/admin payment roles are global for payment operations, but casework payment roles such as Regional Manager and ISET Coordinator are case-scoped. The operational cross-client dashboard is `/iset/payments`; it must still respect packet/case scope for non-global users. Payment batch and full ledger export surfaces are finance/admin-only, not casework-role surfaces. Do not add new payment packet/list/export routes without using the shared packet-scope access checks in `isetadminserver.js`.
- Current payments transformation status (updated 2026-07-12): NWAC's email payment workflow has completed DEV automation and an historical TEST rehearsal, but has not been rolled out to PROD or enabled for real Finance email sends. Finance/Sage remains the system of financial record; PATH tracks operations-side request, evidence, email handoff, communications, and follow-up state. R7 local safety requires a durable committed `payment_submission_attempt` before provider dispatch, replay without resend after known acceptance, and quarantine/review rather than blind retry for ambiguous outcomes. Payment follow-up documents must also be packet-contained, and payment UI state/actions must remain owned by the current case/filter scope. Before resuming payment work, read `docs/planning/payments-transformation-plan-2026-05-11.md`, `docs/planning/payments-target-operating-model-2026-05-11.md`, and `docs/testing/payments-workflow-automation.md`; perform fresh TEST rehearsal and read-only PROD preflight, and get explicit approval before any PROD payment-email routing change or real send.
- Current public-portal legacy fallback rule (2026-04-25): the hardening pass in `../ISET-intake` removes the highest-risk identity/linking fallbacks from current code and TEST release `portal-security-hardening-message-20260425-test`. Do not preserve or add public-portal auth email fallback that maps a Cognito subject to an already-bound `user` row, staff-recipient email fallback that links a `staff_profiles` row to an arbitrary shared `user` row, or client email/name/SIN fallback that overwrites `client.applicant_cognito_sub`. Applicant data routes must require the primary applicant portal Cognito client and reject staff/admin tokens; legacy portal endpoints that accept caller-supplied `user_id` must be retired or bound to `req.user.userId`. See `docs/planning/public-portal-legacy-fallback-security-review-2026-04-25.md`.
- Current staff/shared-user identity rule (updated 2026-04-27): mapping an existing shared `user.id` to `staff_profiles.id` must use Cognito subject only. Do not add email fallback in message routing, display-name, signing, funding-agreement, assignment, or audit helper paths; TEST/PROD data that cannot resolve by subject must be repaired or quarantined rather than guessed by email.
- Current public AI support privacy rule (updated 2026-05-07): `../ISET-intake` `/api/ai-support` is public and may call OpenRouter, so it must reject obvious sensitive content in both the current prompt and recent chat history before model calls. Do not send raw applicant case/account details to that endpoint; account-specific support belongs in secure Messages. The public portal Contact function is retired for new submissions, and Contact Communications is legacy triage only.
- Current admin AI privacy rule (updated 2026-07-30): admin `POST /api/ai/chat` may call OpenRouter, so it must reject obvious raw applicant/client identifiers, credentials, contact details, PATH references, and live-record JSON fields in messages and chat context before the proxy call. Do not send applicant denial context, case numbers, tracking IDs, names, emails, SINs, phone numbers, or other live-record facts to external AI. Denial-letter drafts are local-template only. Application approval-letter drafting may send only generic opening/closing templates plus non-personal mode flags; merge all applicant, case, program, institution, funding, payee, date, and staff details locally after the call. Keep the decision-letter external-AI boundary tests and privacy-route tripwire current. AI-backed dummy-data generators must remain behind `ENABLE_UNSAFE_ADMIN_DEBUG_ROUTES=true` plus System Administrator access.
- Current admin AI quality rule (2026-05-07): `Ask the AI` should become a grounded PATH workflow assistant, not a generic chatbot. Use `docs/planning/admin-ai-chatbot-knowledge-base-transformation.md` for the transformation runbook and `docs/planning/admin-ai-chatbot-coverage-register.md` as the whole-system execution map. Workflow-specific answers should come from verified PATH guidance cards, current code/help context, and approved examples; when no strong grounding exists, prefer a safe no-grounding response or clarifying question over invented controls. `/documentation` training content is a policy/process source, not proof of current UI behavior.
- Current admin AI inventory helper (2026-05-07): run `npm run ai:inventory -- --format=summary` or `npm run ai:inventory -- --format=markdown` before broad chatbot coverage updates. The helper is read-only and inventories routes, help-panel `aiContext` files, training sections, and docs groups; it does not prove guidance coverage by itself.
- Current admin AI eval rule (2026-05-07): high-risk chatbot knowledge cards need eval fixtures in `docs/testing/admin-ai-chatbot-eval-fixtures.json`; run `npm run ai:eval:check` after edits. The current check validates fixture shape only and does not call OpenRouter.
- Current admin AI diagnostics rule (2026-05-07): `ADMIN_AI_GUIDANCE_DEBUG=true` makes `/api/ai/chat` include `_guidance` match/no-match metadata for System Administrator callers only. Keep this debug metadata out of ordinary staff responses.
- Current notification configuration rule (updated 2026-04-27): notification templates, notification matrix rows, and shared sender/reply-to settings control who receives PATH email/bell alerts. Backend routes for `/api/templates`, `/api/notifications`, and `/api/config/notifications/email-settings` must stay restricted to System Administrator and NWAC Administrator roles; do not rely on frontend route hiding for these settings.
- Current secure-message notification event rule (updated 2026-05-02): secure-message notification routing is direction-specific. Public-portal applicant-to-staff messages emit `applicant_secure_message_received`; admin case-workspace staff-to-applicant messages emit `staff_secure_message_sent`. Do not reuse one generic `message_received` row for both directions, because staff-facing applicant-message templates and applicant-facing staff-message templates have different recipients and privacy expectations. `applicant_secure_message_received` is owner-scoped for both bell alerts and email: the assigned owner uses the row for their actual staff role, case watchers use the `ISET Coordinator` row, and System/NWAC/Regional settings must not broadcast this event to every user in those roles.
- Current PTMA data rule (updated 2026-07-12): the never-used PTMA management UI/API was retired locally under audit tranche R6b. Do not reintroduce `/ptma-management`, `/api/ptmas`, or model current case/application ownership around PTMA unless Bill explicitly revives the concept with a new business contract. The physical `ptma` table is deliberately retained pending separately authorized environment dependency/data proof. Active NWAC Hub Management uses the System Administrator-only `/api/hubs` surface, which filters every read/write to `type='Hub'`.
- Current user-directory rule (updated 2026-04-27): generic `/api/users` shared-table endpoints are retired because the legacy `user` table mixes applicant and historical local identities. Use `/api/admin/users` for scoped staff administration and `/api/admin/applicants` for applicant-account lookup; do not reintroduce broad shared-user list/detail APIs.
- Current public-portal browser auth rule (2026-04-25): for the deployed cookie-based portal, the HttpOnly `iset_access` cookie is the authoritative browser session token. Do not let a stale `Authorization: Bearer` header shadow that cookie on normal portal requests; bearer-only auth remains acceptable only when no portal cookie is present.
- Current public-portal runtime packaging rule (2026-04-25): `../ISET-intake/server.js` and `../ISET-intake/auth/cognitoAuth.js` are runtime-coupled. TEST once deployed the new applicant-account gate in `server.js` without the matching `auth/` helper because `deploy-portal-test.ps1` staged `auth/` but did not install it into `/opt/nwac/portal/auth`; that made real applicant-pool users fail with `applicant_account_required`. Future TEST portal deploy changes must ensure runtime directories such as `auth/`, `db/`, `notifications/`, `pdf/`, `public/`, and `src/` are both staged and copied on-instance, and post-deploy verification should grep `/opt/nwac/portal/auth/cognitoAuth.js` for the expected release code when auth behavior changed.
- Current TEST deployment rehearsal rule (2026-04-30): TEST should rehearse PROD user-facing maintenance behavior. Any TEST deploy that can restart app processes, make a surface unavailable, or expose transient `502 Bad Gateway` responses must set a scoped maintenance warning first or put the affected surface behind the ALB fixed-response maintenance page. TEST does not require PROD's `--yes` approval gate for ordinary app deploys, but it should not intentionally show raw 502s while down. TEST maintenance copy must identify the affected surface by the user-facing environment name, `Test and Training environment`, and state that Production is not affected.
- Current TEST public-portal idle-protection rule (reconfirmed 2026-07-30): keep `nwac-public-test.awentech.ca` behind the portal-scoped ALB fixed-response protection page whenever active public-portal testing is not underway. A TEST portal/deployment/acceptance thread may clear that rule only for the testing window and must restore it before closing the thread; do not let an all-surface fallback clear leave the idle TEST portal publicly forwarded. The TEST admin console remains independently routed. Check/set the durable state with `node scripts/path-maintenance-fallback.js status|set --env test --surfaces portal`.
- Current TEST cost-pruned topology rule (2026-06-08): TEST is intentionally right-sized for low-cost validation, not for a two-instance/three-AZ load-test rehearsal. The expected steady state is one `nwac-test-asg` app instance in private subnet `subnet-0806c0c17fb286162` / `ca-central-1d`, one NAT gateway in `ca-central-1d`, ALB attached to the two public subnets AWS requires for resilient load-balancer placement, and Aurora Serverless v2 in the existing DB subnet group. Do not hard-code old TEST instance IDs or require "both" targets in smokes; `path:deploy`, `test:db:refresh`, and TEST SQL helpers must auto-discover the current healthy SSM/ASG host(s). A TEST target-group smoke is green when every registered target is healthy, currently one admin target and one portal target.
- Current PROD-like TEST database refresh rule (2026-04-30): refresh TEST from a sanitized PROD database dump only, not by copying PROD uploads/supporting-document S3 objects. Preserve TEST Cognito pools; after restore run the identity overlay so approved TEST staff profiles, currently `bill@sillery.co.uk` and `program.admin@awentech.ca`, are rebound by explicit Cognito subject. Missing-object errors for historical PROD `iset_document` rows in TEST are acceptable unless a separate privacy-reviewed artifact-copy task is explicitly approved. See `docs/ops/environments/test-prod-migration-rehearsal.md`.
- Current public-portal staff-email collision rule (2026-04-25): a PROD portal sign-in by `bill@sillery.co.uk` returned `Login succeeded but session not established` because that email is also an active `staff_profiles` email. The cookies and tokens were valid, but `/api/me` correctly stayed Anonymous because `ensureLocalUser` now refuses staff-profile email fallback into applicant sessions. This is expected hardening behavior, not a general applicant-login failure; use a non-staff applicant account for PROD portal smoke tests and do not relax the staff-email conflict guard.
- Frontend auth state is centralized in `src/context/AuthContext.js`. Prefer `useAuth()` and `useCurrentUser()` over direct token/session reads in page or widget code.
- The OAuth callback is handled as a shell-less bootstrapping route. If sign-in behavior changes, inspect `src/App.js`, `src/pages/AuthCallback.js`, `src/auth/cognito.js`, and `src/auth/apiClient.js` together.
- Server middleware and admin-user routes no longer support `AUTH_PROVIDER=none`, mock admin users, or auth-disabled mutation fallbacks. If auth is misconfigured, fail explicitly instead of inventing local placeholder behavior.
- Raw debug/file-maintenance endpoints must stay behind explicit server-side enablement (for example `ENABLE_UNSAFE_ADMIN_DEBUG_ROUTES=true`) plus System Administrator access. Do not leave purge or direct file read/write helpers broadly reachable.
- Current WSL local-dev rule (updated 2026-07-08): active local coding now lives in the WSL2 Ubuntu filesystem under `/home/bill/ISET`. Open the saved multi-root workspace `/home/bill/ISET/path-dev-wsl.code-workspace`, which contains `admin-dashboard`, `ISET-intake`, `shared`, and `intacct-mock-service`; then run task `dev:all` for the full local stack. The old `/mnt/x/ISET/admin-dashboard` Windows-mounted path is not the preferred day-to-day coding/Codex workspace. See `docs/guides/wsl-local-development.md`.
- Current WSL DEV database rule (2026-05-07): local Node processes in Ubuntu should use `DB_HOST=172.26.176.1` to reach the Windows-hosted MySQL DEV service, not `localhost`. If WSL networking changes after reboot, refresh the host IP in the local `.env` files and `.vscode/tasks.json` together.
- Current WSL Puppeteer rule (2026-05-07): admin and public-portal PDF generation use Puppeteer's bundled Chrome, which needs Linux NSS/NSPR libraries in WSL. The current local workaround is `LD_LIBRARY_PATH=/home/bill/.local/chrome-deps/extract/usr/lib/x86_64-linux-gnu` in the admin and portal `.env` files plus both backend tasks in `dev:all`. See `docs/guides/wsl-local-development.md`.
- Current deploy checkout rule (updated 2026-07-11): the WSL2 Ubuntu checkout under `/home/bill/ISET` is the active source of truth for Codex TEST and PROD deployment work. Run `path:deploy` from `/home/bill/ISET/admin-dashboard`; the orchestrator builds/packages admin, portal, and shared artifacts from the WSL admin, portal, and shared trees and deploys them through WSL AWS CLI + SSM. `/home/bill/ISET/shared` is now a local Git repo with private GitHub remote `https://github.com/BrimborionInc/iset_shared.git`, and must be checked/committed/pushed like admin and portal before TEST/PROD deploys. Do not use stale `/mnt/x` or `X:\ISET` checkout guidance for active deploys. For local development, `npm run dev` in either app uses the shared Node launcher and `npm run dev:plan` performs a no-listener contract check. PROD app rollouts from WSL are active, but must follow the PROD runbook: scoped warning, ALB 503 fallback when restarts/routing changes can expose downtime, fallback clear for ELB target-health evaluation when needed, normal-routing smoke, then warning clear.
- Current ASG bootstrap rule (2026-07-27): `scripts/bootstrap/app-bootstrap.sh` installs pinned Node `20.20.2` from the official Node distribution with SHA-256 verification; do not restore the NodeSource `setup_20.x` dependency. TEST bootstraps from `bootstrap/admin-dashboard-latest.zip` and `bootstrap/portal-latest.zip`, which every TEST app deploy must refresh, and uses the shared runtime bundled with the admin artifact. Fresh-host TEST refresh `a14cf57b-d4eb-45b8-85fd-a26901412f77` proved exact July 27 provenance, both local readiness checks, Node 20, and PM2 zero-restart startup. PROD replacement `i-0b638e44eee6b27a6` required the same instance-local recovery after NodeSource returned `403`; the reduced PROD operator and instance roles cannot overwrite `s3://nwac-prod-artifacts/bootstrap/app-bootstrap.sh`, so promoting the tested bootstrap to that canonical key remains an explicit IAM-gated operations follow-up. Do not start another PROD ASG refresh until that key is updated or the same guarded recovery is ready under maintenance fallback.
- Local repo dev launcher note: legacy `start-dev.ps1` starts the Admin Backend with `ENABLE_UNSAFE_ADMIN_DEBUG_ROUTES=true` so Demo Controls like `Clear ISET test data` and `/api/dev/*` work in local dev. Manual admin-backend starts still need that env var set explicitly.
- Current Demo Controls rule: `Create Dummy Draft` should list portal-sign-in applicant users from the local `user` table, excluding staff identities, rather than relying only on client-linked applicant-account rows.
- Participant-name data rule (2026-05-14): fields named `preferredName`, `preferred_name`, or `preferred-name` are nicknames/display preferences, not a reliable full preferred/legal name. When documents, PDFs, letters, signatures, reports, search results, or formal applicant labels need a participant's full name, prefer explicit full-name fields or first/middle/last name parts from the application/submission/case context; use preferred-name fields only as a last-resort display fallback.
- Staff-name data rule (2026-06-24): do not infer a staff member's first name from an email address. For feedback reports, notes, audit summaries, notifications, or staff-facing answers, use `staff_profiles.display_name` / `staff_profiles.name` when verified, or refer to the person by email/their role if the profile name has not been checked.

## Development data policy (no legacy fallbacks)

- This system is in active development; do not assume legacy data constraints by default.
- After refactors, dev/test databases can be purged of old records that would otherwise require backward-compatibility handling.
- As a principle, avoid adding legacy fallback fields, compatibility branches, or dual-write/dual-read logic when they increase code or data complexity without current operational need.
- Prefer a clean target model and simple code paths. Only introduce compatibility handling when explicitly required and validated from current production constraints.
- Current entity-model planning note (updated 2026-05-08): the agreed target model is `one client -> one case -> many applications`, with every submitted application carrying both `client_id` and `case_id`. DEV has physically retired `iset_case.application_id` and made `iset_application.client_id` / `case_id` required; use `docs/planning/client-case-application-target-model.md` when reasoning about client/application/case changes. Application assessment is a known remaining model gap being handled through Option B containment: DEV/TEST now have additive `iset_application_assessment`, selected-application backend/frontend containment code, an `applicationDecisionLetters[application_id]` context scope for approval-letter/review workflow state, table/context backfill tooling, and repeat-application smoke tooling, while legacy `iset_case_assessment` remains case-scoped compatibility. DEV and TEST backfill/smoke evidence is recorded in `docs/planning/application-assessment-application-scope-migration-plan.md`; do not deploy this to PROD automatically from that success, and do not default to the full in-place `iset_case_assessment` ERM correction without revisiting its documented blast radius.
- Current duplicate-case consolidation rule (updated 2026-04-28): case creation must be prevented in code by resolving the client first and reusing that client's existing case; a database uniqueness constraint is only a backstop after endpoint behavior and historical consolidation are proven. The PROD-like TEST rehearsal found four duplicate client case groups and the prepared ops scripts are `sql/ops/privacy-erm-duplicate-case-consolidation-preview.sql` plus `sql/ops/privacy-erm-duplicate-case-consolidation-apply.sql`. The apply script records `iset_case_merge_audit`, repoints case-owned child rows, archives/detaches merged-away case rows by clearing `client_id`, and should be run only after preview blockers are resolved.
- Current duplicate applicant identity repair rule (updated 2026-05-04): when an imported client/case and a later public-portal application represent the same applicant, do not fuzzy-link automatically or move the portal application onto the imported record. Treat the authenticated public-portal application as canonical when it is the applicant-submitted truth, then use a snapshotted, guarded PROD repair to move staff work/documents forward and retire the imported duplicate. Start with `docs/guides/prod-duplicate-applicant-identity-merge.md`.
- Current case/application guardrail (updated 2026-04-27): do not create `iset_application` rows without client/case scope. The old `POST /api/applications/ingest-from-submission` endpoint is retired with `410`; use `POST /api/cases` or the current intake/manual-intake flows so `iset_application.client_id` and `case_id` are set at insert time.
- Current migration-planning note (2026-04-16): use `docs/planning/client-case-application-migration-plan.md` for the staged schema, data, and workflow migration plan covering test rehearsal, production cutover, historical merge, and constraint enforcement.
- Current privacy ERM cleanup plan (2026-04-26): use `docs/planning/privacy-erm-cleanup-grand-release-plan.md` for the DEV-first plan to remove unsafe identity-domain confusion, rebuild secure messaging around case/application scope, tighten document/message attachment relationships, retire dead experiments, and rehearse the eventual PROD cleanup migration. Use `docs/planning/privacy-erm-cleanup-progress.md` for stage progress and open next steps.
- Current status-overhaul planning note (2026-04-16): use `docs/planning/status-architecture-overhaul.md` for the target status model, including the split between lifecycle state, decision state, queue state, and intervention proposal review state.
- Current cutover dependency inventory (updated 2026-04-27): `iset_case.application_id` is retired in DEV; use `docs/planning/client-case-application-target-model.md` for the current model and treat the older cutover inventory as historical context only.

## High-value repo map

- Docs base path: active WSL checkout `/home/bill/ISET/admin-dashboard/docs`; treat older `/mnt/x/ISET/...` paths as stale unless a task explicitly asks to inspect an archive.
- WSL local-development guide: `docs/guides/wsl-local-development.md`
- AwenTech billing invoice generator / Codex Agent guide: `docs/guides/awentech-billing-agent.md`
- Project memory standing directive: `docs/meta/standing-directive.md`
- Documentation cleanup audit: `docs/meta/documentation-audit-2026-04-29.md`
- Documentation cleanup execution tracker: `docs/meta/documentation-cleanup-plan-2026-04-29.md`
- Cross-system engineering audit register: `docs/planning/engineering-audit-register.md`
- Consolidated engineering-audit application release boundary: `docs/planning/engineering-audit-release-wave-manifest.md`
- Codex thread/context recovery index: `docs/meta/codex-thread-index.md`
- Public portal documentation entry point: `../ISET-intake/docs/AGENTS.md`
- Applicant-account activation data model: `docs/data/applicant-account-activation.md`
- Client/case/application target model: `docs/planning/client-case-application-target-model.md`
- Client/case/application migration plan: `docs/planning/client-case-application-migration-plan.md`
- Application assessment application-scope migration plan: `docs/planning/application-assessment-application-scope-migration-plan.md`
- Regional Manager two-step review workflow: `docs/planning/rm-two-step-review-workflow.md`
- Staff record correction-controls proposal and progress: `docs/planning/staff-record-correction-controls-proposal-email.md`
- Regional Manager review job aid draft: `docs/guides/rm-two-step-review-user-guide.md`
- Client/case/application cutover dependency inventory: `docs/planning/client-case-application-cutover-dependency-inventory.md`
- Status architecture overhaul: `docs/planning/status-architecture-overhaul.md`
- Document checklist configuration guardrails: `docs/planning/document-checklist-config-widget.md`
- Current document checklist gate/type snapshot: `docs/data/document-checklist-current-configuration.md`
- Privacy/security systematic review: `docs/planning/privacy-security-systematic-review-2026-04-25.md`
- Public portal legacy fallback security review: `docs/planning/public-portal-legacy-fallback-security-review-2026-04-25.md`
- Privacy ERM cleanup grand release plan: `docs/planning/privacy-erm-cleanup-grand-release-plan.md`
- Privacy ERM cleanup progress log: `docs/planning/privacy-erm-cleanup-progress.md`
- DEV privacy ERM audit report: `docs/data/privacy-erm-audits/dev-20260426.md`
- Client-file import guide: `docs/guides/client-file-imports.md`
- PROD duplicate applicant identity merge guide: `docs/guides/prod-duplicate-applicant-identity-merge.md`
- Client Batch Import dashboard reference: `docs/dashboards/client-file-import-dashboard.md`
- Data and Results dashboard reference: `docs/dashboards/data-and-results-dashboard.md`
- Financial Reports dashboard reference: `docs/dashboards/financial-reports-dashboard.md`
- Sage Intacct interface fidelity audit: `docs/planning/intacct-interface-fidelity-audit.md`
- Sage Intacct mock dashboard design: `docs/planning/intacct-mock-dashboard-design.md`
- Sage Intacct local contract audit manifest: `docs/data/integrations/intacct-interface-fidelity-manifest.json`
- Applicant Watchlist dashboard reference: `docs/dashboards/applicant-watchlist-dashboard.md`
- Admin feedback reporting reference: `docs/features/admin-feedback-reporting.md`
- Homepage Metrics dashboard reference: `docs/dashboards/admin-home-metrics-widget.md`
- Query Editor dashboard reference: `docs/dashboards/query-editor-dashboard.md`
- Environment DB access from Codex/WSL: `docs/ops/agent-operational-access.md`
- PROD repair owner-notification log: `docs/ops/prod-repair-notification-log.md`
- Current PROD feedback batch repair tracker: `docs/ops/prod-feedback-batch-repair-tracker-20260705.md`
- Major workflow release-management gate: `docs/ops/deployments/major-workflow-release-management.md`
- PROD Aurora provisioned downsize runbook: `docs/ops/runbooks/prod-aurora-provisioned-downsize.md`
- PROD app instance right-size runbook: `docs/ops/runbooks/prod-app-instance-rightsize.md`
- PROD NAT gateway consolidation runbook: `docs/ops/runbooks/prod-nat-gateway-consolidation.md`
- Test DB access from Codex/WSL: `docs/guides/test-db-access-from-codex.md`
- Operational reporting workbook reference: `docs/data/NWAC - data info 2025-26.xlsx`
- NWAC staff training extract for PATH-aligned help content: `docs/training/TRAINING_MODULES_September_2025_extracted.md`
- Synthesia tutorial/training video production runbook: `docs/guides/synthesia-training-video-production.md`
- Admin AI chatbot knowledge-base transformation plan: `docs/planning/admin-ai-chatbot-knowledge-base-transformation.md`
- Admin AI chatbot coverage register: `docs/planning/admin-ai-chatbot-coverage-register.md`
- Admin AI source inventory helper: `scripts/admin-ai-inventory.js` / `npm run ai:inventory`
- Admin AI eval scaffold: `docs/testing/admin-ai-chatbot-evals.md`, `docs/testing/admin-ai-chatbot-eval-fixtures.json`, `scripts/admin-ai-eval-fixtures-check.js`, `npm run ai:eval:check`
- Browser workflow smoke automation guide: `docs/testing/browser-workflow-smoke-automation.md`
- Authoritative release qualification and PROD authorization runbook: `docs/ops/deployments/release-qualification-runbook.md`
- Machine release coverage inventory: `docs/testing/release-coverage-inventory.json`
- Case Assignment dashboard local browser smoke: `npm run smoke:case-assignment:browser`
- Home Overdue queue local browser smoke: `npm run smoke:home-overdue:browser`
- Manual Application Intake local browser smoke: `npm run smoke:manual-intake:browser`
- Manage Components / Intake Steps local browser smoke: `npm run smoke:manage-components:browser`
- Modify Intake Step editor local browser smoke: `npm run smoke:modify-component:browser`
- Application Overview local browser smoke: `npm run smoke:application-overview:browser`
- Application Workspace local browser smoke: `npm run smoke:application-workspace:browser`
- Application Assessment workflow local browser smoke: `npm run smoke:application-assessment:workflow:browser`
- Admin AI guidance feature reference: `docs/features/admin-ai-guidance.md`
- Admin intake preview renderer: `apps/web/src/features/intake/ComponentRenderer.tsx`
- Public portal renderer (legacy deployed repo): `../ISET-intake/src/renderer/renderers.js`
- Current TEST/PROD public portal deploy target: `../ISET-intake`. The PATH deploy orchestrator sets `PORTAL_ROOT = ../ISET-intake`; WSL-native deploys package the portal tree directly in `scripts/path-deploy.js` and install the artifact under `/opt/nwac/portal` with a sibling link for `/opt/nwac/ISET-intake`.
- Parked/experimental public portal rebuild: `../iset-public-portal/apps/web/src/features/intake/IntakeRunner.tsx`, `../iset-public-portal/apps/api/src/server.ts`. Do not treat this repo as TEST/PROD behavior unless a thread explicitly changes the deploy path.
- Help panel content: `src/helpPanelContents/*`
- Admin TEST deploy implementation: `scripts/path-deploy.js` WSL-native TEST app deploy helpers; legacy reference script `scripts/deploy-admin-test.ps1`
- Portal TEST deploy implementation: `scripts/path-deploy.js` WSL-native TEST app deploy helpers; legacy reference script `../ISET-intake/scripts/deploy-portal-test.ps1`
- PATH deploy orchestrator guide: `docs/ops/deployments/path-deploy-orchestrator.md`
- PATH deployment quick guide: `docs/ops/deployments/deployment-quick-guide.md`
- PATH deploy orchestrator CLI: `scripts/path-deploy.js`
- Test DB SQL helper for Codex/WSL: `bash scripts/run-test-sql-via-ssm.sh`
- Staff Cognito legacy attribute cleanup guide: `docs/guides/staff-cognito-legacy-attribute-cleanup.md`
- Prod deploy guide: `docs/ops/deployments/prod-deployment-guide.md`
- Prod environment guide: `docs/ops/environments/prod-env-guide.md`
- Test deployment notes: `docs/ops/deployments/deploy-test-notes.md`
- Test DB refresh planning note: `docs/ops/environments/test-env-db-refresh.md`
- TEST prod-like migration rehearsal: `docs/ops/environments/test-prod-migration-rehearsal.md`
- Privacy ERM grand cleanup rehearsal: `docs/ops/environments/privacy-erm-grand-cleanup-rehearsal.md`
- TEST DB refresh CLI: `scripts/path-test-db-refresh.js`
- TEST DB restore helper: `bash scripts/run-test-db-restore-via-ssm.sh`
- Legacy reference only: `scripts/deploy-test-db.ps1` (the npm alias `deploy:test-db` now routes to `scripts/path-test-db-refresh.js run`)
- Prod DB SQL helper for Codex/WSL: `bash scripts/run-prod-sql-via-ssm.sh`
- Prod DB SQL helper for Windows/PowerShell: `scripts/run-prod-sql.ps1`
- PATH data promotion catalog: `docs/ops/deployments/data-promotion-catalog.md`
- PATH data sync CLI: `scripts/path-data-sync.js`
- Retired DEV legacy document experiment table: `sql/migrations/20260427_0019_retire_zzz_legacy_documents_table.sql`
- Privacy ERM smoke checker: `scripts/privacy-erm-smoke.js` / `npm run smoke:privacy-erm`
- Privacy route-scope smoke checker: `scripts/privacy-route-scope-smoke.js` / `npm run smoke:privacy-routes`
- Privacy live route-denial smoke checker: `scripts/privacy-route-denial-smoke.js` / `npm run smoke:privacy-denials`

## Documentation gateway

- Start here for current orientation, then go deeper into the docs below rather than relying on planning notes alone.
- Docs directory index: `docs/README.md`
- Project memory maintenance rules: `docs/meta/standing-directive.md`
- Documentation cleanup inventory: `docs/meta/documentation-audit-2026-04-29.md`
- Documentation cleanup execution tracker: `docs/meta/documentation-cleanup-plan-2026-04-29.md`
- Cross-thread context recovery index: `docs/meta/codex-thread-index.md`
- Public portal documentation entry point: `../ISET-intake/docs/AGENTS.md`
- Planning directory gate: `docs/planning/README.md`
- Change-request directory gate: `docs/change-requests/README.md`
- Data/reference directory gate: `docs/data/README.md`
- Operations directory gate: `docs/ops/README.md`
- Guides directory gate: `docs/guides/README.md`
- Requirements directory gate: `docs/requirements/README.md`
- Training directory gate: `docs/training/README.md`
- Live dashboard behavior: `docs/dashboards/*`
- Import and data-backload constraints: `docs/guides/client-file-imports.md`
- Client/case/application target model: `docs/planning/client-case-application-target-model.md`
- Client/case/application migration plan: `docs/planning/client-case-application-migration-plan.md`
- Status architecture overhaul: `docs/planning/status-architecture-overhaul.md`
- Client Batch Import dashboard: `docs/dashboards/client-file-import-dashboard.md`
- Data and Results dashboard: `docs/dashboards/data-and-results-dashboard.md`
- Widget-level docs index: `docs/widgets/admin/README.md`
- Workflow-level docs index: `docs/workflows/admin/README.md`
- Project structure / architecture map: `docs/meta/project-map.md`
- Historical plans and design notes: `docs/planning/*` (useful for intent/history, but verify against code before treating as current behavior)
- Historical change-request source material: `docs/change-requests/*` (useful for provenance, not current behavior unless a current doc says so)

## Subsystem pointers

Use these canonical docs for subsystem-specific detail instead of expanding this entry point:

- Query Editor: `docs/dashboards/query-editor-dashboard.md`
- Applicant Watchlist: `docs/dashboards/applicant-watchlist-dashboard.md`
- Admin feedback reporting and PROD feedback triage: `docs/features/admin-feedback-reporting.md`
- PATH deploy control plane: `docs/ops/deployments/deployment-quick-guide.md`, `docs/ops/deployments/path-deploy-orchestrator.md`, and `docs/ops/agent-operational-access.md`
- Client Batch Import: `docs/dashboards/client-file-import-dashboard.md` and `docs/guides/client-file-imports.md`
- Homepage/work queues/metrics/approvals: `docs/features/landing-page.md`, `docs/dashboards/admin-home-metrics-widget.md`, `docs/dashboards/admin-home-approvals-items-widget.md`, and `docs/dashboards/admin-home-system-admin-homepage.md`
- Operational reporting: `docs/dashboards/data-and-results-dashboard.md`, `docs/dashboards/financial-reports-dashboard.md`, and `docs/data/regional-snapshot-reporting.md`
- Finance/payments/salaries: `docs/features/payments-module.md`, `docs/planning/payments-implementation-review-2026-05-11.md`, `docs/planning/payments-target-operating-model-2026-05-11.md`, `docs/planning/payments-transformation-plan-2026-05-11.md`, `docs/testing/payments-workflow-automation.md`, `docs/dashboards/financial-reports-dashboard.md`, and `docs/data/finance-regional-salaries.md`
- Applicant account activation: `docs/data/applicant-account-activation.md` and `docs/features/user-management.md`

When a subsystem doc disagrees with current code, migrations, package scripts, or live environment evidence, fix the subsystem doc instead of copying more detail back into this file.

## Known pitfalls

- Program Admin "Unassigned Applications" must use `/api/applications`, not `/api/cases`, or applicant names are missing.
- NWAC Administrator homepage `All Cases` must use `/api/dashboard/all-client-cases`, not the generic `/api/cases` list, because the homepage queue is case-based and must exclude only `closed` and `archived` while keeping global scope.
- Regional Manager homepage `Clients in My Region` must use `/api/dashboard/regional-client-cases`, not the generic `/api/cases` list, because the homepage queue is case-based and must respect owner-region/portfolio-region scope with only `closed` and `archived` excluded.
- Schema no longer has `iset_case.application_id` in DEV, and `iset_application.client_id` / `case_id` are required. Case create/update/list flows support application-less client files, while application-backed context is derived from `iset_application.case_id`.
- Supporting Documents has a case-based mode for application-less client files: it reads from `GET /api/cases/:id/documents`, uploads through `POST /api/cases/:id/documents/upload`, hides the checklist tab, and allows `client`, `case`, `action_plan`, plus application-type document categories. Application-type uploads fall back to action-plan or case storage only when no real application context exists; do not create a fake application record.
- Supporting Documents manual uploads must not drop case scope when resolving application/action-plan/client document categories. A failed `chk_iset_document_manual_upload_scope` insert means the backend lost required scope; the constraint is the privacy guard, not the thing to relax.
- `iset_document` is not the storage model for global finance exports. If a generated artifact has no owning case/client, keep it out of Supporting Documents and store only the object metadata needed by the finance/export workflow.
- Current document-type review artifact for NWAC requirement alignment lives in `docs/planning/path-document-type-canonical-review.md` with companion CSVs. Treat the seeded `document_type` catalog plus that issue register as the current implementation baseline; do not assume older planning docs or checklist notes are still authoritative, especially around `evidence_income` / `evidence_expense` vs `financial_records` / `financial_evidence` and around scope assignments for payment- and case-related document types.
- Supporting Documents and other shared `View document` actions no longer rely on the browser or Microsoft 365 to render raw Word files. `GET /api/documents/:id/presign-download` now detects `.doc` / `.docx`, generates or reuses a cached internal preview under object storage prefix `WORD_PREVIEW_OBJECT_PREFIX` (default `previews/word`), and returns a presigned URL for that preview instead of the original Office object. The preferred artifact is PDF when server-side rendering is available; the fallback artifact is a self-contained HTML preview when the host cannot launch Chromium. The preview artifact is cached in object storage only and must not be inserted as a separate `iset_document` row.
- Supporting Documents now also has a separate original-file download path for canonical admin roles only. The inline `Download` action is available only to `System Administrator` / `NWAC Administrator` users (Cognito groups `System_Administrator` / `NWAC_Administrator`), requires a privacy warning confirmation, and calls `GET /api/documents/:id/presign-download?mode=original`, which bypasses Word-preview substitution and forces attachment download of the original stored object.
- Secure Messaging now supports application-less client-file cases when the case is linked to a participant PATH account; imported cases without a participant account can still manage documents, plans, and interventions, but messaging remains unavailable until that account exists.
- Financial Overview secure-message requests use workflow `52` (`Financial Overview`, `document_type='financial_overview'`) but the runtime signing schema is now editable. Staff choose `Pre-fill with PATH data` or `Send blank form` in the secure-message compose panel; sending still creates a case-scoped Financial Overview version and withdraws unsigned prior Financial Overview sends. The public portal participant form is intentionally limited to monthly household income fields, monthly household expense fields, the "other income/source" and "other expenses/list" text fields, and signature. Do not ask the participant for staff/program context such as `requested-supports`, top-up amount, childcare funding status, transportation category/mileage, or student loan/grant details in this form. Prefilled signing-request initial values are filtered to the participant-editable income/expense keys so hidden support-request values are not resubmitted by the portal. On submission, PATH writes the submitted income/expense values back to `iset_case.case_context_json.applicationAnswers` plus Participant Details convenience fields, updates the version snapshot, and stores an official signed Financial Overview PDF; existing staff/application context can still flow into the PDF snapshot from current case/application data. The original application payload is not updated. Assessment submission/resubmission must automatically preserve every case with a version-managed Financial Overview and must never archive a Financial Overview document linked through `funding_overview_version_documents`, carrying `metadata.funding_overview_version_id`, or linked to a signing request. Only the legacy assessment-generated unversioned overview may be replaced by assessment submission. Earlier TEST/PROD release history for the read-only signature flow remains in `docs/requirements/SentToApplicant/README.md` and `docs/planning/cfa-versioning-spec.md`; verify current code before changing this flow.
- Applicant-facing secure-message/document-reminder emails only render clickable portal links when the public-portal runtime can resolve a portal URL. Preferred envs are `APPLICANT_PORTAL_URL` / `APPLICANT_PORTAL_BASE`; runtime fallback also checks `PUBLIC_PORTAL_BASE_URL`, `REACT_APP_PORTAL_URL`, `REACT_APP_API_BASE_URL`, and `PORTAL_DOMAIN`. If none resolve in production, `[link url="{portal_dashboard_url}"]...[/link]` degrades to plain text with no anchor.
- Case Header exposes explicit historical-entry quick actions on non-archived cases for System Administrator, NWAC Administrator, and Regional Manager: `Add existing action plan`, `Add existing intervention`, and `Upload existing documents`. These actions are no longer limited to application-less cases; they require a historical-record warning and backend `backloadMode` / `entryMode=backload` role enforcement. See `docs/planning/case-workspace-quick-actions.md` before changing the gate.
- Admin-side manual supporting-document uploads now accept Word files (`.doc`, `.docx`) in addition to PDF and common image formats.
- Current backload integrity rule: `Add existing intervention` must preserve real plan/intervention lifecycle state. Archived plans are read-only, closed plans can receive only `completed`/`cancelled` interventions, `in_progress`/`suspended` interventions require an active plan, closed backloaded interventions must carry an end date, and `manual_backload` interventions must stay silent on later edit/close flows instead of auto-creating payment-packet, finance-email, or CFA side effects. New `manual_backload` interventions stamp `reviewed_at` from the entered start date for approval-date financial reporting while keeping `created_at` as the PATH entry audit date. Their `actual amount` now writes historical posted finance ledger history only; unpaid remainder should move into a new live intervention. Narrow exception: if staff explicitly complete an approved intervention proposal/revision follow-up by sending the client approval/funding revision letter, the secure-message send path may create the required action-plan-scoped CFA draft/signing artifact instead of failing because the original intervention was manually backloaded.
- Current repeat-application CFA rule: application-backed approval materialization is isolated by `iset_case_action_plan.application_id`; an older application-less or different-application plan must not block the selected application's plan or supply its CFA interventions. Plan-based CFA snapshots retain the plan's application lineage, every newly materialized application plan creates the next CFA version, and portal signing must finalize/link that exact version only after case/application/version/document scope checks. See `docs/ops/feedback-173-cfa-repeat-application-hotfix.md` before changing this workflow.
- Coordinator-facing Case Workspace help panels and embedded AI guidance should explicitly support historical backload work. When relevant, explain the Case Header add-existing actions, the silent historical nature of those actions, the intervention lifecycle guardrails, and document scoping for application-backed versus application-less cases.
- `src/widgets/CaseCalendarWidget.js` is shared by the case workspace and application workspace. Treat date-only values (`YYYY-MM-DD`) as local Canadian calendar dates; UTC-based parsing/weekday anchors will shift headers or event days backward.
- Tutorial updates must be validated end-to-end (including `Next` progression on every step) so no step dead-ends due to missing hotspots.
- If a change introduces new deployment artifacts, update the relevant deploy script(s) so files are staged.

## Documentation maintenance

- Treat documentation maintenance as part of task completion, not optional cleanup. Keep docs optimized for future agents who lack prior chat history.
- Before changing docs, distinguish source-of-truth artifacts from guidance: code, schema, migrations, package scripts, config, tests, and live database checks outrank narrative docs.
- Do not let stale docs accumulate. If a doc is contradicted by code or a newer canonical note, revise it, merge it, mark it historical, or delete it.
- Opportunistically clean up Windows-to-WSL migration leftovers whenever a task passes through them. Current operational guidance should point at the WSL/Linux checkout under `/home/bill/ISET`; old `X:\ISET`, `/mnt/x/ISET`, Windows PowerShell, or Windows client instructions should remain only when explicitly labeled as legacy/reference or still required for a verified workflow.
- Keep `docs/AGENTS.md` as an entry point and map. Avoid turning it into a transcript or dumping ground.
- Use `docs/meta/standing-directive.md` for the project-memory maintenance contract and `docs/meta/documentation-audit-2026-04-29.md` for the current cleanup strategy.
- Update `docs/meta/changelog.md` for user-visible or operational changes.
- Record major structural doc reorganizations in `docs/meta/project-map.md`.
- Maintain `docs/meta/next-release-notes-log.md` as a standing running log for the next Landing Page release-notes update.
- Keep the bilingual draft bullet sections in `docs/meta/next-release-notes-log.md` current before deploy. The build now fails if the required fallback `What's New` / `Nouveautes` draft sections are missing or empty.
- Landing-page release notes must use the standard public shape: `What changed`, `Known Bugs`, and `What's Coming`. Do not publish an `Earlier changes` section. Under `What changed`, keep three expandable release-package groups for the three most recent release packages, newest first, using `What Changed Packages (draft - EN)` and `Lots de changements (brouillon - FR)` in the release-notes log. When a release ships, add it as the first package and retire the oldest fourth package.
- Landing-page release-note identity is now tied to the deployed build release ID/date rather than a separate hardcoded semver string in `src/pages/LandingPage.jsx`.
- When drafting user-facing hotfix notes or Landing Page "What's New" bullets, keep them short and outcome-first. Prefer plain openers like `Fixed a bug...` or `Made a change...`, and do not mention internal bug/CR IDs, reporter names, or that the item came from a complaint.
- Current user-requested weekly-summary workflow (2026-04-17): when Bill asks for a bullet summary of fixes/changes from the last week that will interest users, do not limit the review to bug reports, CRs, or the feedback queue. Review `docs/meta/next-release-notes-log.md` and also inspect `docs/meta/codex-thread-index.md` for user-visible work from recent threads that was not driven by a bug report or change request, then return a short user-facing bullet list of the most relevant changes.
- If future threads need the origin of this release-notes pattern, open `docs/meta/codex-thread-index.md` and find the Task History entry `Investigate edit digital forms`; that thread captured the durable rule for neutral, outcome-first PATH notes and hotfix bullets.
- Keep credentials and environment-specific secrets out of docs.
- The thread index exists to recover the exact item in Codex Task History. The Task History label is the point of the index; topic labels are only fallback search aids.
- When updating `docs/meta/codex-thread-index.md` for the current thread, record the exact Codex Task History label verbatim. If the user gives you the label, store that exact string even if the thread later covers additional topics.
- Do not substitute a descriptive topic label for the Task History label. A human-written summary heading is not an acceptable replacement for the recovery key.
- Do not create or finalize a new thread-index entry for the current thread unless the exact Task History label is known. If it is not visible in the current session, ask the user for it first.
- Only use `exact original task title not preserved` for true historical backfills where the original Task History label cannot be recovered. Those entries should be treated as incomplete recovery records, not first-class indexed threads.
- When refactoring dashboards/widgets, update matching help panel content (`src/helpPanelContents/*`) and related `aiContext` strings in the same change.
- Coordinator-facing PATH help content should bias toward staff workflow, compliance reminders, timelines, documentation expectations, and next-step coaching instead of frontend implementation detail.
- When AI help output quality is part of the task, validate both layers: the page/widget `aiContext` in `src/helpPanelContents/*` and the shared help-chat system prompt in `src/AppContent.js`.
- For Case Workspace backload flows, validate both layers against imported/application-less use cases: page/widget help should mention historical action plans, interventions, and supporting documents, and the shared AI prompt should reinforce the same silent-workflow and no-fake-application rules.

## Database documentation and access

- Start at `docs/data/database-documentation.md` for DB index and cross-app pointers.
- When tables/columns/relationships change, update the index and linked domain docs.
- Operational access details now live in `docs/ops/agent-operational-access.md`; read that doc before DB, TEST, PROD, or AWS profile work.
- Regenerate schema dump after schema changes, but do not commit dump files: `npm run dump:dev-schema`.
- Never run destructive broad SQL or cloud operations unless explicitly requested and verified against the current environment.
- If DB access fails from WSL, prefer read-only analysis from generated schema/docs until the access path is fixed.

## Cross-app boundaries

- Admin dashboard and public portal are separate apps/repos.
- Do not copy env files or code between apps without explicit approval.
- Confirm which renderer you are editing before making intake-rendering changes.
- PATH-generated SES sender settings are now shared through `iset_runtime_config` (`scope='notifications'`, `k='path.email'`) and edited from the admin Notification Settings widget; keep admin and portal mailers aligned to that runtime sender email / sender name / reply-to payload rather than hardcoded app-local defaults.
- Current durable event-delivery rule (2026-07-11): new canonical events explicitly use `queued` or `suppressed` notification mode; historical rows remain `legacy`. Shared `iset_event_delivery` workers own fan-out by catalogue source (`portal` vs admin), retry known failures, and quarantine uncertain email outcomes as `ambiguous` for reasoned System Administrator replay. Preserve event/channel/audience uniqueness and deploy shared + admin + portal + migration `20260711_0003_add_durable_event_delivery.sql` together. Read `docs/runtime/events-tracking-overhaul.md` and `docs/runtime/reminders-events.md` before changing event, notification, or reminder-worker delivery semantics.
- Current AI runtime rule (2026-07-11): `iset_runtime_config` keys `public/ai.model`, `public/ai.params`, and `public/ai.fallbacks` are the durable shared overrides; deployment environment values are defaults only. Admin and portal must resolve them through `../shared/ai/runtimeConfig.js`, validate models at use time, preserve omitted parameter values, and never rewrite a deployed `.env` or create a process-local model override from the configuration UI. See `docs/data/runtime-config/ai-runtime-config.md`.
- Staff bell alerts are fetched from `/api/me/notifications`, backed by `iset_internal_notification`, and rendered in the app shell `Flashbar` from `src/AppContent.js`. Direct staff audiences are typed through `audience_actor_type='staff_profile'` and `audience_staff_profile_id`; the old physical `audience_user_id` shadow is retired in DEV by `20260427_0011`.
- Current admin-shell notification rule: `src/AppContent.js` mounts the global `AppLayout.notifications` rail, while `src/layouts/SideNavigation.js` footer item `Notifications` only triggers a manual refresh of that rail. The `Newest` / `Urgency` sort toggle is intentionally hidden while the notification stack is collapsed and appears only after staff expand it. There is no existing hot-push/polling loop for service-wide warnings.
- Current bell-alert timestamp rule: the heading uses `delivered_at` when populated and otherwise `created_at`, formatted in the viewer browser's IANA timezone via `Intl.DateTimeFormat().resolvedOptions().timeZone`, with `America/Toronto` as the fallback display timezone.
- Current timezone limitation: no staff/applicant timezone preference is persisted in the database yet. Treat the stored notification row timestamp as the audit/source-of-truth value, and do not infer a person's timezone from province/region alone.
- Reminder due/overdue classification is intentionally not viewer-local. Reminder events, Case Calendar reminder badges, and note follow-up badges all use the PATH business day in `America/Toronto`, ignoring time-of-day when deciding whether a reminder is due today or overdue.
- Current approval-routing rule: Regional Managers do not record final application or intervention proposal/revision decisions; Decision Makers record those decisions through the `NWAC Administrator` system role. System Administrator access is technical support/superuser behavior only and is not part of the business workflow model. Application assessments, new intervention proposals, and intervention amendments now have a first-pass Regional Manager review/sign-off stage behind the runtime toggle and appear to RM in `Pending Review` when staged to RM. See `docs/planning/rm-two-step-review-workflow.md` and keep frontend gating, backend enforcement, queues, generated PDFs, and staff guidance aligned.
- Current public-portal banner rule: portal-wide service notices belong in `../ISET-intake/src/App.js` because that file owns the shared shell (`Header` + routes). Existing GOV.UK notification banners in `../ISET-intake/src/pages/userDashboard.js` and `../ISET-intake/src/pages/Welcome.js` are page-scoped patterns only, not a global announcement rail.
