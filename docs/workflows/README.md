# Workflow Docs

Status: workflow-level reference docs.

Workflow docs live mainly under `admin/` and describe staff workflow surfaces and integration points. Verify against current routes, widgets, backend endpoints, and runtime workflow config before changing behavior.

Use these files for orientation and scope, not as the sole source of current implementation truth.
