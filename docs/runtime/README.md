# Runtime Docs

Status: runtime/workflow/event design notes, mixed current and historical.

Use these docs for workflow publication, normalization, renderer, reminder, and event-system context. Verify against `src/workflows/`, `src/utils/intakeConditionalVisibility.js`, `src/utils/manualIntakeRuntime.js`, `isetadminserver.js`, and the current public portal repo before changing behavior.

When touching runtime docs, mark whether the file is current implementation, implementation plan, historical, or needs verification.
