# Widget Docs

Status: widget-level reference docs.

Widget docs live mainly under `admin/` and describe expected behavior, data contracts, and dashboard composition. Verify against current widget source in `src/widgets/` and `src/pages/**/widgets/` before acting.

Use widget docs as orientation, not proof, unless recently verified against source.
