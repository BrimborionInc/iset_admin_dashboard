# Components Docs

Status: component contracts, patterns, and custom component infrastructure notes.

Use these docs for authoring components and workflow-rendering patterns, but verify against `src/component-lib/`, `src/widgets/StepPropertiesWidget.js`, `src/server/componentRenderRegistry.js`, `src/server-macros/`, and current portal renderer behavior.

Component pattern docs may describe intended authoring/rendering behavior rather than complete runtime parity.
