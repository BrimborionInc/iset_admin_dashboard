# Changelog - Admin Dashboard

Format: YYYY-MM-DD - Category: Short description

## 2026-08-06
- Planning/nForm: Reconfirmed that Codex owns the extraction workstream, implementation sequence, progress ledger, verification, and persistent resumable checkpoints. Bill supplies business objectives and should be consulted on genuine product uncertainty, but is not expected to track or manage the engineering work.
- Planning/nForm: Established a hard PATH-protection boundary: the operational PATH code, data, configuration, infrastructure, deployments, and Git history are read-only reference material during extraction. nForm is built additively in `/home/bill/nForm`; any future PATH migration requires a separately authorized migration programme.
- Planning/nForm: Created the private canonical GitHub repository `BrimborionInc/nForm`, connected local `main` to `origin/main`, and pushed the complete clean history through checkpoint `0d8af1a`. Codex Cloud environment configuration and isolation validation are the next setup step; no PATH or AWS resource was changed.

## 2026-08-05
- Ops/Data Integrity/PROD: Completed the guarded exceptional recovery for feedback `#178`. Application `61` / Case `138` is now `pending_approval / pending_decision`, and review workflow `17` is back at `rm_review` for Derry Yellowfly to use the normal `Return to Coordinator` action before Danielle Burdett corrects and resubmits the assessment and Madison Coppola records a renewed decision. All nine prior workflow events remain intact and a System Administrator recovery event/note was appended. Draft action plan `166`, generated interventions `351-353`, unsigned CFA version `37`, and generated CFA document `9195` were withdrawn from current use; the unsubmitted ESDC seed was removed. Preflight proved there were no proposals, intervention-document links, payments, finance transactions, or signing requests to preserve. Aurora snapshot `path-prod-feedback-178-recovery-20260805-1606` is available; guarded apply command `406dc222-15b2-4788-9836-1ead3733cb97` and independent verification command `d70cb67b-98df-4fad-9d8c-272ed2434968` passed. Feedback `#178` remains `in_progress` until the normal staff correction/reapproval workflow and replacement artifacts are complete.
- Safety/Database: After an agent selected a similarly named column from the wrong live table during read-only PROD investigation, stopped ordinary work, proved the failed statement made no change, and restarted from live metadata. Strengthened `AGENTS.md`, `docs/AGENTS.md`, and the standing directive with a second prominent gate requiring the finished SQL to be checked identifier by identifier against exact live metadata, one table at a time; all PROD repair SQL must now be reviewed under `sql/ops/` rather than improvised in the shell.
- Ops/CFA/PROD: Deployed portal-only hotfix `20260805-cfa-signing-hotfix-r3` from exact portal commit `8dcae3d6fb3c91c42e678c12bca99a194aadb019` under TEST evidence `9397487554aa7c0b97930241d1f500049bc09a21fea2271df2bd1373021ae9ca`. A five-minute portal warning preceded the ALB fallback; the production bundle was built before the warning and installed through successful ASG refresh `797a0130-2c6d-4b79-9a96-67362bf93a0e` on replacement `i-057ea680c05fff947`. Deployed provenance and `server.js` SHA-256 matched the qualified source, both target groups were healthy, all three public `/readyz` checks returned `200`, the post-restart portal error log was empty, normal forwarding was restored, and the maintenance-announcement row count returned to zero. Requests `152`, `159`, and `162` remain unchanged, unclaimed, and safely resumable; no CFA/request/document/participant repair or replacement was performed. Feedback `#177` is `in_progress` pending participant retry and completed signing-chain verification. No schema, data, admin/shared artifact, workflow/runtime promotion, provider operation, or synthetic PROD signing ran. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260805-cfa-signing-hotfix-r3--2026-08-05T14-37-14-470Z.json`.
- Fix/CFA/DEV/TEST: Prepared and qualified clean portal hotfix `20260805-cfa-signing-hotfix-r3` for feedback `#177`. The production signing route no longer reads nonexistent `signing_request.application_id`; it carries the application already resolved from the linked message/case into the signed document, exact CFA version finalizer, materialization and audit event. Added a real Express-route regression whose schema-faithful signing row omits that field plus authenticated real-DEV and deployed-TEST signing smokes covering live metadata/DDL proof, canonical signed PDF/version lineage, idempotency and zero-residue cleanup. The first TEST acceptance correctly returned `NO-GO` because the partial-deployment postflight compared an untouched admin artifact to the new candidate and the two-step fixture's fixed dates had aged behind the active plan start; both qualification-harness defects were corrected before the final clean r3 DEV `GO` (16/16) and deployed TEST `GO` (12/12).
- Ops/Feedback/PROD: Triaged the two newly submitted live feedback reports. Report `#177` is `critical / triaging` after live request, database, portal-log and source inspection proved that the corrected participant name was accepted but versioned Client Funding Agreement completion fails systemically with `cfa_signing_scope_invalid`; three active CFA requests have incomplete attempted completions. Report `#178` is `high / triaging`: the approved packet has not been sent, but the finally decided assessment has no audit-preserving Regional Manager return-to-submitter transition for correcting approved funding. Both reports have matching status-history and internal-note rows, and no feedback reports remain `submitted`. No case, application, assessment, plan, intervention, document, signing, runtime, provider or deployed-code mutation was made.
- Planning/Correction controls: Recorded NWAC management direction that post-submission assessment or approved-funding corrections should move through a Regional Manager return to the submitter, preserving the return reason, edits, resubmission and renewed decision. System Administrator status/data intervention remains an exceptional, disfavoured recovery path because it does not create the normal business audit trail. A missing post-decision return transition is a workflow gap, not justification for direct coordinator editing of a finally decided assessment.

## 2026-08-03
- Safety/GitHub/Terraform: Disabled the legacy `Terraform Infra` workflow directly in GitHub and removed `.github/workflows/terraform.yml` after the 2026-08-02 canonical-branch reconciliation exposed its unsafe automatic PROD apply trigger. The failed run stopped at AWS OIDC credential configuration; Terraform setup, init, plan and apply were skipped. A cross-repository GitHub and real-PROD AWS audit found no other Actions run, webhook, cleanup-time deployment, artifact upload, SSM command, Auto Scaling refresh, RDS control-plane operation, or database/code deployment caused by the cleanup. Added the canonical source-control operations guide and a standing pre-push requirement to inspect the complete pushed range plus workflows, integrations and webhooks. The separately authorized `20260801-returned-assessment-edit` admin rollout completed at 08:17 EDT, about one hour before cleanup began, and is not part of the incident.

## 2026-08-02
- Ops/Source control: Reconciled the active WSL repositories onto one canonical GitHub `main` branch per repository. Admin `main` now contains the complete linear deployed history through `20260801-returned-assessment-edit` plus the retained late-July/early-August repair artifacts; portal `main` now contains the deployed signed-CFA completion fix; shared `main` was already current and received the standing database-safety instruction. Obsolete remote `backup-main`, hotfix and release branches were deleted only after their tips were proven ancestors of `main`; obsolete local branches and generated-build stashes were removed. Release worktrees that still hold ignored qualification/deployment evidence remain detached and are not competing source branches. Three unrelated business-development procurement PDFs were moved outside the repository to `/home/bill/ISET/business-development/grants-management-opportunity/`.
- Ops/PROD/Release: Deployed admin-only release `20260801-returned-assessment-edit` from exact clean admin commit `6bd87e02f7d7456bfa5b5441884597a5b3b9efc1`, retaining the already-live portal `5afed494fd06808c2a3d009e92ba389cd3218fcc` and shared `e8dc303d8ecc057e52509b00854106f230823d96`. The complete deployed TEST gate passed all 11 checks under evidence `f51d72c13f3c0c9c0de66029460f36de5b2d8405547074454351c47f0078a9a5`; PROD preflight passed 93 suites / 427 tests, quiet lint, privacy-route checks and the production build. Immutable admin artifact SHA-256 `01c9022f9802dfd81ce9d18f189e3c23c69021051c7978bd5f45ba0cad1d5e4d` was installed by successful ASG refresh `e28ee2bb-9af1-4c95-8a54-f085d21da9d5` on replacement `i-0f13368e36c9da21f`. Exact provenance and affected source hashes matched, both services were online with zero restarts, the two live affected workflows remained `returned_to_submitter` with their original submitter identities, all three public `/readyz` checks returned `200`, all host rules returned to normal forwarding, and the maintenance announcement count returned to zero. Feedback `#168` and `#170` were resolved immediately after technical verification. No schema, application/workflow data, runtime configuration, portal/shared artifact, email, AI, Finance, Intacct/Sage or other provider operation was included. Manifest: `/home/bill/ISET/release-20260801-returned-assessment-edit/admin-dashboard/tmp/path-deploy/prod/20260801-returned-assessment-edit--2026-08-02T12-14-47-430Z.json`.

## 2026-08-01
- Ops/TEST/Release: Deployed release `20260801-returned-assessment-edit` to TEST from exact admin commit `6bd87e02f7d7456bfa5b5441884597a5b3b9efc1`, unchanged portal `4ac580deb5dbacc00811748fe40623c7eaa08f69`, and unchanged shared `e8dc303d8ecc057e52509b00854106f230823d96`. All 16 DEV qualification checks passed under evidence `0fe242d34e20bd7a32dd127df542ac9fa24fd4ef3134ba004333e0d221f8a76e`; all 11 deployed TEST acceptance checks passed under evidence `f51d72c13f3c0c9c0de66029460f36de5b2d8405547074454351c47f0078a9a5`, including the targeted dual-role returned-assessment edit/save journey, exact provenance, role workflows, intake completion, applicant scope, live privacy denials, payment rollback, cleanup and source stability. The first acceptance run correctly stopped on stale unchanged-portal provenance; the unchanged portal candidate was redeployed to align the release stamp and the complete acceptance gate was rerun without bypass. Both target groups ended healthy, the admin console remained normally forwarded, and the TEST public portal was returned to its portal-scoped fixed-response protection page. No schema, data, workflow, runtime-config, database refresh, provider or PROD operation ran. Reports `#168` and `#170` remain `planned` pending PROD deployment.
- Planning/Correction controls: Reconciled the ongoing Regional Manager/Program Administrator correction-controls workstream in the persistent project memory. The proposal is recorded as sent and awaiting Émilie/client-PM feedback; related feedback repairs and narrow delivered safeguards are mapped separately from the still-undelivered general correction, reversal and retraction controls. No application behavior, permissions, environment data or deployment changed.
- Fix/Application Assessment/DEV: Fixed feedback `#168` and `#170` as one dual-role edit-access defect. A Regional Manager who originally submitted an Application Assessment can now edit it after the review workflow returns it to that submitter; exact `submitted_by_staff_profile_id` lineage is required, so a different Regional Manager, missing workflow identity, other review stages, and non-`in_review` applications remain read-only. Existing Regional Manager access to an unsubmitted in-review draft is preserved. Added executable policy coverage for matching and non-matching staff profiles, every locked review stage, incomplete workflow data, role boundaries, and application-state boundaries, plus a compiled-browser journey that edits and saves a returned assessment without changing its workflow stage; the earlier full admin aggregate passed 93 suites / 427 tests, lint completed with zero errors, and the production compile succeeded.
- Ops/Feedback/PROD: Reconciled the open BUG queue under the standing immediate-close rule. Reports `#154`, `#165`, and `#166` moved to `resolved` after their previously completed containment/repair/prevention work, while `#163` moved to `closed` as recipient-mailbox support with no established PATH failure; each received an exact status-history row and technical closeout note. Implemented the remaining shared defect behind `#168` and `#170` on clean commit `a8debef`: a dual-role Regional Manager can edit a returned Application Assessment only when their active staff profile exactly matches the workflow submitter. Both reports are now `planned`, meaning complete and tested but not deployed. No client, case, application, assessment, workflow, message, document, schema, runtime, provider, or deployed code changed during the feedback-log updates.
- Fix/Application Assessment/Documents/Release: Fixed feedback `#171` as a repeat-application document-lineage defect on top of the exact admin source currently running in PROD. Assessment submission now preserves an Application Form or Financial Overview only when the active document belongs to the application being submitted; older-application and case-level documents no longer suppress generation of the required current-application documents. Missing application context fails closed to generation. Focused unit coverage and a rollback-only real-MySQL release contract cover current-application preservation, older-application and case-level isolation, metadata/link compatibility, and both server submission paths.
- Ops/PROD/Release: Deployed admin-only release `20260801-assessment-document-lineage-r2` from clean commit `aa2817711154f07ba051dc0af2d520383e066a12` after all 16 DEV gates and all 11 deployed TEST gates returned `GO` under TEST evidence `fc1cb076aaa608e72134f36b58d4a41819f28e8b64c0b202393700af76d7dfc8`. The repeat-application fix now prevents an older case/application Financial Overview or Application Form from suppressing required-document generation for the application being submitted. Immutable admin artifact SHA-256 `ca3b6dc0bf21acfb42250166e80c6a8ad612b079733c01698231fa2e4d1494ee` was installed by successful ASG refresh `33f8be0e-7dac-4e86-b139-b0e76ba4099d` on replacement `i-0ec48d0f0d226be2f`; both services remained online with zero restarts, all three public `/readyz` checks returned `200`, normal forwarding was restored, and the maintenance row count returned to zero. Deployed verification for Case `76` / Application `123` returned `preserveExistingFinancialOverview=false`. No schema, client/case/application/assessment/document data, workflow, provider, or portal/shared artifact changed. Feedback `#171` was closed immediately after verification. Manifest: `/home/bill/ISET/release-20260801-assessment-document-lineage-r2/admin-dashboard/tmp/path-deploy/prod/20260801-assessment-document-lineage-r2--2026-08-01T11-28-25-843Z.json`.

## 2026-07-31
- Fix/Application Assessment/Documents/DEV: Fixed feedback `#171` as a repeat-application document-lineage defect. Assessment submission previously treated any version-managed Financial Overview on the case as a reason not to generate one, even when that document belonged to an older application, and then correctly failed the current-application required-document check. The submit dialog could also offer to preserve a legacy case-level Application Form or Financial Overview with no application link, producing the same mismatch. Both submission paths now accept automatic or explicit preservation only when the active document is linked to the exact application being submitted; missing application context fails closed to generation. Assessment PDF generation was audited and was already application-scoped. Added focused regression coverage plus a rollback-only real-DEV MySQL contract for valid current-application preservation, older-application and case-level isolation, metadata/link compatibility, missing-application fail-closed behavior, and both server call sites. Live PROD report `#171` remains `planned` and now has the complete qualification/maintenance-window note; no PROD client, case, application, assessment, or document data, runtime configuration, or deployed application was changed.

## 2026-07-30
- Ops/Feedback/Data Integrity/PROD: Completed guarded database-only repairs for feedback `#169`, `#172`, and `#175`; duplicate `#176` remains closed. Accidental denials on Case `109` / Application `27` and Case `160` / Application `90` were restored to the two-step `returned_to_rm / changes_requested` state, their applications returned to `pending_approval / pending_decision`, their cases returned to intake, and the false denial-only plans, four reporting interventions, two unsubmitted ESDC rows, and denial reporting context were removed. Accidental dependency-free manual-backload Intervention `316` and compatibility proposal `389` were removed from Case `30` while Action Plan `146` stayed active for re-entry. Restore point `nwac-prod-feedback-db-repair-20260730-204214`, 22 before/after audit rows, guarded rollback SQL, and an independent second-session verification are retained. Reports `#169`, `#172`, and `#175` were closed after technical verification rather than left open solely for staff confirmation.
- Security/Decision letters/DEV: Removed the unreachable legacy decision-letter OpenRouter branch that assembled applicant, case, assessment, funding, education, identity, and staff context. Application denial drafts remain fully local; application approval drafting retains only its narrow generic-template copy-editing call and merges all record-specific facts locally afterward. Added source-level regression coverage proving that exactly one decision-letter AI call remains and that the removed personal-context fields cannot be reconstructed in that drafting function. The full admin aggregate passed 90 suites / 406 tests and quiet lint passed; no database, runtime configuration, provider call, or deployment occurred.
- Fix/CFA/PROD: Deployed `20260730-feedback-173-cfa-hotfix`. Repeat-application approval now scopes action-plan duplicate checks and CFA intervention selection to the selected application, so a historical application-less plan cannot block or replace the newly approved intervention data. Every newly materialized application plan creates the next CFA draft, and participant signing now finalizes only the matching version/document after case/application/participant scope validation.
- Ops/Data Repair/PROD: Completed the guarded feedback `#173` repair for Case `12` / application `95`. The repair preserved the prior signed evidence, created application-linked plan `149` with approved interventions `323`-`325`, created corrected staff-review CFA draft v2, withdrew and archived the incorrect v1 artifacts, reopened the application for follow-up, and sent no participant message. Report `#173` remains `in_progress` pending staff review/send and participant re-sign verification.
- Ops/PROD/Release: Exact-source DEV and deployed TEST qualifications returned `GO`; snapshot `path-prod-feedback-173-case12-20260730t1510` was available before repair; ASG refresh `3793b3c8-c754-4f77-bda3-af3211e86641` completed on replacement `i-09fe637ba5dd582e7`; all three public readiness URLs and both target groups are healthy; both PM2 services are online with zero restarts; normal forwarding is restored; and SQL command `534c2e6c-d214-41ff-bb3c-c876d68f7d47` confirmed zero active maintenance announcements. Manifest: `/home/bill/ISET/hotfix-173/admin-dashboard/tmp/path-deploy/prod/20260730-feedback-173-cfa-hotfix--2026-07-30T15-26-36-289Z.json`.
- Ops/Public Portal/TEST: Restored the idle TEST public portal to its portal-scoped ALB fixed-response protection page after finding the listener forwarding publicly. `nwac-public-test.awentech.ca` now has `maintenancePageEnabled=true` with the established PATH Testing and Demonstration Portal notice; the TEST admin console remains on normal forwarding and the live PATH system was not changed. Reconfirmed the standing rule that portal testing/deployment threads may open TEST only for an active testing window and must restore portal protection before closing.

## 2026-07-29
- Ops/Data Integrity/PROD: Completed the Manitoba client repair under run `prod-mb-client-repair-20260729` and encrypted Aurora recovery point `path-prod-mb-client-repair-20260729163423`. Susan Guimond's current portal client `244`, user `180`, case `172`, application `103`, and submission `103` remain canonical; application `103` was not modified and independently remained `in_review` at row version `49`. The typo-email local user `23` is suspended, imported client `20` is retained with merge metadata, its sole account event moved to the canonical client, and empty imported case `20` is archived/detached with client/case merge audits. Stephanie Swampy was created through the application-less client-file model as client `384` / case `264` (`CASE-2026-0000264`), assigned to Judy Cook in Manitoba, with the source report's EI / `$4,000` context retained only as import metadata; no applicant user, application, submission, assessment, action plan, intervention, payment, or finance row was fabricated. Final verification found no operational FK references on Susan's retired client/case beyond the intentional merge audits, and the repair procedure, application lock, and admin maintenance warning all ended at zero.
- Safety/Agent operations: Made the prohibition on guessed database schema and SQL the first instruction in every PATH workspace `AGENTS.md`, including new workspace-, portal-, and Intacct-mock entry files. The canonical standing directive and database-access guide now require exact target verification, live metadata discovery, full live DDL/collation/constraint proof before mutation, discovery-only handling after any schema-related failure, and immediate stop/rollback/state-proof/cleanup/disclosure if the rule is breached. This is an agent-operating safeguard only; it did not change application code or database data.

## 2026-07-28
- Ops/PROD/Release: Deployed release `20260728-regional-snapshot-data-quality-funding` as shared + admin + portal from admin `a252416`, portal `4ac580d`, and shared `e8dc303`. All 16 DEV checks returned `GO` under evidence `5f44fb97d4435dd67839221bed02d02fc284ce9e01950402f7c30d1b13197f55`; deployed TEST acceptance returned `GO` under evidence `cb781fce6bb0d364fd9c664e5c2c1cc31aba1b5a48c9c49e31d67a44e5283075` with zero fixture residue. PROD staged immutable and compatibility artifacts, ASG refresh `314b79a4-06e9-49a4-9d88-db715e88f205` completed on replacement `i-06d90fc988eec1886`, all three public `/readyz` URLs returned `200`, the five Regional Snapshot source hashes matched the qualified files, the compiled bundle contained the new release ID/messages, both PM2 services were online with zero restarts, all host rules returned to normal forwarding, and SQL command `2361a208-039f-427c-a8e2-0cfe0c3fafe6` confirmed zero active maintenance announcements. No schema, data, workflow, runtime-config, email, AI, Finance, Intacct/Sage, or other provider operation ran. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260728-regional-snapshot-data-quality-funding--2026-07-28T22-40-21-305Z.json`.
- Ops/ASG Bootstrap/PROD: The canonical `s3://nwac-prod-artifacts/bootstrap/app-bootstrap.sh` remains the old NodeSource-based script because the reduced roles still cannot update it. The guarded recovery transferred and SHA-256-verified hardened source `fd5f5cfce2d66c98e97af3955baef3e11a4f889b75395051a4ee6a41c0a5f3cc`, installed official Node `20.20.2`, and then exposed a new current-AMI conflict: the scripted PulseAudio/session-manager package set conflicts with installed PipeWire/WirePlumber. The first package pass stopped before app start; an overlapping retry also stopped before app start. After cloud-init was confirmed failed/stopped, recovery preserved the AMI package set, reinstalled both app dependency trees once, validated both Puppeteer runtimes, and started both services successfully under SSM command `1b4a128b-1d0f-405a-9d51-c452cb5e29ff`. Keep future PROD refreshes behind all-surface fallback with this recovery available until the canonical key can be updated and the package list is made compatible with the current AMI and proven on a fresh replacement.
- Ops/Release qualification: Added the root `bootstrap/` source tree to the machine coverage inventory's deployment-control domain. This closes an unmapped-file stop found while planning `20260728-regional-snapshot-data-quality-funding`; the qualifier now subjects both canonical bootstrap copies to the normal exact-source release gate.
- Fix/Regional Snapshot/Release deployed: Section C retains valid positive approved funding from non-manual interventions when application lineage is missing or conflicting, while Section B remains fail-closed and the data issue remains visible. Explicit manual-backload adjustments remain offline-only and were excluded from the application deployment.
- UX/Regional Snapshot/Release deployed: Data-quality issues identify the participant, application, and intervention business type and present the reporting effect and required correction as plain-English guidance instead of case references, database IDs, and separate technical columns.
- Ops/Public Portal/PROD Runtime: Applied the approved targeted workflow `21` runtime patch to `iset_runtime_config(scope='publish', k='workflow.schema.intake')` without copying DEV/TEST runtime data or changing workflow authoring rows. The six remaining image-only document slots now use `image/*,.pdf` in both payload copies, preserving image/camera eligibility while accepting PDFs; Contact Information `address-postcode` now explicitly uses `inputMode: text` in both copies so its Canadian postal-code mask does not trigger the renderer's numeric-keyboard fallback. The complete prior row is retained at `ops-backup/workflow.schema.intake.pre-20260728-pdf-postcode`; checksum moved from `de1c4b586fabb7b97ff2ebd6919a7ac1b75328eb79153553679232ecbef6eb35` to `645c5d669a01bf21ad5e853660be42dca9a315b7ef02210306d3bd61fed004c2`. Apply command `d0ccfd60-a7bb-43d7-8475-a6c98d5a9636` changed exactly one row; verification command `63cfa2b4-9600-434b-967d-23aa6c3b63d3` confirmed both schema copies, one rollback row, no DEV experiment marker, and `unchanged_except_approved_paths = 1`. All three live health endpoints remained `200`. Apply, verification, and rollback artifacts are under `sql/ops/prod-intake-runtime-pdf-postcode-*-20260728.sql`.

## 2026-07-27
- Ops/PROD/Release: Deployed clean app-only release `20260727-regional-snapshot-financial-overview` as shared + admin + portal from admin `a0455e1`, portal `4b8135d`, and shared `e8dc303`; zero schema, data, workflow, runtime-config, email, AI, Intacct/Sage, or other provider operations were included. All 16 DEV qualification checks and the complete deployed TEST acceptance gate returned `GO`. PROD immutable and compatibility artifacts staged, ASG refresh `56f3257b-a84f-4953-a3fb-e616ca77c53f` completed on replacement `i-0b638e44eee6b27a6`, all three public `/readyz` URLs returned `200`, exact deployed Regional Snapshot file hashes and provenance matched the qualified commit, both PM2 services ended online with zero restarts on Node `20.20.2`, normal forwarding was restored, and SQL command `a7ccf31f-a2dc-4f93-8551-7463cb876b71` confirmed zero maintenance rows. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260727-regional-snapshot-financial-overview--2026-07-27T23-18-43-177Z.json`.
- Ops/ASG Bootstrap/TEST+PROD: The PROD replacement initially failed before service start because `https://rpm.nodesource.com/setup_20.x` returned `403`; maintenance fallback remained active while verified official Node `20.20.2` was installed and PM2 was linked onto the sudo path. A fresh TEST-host rehearsal then exposed stale 2025 default artifact names. The durable source fix pins and verifies Node, uses stable TEST bootstrap app keys, keeps those keys current during future TEST deploys, and prefers the admin-bundled shared runtime. Fresh TEST refresh `a14cf57b-d4eb-45b8-85fd-a26901412f77` on `i-0db0637d66d66de8c` passed exact release provenance, local readiness, PM2 zero-restart startup, and both target groups healthy. TEST canonical bootstrap is updated. Attempts to back up or promote the same script to `s3://nwac-prod-artifacts/bootstrap/app-bootstrap.sh` were denied by the reduced operator/instance roles (`s3:PutObject` / `kms:GenerateDataKey`), changed no PROD S3 object, and remain a named IAM-gated follow-up; the current live host is repaired and healthy.
- Ops/Feedback/PROD: The required post-release recheck for feedback `#166` found signed Financial Overview v1 document `5539` had been archived again at `18:08:26` UTC by the pre-release path after its earlier restoration. The deployed preservation code was already proven by exact source hashes; the underlying 225,018-byte PDF object was reverified in the PROD uploads bucket. Guarded SQL `sql/ops/prod-feedback-166-postdeploy-signed-v1-restore-20260727.sql` restored only v1 to active while version `18` remained withdrawn and signing request `136` remained cancelled. Feedback `#166` remains `in_progress` with a current internal note pending an authenticated Case 172 document-visibility recheck; it was not prematurely marked resolved.
- Reporting/Regional Snapshot/Local: Added an explicit partial manual-adjustment JSON overlay to the read-only PROD workbook generator. The confirmed 21-client source uses transaction dates for this one-off adjustment, adds an approved application and funded client for a positive FY 2026-27 payout while preserving distinct pending/withdrawn applications, deduplicates Kaitlyn Kitson's existing counts, and allocates funding using PATH's stored CRF/EI source. The interim workbook totals 202 applications, 32 approved applications, 32 funded clients, $144,414.62 CRF, and $113,526.02 EI; two Alberta and seven Saskatchewan clients remain unadjusted pending feedback.
- Ops/Data Integrity/PROD: Completed the full fiscal-period repair for Solana Henderson Case `41` / `CASE-2026-0000041`. Closed action plan `23` at March 31 with result `Returned to school`; activated April 1 renewal plan `143` as `EI Reach Back`; moved and completed intervention `311` under the renewal plan with verified actual `$3,077.21` and outcome `Complete`; restored its two evidence-backed cost lines and historical posted finance transaction; removed revision contamination from January-March intervention `32`; created a separate renewal ILMP submission; corrected case counters; and archived the orphaned returned-revision workflow `40` plus generated document `7312`. Restore point `path-prod-solana-case41-pre-repair-20260727182903` is available. Guarded apply postflight and an independent relationship/finance-rollup audit both passed.
- Fix/Regional Snapshot/DEV: An approved new-intervention proposal now classifies its linked application as an Approved Application even while the operational application status is `docs_requested / awaiting_applicant`. The calculation does not create a synthetic application, continues to deduplicate the linked application once per reporting period, does not treat revision proposals as additional applications, and preserves current withdrawal/denial precedence. In the regenerated FY 2026-27 workbook, British Columbia now reconciles as 21 applications: 2 approved, 13 denied/ineligible/withdrawn/NC, and 6 pending; PROD remained read only.
- Fix/Regional Snapshot/DEV: Original draft interventions with positive scheduled funding now contribute to Section C because activation does not control the report. Draft revisions identified through their source intervention remain excluded until operative, preventing cloned amendment schedules from being double-counted. The corrected FY 2026-27 workbook now includes Leah Plaited Hair's $8,783 CRF in Alberta; automated totals are 19 funded clients, $132,051.50 CRF, and $74,423.39 EI.
- Fix/Regional Snapshot/DEV: A broader read-only PROD integrity sweep found that archived interventions could inherit an older compatibility proposal's `approved` state and incorrectly contribute current funding. Regional Snapshot now gives archived action-plan/intervention state precedence for Section C while retaining dated activity for Section B, resolves application provenance only when authoritative plan/proposal/ESDC sources agree, fails closed on conflicts, and surfaces header/cost-line mismatches plus active funding on withdrawn applications. The corrected FY 2026-27 workbook removes one Ontario funded client and $8,015.74 CRF; automated totals are 18 funded clients, $123,268.50 CRF, and $74,423.39 EI. PROD was read only during this sweep and no deployment occurred.
- Ops/Data Integrity/PROD: Backfilled deterministic application provenance on 14 historical auto-assessment action plans covering 44 interventions, plus three null proposal links and two null ESDC submission links. Every mapping was proven through stored proposed-intervention IDs against exactly one same-case application assessment; no case-level inference was used. A separate guarded follow-up restored two denied-reporting plan links from their unique same-case ESDC submissions. The transactions inserted 16 case audit events, changed no intervention/funding/status/document facts, and left no unlinked auto-assessment, denied-reporting, or withdrawn-reporting plans. The initial regenerated workbook showed 19 funded clients, $131,284.24 CRF, and $74,423.39 EI before the later DEV archived-funding correction recorded above.
- Fix/Regional Snapshot/DEV: Missing application provenance is no longer silently treated as evidence that an intervention is a manual backload. The calculation excludes only explicit manual/existing-entry records under the temporary manual-adjustment boundary; any other unlinked intervention now emits an actionable `missing_application_lineage` issue. The current PROD-backed workbook exposes nine unclassified historical rows for review while keeping them out of totals.
- Feature/Regional Snapshot/DEV: Reworked Client Activity period assignment around intervention start dates and approved funding-line due-date occurrences, with application-level period deduplication and a reconciled current-outcome partition. Reworked Funding around positive scheduled occurrences, unique funded clients, CRF/EI allocation by due period, and internally derived client averages. Application-less manual records remain excluded pending a separate client-supplied adjustment overlay. Added actionable data-quality reporting to the dashboard and all-regions Excel Summary, plus a read-only PROD-backed generator that produced the FY 2026-27 pre-deployment workbook without changing PROD.
- Ops/Public Portal/TEST: Replaced the intentionally offline TEST portal's generic scheduled-maintenance page with a plain-language notice identifying it as the PATH Testing and Demonstration Portal, stating that it is not the live PATH portal, explaining that it is kept offline when not in use, and directing access requests to the portal manager. The TEST portal remains behind the ALB fixed-response page with HTTP `503`; PROD and the TEST admin console were not changed.
- Fix/Application Assessment/Documents: Prepared a DEV repair for feedback `#166` so assessment submission automatically preserves version-managed Financial Overviews and the legacy replacement path cannot archive version-linked, signing-request-linked, or signed documents. Added focused unit/wiring coverage plus a rollback-only real-DEV MySQL contract proving the signed version remains active, the legacy unversioned row remains replaceable, application-form behavior is unchanged, and fixture residue returns to zero. The signed July 6 PROD document for Case 172 was restored separately through guarded SQL. After Emilie confirmed that v1 is the required signed copy, the unnecessary v2 version/request/message/documents/reminders were withdrawn or archived; the application remains in `docs_requested` because Rent Assist supporting documentation is still required.

## 2026-07-23
- Ops/PROD/Release: Deployed clean release `20260723-end-of-day-r4` as shared + admin + portal with zero schema migrations and no data, workflow, runtime-config, email, AI, Intacct/Sage, or other provider operation. Exact-source DEV and deployed TEST qualification reached `GO` under TEST evidence `691a8e6d1f5f9341959049c0a32577fac1126397d817e3e885ae5f4cae3aa373`; immutable artifacts staged, ASG refresh `fb622ab9-cab0-4201-8466-0b1d2c736971` completed on replacement `i-0def2db5d6ba0e346`, and local/public readiness, clean provenance, PM2 zero-restart status, normal forwarding, and zero PROD maintenance rows passed. Targeted PROD verification confirmed case `17` still has an external parent action plan and an internal intervention, so the deployed edit-mode fix preserves the intervention's own `Paid from` value without a data repair. Feedback `#164` is resolved. The TEST admin console remains available while the TEST public portal is intentionally left on its ALB maintenance page at Bill's request.
- Planning/nForm: Set `System Administrator` as the only built-in role; business roles and permissions are solution-defined. Corrected stale scope language so cases and VAC scheduling/customer models remain solution-owned while generic workspace/dashboard composition stays in core.
- Planning/nForm: Defined the staff portal and public portal as permanent first-class nForm surfaces, with generic internal-operator and external-user identity infrastructure while all solution terminology remains outside core.
- Planning/nForm: Made public authentication configurable per workflow so nForm supports both anonymous forms and sign-in-required external-user journeys.
- Planning/nForm: Established codeless policy-maker intake-wizard authoring, preview, versioning, publication, and rollback as nForm's defining capability, with constrained components and platform-enforced validation/security/privacy controls replacing normal code-release cycles for authored changes.
- Planning/nForm: Made publication governance configurable so solutions can allow trusted direct publishing or require separate author, reviewer, and publisher permissions.
- Planning/nForm: Corrected an overbroad case-management assumption: nForm's codeless workflow ends at intake-journey execution/submission and does not impose configurable staff review stages, tasks, cases, or work queues; downstream bookings, registrations, payments, requests, and other processes are solution-owned.
- Planning/nForm: Added the neutral staff-side submissions registry to core while keeping all downstream business meaning/process solution-owned, and moved the work from decision-by-decision interviewing into a Codex-led dependency/coupling audit with Bill consulted only for genuine product ambiguities.
- Planning/nForm: Added the initial verified dependency/coupling map. It rejects bulk copy-and-prune: authoring primitives are reusable, but publication, public runtime state, drafts, uploads, and completion are bound to one authenticated ISET workflow and automatic client/case/application creation, requiring subsystem extraction and a clean platform schema.
- Planning/nForm: Added the clean v1 schema/solution-extension contract and path-level machine-readable extraction manifest. The target uses separate operator/public identities, immutable intake publications, anonymous/authenticated server-side sessions, neutral submissions, a narrow transaction-safe solution hook, durable outbox delivery, and explicit keep/split/rewrite/drop/new handling across all three current repositories.
- Planning/nForm: Created the standalone local WSL Git repository at `/home/bill/nForm` with npm-workspace boundaries, clean baseline schema, persistent product memory, and empty-template boundary tests. `npm run check` passes; no remote, AWS resource, sample intake, demo data, or runnable application has been created yet.
- Planning/nForm: Added and locally committed the first extracted core subsystem in `/home/bill/nForm`: a versioned neutral component registry and deterministic intake compiler/validator with route reachability/termination, response-key, property, authentication/save-mode, and checksum coverage. Local nForm commits are `65d4906` and `c3eba1d`; no remote exists.
- Planning/nForm: Added and locally committed the framework-independent staff-authoring domain service (`ecf4a3d`) with empty drafts, optimistic locking, validation, direct or strictly separated publication governance, immutable publication history, automatic next drafts, and rollback. `npm run check` passes; MySQL/HTTP adaptation and the staff editor remain next.

## 2026-07-21
- Planning/nForm: Reactivated the nForm extraction work and recorded the agreed v1 direction: an empty, self-contained project-template repository retaining generic nForm infrastructure while removing PATH/ISET/NWAC solution behavior, with VAC reserved as a downstream validation case rather than the source of the core boundary.
- Planning/nForm: Set the v1 infrastructure boundary to retain Cognito, S3, SES, and the current AWS deployment model with environment-driven resources; cloud-neutral replacement is not part of extraction.
- Planning/nForm: Set nForm v1 to single-tenant per deployment; first-class tenant partitioning and cross-tenant administration are outside the initial template boundary.
- Planning/nForm: Excluded clients, cases, applications, appointments, and other business records from the core data model; derived solutions own domain entities while nForm supplies identities, forms/submissions, workflows/tasks, documents, notifications, and events/audit infrastructure.
- Planning/nForm: Added a dated end-of-session checkpoint and thread-index recovery entry; discovery is paused at the built-in role boundary, and no target repository or runtime/infrastructure changes have been made.

## 2026-07-20
- Ops/PROD/Release: Deployed `20260719-rm-cross-region-reassignment-r3` as clean shared + admin + portal with no schema, data, workflow, runtime-config, restore-point, or provider operation. Exact-source TEST evidence was `GO`; immutable artifacts staged; ASG refresh `1b9eaeb3-7900-4f37-b29a-f6d2df43bd19` completed on `i-025f0c9390a64f53a`; all three public `/readyz` checks, deployed provenance, targeted RM assignment policy, normal forwarding, and zero maintenance rows passed. Feedback #160 is resolved.
- Ops/Release Process: Recorded the qualification-granularity gap exposed by the `r2` to `r3` release-note-only correction. Current exact-tree admission remains enforced until tooling provides a machine-checked non-runtime-drift path, but the intended policy is focused bundle/content revalidation when runtime code, dependencies, migrations, operations, and generated behavior are proven unchanged.

## 2026-07-19
- Fix/Release Notes/Local: PROD preflight stopped before maintenance because the flat English/French landing-page fallback mixed the RM reassignment package with two older Client Monthly Attendance Report bullets. The fallback now describes only the newest RM package, and the corrected source requires a fresh `r3` DEV/TEST qualification chain before PROD.
- Fix/Release Qualification/Local: The first RM reassignment DEV qualification correctly retained `NO-GO` after the compiled home Overdue/conflict journey found the intended conflict-row `Resolve` control but its physical centre-coordinate click did not open the modal after the board reflowed. The harness now activates the specifically matched row action directly, matching the deterministic control-selection pattern used elsewhere. The failed evidence is retained; a fresh complete DEV qualification is required against the new source fingerprint before TEST.
- Fix/Case Assignment/DEV (feedback #160): Regional Managers can now assign or reassign a case they are authorized to access to any active staff member in the assignable pool, including another Regional Manager or an ISET Coordinator outside their own region. The shared backend target guard, Manage ISET Applications modal, Application Overview quick action, and conflict-driven reassignment now use that target policy while existing source-case access rules remain unchanged. Disabled Cognito users and inactive staff profiles are excluded/rejected. Focused policy/case-access tests, lint, syntax checks, diff validation, and the local case-assignment browser smoke pass. TEST qualification and deployment are deliberately deferred until the 2026-07-19 PROD feedback-review batch is complete.

## 2026-07-13
- Ops/Signing/PROD: Committed, requalified, and deployed release `20260714-client-monthly-attendance-prod` as shared + admin + portal plus only the allowlisted workflow `54` authoring graph. Fresh committed-source DEV evidence and a repeated deployed TEST acceptance both reached `GO`; PROD captured restore point `path-prod-20260714-client-monthly-attendance-prod-2026071401485`, staged immutable component artifacts and descriptor, and completed ASG refresh `76f0831c-0fdf-490a-b6c5-d6eedb2053d3` on `i-0dacb7480cf2121b4`. Normal-routing smoke returned `200` for all three hostnames; exact provenance, workflow counts, runtime source markers, removal of the redundant recipient/case checkbox, zero maintenance rows, and normal forwarding passed. No schema migration, intake runtime publication, broad config copy, real participant data, Finance/email/AI/Intacct provider action, or PROD synthetic fixture was included. Manifest: `tmp/path-deploy/prod/20260714-client-monthly-attendance-prod--2026-07-14T01-48-52-166Z.json`.
- Fix/Signing PDF/DEV: Added clear vertical separation between the Client Monthly Attendance Report's NWAC logo/header and the report title; the unrelated participant-page spacing remains unchanged.
- Fix/Signing Dates/DEV: Bound every Client Monthly Attendance Report absence date picker to the reporting month selected on the prior step. Workflow normalization now carries schema-authored month-field bounds into the portal, which calculates native date-input `min`/`max` values including month length and leap years; browser smoke asserts the exact bounds, while backend validation continues to reject out-of-month submissions.
- Fix/Signing PDF/DEV: Replaced the Client Monthly Attendance Report's generic field-dump PDF with a dedicated NWAC-style letter-size document. The signed artifact now has the bilingual NWAC header treatment, compact source-form instructions, attendance detail lines, marked attendance choice, a proper four-row absence table with supporting-document filenames, declaration, signature panel, and footer. The DEV smoke retains the generated PDF for visual evidence.
- Fix/Signing/DEV: Replaced four always-visible attendance absence rows with schema-driven repeatable fields. The participant form now starts with one date/reason pair and reveals up to three more through `Add another absence`; removing the last added row clears its values. Workflow normalization preserves the reusable repeatable metadata, and DEV browser coverage verifies progressive disclosure and signed payload persistence.
- Fix/Signing/DEV: Replaced the Client Monthly Attendance Report's generic PATH-prefill notices with field-specific participant guidance for legal name, institution, and program. The DEV smoke now verifies the useful guidance and rejects the obsolete system-centric wording.
- Fix/Secure Messaging/DEV: Removed the redundant `I have checked the recipient and case` self-attestation and its send gate from staff secure-message compose. The real safeguards remain: the recipient is derived from the active case, displayed read-only with the case reference, and enforced by server-side case/applicant routing. The compose body now also suppresses unintended horizontal overflow.
- Fix/Signing QA/DEV: Replaced the monthly-attendance smoke's misleading absence-evidence fixture, which had copied the unrelated Authorization for Release PDF under an attendance-proof filename, with an unmistakable synthetic PNG containing no real participant or medical information.
- Feature/Signing/DEV: Added workflow `54`, Client Monthly Attendance Report, as an attachable secure-message digital form. It supports editable participant/institution/program prefill, reporting-month selection, full-attendance vs absence branching, up to four progressively disclosed absence rows, required participant-owned supporting evidence for absences, and participant signature. Signed PDFs use `attendance_form`; embedded evidence materializes separately as `medical_documentation`. This entry records the initial DEV implementation; the later committed TEST and PROD release is recorded above.
- QA/Signing/DEV: Added and passed `npm run smoke:monthly-attendance-report:dev` with real disposable DEV Cognito users, admin secure-message send, participant browser completion, upload ownership validation, PDF/document assertions, idempotent repeat signing, screenshots, and zero DB/Cognito/MinIO residue. The smoke exposed and fixed a portal renderer bug where a completed required upload left its hidden file input natively required and blocked the next step.
- QA/Release Qualification/Local: The first attendance release qualification correctly retained `NO-GO` when the compiled home Overdue-queue smoke clicked the neighbouring Unresolved Conflicts card and timed out. The harness now activates the specifically matched Cloudscape radio directly instead of using a potentially intercepted screen coordinate; the focused compiled smoke passed before the required fresh full qualification.
- Fix/Public Portal/PROD: Deployed emergency portal-only release `20260713-prod-incident-requalification` after read-only PROD logs confirmed authenticated message endpoints were repeatedly throwing `ReferenceError: assertRuntimeTableReady is not defined` while `/readyz` remained green. The fix imports the deployed schema-readiness helper and keeps schema-dependent intervention readiness inside route error handling. Exact-source DEV and deployed TEST qualification reached `GO` with all mandatory role, applicant, messaging, privacy, payment, worker, provenance, rollback, cleanup, and maintenance checks passing; TEST evidence ID `b286a289352f323840535b48920fffcfd880a3b5e4aafbfcb613f7e939bb9717`. PROD scope excluded schema, data, admin, shared, runtime configuration, and provider operations. Immutable portal artifact SHA-256 `9264f863f4d5baff3882955d40aedfc090a6b7d91a21d76324d97f85858ad361` staged successfully; ASG refresh `b3f701e2-906c-4900-bd66-7b3fdc526ca3` completed on `i-0858542063c4281a2`. Local and public readiness, deployed provenance/source, normal ALB forwarding, zero maintenance rows, and a post-cutover error watch passed. Manifest: `tmp/path-deploy/prod/20260713-prod-incident-requalification--2026-07-13T16-32-38-251Z.json`.
- Incident/Portal/PROD: Pre-patch read-only SSM verification confirmed that the live PROD portal repeatedly threw `ReferenceError: assertRuntimeTableReady is not defined` on authenticated message routes while `/readyz` still returned `200 ready`. This active incident evidence triggered the emergency qualification and portal-only release recorded above.
- Fix/Release Qualification/Local: The repaired candidate's second deployed TEST acceptance exposed two qualification-harness defects after the affected portal journey passed: runtime/payment postflight inline JavaScript lost shell quotes through SSM, and strict portal wrong-owner checks supplied Cognito ID tokens even though deployed portal bearer authentication requires access tokens. Runtime probes now use shell-safe single-quoted inline programs with executable regression coverage, and disposable privacy fixtures now supply `iset_access`. The failed TEST run retained `NO-GO`; all synthetic Cognito/database fixtures cleaned to zero, and a fresh DEV/deploy/TEST evidence chain is required.
- QA/Release Qualification/TEST: The first complete deployed acceptance for `20260713-prod-incident-requalification` correctly returned `NO-GO`. Provenance, rollback artifacts, target health, intake completion/retry, maintenance recovery, and source stability passed, but an authenticated portal mailbox path threw `ReferenceError: assertRuntimeTableReady is not defined` and left the async request open despite green `/readyz`; this blocked applicant scope and strict-denial smokes. The gate also exposed immediate-notification assumptions against the 15-second durable worker, missing browser timeout diagnostics, and 11 historical failed migration checksums being misclassified as unresolved even though the canonical plan had zero pending migrations. Both hung smoke commands were cancelled; the two exact fixture stamps were transactionally removed, with zero matching DB rows and Cognito users, no scoped S3 objects, normal ALB forwarding, and healthy targets. No PROD mutation occurred during that failed run.
- Fix/Release Qualification/Local: Added the missing portal schema-readiness import plus a complete-stack authenticated route regression, moved the intervention schema assertion inside response handling, made applicant-scope response bodies time-bounded with unconditional fixture cleanup, made two-step notification assertions poll across the durable worker interval, and retained route screenshots/body text on browser timeouts while continuing the remaining workflow assertions. Migration planning now distinguishes unresolved failures for current canonical checksums from retained historical failures for obsolete checksums. Focused regressions and both complete aggregate/lint contracts pass; fresh DEV qualification and a TEST fix-forward rehearsal are still required.
- Fix/Release Qualification/Local: The approved `20260713-prod-incident-requalification` TEST attempt stopped before artifact upload or application deployment when portal preflight rebuilt the tracked `build-test/` tree and correctly tripped exact-source stability. Normal TEST routing and the warning were restored immediately. TEST portal preflight now builds once in an ignored release-scoped admin `tmp/` directory, packages that exact output after checks pass, and removes it on every command exit; regression coverage prevents tracked-output deletion from returning. The maintenance sequence now clears the warning before deployed acceptance because zero residual maintenance state is itself a required TEST assertion. Fresh DEV GO evidence is required before the rehearsal retries.
- QA/TEST Qualification/Local: TEST preflight stopped safely before maintenance or deployment because the strict live-denial gate still depended on 11 manually supplied token/fixture variables. Replaced that memory-dependent prerequisite with a bounded autonomous TEST smoke mode that creates real disposable Coordinator, Decision Maker, and applicant Cognito identities; seeds out-of-scope case/application/document/message/payment fixtures; runs every denial with no skip; removes Cognito and relational fixtures; and asserts zero residue. Ephemeral tokens are not emitted into qualification evidence or logs. The candidate must receive fresh DEV GO evidence before the approved TEST rehearsal resumes.
- QA/Release Qualification/Local: The first clean-commit qualification correctly produced `NO-GO` after exposing two intermittent gate defects that prior runs had not reproduced: portal progress was asserted before asynchronous applicable-step hydration completed, and the Modify Intake Step browser smoke selected a draggable card through an unstable centre-coordinate click that could land on an embedded control. The portal assertion now waits for the observable progress state and passed five consecutive focused runs; the compiled editor smoke now dispatches selection on the resolved card and passed its complete focused journey. The failed evidence is retained; full clean-source qualification is required again before TEST.
- Ops/PROD/IAM: Bill attached customer-managed policy `PATHProdImmutableReleaseArtifacts` to reduced role `nwac-prod-codex-operator`, closing the known identity-policy gap for SHA-addressed `nwac-prod-artifacts/releases/*` packages and descriptors without granting delete or broad administrator access. A read-only role-backed probe in account `468278742295` successfully listed the empty `releases/` prefix. No object was uploaded or deleted; `PutObject` remains for the first explicitly approved immutable PROD staging operation.
- Ops/Release Qualification/Local: Requalified the complete current admin + portal + shared candidate after the authenticated-admin production incident. All 16 mandatory DEV gates passed: coverage inventory, both aggregate suites and lint contracts, route privacy checks, canonical DEV schema plan, real-MySQL readiness/authenticated-request/write-and-rollback contracts, both isolated builds, 12 compiled admin browser journeys including failure/retry/state behavior, privacy ERM, payment rollback, Intacct drift, AI fixture, cleanup, and final source stability. No runtime/data operations were declared, no required check was skipped, and TEST/PROD were not accessed or changed. Exact deploy-admissible evidence must be regenerated after this memory update so it fingerprints the recorded state.
- Ops/Release Qualification/Local: Replaced the former unit/lint/health admission model with an enforced two-stage release qualification system. The machine coverage inventory maps admin, portal, shared runtime, schema, auth/identity, frontend journeys/failures, configuration, jobs/notifications, files/messages/signing, Finance, and external integrations to mandatory evidence. DEV qualification now includes real MySQL request/readiness/write-and-rollback contracts plus compiled browser journeys; TEST acceptance requires exact deployed provenance, retained rollback artifacts, on-instance process/schema/config/worker proof, role/applicant/cross-app journeys, strict live denials, zero fixture residue, and maintenance recovery. `path:deploy` requires exact unexpired DEV evidence for TEST and TEST evidence for PROD and embeds provenance in artifacts. No TEST or PROD environment was accessed or changed.
- Fix/Admin/TEST: Deployed admin-only `20260713-admin-schema-readiness-hotfix` after read-only inspection proved TEST had the same canonical `staff_profiles` schema and broken R4b middleware assertion as PROD. The TEST-specific build, warning, ALB fallback, in-place SSM deployment, normal-routing smoke, deployed-source/readiness/log checks, exact live schema probe, and maintenance cleanup passed. Schema, data, TEST DB refresh, runtime configuration, shared, portal, and PROD were not changed.
- Fix/Admin/PROD: Deployed emergency hotfix `20260713-admin-schema-readiness-hotfix` after the engineering-audit release made every authenticated admin API request return `503 schema_not_ready`. PROD schema proof showed `staff_profiles` has the canonical columns used by staff hydration but no `last_seen_at`; R4b had accidentally copied that legacy runtime-DDL column into the new read-only readiness assertion. The hotfix uses one exact staff-profile column contract for both `/readyz` and request middleware and adds complete-stack regression coverage. Clean admin commit `a7071a8` deployed through the admin-only compatibility path with no schema, data, runtime configuration, shared, or portal payload; ASG refresh `3f713b7b-3d1c-4e64-a710-c6a121aa7528` completed on replacement `i-00281ae2ba2104b72`. Local and public admin/portal readiness passed, Bill's active System Administrator profile is present, maintenance state is clear, and recent admin logs contain no schema-readiness errors.
- Ops/Engineering Audit/PROD: Deployed coordinated release `20260713-engineering-audit-prod` as shared + admin + portal + five canonical additive migrations. Restore point `path-prod-20260713-engineering-audit-prod-20260713072840` reached `available`; release admission, local replacement health, ASG refresh, source markers, exact migration checksums, schema-aware public readiness, and maintenance cleanup passed. No dataset/workflow/runtime promotion, historical repair, feedback mutation, payment send, Intacct call, or infrastructure activation was included; Finance email and Intacct routing remain disabled.
- Ops/Release Recovery: The reduced PROD role rejected new immutable `releases/*` artifact objects before any app artifact or instance refresh. Added an explicit `--compatibility-only` recovery mode that records the downgrade, skips the unused pre-EA-028 immutable descriptor, updates the three established live-bootstrap artifacts, and retains the waited ASG refresh. Release `20260713-engineering-audit-prod` used that reviewed recovery successfully; the immutable-object IAM prerequisite remains part of separately authorized `EA-028` work.

## 2026-07-12
- Ops/Engineering Audit/TEST: Completed coordinated TEST rehearsal `20260712-engineering-audit-test` as shared + admin + portal + five canonical migrations, with no TEST refresh, dataset/workflow promotion, provider call, email send, or PROD change. Clean-source admission, readiness, target health, published-workflow completion/retry, client-import and allocation concurrency, durable delivery replay, payment rollback, Hub/PTMA retirement, AI runtime, and cleanup checks passed. TEST finance email routing was found unexpectedly enabled during preflight and was explicitly returned to disabled without changing its 14-region map; Intacct remained unconfigured.
- Fix/Release Readiness/TEST: The rehearsal found and fixed two portal release-contract defects before acceptance: `/readyz` probed nonexistent `input_json_state.payload` instead of canonical `input_payload`, and clean builds displayed a false `-dirty` label because tracked generated build output had been removed. Portal fix-forwards now report `ready` and clean commit `357e2ad9`; all maintenance warnings/fallbacks are clear and both TEST target groups are healthy.
- Ops/Engineering Audit/Local: Consolidated the completed local remediation programme into one shared + admin + portal + planned-schema release boundary for future TEST rehearsal and exact-source PROD promotion. Infrastructure descriptor activation, payment/Intacct activation, runtime/workflow/data promotion, and historical repair remain separate authority gates; no deployment was performed.
- QA/Backend Composition/Local: Closed the audit's last locally actionable test gap. The complete admin and portal Express stacks now mount under an explicit injected test mode without listeners or startup jobs; disposable HTTP tests exercise real authentication, parsing, late route order, and the portal's core completion transaction. Normal process startup is unchanged, full aggregate tests pass, and no environment or database was accessed.
- Fix/Payments, Budgets, and Intacct/Local: Completed engineering-audit tranche R7 locally. Payment evidence is packet-contained; case changes cannot retain or restore another case's payment packet/actions; allocations apply exactly once under competing callers; external handoffs commit durable attempts and quarantine uncertain outcomes; and PATH/mock parse current Sage-style success envelopes with required external IDs. The additive schema and full regressions pass. Payment routing remains disabled and no email, Sage provider, DEV, TEST, or PROD environment was accessed.
- Fix/Legacy Cleanup and Workflow Studio/Local: Completed engineering-audit tranche R6b locally. Intake component authoring now offers only supported static options; Clear Test Data removes client-account events before clients and verifies the relationship before commit; and the never-used PTMA UI/API/application compatibility surface is retired. NWAC Hub Management remains available through a separate System Administrator-only, Hub-scoped partial-update API. The physical legacy-named table was retained and no environment or database was accessed; TEST and PROD were not changed.

## 2026-07-11
- Fix/AI Configuration/Local: Completed engineering-audit tranche R6a locally. Admin and portal now resolve model, parameters, and fallback chain from one DB-backed runtime contract with deployment environment defaults; partial parameter saves preserve untouched values; configuration buttons send real JSON and surface errors; ordinary staff cannot override the approved model; and both apps fail closed on disallowed configured models. TEST and PROD were not accessed or changed.
- Fix/Notifications/Local: Completed engineering-audit tranche R5b locally. Reminder due/overdue workers now use stable lifecycle-generation claims, and committed workflow events enqueue durable bell/email fan-out with multi-instance claims, retry/backoff, recipient dedupe, dead-letter/ambiguous quarantine, System Administrator inspection/replay attribution, and 90-day successful-delivery retention. Shared, admin, portal, and additive schema must release together; TEST and PROD were not accessed or changed.
- Fix/Data Integrity/Local: Completed engineering-audit tranche R5a locally. Application-backed action plans now retain their validated selected-application lineage; approval linking uses the application's canonical client and fails closed on ownership conflict; contact notes no longer guess staff authors by email; and Client Batch Import transactionally claims retries/identities and rechecks case cardinality after locking. Additive schema and focused concurrency/identity tests passed; ambiguous historical rows remain separate read-only review queues. TEST and PROD were not accessed or changed.
- Ops/Schema Safety/Local: Completed engineering-audit tranche R4b locally. Applied migration filenames now fail on unrecognized checksum drift; runtime admin/portal/shared paths no longer create or alter schema; schema-aware readiness is the PROD smoke target; the ESDC prepared event enum is migration-owned; and Case Detail fails explicitly on canonical schema mismatch instead of returning partial data. Full tests passed; TEST and PROD were not accessed or changed.
- Ops/Release Safety/Local: Completed engineering-audit tranche R4a locally. Standard WSL launchers no longer depend on PowerShell/Windows environment syntax; deploys run exact-tree tests, lint, and privacy checks before mutation; prebuilt bundles require exact checksummed provenance; and full PROD packaging prepares immutable component objects plus a release descriptor before refresh. The live bootstrap remains on compatibility latest keys pending a separate activation plan. Full admin/portal tests passed; TEST and PROD were not accessed or changed.
- Fix/Public Portal/Local: Completed engineering-audit tranche R3c locally. Protected portal screens now require an applicant account; intake navigation keeps prior answers server-side; message/signing badges and lists use least-data responses; signed copies are authorized and presigned only at click time; Clear Deleted Items reaches its owner-scoped route; and the stale applications endpoint is retired. Full portal tests passed; TEST and PROD were not accessed or changed.
- Fix/Case Workspace and Workflow Library/Local: Completed engineering-audit tranche R3b locally. Case/application route changes now hide prior Case Workspace records and selections immediately, disable record actions until the new scope is loaded, release the prior application lock, stop abandoned retries, and ignore stale responses. Workflow Library now issues one initial load instead of starting and aborting a duplicate. Full admin tests passed; TEST and PROD were not accessed or changed.
- Fix/Manual Intake/Local: Completed engineering-audit tranche R3a locally. Manual Intake no longer stores incomplete applicant answers, notes, searches, or selected clients in browser storage; legacy residue is cleared at page/auth boundaries. Existing-client selection is bound to the current query, applicant identity, and search generation, stale responses are ignored, and the backend fails closed when selected-client strategy or available email/DOB evidence conflicts. Full admin tests passed; TEST and PROD were not accessed or changed.

## 2026-07-10
- Fix/Public Portal/Local: Completed engineering-audit tranche R2 locally. Applicant signing requests now reserve one immutable completion identity, conditionally create one signed PDF object, structurally link one canonical document, and emit one stable `document_signed` event across concurrent submissions and partial-failure retries. The additive migration, shared runtime, and portal code are not deployed; TEST and PROD were not accessed or changed.
- Ops/Public Portal/PROD: Deployed portal-only release `20260710-r1-intake-completion-prod` from clean, pushed portal commit `1b4734b`; schema, data/runtime/workflow promotion, admin, separate shared deployment, historical repair, and synthetic PROD submission were excluded. Portal warning/five-minute notice/ALB fallback/ASG refresh/fallback clear/normal-routing smoke/source-and-integrity-check/warning-clear sequencing passed. ASG refresh `4f1fdc39-0de0-40ca-8db8-c22be38c9dee` completed on `i-08d327a3ad96c6b77`; both public portal health URLs returned `200`; source command `5c64b682-60cc-4439-82aa-ef2ec9b67eb3` confirmed the R1 router/validator/transaction markers; postflight SQL `b43bda4e-2582-4055-bc70-5a8ef0911178` found zero orphan submissions, duplicate links, or active/non-terminal ownership conflicts; feedback `#35` received guarded prerelease and closeout notes and remained closed; final maintenance SQL `3ff117a8-e35d-4811-9438-bafdcd3f7b8b` returned zero announcement rows. Manifest: `tmp/path-deploy/prod/20260710-r1-intake-completion-prod--2026-07-10T22-20-48-451Z.json`.
- Ops/Public Portal/TEST: Deployed portal-only release `20260710-r1-intake-completion-test` from clean portal source `4538702` with schema, data/runtime, admin, separate shared deployment, TEST DB refresh, and PROD skipped. Portal-scoped warning/fallback sequencing, normal target health, and deployed-source markers passed. Authenticated SSM rehearsal `d4f5868a-1475-4a21-beb6-a52212dee0ee` proved published workflow `21` rejects missing consent with field/step `422` and zero writes, creates one coherent core result plus five signed-form objects for valid input, returns that result idempotently on retry without repeated work, and cleans DB/object/Cognito residue. PROD remains unchanged.
- Fix/Public Portal: Completed engineering-audit tranche R1 locally, in TEST, and in PROD. Final intake completion now validates all applicable published-workflow requirements, including required signatures and conditional branches, before writing; client/submission/application/case persistence commits as one rollback-safe transaction, and coherent retries return the existing result. Actual-route failure injection, deployed real-MySQL TEST evidence, and read-only PROD source/integrity acceptance all pass.
- Security/PTMA/Local: Moved all retained `/api/ptmas` reads and mutations into one router protected by shared System Administrator middleware. Added request-level coverage for all five methods across non-admin roles and System Administrator success. This is local-only and not yet deployed; the broader dormant/broken PTMA surface remains tracked separately.
- Ops/Migrations/Local: Made canonical migration failure fail closed after its audit row is recorded. Local and remote applies now throw `schema_migration_apply_failed`, the schema CLI exits nonzero, and deploy/TEST-refresh parents defensively reject failed child payloads before later steps or successful manifests. Failure-injection tests cover rollback, tracking, halt-before-next-file, and parent validation; no schema or environment was changed.
- QA/Testing: Made `npm test` / `npm run test:all` deterministic aggregate commands for admin and portal. Admin now runs 52 frontend suites / 221 tests plus 9 backend/authorization/validation/tooling suites / 40 tests; portal runs 13 frontend suites / 62 tests plus all four backend auth/notification files / 24 tests. Reconciled retired RBAC role assertions, enabled the outside step-validator suite through supported Babel/Jest config, and converted the radio validation logger into assertions.
- Security/Testing: Replaced the four stale privacy route-smoke patterns with route-isolated Express checks for both conflict actions, current feedback-review policy, and each template/notification/sender route. All 71 checks pass, and mutation self-tests prove removed guards fail without borrowing markers from sibling routes.
- Ops/PROD/Release: Deployed `20260710-conflict-disposition-notes-prod` as shared + admin + planned-schema with `--skip-data --skip-portal --skip-smoke`; no portal artifact, runtime/config promotion, allowlisted data, arbitrary SQL, feedback-record mutation, database restore, or Terraform change was included. The plan confirmed clean packaged source, one pending migration, and automatic restore point `path-prod-20260710-conflict-disposition-notes-prod-202607101040`, which reached `available` before schema apply. Sequence used all-surface warning, full five-minute notice, all-surface ALB fallback, migration/shared/admin deploy, replacement local health, fallback clear for ELB evaluation, normal-routing all-surface smoke, deployed-source/schema checks, warning clear, and maintenance cleanup. Shared and admin artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip` and `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; ASG refresh `4d296c74-9ed3-4dc9-b627-2a9220bb5643` completed successfully on replacement instance `i-022944b5d9fbc3658`. Final smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`. Deployed-source SSM command `a89b9e33-ae7b-4a29-a383-cd08e2b16812` confirmed release `20260710-conflict-disposition-notes-prod`, production build commit `13ee8be3`, transactional admin markers, shared event markers, local admin/portal health, and both PM2 processes online. SQL-over-SSM command `a99e0d95-8662-4852-8ea9-70a313a731d4` confirmed migration `20260709_0001_add_conflict_declaration_disposition.sql` succeeded at `2026-07-10 10:44:08` UTC with all four columns and resolver FK. Fallback returned to normal and SQL-over-SSM command `4c2e7a90-1455-4cd6-8a03-deaf98ba7256` confirmed `service_announcement_rows = 0`. Manifest: `tmp/path-deploy/prod/20260710-conflict-disposition-notes-prod--2026-07-10T10-40-08-931Z.json`.
- Ops/TEST/Release: Deployed `20260710-conflict-disposition-notes-test` as admin + planned-schema with `--skip-data --skip-portal --skip-smoke`; the admin artifact packaged shared commit `49ccb73`, and no portal artifact, runtime/config promotion, allowlisted data, arbitrary SQL, TEST DB refresh, PROD change, or feedback-record mutation was included. Sequence used an admin-scoped Test and Training warning, full five-minute notice, admin ALB fallback, migration/app deploy, fallback clear, normal-routing target smoke, deployed-source/schema checks, instance-local deployed-bundle browser smoke, warning clear, and maintenance cleanup. Migration `20260709_0001_add_conflict_declaration_disposition.sql` applied successfully at `2026-07-10 10:22:14` UTC; SQL-over-SSM command `383f7928-7560-4d55-84a8-f22e4c5d5749` confirmed all four disposition columns and resolver FK. Admin target `i-052566d75e0214d00:5001` was healthy; source command `6af8b7a0-e1ad-4249-b086-e1a8d403780b` confirmed the transactional admin and shared event markers; instance-local browser command `752b6ecd-bb26-4b9e-a9de-207c769f1809` exercised required Resolve notes and atomic Reassign payloads against the deployed bundle. Fallback returned to normal and SQL-over-SSM command `5588bf9e-e923-42f9-b247-c5539d06aab0` confirmed `service_announcement_rows = 0`. Manifest: `tmp/path-deploy/test/20260710-conflict-disposition-notes-test--2026-07-10T10-21-30-277Z.json`.

## 2026-07-09
- Ops/PROD/Release: Deployed portal-only PROD release `20260709-portal-application-start-gate-prod` with `--skip-schema --skip-data --skip-admin --skip-shared --skip-smoke`; no schema, data/runtime config, admin/shared artifact, TEST DB refresh, PROD data repair, arbitrary SQL repair, intake runtime promotion, or Terraform change was deployed. Sequence used a portal-scoped warning, portal ALB fallback, PROD deploy, local replacement health check, fallback clear for ELB evaluation, normal-routing portal smoke, deployed-source marker check, warning clear, and maintenance cleanup. Portal artifact `s3://nwac-prod-artifacts/portal/portal-latest.zip` installed through PROD ASG refresh `fb224fde-5bc9-4237-910b-82188e750ab7` on replacement instance `i-000fb0ded441b6d32`; normal-routing smoke returned `200` for `https://iset.nwac.ca/healthz` and `https://nwac-public.awentech.ca/healthz`; local health command `96deba71-2240-4655-965f-10fe2c940c3b` confirmed portal `:5000` and PM2 online before fallback clear; deployed-source command `e1a87e64-fb6b-4910-b2bd-e27f9bed9235` confirmed release id `20260709-portal-application-start-gate-prod`, portal commit `059c793f`, production build, application-only `submission_id IS NULL` start gate, dashboard `detail_available` handling, eligibility route, and local portal health. Fallback returned to normal and SQL-over-SSM command `543b506a-4e7a-41a6-b1dd-e254274ecff6` confirmed `service_announcement_rows = 0`. Feedback report `#158` closeout was already present: SQL-over-SSM commands `57052124-d988-42d4-8003-a593c3bc3c73` and `6b125306-6a07-4754-89b9-cc8b96555303` confirmed status `closed`, status history, and the Codex triage note; no additional feedback-log mutation was applied. Manifest: `tmp/path-deploy/prod/20260709-portal-application-start-gate-prod--2026-07-09T23-13-26-953Z.json`.
- Ops/TEST/Release: Deployed portal-only TEST release `20260709-portal-application-start-gate-test` with `--skip-schema --skip-data --skip-admin --skip-shared --skip-smoke`; no schema, data/runtime config, admin/shared artifact, TEST DB refresh, PROD change, or arbitrary SQL repair was deployed. Sequence used a scoped Test and Training portal warning, portal ALB fallback, portal deploy, fallback clear, normal-routing portal target smoke, deployed-source marker check, warning clear, and maintenance cleanup. Final portal artifact `s3://nwac-test-artifacts/portal/portal-20260709-152739.zip` installed on `i-052566d75e0214d00`; target smoke was healthy for portal `:5000`; deployed-source command `67fb17f0-e90a-4550-bf6d-61efcf59f8df` confirmed the release id, application-only `submission_id IS NULL` start gate, dashboard `detail_available` handling, eligibility route, and local portal health; SQL-over-SSM command `ea7c0583-0071-4256-88f0-e0b8e8a44024` confirmed `service_announcement_rows = 0`. Manifest: `tmp/path-deploy/test/20260709-portal-application-start-gate-test--2026-07-09T19-27-39-671Z.json`.
- Fix/Conflicts/DEV: Reworked conflict-of-interest disposition so resolving a declared conflict preserves the original declaration, requires reviewer notes, writes a `cleared` audit disposition, unlocks the declaring staff member, and sends them a direct applicant-name notification with the notes. Conflict-driven reassignment now accepts optional reviewer notes, writes a `reassigned` disposition/event, and sends a direct staff notification naming the applicant, new assignee, and note when supplied.

## 2026-07-08
- Fix/Casework/DEV: `Add existing intervention` now stamps manual-backload interventions with `reviewed_at` from the entered intervention start date, so approval-date financial reporting places future historical entries in the inferred historical fiscal year while preserving the PATH entry audit date in `created_at`.
- Ops/TEST/Release: Deployed TEST release `20260708-critical-feedback-reporting-test` as app/schema-only with `--skip-data --skip-smoke`; the canonical `admin_feedback_critical` notification migration applied successfully, no TEST DB refresh/runtime config/data promotion/PROD change was run, and final normal-routing target smoke was green for admin `:5001` and portal `:5000` on `i-052566d75e0214d00`. Deployed-source marker command `d3c6bce6-04d5-4ccf-86a2-92f5ee536fa3` confirmed release id, manual backload approval-date code, Critical feedback notification code, shared dispatcher code, Regional Snapshot label, and PM2 processes; maintenance fallback returned to normal and `service_announcement_rows = 0`.
- Ops/PROD/Release: Deployed PROD release `20260708-critical-feedback-reporting-prod` as full shared + admin + portal app/schema-only with `--skip-data --skip-smoke`; no data promotion, runtime config, arbitrary SQL repair, intake runtime promotion, Terraform, or DB restore was deployed. Restore point `path-prod-20260708-critical-feedback-reporting-prod-20260709005` was captured before applying `20260708_0001_seed_admin_feedback_critical_notification.sql`; ASG refresh `d09452ad-b3a4-4580-8943-d60047f6393b` completed on replacement instance `i-09b726950738aae8e`; final normal-routing smoke returned `200` for admin, `iset.nwac.ca`, and public portal. Source-marker command `4156f52f-ff21-4f6d-a4b1-128e87a03e64` confirmed release id, manual-backload approval-date code, Critical feedback notification code, shared dispatcher code, Regional Snapshot label, and PM2 processes. SQL-over-SSM command `37e19a68-df6f-4c5b-8cac-34ad45b14f02` confirmed the migration succeeded and exactly one enabled System Administrator `admin_feedback_critical` email setting exists; fallback returned to normal and `service_announcement_rows = 0`.
- Ops/PROD/Data Repair: Completed the global follow-up sweep for manual-backloaded historic interventions by setting `reviewed_at` from `start_date` on all 31 remaining rows with blank `reviewed_at`, preserving PATH entry dates in `created_at`, and inserting 31 `data_repair` case events. Repair by region: AB 4 / `$18,879.70`, BC 2 / `$0.00`, NB 3 / `$8,900.00`, NS 2 / `$26,747.00`, PE 1 / `$13,949.00`, SK 17 / `$89,102.35`, Unknown 2 / `$0.00`. SQL artifact: `sql/ops/prod-global-backload-intervention-reporting-date-repair-20260708.sql`; SSM command `3ea4e437-696c-4e37-b2b5-d09a1dc6c48f`; verification command `9ac4a122-7112-4878-88ef-ca705655e326` confirmed zero remaining candidate rows.
- Ops/PROD/Data Repair: Corrected 13 BC manual-backloaded historic funded interventions so Financial Reports use inferred historic approval dates from intervention `start_date` instead of PATH entry dates. The guarded repair updated `reviewed_at`, inserted 13 `data_repair` case events, and moved BC FY 2026/27 approved-funding totals from 17 interventions / 14 clients / `$227,141.82` to 4 interventions / 4 clients / `$24,052.38`. SQL artifact: `sql/ops/prod-bc-backload-intervention-reporting-date-repair-20260708.sql`; SSM command `0768b005-11a4-479e-bcc3-eaf5bbc4297d`; verification command `31a32046-1791-488a-b5f2-0ef73b742adc`.
- Fix/Notifications/DEV: Critical bug reports and change requests now emit `admin_feedback_critical` when first saved as Critical or when an existing report is upgraded to Critical. Added a System Administrator email template/setting seed, a severity-save endpoint, and a severity control in the Bugs and Change Requests review panel.
- Ops/PROD/Data Repair: Assigned case `112` / application `30` (`ISET-20260420-F9AB7B`) to Derry Yellowfly, stamped the case portfolio region as Alberta, set review workflow `10` current owner to Derry, and inserted the missing Derry `Pending Review` bell notification. Amanda Curtis remains the submitting RM in the review event history. Preview SSM command `268c9ea8-d96f-4dbd-bc65-d84a2982ba67`; apply SSM command `1ff1c786-2e0c-47bb-9e3c-67ab84186936`; verification command `0426a5bc-9b69-4f66-89b3-294f4a223148` confirmed the item is visible to Derry under current queue logic.
- UX/Reporting: Renamed the Regional Snapshot Client Activity label from `Approved / Funded Applications` to `Approved Applications` in the dashboard and Excel export so readers do not compare application-status counts with the separate approved-funding funded-client metrics.
- Ops/Source Control: Converted `/home/bill/ISET/shared` from an untracked sibling directory into a Git repo for shared PATH runtime code used by admin and portal, created private GitHub remote `BrimborionInc/iset_shared`, and pushed `main`. Added `README.md`, `AGENTS.md`, `.gitignore`, and `.gitattributes` in the shared repo; updated deployment docs so preflight checks admin, portal, and shared status explicitly. Updated `scripts/path-deploy.js` so staged deploy artifacts exclude `.git` directories before packaging.
- Ops/TEST/Release: Deployed TEST release `20260708-admin-user-ei-notification-fix` as app/schema-only with `--skip-data`; schema pending count was `0`, and no TEST DB refresh, runtime config, allowlisted data, Terraform, or PROD data change was run. Sequence used the Test and Training warning, all-surface ALB fallback, deploy, fallback clear, normal-routing smoke, deployed-source check, warning clear, and maintenance cleanup. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260708-055531.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260708-055906.zip` installed on `i-052566d75e0214d00`; final target smoke was healthy for admin `:5001` and portal `:5000`; source-marker command `87e733cd-c2cc-44c6-9883-94326353c8a4` confirmed the release id, suppressed Cognito create flow, EI dependency guard, and shared applicant-name resolver; `service_announcement_rows = 0`.
- Ops/PROD/Release: Deployed PROD release `20260708-admin-user-ei-notification-fix` as full shared + admin + portal app/schema-only with `--skip-data --skip-smoke`; schema pending count was `0`, restore point skipped as `no-db-mutation-planned`, data dataset was `none`, and the source guard was clean. ASG refresh `c5c6503c-1fef-4301-a6cf-89710b6e52b5` completed on replacement instance `i-02150848df7b6aca7`; local health recheck `4b33d6c4-c1fc-4680-8b4c-d13a4011bf5a` passed before clearing fallback for ELB evaluation; final smoke returned `200` for admin, `iset.nwac.ca`, and public portal. Source-marker command `71540db2-8328-4071-ab6f-13c8263ad243` confirmed release ids in admin/portal plus the Cognito, EI, and shared notification markers. Fallback returned to normal forwarding and `service_announcement_rows = 0`.
- Ops/Feedback/PROD: Reconciled feedback report `#157` after PROD release `20260708-admin-user-ei-notification-fix`. The earlier PROD EI data correction remains complete, and the deployed product fix was verified by focused DEV Cognito/browser smoke, TEST deploy/source/smoke, and PROD deploy/source/smoke. Report `#157` moved from `planned` to `resolved` by SQL-over-SSM command `a4372be3-1a30-474f-b083-474d098950c8`; SQL artifact: `sql/ops/prod-feedback-157-resolved-after-release-20260708.sql`.

## 2026-07-07
- Fix/Auth/User Management: Hardened staff user creation so Cognito setup emails are sent only after the requested Cognito group and DB-backed `staff_profiles` / `staff_region` access sync succeeds. Create now uses suppressed Cognito creation first, transactional staff-profile/region sync where supported, then Cognito `RESEND`; failures attempt to delete the newly created Cognito user and return an error instead of showing a false dashboard success.
- Fix/Auth/User Management: Staff-user create now sends the submitted staff name to Cognito's standard `name` attribute during both the silent create and final invite resend, keeping Cognito profile details aligned with the DB-backed `staff_profiles` row.
- Fix/Notifications/DEV: Shared bell and staff-email notification content now resolves applicant display labels from case/application/client/submission context before account display fields, so applicant-related alerts show first/last or legal/full names consistently when present and fall back to the registered applicant email only when no name is available. This covers two-step review final-decision alerts and secure-message alerts that previously could show email-shaped account display names.
- Ops/Auth/User Management: Updated the DEV, TEST, and PROD staff Cognito admin-created-user invite templates via AWS CLI so setup emails identify NWAC PATH, include a simple `Environment: ...` line, and put username/password values on separate copyable lines instead of using Cognito's generic temporary-password copy. Terraform identity modules now carry environment-specific invite subject/body variables for DEV/TEST/PROD alignment. The PROD live update was applied to pool `ca-central-1_IBtdWzSIW` without an app restart after `nwac-prod-codex-operator` received the required Cognito user-pool permissions.
- Ops/PROD/Data Repair: Applied guarded correction for feedback `#157`, changing case `109` / application `27` assessment EI eligibility from `EI Active Claim` to `EI Reach Back` after verifying no action-plan or intervention dependencies existed. Application row version advanced to `32`, case event `199` records the repair, and the feedback report moved to `in_progress` pending a product UI fix for authorized post-submission EI corrections. SQL artifact: `sql/ops/prod-feedback-157-ei-status-correction-20260707.sql`; SSM command `ac4549e1-f9c9-43d2-a552-27b2b61d025a`.
- Fix/Assessment/DEV: Kept the existing Application Assessment EI status dropdown editable for Regional Manager, NWAC Administrator, and System Administrator users after submission while the application is not final/locked. The backend now blocks changed EI eligibility once the case has action-plan or intervention dependencies, marks ILMP readiness stale when applicable, bumps the application row version, and writes a quiet case-event audit row for true corrections. The UI now lets the dependency-block message surface instead of mislabeling it as a row-version conflict.
- QA/Assessment/DEV: Added and passed `npm run smoke:application-assessment:ei-correction:dev`, a focused DEV smoke for feedback `#157` that creates disposable DEV Cognito Regional Manager and ISET Coordinator users, seeds a synthetic Nunavut submitted-assessment fixture, authenticates real staff tokens via Hosted UI fallback, verifies RM EI correction through the real backend and browser dropdown, verifies Coordinator denial, verifies dependency blocking after an active action plan, and cleans up all synthetic Cognito/DB rows.
- QA/Auth/User Management: Added route-level tests for the staff create flow covering silent Cognito create, post-DB invite send, rollback on DB sync failure, and DB-unavailable failure before Cognito mutation. Live DEV and TEST Cognito smoke checks created suppressed disposable admin users, added `Regional_Manager`, exercised invite `RESEND`, verified group/status, and deleted the users from pools `ca-central-1_inn3R0tte` and `ca-central-1_uvypDUOwa`.

## 2026-07-06
- Ops/PROD/Approvals: Completed a read-only application-assessment two-step status discovery audit. PROD has 133 application-assessment rows and 12 active `application_assessment` workflows; active workflow/status pairings were coherent, with zero active workflows missing assessment rows, zero duplicate active workflow subjects, and zero pending-decision application assessments without a workflow. No broad status migration is indicated from this discovery pass.
- Ops/PROD/Data Repair: Applied a guarded targeted repair for application `11` / case `93` / `ISET-20260410-EC36E2`, changing only the stale scoped `case_context_json.applicationDecisionLetters["11"].assessment_nwac_review_status` value from `approve` to `reject` so it matches the authoritative denied application outcome and `no_recommend` / `agree` assessment state. Preview SSM command `d2ba4182-36c2-4bd5-80f4-0487049da940`; apply SSM command `44075235-a61f-49ed-95f4-a7d3b23a4882`; post-repair mismatch audit command `beba8099-304a-4838-b7f4-3f34da74ea6f` returned zero remaining decision/context mismatches.
- Ops/PROD/Approvals: Parked application `27` as requested after noting a document-classification oddity that does not change workflow status. Application `30` remains an owner-routing question because its active RM-review workflow has no RM notification row and no resolved owner from portfolio region, assigned Regional Manager region, or workflow owner.
- Ops/PROD/Approvals: Completed a read-only new-intervention-proposal two-step status discovery audit using `sql/ops/prod-intervention-proposal-two-step-discovery-20260706.sql`. PROD has two active `intervention_proposal` workflows: proposal `332` at Decision Maker review and proposal `339` final-approved. Summary command `30235ba3-6b96-4687-b6e1-44b78e08821b` returned zero active workflow/status issues, zero stranded submitted/in-review new-proposal candidates, zero duplicate active workflow subjects, zero missing generated packet document links, zero active RM-review routing gaps, and zero submitted/in-review underlying intervention rows without a compatibility proposal/workflow. No new-intervention-proposal data repair or broad migration is indicated.
- Ops/PROD/Approvals: Completed a read-only intervention-revision two-step status discovery audit using `sql/ops/prod-intervention-revision-two-step-discovery-20260706.sql`. PROD has one active `intervention_revision` workflow, the repaired feedback `#148` Case `16` item now final-approved; zero live revision review workflows remain active, zero submitted/in-review revision proposals or draft interventions are stranded without workflow, zero duplicate active subjects were found, and zero final-approved revision follow-up gaps were found. One old Case `7` revision remains a draft only, and workflow `9` has a non-blocking historical subject-column oddity after its temporary proposal/draft rows were removed. No intervention-revision migration or data repair is indicated.

## 2026-07-05
- Fix/Approvals/TEST: Fixed the remaining two-step review notification contract gaps found by the live TEST smoke. New intervention proposals created from an action plan now emit `rm_review_requested` when they enter Regional Manager review, and intervention Decision Maker request-changes events use `nwac_review_changes_requested` while keeping the proposal/revision's persisted review status as submitted during `returned_to_rm`. The shared bell formatter now uses intervention wording for those two-step change-request alerts.
- QA/Approvals/TEST: Added and passed `scripts/two-step-review-test-smoke.js`, a live TEST smoke covering application assessments, new intervention proposals, intervention revisions, real Cognito role hydration, role/final-decision guards, edit locks, route text, generated documents, notification routing, browser errors, and cleanup. Passing SSM command: `8428df04-2235-46e6-9533-7187a7260ac3`; cleanup reported zero synthetic DB rows/documents/notifications/staff users and follow-up Cognito checks found no smoke users.
- Ops/TEST/Release: Recovered and deployed TEST release `20260705-two-step-review-test-notification-fix` on replacement host `i-052566d75e0214d00` after the old single TEST app host stalled during an admin deploy. The recovery exposed an admin/portal runtime coupling on fresh ASG instances; portal runtime was installed before the final admin redeploy. Final normal-routing smoke reported admin and portal target groups healthy, fallback returned to normal forwarding, and `service_announcement_rows = 0`.
- Ops/TEST/Deploy Tooling: Hardened the TEST deploy orchestrator so admin and portal dependency installs use isolated npm caches and admin deploy starts `nwac-admin` when PM2 has no existing process. This prevents the fresh-ASG-instance failure mode found during the two-step review TEST recovery.
- Ops/PROD/Release: Deployed full shared + admin + portal PROD release `20260705-two-step-review-test-notification-fix` with `--skip-schema --skip-data --skip-smoke`; no schema, data, runtime-config, intake runtime, Terraform, or cross-environment data promotion was deployed. Sequence used all-surface warning, all-surface ALB fallback, deploy under fallback, local replacement health check, fallback clear for ELB evaluation, normal-routing smoke, deployed-source marker check, and warning clear. PROD identity was `arn:aws:sts::468278742295:assumed-role/nwac-prod-codex-operator/codex-prod-operator`; ASG refresh `82a399ed-0a01-40a1-bc4d-c31d04674537` completed on replacement instance `i-0b035421748e15738`; final smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; source-marker SSM command `a37dda23-0198-4f27-aba1-c48715d357ed` confirmed the release ID, admin intervention notification markers, and shared intervention bell wording marker. The deploy used a recorded dirty-source override approved for this specific release package; the portal repo was clean and no unrelated data/runtime scope was included. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260705-two-step-review-test-notification-fix--2026-07-05T15-44-14-208Z.json`.
- Ops/Release Management: Added the user-requested release-scope ownership rule to `docs/AGENTS.md` and the deployment runbooks: Codex owns deciding which code/config files belong in a release and should only pause for true business/data/runtime ambiguity with concrete consequences, never for implementation-level file-composition questions.
- Ops/Release Management: Added `docs/ops/deployments/major-workflow-release-management.md` as the durable corrective gate from the two-step review rollout incident. Future major workflow releases must be controlled by a business-state contract covering roles, states, queues, editability, generated artifacts, notifications, data repair, feedback reconciliation, and owner communication. Linked the gate from the agent entry point, deployment quick guide, and two-step review workflow note.
- Ops/PROD/Release: Deployed full admin + portal PROD release `20260705-secure-message-batch` with `--skip-schema --skip-data --skip-shared`; no schema, data/runtime config, shared, Terraform, or cross-environment data promotion was deployed. Sequence used all-surface warning, all-surface ALB fallback, deploy with `--skip-smoke`, local replacement health check, fallback clear for ELB evaluation, normal-routing smoke, deployed-source marker check, feedback closeout, and warning clear. ASG refresh `d4f1d52e-bba3-45ea-9cab-6aaafb27e74b` completed on replacement instance `i-0a24b80359df52380`; final smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; source-marker command `bd63dd80-c45b-4267-857d-9ac2014a137b` confirmed the secure-message withdrawal/recipient-confirmation and applicant-name markers. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260705-secure-message-batch--2026-07-05T13-34-06-602Z.json`.
- Ops/Feedback/PROD: Reconciled feedback reports `#154` and `#155` after PROD release `20260705-secure-message-batch`. Read-only validation confirmed wrong-recipient message `1128` remains withdrawn/redacted with both mailbox copies deleted and no linked attachments/signing requests; deployed selector validation returned `Molly Hink` for report `#155` context `ISET-20260429-AF259F` where the portal account display name is email-like. `#154` remains `in_progress` pending final incident/business-owner follow-up, and `#155` remains `in_progress` pending a real applicant-origin email/staff confirmation rather than being marked resolved without a sent artifact. SQL artifacts: `sql/ops/prod-feedback-secure-message-batch-prerelease-20260705.sql` and `sql/ops/prod-feedback-secure-message-batch-prod-closeout-20260705.sql`.
- Ops/TEST/Release: Deployed full admin + portal TEST release `20260705-secure-message-batch` with `--skip-schema --skip-data --skip-shared`; no schema, data/runtime config, shared, Terraform, PROD change, or feedback-log mutation was run. Sequence used Test and Training warning, admin+portal ALB fallback, deploy, fallback clear, normal-routing smoke, warning clear, deployed-source check, and maintenance-state cleanup. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260705-091238.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260705-091716.zip` installed on TEST host `i-0a8be782ed8604211`; final smoke reported admin `:5001` and portal `:5000` healthy; deployed-source command `ce4cbac4-f71b-46ac-b7d7-db16774efdb0` confirmed the secure-message withdrawal/recipient-confirmation and applicant-name markers; `service_announcement_rows = 0`.
- Ops/Feedback/DEV: Added `docs/ops/prod-feedback-batch-repair-tracker-20260705.md` to track the current PROD bug/CR repair batch separately from the live feedback table. The tracker records #154 as contained with an admin fix prepared, #155 as a portal notification-name fix prepared, and #96/#97/#123 as still pending re-triage; it also records the no-piecemeal-deploy batch rule for this repair stream.
- Fix/Secure Messaging/DEV: Added an explicit audit-preserving `Withdraw sent message` path for plain staff-to-applicant secure messages after feedback `#154`. Staff compose now locks the case-derived recipient display and requires recipient/case confirmation before Send; local Deleted Items wording now says mailbox cleanup instead of implying recall. Backend withdrawal redacts the live message subject/body, marks the master message deleted/archived, moves sender/applicant mailbox rows to Deleted, redacts the original send-event subject, records a withdrawal audit event using the existing `message_deleted` event type, and fails closed for messages with linked files or signing requests pending a reviewed artifact-aware repair.
- Ops/Feedback/PROD: Contained feedback report `#154` (`Deleted secure message`) after wrong-recipient secure-message triage. Bill approved guarded PROD SQL that redacted message `1128` subject/body to a neutral withdrawal notice, marked the master message deleted/archived, moved sender and recipient mailbox rows to deleted state, redacted the `staff_secure_message_sent` event subject, and added a live feedback note while leaving the report `in_progress` for privacy/business follow-up and a product fix for true recall/delete-for-everyone. Preview SSM command `76528c5b-b886-4735-8c5a-5f829c1f2ad9`; apply SSM command `d290da6c-2078-4e92-96d3-9163f05e9b68`.
- Ops/Release Safety: Added a PROD dirty-source guard to `scripts/path-deploy.js`. PROD app deploy runs now fail before restore points, schema/data steps, artifact uploads, ASG refresh, or smoke checks when the packaged admin, portal, or shared source tree is dirty. Emergency exceptions require `--allow-dirty --dirty-reason "<specific approved reason>"` and are recorded in the deploy manifest. Updated the deployment quick guide, PROD guide, and orchestrator reference.
- QA/Approvals: Recast the two-step review transition tests around the business acceptance matrix for ISET Coordinator, Regional Manager, and NWAC Administrator across application assessments, new intervention proposals, and intervention revisions. System Administrator behavior remains technical support/superuser-only and no longer defines the business matrix or staff-facing decision wording.
- Fix/Documents/Approvals: Fixed generated intervention assessment PDFs locally so new intervention proposal/revision packets create `iset_document_intervention` links as well as document metadata once deployed. PROD audit found existing packet PDFs for workflows `12` and `13` were generated but not intervention-linked; Bill approved and Codex applied guarded PROD repair SQL for those exact five document links.
- Fix/Approvals: Fixed intervention proposal compatibility syncing locally so final-decision updates no longer overwrite the proposal `submitted_at` timestamp once deployed. PROD audit found proposal `339` had `submitted_at` set to its final approval time instead of workflow submit time; Bill approved and Codex applied guarded PROD repair SQL for that exact timestamp repair.
- QA/Approvals: Added `npm run smoke:two-step-review:prevention`, a focused stubbed regression guard for the intervention document-link and proposal timestamp prevention fixes.
- QA/Approvals: Completed the current two-step review release gate locally on 2026-07-05: full Jest passed (48 suites, 203 tests), production build passed with existing warnings, `smoke:two-step-review:prevention` passed, and both application-assessment and intervention-assessment workflow browser smokes passed against the production bundle.
- Ops/TEST/Release: Deployed admin-only TEST release `20260705-two-step-review-prevention-test` with `--skip-schema --skip-data --skip-portal --skip-shared`; no schema, data, portal, shared, Terraform, runtime-config promotion, or PROD change was run. Sequence used a Test and Training admin warning, admin ALB fallback, deploy, fallback clear, normal-routing admin smoke, warning clear, deployed-source SSM check, and maintenance-state cleanup. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260705-073634.zip` installed on TEST host `i-0a8be782ed8604211`; final smoke reported `nwac-test-admin-tg` healthy on `:5001`, deployed-source command `1eb9b59e-6bc0-485f-b391-555d27cd8990` confirmed both prevention markers, and `service_announcement_rows = 0`.
- Ops/PROD/Release: Deployed admin-only PROD release `20260705-two-step-review-prevention` with `--skip-schema --skip-data --skip-portal --skip-shared --skip-smoke`; no schema, data, portal, shared, Terraform, or runtime-config change was deployed. Sequence used admin warning, admin ALB fallback, deploy, local replacement health check, fallback clear for ELB health evaluation, normal-routing admin smoke, deployed-source check, post-deploy two-step audit SQL, and warning clear. Source control was clean at commit `d0ddc56957a46761471b32c9e04956bfdf1ea53e`; ASG refresh `56e2371d-7fce-4f78-8d7c-257272bfa177` completed on replacement instance `i-0307e0c730b98a7bc`; final admin smoke returned `200`; deployed-source command `a1129867-9c9e-42cd-89ef-4635fdf12824` confirmed both prevention markers; post-deploy audit command `7405d66e-0bf6-4076-808d-114b562715e7` returned no known mismatch rows; fallback and warning were cleared.
- Ops/PROD/Approvals: Added workflow-first PROD audit SQL for two-step review damage. The initial read-only run found no broad missing workflow rows, invalid stages, or workflow/status mismatches across application assessments, intervention proposals, and intervention revisions. After the approved repairs, the audit returned no known mismatch rows.
- Ops/Feedback/PROD: Reconciled and resolved feedback report `#149` (`Pending Decision - Kitson/Wallace`) after the two-step data repair and prevention deploy. Live recheck confirmed Kitson proposal `332` / workflow `12` is correctly at `nwac_review` with repaired packet links, Wallace proposal `339` / workflow `13` is final-approved with the corrected submit timestamp, and the post-deploy audit has no known mismatch rows. SQL artifact: `sql/ops/prod-feedback-149-resolved-two-step-prevention-20260705.sql`; SQL-over-SSM command `84b08c90-55c2-499e-8691-a1692fb1e674`.

## 2026-06-26
- Fix/Approvals/DEV: Prepared systematic role-matrix hardening for the two-step Regional Manager review workflow after PROD feedback `#147` and `#148`. ISET Coordinators and Regional Managers can start supported application-assessment, new-intervention, and intervention-revision workflows; admin roles cannot start workflows and remain final-decision actors only. Regional Managers can submit and then RM-review/sign off their own submitted item for the agreed first-pass audit trail, but still cannot record final Decision Maker decisions. Added matrix transition coverage plus browser smoke coverage for two-step Coordinator assessment submit, RM draft assessment submit, RM draft new-intervention submit, and RM draft intervention-revision submit.
- Ops/TEST/Release: Deployed TEST release `20260626-rm-two-step-role-matrix-test` as an admin-only hotfix rehearsal with `--skip-schema --skip-data --skip-portal --skip-shared`; no schema, data, portal, shared, Terraform, or runtime-config change was deployed. Sequence used Test and Training admin maintenance warning, wait, admin ALB fixed-response fallback, deploy, fallback clear, normal-routing target-group smoke, deployed-source SSM check, and warning clear. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260626-182057.zip` installed on TEST host `i-0a8be782ed8604211`; final admin target-group smoke was healthy on `:5001`.
- QA/TEST/Approvals: Completed live TEST role-route verification for the 2026-06-26 two-step workflow fix using dedicated TEST Cognito/staff users for ISET Coordinator, Regional Manager, NWAC Administrator, and System Administrator. Deployed-source checks confirmed only ISET Coordinator and Regional Manager can start `application_assessment`, `intervention_proposal`, and `intervention_revision` workflows, while NWAC/System Admin and stale `Case Manager` cannot. Live API checks passed: RM application-assessment submit created workflow `application_assessment/rm_review`; NWAC/System Admin new-intervention submit attempts returned `403 review_workflow_transition_forbidden`; RM new-intervention submit created `intervention_proposal/rm_review`; RM intervention revision submit created `intervention_revision/rm_review`; revision submission generated submitted v2 and redline v2 PDFs. Disposable TEST cases, rows, locks, and S3 objects were removed after verification.
- Ops/PROD/Release: Deployed PROD release `20260626-rm-draft-submit-hotfix` as an admin-only hotfix with `--skip-schema --skip-data --skip-portal --skip-shared`; no schema, data, portal, shared, Terraform, or runtime-config promotion was deployed. Sequence used an admin in-app warning, admin ALB fixed-response fallback, PROD ASG refresh `4eafe107-55a1-473a-9cbc-e7fd20962f4f`, fallback clear after refresh, normal-routing health smoke, deployed-source SSM check, runtime transition check, targeted live Case 129 recheck, and warning clear. Replacement instance: `i-0253edba712ce48ca`; final admin health smoke returned `200`.
- Ops/Feedback/PROD: Reconciled live feedback report `#147` (`'Review workflow transition forbidden"`) after PROD release `20260626-rm-draft-submit-hotfix`. Runtime source and transition checks confirmed Regional Managers can now start application-assessment review while intervention starts/final approval remain guarded; live data recheck showed Case 129 / application 50 is still `in_review`, assigned to Emilie Marion, and has no review workflow row, so the deployed fix applies when she retries.
- Ops/PROD/Release: Deployed PROD release `20260626-rm-two-step-role-matrix-prod` as an admin-only hotfix with `--skip-schema --skip-data --skip-portal --skip-shared`; no schema, data promotion, portal, shared, Terraform, or runtime-config change was deployed. Sequence used admin warning, admin ALB fallback, PROD ASG refresh `9eed731d-4330-4191-9583-12a333c8142d`, fallback clear, normal-routing admin smoke, deployed-source/runtime checks, targeted Case 16 repair, feedback closeout, and warning clear. Replacement instance: `i-047ed87247d2e408e`; final admin health smoke returned `200`.
- Ops/Feedback/PROD: Repaired and resolved feedback report `#148` after PROD release `20260626-rm-two-step-role-matrix-prod`. Case `16` / application `54` had source intervention `154` approved and revision intervention `198` / proposal `320` submitted by Emilie Marion (`staff_profiles.id=55`) with no review workflow and no v2 assessment/redline documents. Targeted repair created workflow `9` (`intervention_revision:proposal:320`, `rm_review`, owner `Regional Manager`) and generated active Supporting Documents `4913` (`Case manager assessment v2`) and `4914` (`Case manager assessment redline v2`); feedback `#148` moved from `in_progress` to `resolved`.

## 2026-06-24
- Fix/Assessment/DEV: Restored Regional Manager edit access for application-assessment drafts while the application is still `in_review` and no two-step review workflow exists. Submitted assessments remain read-only for Regional Managers and must move through RM review actions.
- Ops/TEST/Release: Deployed TEST release `20260624-rm-draft-edit-hotfix` as an admin-only app hotfix with `--skip-schema --skip-data --skip-portal`; no schema, data, portal, shared, Terraform, or runtime-config change was deployed. The build was precompiled with `PATH_DEPLOY_ENV=test PATH_RELEASE_ID=20260624-rm-draft-edit-hotfix npm run build:test` and deployed with `--skip-build --skip-smoke` under admin warning/fallback. The first TEST SSM install command stalled and was cancelled under fallback; after SSM recovered, deployed-source check confirmed the hotfix on the TEST host and normal-routing smoke passed.
- Ops/PROD/Release: Deployed PROD release `20260624-rm-draft-edit-hotfix` as an admin-only emergency hotfix with `--skip-schema --skip-data --skip-portal --skip-shared`; no schema, data, portal, shared, Terraform, or runtime-config change was deployed. Sequence used a 10-minute admin warning, admin ALB fallback, prebuilt production bundle with `--skip-build`, PROD ASG refresh `f972930b-62c6-4cc7-b82d-7df49894cb78`, local replacement health check, fallback clear for ELB health evaluation, normal-routing admin smoke, deployed-source check, and warning clear. Replacement instance: `i-07d89e5d81b3a2077`; final admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`.
- Ops/Feedback/PROD: Reconciled live feedback report `#146` (`Can't make edits to assessment`) after PROD release `20260624-rm-draft-edit-hotfix`. The report moved from `planned` to `resolved` with status-history row and closeout note documenting deployed-source verification and the live case/application state.

## 2026-06-23
- Ops/System Admin/IAM: Investigated and resolved the PROD `AWS Environment Status` widget `App Capacity` and `Database Stress` failures. Live PROD admin runtime uses `nwac-prod-app-role` and was denied `autoscaling:DescribeAutoScalingGroups`, `cloudwatch:GetMetricData`, and `rds:DescribeDBClusters`; TEST appeared healthy only because deployed admin `.env` static keys for `SES_backend` had those reads, while `nwac-test-app-role` itself was denied the same actions. Bill added inline policy `nwac-prod-app-aws-status-readonly` to `nwac-prod-app-role` with those three read-only actions, refreshed the widget, and confirmed PROD was working. Terraform desired state is aligned in the compute module.

## 2026-06-22
- Ops/TEST/Release: Deployed TEST release `20260622-path-bugfix-patch` as an app/schema release with `--skip-data`; no TEST DB refresh, runtime/config promotion, or data copy was run. Sequence used all-surface warning, wait, ALB fallback, deploy with `--skip-smoke`, fallback clear, normal-routing smoke, deployed-source SSM check, and warning clear. The first TEST admin SSM install command stalled before producing output and was cancelled under the active fallback; a fresh SSM echo check passed and the retry completed. Final smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260622-path-bugfix-patch--2026-06-23T00-08-36-902Z.json`.
- Ops/PROD/Release: Deployed PROD release `20260622-path-bugfix-patch` as an app/schema release with `--skip-data`; no runtime/config promotion or cross-environment data copy was run. PROD identity was `arn:aws:sts::468278742295:assumed-role/nwac-prod-codex-operator/codex-prod-operator`; schema pending count was `0`, restore point was skipped because the deploy command had no planned DB mutation, and shared/admin/portal artifacts uploaded to the fixed PROD latest keys. Sequence used all-surface warning, wait, ALB fallback, deploy with `--skip-smoke`, local replacement-instance health verification, fallback clear for ELB evaluation, normal-routing smoke, deployed-source SSM check, post-deploy cleanup SQL, feedback report closeout, and warning clear. ASG refresh `c032312c-3fd6-40cf-8ac7-cb35f595cc3e` completed on replacement instance `i-0362df79d25a76d15`; final smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260622-path-bugfix-patch--2026-06-23T00-22-22-529Z.json`.
- Ops/Data/PROD: Applied guarded post-release cleanup `sql/ops/prod-path-patch-bugfix-cleanup-20260622-apply.sql` after the PROD app patch was live. The cleanup cleared 3 terminal document-request flags, cancelled 7 open reminders on terminal cases, backfilled 5 approved decision outcomes, and archived 5 duplicate/unreferenced active signed Financial Overview PDFs. Rerunning the preview returned `0` for all four cleanup categories.
- Ops/Feedback/PROD: Reconciled live feedback reports `#144` (`Financial Overview Document`) and `#145` (`Overdue Items`) after deployed-source checks, normal-routing PROD smoke, and cleanup verification. Both reports moved from `planned` to `resolved` with status-history rows and closeout notes documenting release `20260622-path-bugfix-patch`.
- Fix/Home/DEV: Fixed the homepage Overdue queue status badge mapping so overdue application rows preserve the structured EI eligibility result when the row is rebuilt for `Work Queue Items`. Rows with EI already recorded no longer show a false `Awaiting EI Validation` badge, while rows with blank EI status still show the legitimate pending-EI status. Added `npm run smoke:home-overdue:browser` to cover the rendered Home queue path and idle `/api/applications` behavior.
- Fix/Casework/DEV: Made Financial Overview signing idempotent in both admin and public portal signing APIs. If a signed request is submitted again, PATH now returns the existing signed state instead of rewriting the signed payload, replacing the funding overview version snapshot, or creating another active signed Financial Overview PDF.
- Fix/Workflow/DEV: Terminal application/case cleanup guards now clear `docs_requested_active` when an application becomes terminal, prevent document-request reminder jobs from creating reminders for terminal applications, cancel open reminders when a case moves to `closed` or `archived`, and exclude terminal-case reminders from due-reminder counts/lists.
- Fix/Assessment/DEV: Completed approved application transitions now infer `decision_outcome='approved'` from the assessment recommendation/review when the client workflow reaches a terminal completed state without an explicit outcome value.
- Ops/Data/PROD: Prepared guarded post-release cleanup scripts for the existing PROD rows affected by those bugs. Read-only preview before release found 3 terminal applications with active document requests, 7 open reminders on terminal cases, 5 completed approved applications missing decision outcome, and 4 unreferenced active signed Financial Overview PDFs; the final post-deploy preview found 5 duplicate/unreferenced active signed Financial Overview PDFs before cleanup.

## 2026-06-20
- Ops/TEST/Release: Deployed TEST release `20260620-rm-two-step-review-rollout` as an app/schema release with `--skip-data`; no TEST DB refresh, intake runtime promotion, or data copy was run. Plan reported 4 pending migrations for the RM two-step review workflow and notification settings. Sequence used all-surface warning, wait, ALB fallback, deploy with `--skip-smoke`, fallback clear, normal-routing smoke, one-off activation/config SQL, and warning clear. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260620-100326.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260620-101127.zip` installed on single TEST host `i-0a8be782ed8604211`; final smoke reported `nwac-test-admin-tg` healthy on `:5001` and `nwac-test-portal-tg` healthy on `:5000`. SQL-over-SSM enabled `feature_flags/workflow.two_step_rm_review.enabled` for application assessments, intervention proposals, and intervention revisions and verified all four migrations recorded successful. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260620-rm-two-step-review-rollout--2026-06-20T14-01-37-533Z.json`.
- Ops/PROD/Release: Deployed PROD release `20260620-rm-two-step-review-rollout` as an app/schema release with `--skip-data`; no intake runtime promotion, allowlisted data promotion, or application/case data copy was run. PROD identity was `arn:aws:sts::468278742295:assumed-role/nwac-prod-codex-operator/codex-prod-operator`; plan reported 4 pending migrations and restore point `path-prod-20260620-rm-two-step-review-rollout-20260620142732`. Sequence used all-surface warning, wait, ALB fallback, deploy with `--skip-smoke`, fallback clear for ELB evaluation, normal-routing smoke, approved activation/config SQL, legacy broad `assessment_submitted` / `Regional Manager` row disablement, and warning clear. Shared/admin/portal artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`; ASG refresh `83f76172-0b5a-4556-a769-e0ee77833911` completed on replacement instance `i-04f3c7c4b7084a92b`. Final smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; SQL-over-SSM verified all four migrations successful, the two-step runtime flag enabled for all three workflow types, final-decision admin bell rows enabled, and legacy admin/RM `assessment_submitted` rows disabled. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260620-rm-two-step-review-rollout--2026-06-20T14-27-32-735Z.json`.
- Fix/Notifications/DEV: Corrected staff document-upload bell alerts so staff-originated uploads name the staff uploader and identify the applicant as the document subject. Jacqueline Sillery's DEV `Document uploaded` alert for the EI verification report was repaired from applicant-upload wording to `quebec.manager@awentech.ca uploaded EI Verification.png for Jacqueline Sillery.`
- Notifications/Approvals/DEV: Added `Pending Review` bell alerts for Regional Managers when application assessments, new intervention proposals, or intervention amendments enter the RM review queue. The new `rm_review_requested` event resolves Regional Manager recipients from the case review region, using the assigned case owner's region when the case portfolio region is empty; email remains off until workflow-specific templates are configured. Jacqueline Sillery's current DEV assessment now has `Pending Review` notifications for Quebec-region RM profiles.
- Fix/Notifications/DEV: Normalized two-step review admin bell-alert routing so NWAC/System Administrator users do not receive initial `Assessment submitted` notifications for ISET Coordinator or Regional Manager submissions into RM review, and instead receive `Ready for final decision` alerts when the Regional Manager submits the work to the Decision Maker stage. Jacqueline Sillery's stale DEV admin `assessment_submitted` notifications were removed and current final-decision notifications now exist for both admin roles.
- Workflow/Approvals/DEV: RM and Decision Maker review notes are now mirrored into `Notes and Tasks` and included in review event payloads so the Events Timeline shows the note alongside the review action. Jacqueline Sillery's existing DEV RM return/submit notes were backfilled as internal case notes and linked from their RM review events.
- Fix/Approvals/DEV: Fixed the application-assessment high-value funding guard so non-Shelley Decision Makers are blocked only from `Approve funding` at `$20,000` or above. `Deny funding` and `Request changes` now commit normally while the Shelley approval warning remains visible.
- UX/Notifications/DEV: Moved the `Newest` / `Urgency` notification sort toggle out of the collapsed page-top rail; it now appears only when staff expand the stacked notifications area. The implementation is CSS-only so Cloudscape owns the Flashbar expansion state and the chevron remains stable.
- Fix/Home/DEV: Kept assigned applications in the ISET Coordinator and Regional Manager `My Applications` queue until the application is completed, closed, or archived. Post-decision application rows now use the same assessment-step deep link as `Pending Completion`, opening approval-letter follow-up first and funding forms/signatures after the approval letter is sent.
- Fix/Home/DEV: Aligned the Regional Manager `Pending Review` items table with the review-focused approval table columns (`Province`, `EI status`, `Timeline target`, and workspace access) instead of falling back to the generic mixed-item columns.

## 2026-06-19
- UX/Help/DEV: Refreshed review-workflow help panels, inline widget guidance, and project notes to use `Decision Maker` for the final-decision actor instead of using `NWAC` as shorthand. `NWAC` remains for the organization, program/funding source, and exact system role names.
- Workflow/Approvals/DEV: Started the Regional Manager two-step review foundation for application assessments, new intervention proposals, and intervention amendments. Added the planning note, additive `iset_review_workflow` / `iset_review_workflow_event` migration, disabled-by-default runtime feature flag, shared transition helper, and focused transition tests. The application-assessment first-pass slice is wired into backend submission/final-decision routes and `CoordinatorAssessmentWidget`, with browser smoke coverage for RM return, RM submit to the Decision Maker, the Decision Maker's request-changes action returning to RM, and RM forwarding changes to the submitter. Local DEV is now enabled for all three workflow types; TEST UAT and explicit PROD activation approval are still pending. Target launch remains Monday, July 13, 2026.
- Ops/DEV tooling: Cleared local DEV ISET casework/application test records for Regional Manager workflow testing. The reset removed current cases, clients, applications, assessments, interventions, proposals, payment packets, messages, documents, and related local upload objects while preserving staff accounts, runtime config, workflow authoring, and the application-assessment RM feature flag. The local clear-test table list now also includes `iset_review_workflow_event`, `iset_review_workflow`, and legacy `message_item` rows so future DEV resets do not leave review workflow or mailbox residue behind.
- Fix/Assessment/DEV: Restored the Regional Manager/NWAC/System Administrator EI verification path during active application assessment. The first RM-review slice made Regional Managers read-only for the assessment body, but that broad lock also disabled the pre-existing EI status/report upload control; EI verification now uses its own stage-aware permission so RM can upload during assessment without being able to edit the rest of the assessment or submitted review packet.
- Fix/API/Assessment/DEV: Completed live DEV UI hardening for the application-assessment RM review slice. EI report uploads now persist the reviewer-selected EI status, the application approval deep link hydrates `reviewWorkflow` / `twoStepReviewEnabled` from `/api/cases/:id`, review-action event queries no longer rely on the retired case `tracking_id` column, and RM review events are present in the shared event catalog plus the System Administrator activity allowlist.
- Tooling/QA/DEV: Passed a live DEV browser walkthrough of the application-assessment two-step review using real applicant/coordinator/RM/Decision Maker test role logins. The run covered RM EI status selection/report upload, coordinator submit/resubmit, RM return to coordinator, RM submit for final decision, the Decision Maker's request-changes action returning to RM, RM forwarding changes to the coordinator, final resubmission, and final approval. Final DEV evidence left application `1` approved with workflow `15` at `final_decision_recorded`; screenshots are under `tmp/rm-review-live-ui/2026-06-19T17-22-44-405Z/`.
- UX/Approvals/DEV: Cleaned up the application-assessment RM review first pass before the next DEV test. Regional Managers now get a `Pending Review` homepage bucket for application assessments in `rm_review` or `returned_to_rm`, while NWAC Administrators keep `Pending Decision` for `nwac_review` and legacy/off-toggle pending decisions. After the Decision Maker requests changes, RM can only forward the requested changes to the Coordinator; `Submit for final decision` is no longer offered or allowed from `returned_to_rm`.
- Workflow/Approvals/DEV: Extended the Regional Manager two-step review workflow to new intervention proposals and intervention amendments/revisions. Local DEV is now enabled for all three workflow types; intervention submissions/resubmissions enter RM review, RM can return or submit upward, Decision Maker decisions are gated to `nwac_review`, request-changes returns to RM first, RM forwards requested changes to the submitter, RM/NWAC homepage queues are stage-aware, and final approved assessment PDFs include the RM sign-off row.
- Tooling/QA/DEV: Added deterministic browser workflow smoke coverage for new intervention proposals and intervention amendments/revisions. `npm run smoke:intervention-assessment:workflow:browser` covers RM return, RM submit upward, Decision Maker review with Regional Manager notes and high-value Shelley warning, Decision Maker request-changes returning through RM, submitter-visible Decision Maker/RM notes, revision decision review, and approved proposal/revision communication letter follow-up entry points.
- Policy/Approvals/DEV: Replaced the old split approval-threshold logic with the single current funding policy. Only Shelley Stacey (`sstacey@nwac.ca`) can approve funding of `$20,000` or above, and the rule is enforced for application assessments, new intervention proposals, intervention amendments/revisions, and direct approved-intervention creation. Non-Shelley decision pages show a Shelley-required warning and disable the approve option for high-value requests.
- UX/Approvals/DEV: Standardized submitter-facing wording for the two-step review flow. Application assessments, new intervention proposals, and intervention amendments now use `submitted to Regional Manager review` / `submitted for review` language while the workflow is active, and the coordinator homepage/help copy uses `Submitted for Review` instead of the old `Awaiting Approval` wording.
- UX/Approvals/DEV: Fixed intervention proposal/revision approval deep links so Regional Manager review items open on the `Review and submit` step instead of falling back to Step 1 when the final decision step is not available to the RM. Decision Maker final-decision entries continue to open on the decision step.
- UX/Approvals/DEV: Returned application assessments, intervention proposals, and intervention amendments now show a submitter-facing `Changes requested` panel at the top of the widget. The panel includes the Decision Maker note and the Regional Manager forwarding note so the submitter can see the full instruction trail before resubmitting.
- UX/Approvals/DEV: Reviewed two-step review labels across application assessments, new intervention proposals, and intervention amendments. Submitter actions now use `Submit for review` / `Resubmit for review`, Regional Manager escalation uses `Submit for final decision`, and RM-stage application assessments show `Regional Manager review` instead of the final approval step title.
- Notifications/Approvals/DEV: Wired Regional Manager two-step review handoff notifications into the shared bell-alert pipeline. Decision Maker request-changes events now target the RM reviewer during the `returned_to_rm` stage, RM return/forward events target the submitter/case owner with the RM note, default bell settings are seeded by migration `20260619_0002`, and local dispatcher smoke verified RM-vs-coordinator routing.
- UX/Notifications/DEV: Changed the admin-shell bell-alert rail to default to newest-first chronological order instead of severity order. `/api/me/notifications` now supports explicit `chronological` and `urgency` sort modes, and the shell exposes a compact Cloudscape `Newest` / `Urgency` segmented control when multiple bell alerts are visible.
- Fix/Approvals/DEV: Corrected intervention revision approval follow-up so applying an approved revision resets the current client funding revision letter follow-up to `pending` instead of reusing an older original approval-letter sent marker. The previous sent marker is retained in follow-up history, and the completion queue/widget now require revision-specific send evidence before saying the revision letter was sent.

## 2026-06-18
- Ops/PROD/Release: Deployed PROD release `20260618-prod-financial-overview-editable` as an app/schema-only rollout with `--skip-data`; no runtime config, allowlisted data promotion, intake runtime promotion, participant/case/application data repair, or database restore was run. PROD identity was explicitly verified as `arn:aws:sts::468278742295:assumed-role/nwac-prod-codex-operator/codex-prod-operator`; plan reported schema pending `0`, restore point skipped with `no-db-mutation-planned`, and data dataset `none`. Sequence followed the full all-surface PROD maintenance path: warning, wait, ALB fallback, deploy with `--skip-smoke`, local health verification during the fallback/ELB warm-up window, fallback clear, normal-routing smoke, and warning clear. Shared/admin/portal artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`; ASG refresh `7b20abe1-3b14-4b59-84d4-8c368e893940` completed successfully on replacement instance `i-0ef59338917626359`. Final normal-routing smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; fallback status returned all hostnames to normal forwarding and SQL-over-SSM reported `service_announcement_rows = 0`. Deployed-source markers passed through SSM command `3e47f51c-6cee-4f75-a1c9-dbdc145605fe`, confirming `fundingOverviewMode`, `finalizeFundingOverviewSigningSubmission`, shared `FINANCIAL_OVERVIEW_SUBMISSION_KEYS`, intake-style `per month` copy, and release id `20260618-prod-financial-overview-editable` in admin and portal bundles. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260618-prod-financial-overview-editable--2026-06-19T00-17-24-957Z.json`.
- Ops/PROD/Feedback: Reconciled live feedback report `#143` (`Fillable Financial Form`) after the PROD release. The report moved from `closed` to `resolved` with status-history row `465` and internal note `418`, documenting that new Financial Overview sends are now editable blank or prefilled forms that update case Participant Details and signed Financial Overview artifacts. The note explicitly records that the release fixes future Financial Overview sends and does not rewrite Kaitlyn Kitson's already-signed historical blank PDFs/documents from signing request `66`. SQL artifact: `sql/ops/prod-resolve-feedback-143-financial-overview-editable-20260618.sql`; SQL-over-SSM command `0b84eae8-8a50-4d15-b145-237df58e75e5`.
- UX/Casework/DEV: Tightened the editable Financial Overview participant form so it asks only for monthly household income fields, monthly household expense fields, the other-income/other-expense text fields, and signature. Supports requested, top-up amount, childcare funding status, transportation category/mileage, and student loan/grant details are no longer rendered, carried as hidden initial values, or accepted from participant submissions in new signing requests; the snapshot/PDF path can still use existing case/application context for those non-participant fields.
- UX/Casework/DEV: Updated the editable Financial Overview income and expense fields to match the original intake financial steps more closely, with GOV.UK small-bold labels, explanatory hint text, `$` prefixes, and `per month` suffixes on monthly amount inputs.
- Ops/TEST/Release: Deployed TEST release `20260618-test-financial-overview-editable` as an app/schema-only rollout with `--skip-data`; no TEST DB refresh, runtime/config/intake data promotion, restore, or arbitrary SQL data fix was run. TEST identity was explicitly verified as `arn:aws:iam::124355655255:user/CODEX_CLI_Admin`; plan reported schema pending `0` and data dataset `none`. Sequence followed the TEST maintenance rehearsal path: all-surface warning, wait, all-surface ALB fallback, deploy with `--skip-smoke`, clear fallback, normal-routing smoke, then clear warning; fallback status returned both TEST hostnames to normal forwarding and SQL-over-SSM reported `service_announcement_rows = 0`. Admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260618-164719.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260618-165540.zip` installed on single TEST host `i-0a8be782ed8604211`; final smoke reported `nwac-test-admin-tg` healthy on `:5001` and `nwac-test-portal-tg` healthy on `:5000`. Deployed-source markers passed through SSM command `e70de8bb-6d78-4742-9a70-3a182c481fe7`, confirming `fundingOverviewMode`, `finalizeFundingOverviewSigningSubmission`, shared `buildFinancialOverviewInitialValues`, shared `FINANCIAL_OVERVIEW_SUBMISSION_KEYS`, intake-style `per month` field copy, and release id `20260618-test-financial-overview-editable` in admin and portal build info. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260618-test-financial-overview-editable--2026-06-18T20-46-46-070Z.json`.
- Tooling/QA/DEV: Added `scripts/financial-overview-editable-dev-smoke.js` and completed the editable Financial Overview DEV smoke. The smoke verifies DEV AWS account identity for both admin and portal env files, creates disposable DEV Cognito staff/applicant users, seeds a synthetic case, sends workflow `52` in blank and prefilled modes, drives the participant portal form with Puppeteer, asserts signed payload/case-context/version/document persistence, captures email-ready screenshots, and cleans up synthetic DEV DB/Cognito/object-store data. Passing run artifacts: `tmp/financial-overview-editable-dev-smoke/2026-06-18T17-56-49-226Z/`.
- Ops/Docs: Added a persistent AWS identity guardrail after DEV smoke preparation exposed an unsafe assumption about shell-default credentials. DEV and TEST are documented as separate from PROD, local admin/portal dotenv identities are recorded, and future AWS permission requests must verify the sourced/profiled identity, account, environment, and target ARNs first.
- UX/Casework/DEV: Financial Overview secure-message attachments are now editable digital forms. Staff can send workflow `52` as prefilled from PATH Participant Details/application data or as a blank form; participant submissions update case Participant Details, refresh the Financial Overview version snapshot, and store the official signed Financial Overview PDF.
- UX/Casework/DEV: Standardized `Manage ISET Applications` and `ISET Clients` as widget-based Cloudscape dashboards. Both routes now use the shared Board/BoardItem look and route-level Add widget / Reset layout controls, while the embedded list tables share compact spacing, explanatory copy placement, name-link treatment, striped rows, sticky headers, wrapping, and table accessibility labels. The ISET Clients list now replaces stale task wording with reminder-backed `Follow ups` and `Next follow-up` columns.
- Ops/System Admin/DEV: Expanded the System Administrator `AWS Environment Status` homepage widget with read-only app capacity, Aurora/RDS stress, and DB query-pressure checks alongside the existing Cognito and SES checks. The widget remains cached server-side, adds manual refresh only, and documents the read-only AWS permissions and threshold env vars.

## 2026-06-16
- Ops/PROD/Release: Deployed PROD release `20260617-prod-assessment-recall-guardrails` as an app/schema-only rollout with `--skip-data`; no runtime config, allowlisted data promotion, arbitrary SQL data fix, or database restore was run. PROD plan reported schema pending `0`, restore point skipped with `no-db-mutation-planned`, and data dataset `none`. Maintenance sequence used all-surface warning, ALB fallback, app deploy with `--skip-smoke`, fallback clear after local health passed on replacement instance `i-086a9f06f8419aef8`, normal-routing smoke, and warning clear. ASG refresh `a08b3dcd-5219-4a45-8f93-ceef83858850` completed successfully; final smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; fallback status returned to normal forwarding and `runtime/service.announcement` row count was `0`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260617-prod-assessment-recall-guardrails--2026-06-17T00-14-35-305Z.json`.
- UX/Assessment/DEV: Submitted application assessments, new intervention proposals, and intervention revision/amendment proposals are now read-only while pending decision. Submitters get a guarded `Recall submission` action before any decision is recorded; recall archives the active generated PDFs from the withdrawn submission, moves the workflow back to editable review/draft state, emits `assessment_recalled`, and keeps future redlines based on the last active non-recalled submission.
- Ops/Intake/PROD-TEST: Investigated the accidental Legal Aid Demo intake runtime promotion from release `20260612-212548`. The bad runtime artifact was checksum `6126d57f8fd66f712672b83c2c0d5672fb3f410e20d3148488b64a23d4ed08c9`, `workflowId=53`, 13 Legal Aid steps. After Bill republished PROD workflow `21`, current PROD was verified back on ISET (`workflowId=21`, 26 steps, no Legal Aid strings) and its stable schema matched the May 21 ISET portal schema from `../ISET-intake` commit `107593e`; no PROD replacement was needed. TEST was still on the bad Legal Aid runtime and was repaired from the good DEV ISET `intake-release` dataset with `npm run data:sync:apply -- --dataset intake-release --workflow-id 21 --target-env test`, then verified as `workflowId=21`, 26 ISET steps, no Legal Aid strings.
- Ops/Deployments/DEV: Added a hard workflow-id guard to PATH intake data promotion. `intake-release` and standalone `intake-runtime-publish` now require `--workflow-id`, parse `publish/workflow.schema.intake`, expose the runtime workflow id/name/version in plan output, and abort before SQL generation when the published runtime row belongs to a different workflow than the requested authoring graph. Added focused Jest coverage and updated deployment/data-promotion docs so ordinary TEST/PROD app deploys default to `--skip-data`; any PROD runtime/config/data/SQL mutation must name the exact dataset or SQL, target tables/keys, source environment, reason app/schema-only deploy is insufficient, restore/rollback path, and verification plan before Bill approves that exact data scope.

## 2026-06-15
- Ops/PROD/Feedback: Repaired feedback report `#142` for Danielle Burdett's application `ISET-20260414-E15794` by generating the missing submitted assessment artifacts from the already-saved June 15 assessment data. PROD now has active Supporting Documents `4105` (`Case manager assessment v3`) and `4106` (`Case manager assessment redline v3`), a `data_repair` case event, and feedback `#142` is `in_progress` pending the code/UX workflow fix decision.
- UX/Finance/DEV: Reworked the local Intacct mock dashboard at `/dashboard` into a developer-focused test console with reliable dashboard asset loading, local contract endpoint visibility, summary metrics, AP bill status filtering, optional auto-refresh, sample bill creation, clearer bill detail/action controls, line cards, event timeline, and copyable raw payloads.
- UX/Finance/DEV: Cleaned up the local Intacct mock bill detail view so attachment acknowledgements no longer appear as repeated AP bill lifecycle events; attachment activity is summarized separately and bill action submit labels now use natural verbs like `Approve for payment`.
- Integration/Finance/DEV: Promoted the Intacct mock and PATH Intacct REST sender to canonical Sage-style object paths for vendors and AP bills (`accounts-payable/vendor` and `accounts-payable/bill`) while retaining the old local mock paths as localhost/default compatibility aliases.
- Integration/Finance/DEV: Made the local Intacct mock vendor store persistent in DEV by loading missing DB settings from the sibling admin `.env` and seeding PATH payment `payee_reference` values into `intacct_mock_vendor`; PATH bill payloads now include packet/case/client/intervention metadata and derive bill currency from packet lines when needed.
- Fix/Finance/DEV: Removed the PATH Intacct sender's runtime dependency on the missing local `node-fetch` package by routing server-side HTTP calls through the Node built-in `fetch` wrapper with timeout handling.
- Tooling/Finance/DEV: Added a Sage Intacct interface fidelity audit plus `npm run audit:intacct-contract`, a local PATH/mock drift guard for the current Intacct sender and mock-service contract. The audit records current official Sage evidence, known gaps, and maintenance expectations; no runtime integration behavior changed.

## 2026-06-14
- Ops/PROD/Feedback: Audited the remaining open PROD feedback reports under Bill's clarified status definitions. No reports are currently `planned`; `#96`, `#97`, `#130`, `#134`, `#140`, and `#141` remain `in_progress` because work or validation is still incomplete, and `#123` remains `triaging` pending appeals workflow policy/spec decisions. Added live internal audit notes to reports `#96`, `#97`, `#123`, `#130`, `#134`, `#140`, and `#141`. SQL artifact: `sql/ops/prod-feedback-open-status-review-20260614.sql`.
- Ops/PROD/Feedback: Resolved live feedback report `#137` (`ILMP manual backloaded action plans do not reliably inherit participant/application facts`) after code, deployed-source, and live target recheck. The prevention code is deployed, focused backload/ILMP tests passed, PROD source markers are present, and Shayleen McNabb action plan `53` / ESDC participant submission `115` is now `ready` with BarrierToEmployment `[7,8]`, EIClaimant `2`, and previous NOC `41301` / `2021`. SQL artifact: `sql/ops/prod-resolve-feedback-137-ilmp-backload-20260614.sql`.
- Ops/PROD/Feedback: Reconciled the live PROD feedback queue against fixed/deployed evidence. Reports `#128` (Supporting Documents identity label edit), `#132` (denied application checklist), `#136` (Withdraw application hidden for `closure_notice`), and `#138` (ILMP warning wording) were marked `resolved` with status-history rows and internal notes after regression tests plus PROD deployed-source checks. Reports `#134`, `#140`, and `#141` received current reconciliation notes and remain `in_progress` pending artifact/workflow recheck. SQL artifact: `sql/ops/prod-feedback-reconciliation-20260614.sql`.
- Ops/PROD/Feedback: Resolved live feedback report `#133` (`Regional Snapshots - Export to Excel totals don't add up`) after code and deployed-source verification confirmed the Regional Snapshot Client Activity/export fix is present in PROD. Verification covered the local workbook export test, JS parse checks, and SSM source markers on PROD instance `i-034c7daa416ec6865`; the live feedback log was updated via `sql/ops/prod-resolve-feedback-133-regional-snapshot-export-20260614.sql`.
- Ops/PROD/Cost: Executed the PROD NAT gateway consolidation from three NAT gateways to one after Bill attached temporary policy `NWACProdNatConsolidationTemporaryOperator`. Live prep found the current app instance `i-034c7daa416ec6865` and DB writer `nwac-prod-db-1` both in `ca-central-1d`, so keeper NAT `nat-061b3328c8a74487e` / `nwac-prod-nat-2` in public subnet `subnet-06232beff7d56afc0` was retained. Route tables `rtb-0448f405001135392`, `rtb-0507d2829075a05d3`, and `rtb-02e4f1d20adc69f0c` now all route `0.0.0.0/0` to the keeper NAT. Removed NAT gateways `nat-009f7f0add87674f4` and `nat-039421458cb225a44`, then released EIPs `eipalloc-0b52e577cc52e143c` and `eipalloc-0c1f5bfe99e1029bd`. Final evidence was green: one available PROD NAT, public smoke `200` for all three PROD health URLs, SQL-over-SSM success, SSM online, admin/portal target health healthy, and app-host outbound egress returning keeper NAT IP `15.222.143.60`. Expected saving is about `$82/month` USD before tax from two fewer NAT gateways plus two released public IPv4 addresses. PROD Terraform desired state now passes the existing single-NAT module variables.
- Ops/PROD/Cost: Revalidated the planned Aurora provisioned downsize without mutating RDS. Live PROD remained one `db.r6g.large` writer (`nwac-prod-db-1`) on Aurora MySQL `8.0.mysql_aurora.3.10.3`; public smoke and SQL-over-SSM were healthy. Refreshed 14-day RDS metrics showed CPU average `8.963%`, p95 `9.188%`, max `12.425%`, connections average `16.33` / max `31`, write IOPS average `4.042` / max `36.361`, write latency average `1.514 ms` / max `2.745 ms`, and swap `0`, but memory remained the constraint with freeable memory average `5.958 GiB` / minimum `5.904 GiB` on the current 16 GiB class. AWS Compute Optimizer marked the DB instance `Optimized` and recommended staying on `db.r6g.large`. Cost Explorer plus official AWS public pricing show `db.r6g.large` at about `$212.78/month` and `db.t4g.large` at about `$117.55/month`, a theoretical compute saving of about `$95.23/month` before tax, but the runbook now records this as an explicit-risk trial only, not the next automatic cost-saving execution. Added `docs/ops/runbooks/prod-aurora-downsize-prep-summary-20260614.md` and temporary policy name `NWACProdAuroraDownsizeTemporaryOperator` to the runbook.
- Ops/PROD/Cost: Prepared and executed the first low-risk PROD cost-saving change to right-size the single app host from `t3.large` to `t3.medium`. Preflight showed `nwac-prod-asg` at min/desired `1`, current instance `i-07b1b6ede5bb88a6a` healthy on `t3.large`, public PROD smoke green, 2026-06-01 through 2026-06-14 CPU p95 hourly average `1.135%`, and SSM memory evidence with about `6747 MB` available out of `7823 MB`. After Bill attached temporary policy `NWACProdAppRightSizeTemporaryOperator`, launch-template version `2` changed only `InstanceType` to `t3.medium`; ASG refresh `24c2eb5f-6843-4685-9df1-35d41eb193ec` completed successfully on replacement instance `i-034c7daa416ec6865`, and the launch-template default was aligned to version `2` afterward. Final checks showed both ALB target groups healthy, all three PROD health URLs returning `200`, fallback off, in-app maintenance row count `0`, and post-change memory about `2837 MB` available out of `3839 MB`. Added `docs/ops/runbooks/prod-app-instance-rightsize.md` and updated PROD Terraform app-tier defaults to `t3.medium` with min/desired `1`.

## 2026-06-12
- Ops/PROD/Release: Deployed full PROD release `20260612-212548` after an all-surface maintenance warning and ALB fixed-response fallback. Restore point `path-prod-20260612-212548-20260613012128` was captured and available before mutation; schema apply reported `0` pending migrations; `intake-release` promoted workflow `21` (`ISET Intake`) and published runtime checksum `6126d57f8fd66f712672b83c2c0d5672fb3f410e20d3148488b64a23d4ed08c9`. Shared/admin/portal artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`; ASG refresh `8e559286-0a7d-4207-bfcc-34034519fa32` completed successfully on replacement instance `i-07b1b6ede5bb88a6a`. Fallback was cleared after local health passed so ELB could evaluate real targets; both target groups became healthy, normal-routing smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`, and the in-app warning was cleared (`runtime/service.announcement` row count `0`). Deployed bundle/source SSM checks confirmed the `Release 20260612-212548` landing-page notes, the System Administrator closed-plan reopen note, the server-side reopen recovery markers, and the plan-first CFA draft fallback markers. PROD feedback reports `#140` and `#141` received deploy notes and remain `in_progress` pending authenticated staff workflow/artifact recheck. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260612-212548--2026-06-13T01-21-28-902Z.json`.
- Ops/Deployments/TEST: Audited TEST release `20260612-212548` after finding the landing-page release notes were stale despite the successful deploy. The forensic check confirmed admin and portal artifacts, schema state, `intake-release` runtime checksum, maintenance fallback clear, in-app warning clear, and target-group smoke were healthy; the missed step was promoting dated release-log entries into the generated release-note package before deploy. Deployment runbooks now make that release-note gate explicit and require the first generated package to match the release being deployed. A corrective admin-only TEST redeploy then shipped the corrected `20260612-212548` release-note package and verified the deployed bundle contains that package before the prior `20260610` package.
- UX/Workflow Studio/DEV: Added configuration-based intake step grouping without a schema migration. Manage Intake Steps now exposes group filtering, group badges, workflow usage, and a group catalogue editor; Modify Intake Step can assign a step group through the step properties panel; group membership is stored in existing `step.ui_meta` and the catalogue in `iset_runtime_config`.
- UX/Casework/DEV: Added a System Administrator-only Case Workspace quick action to reopen a closed action plan for legitimate post-closeout recovery. The new guarded endpoint requires a reason, blocks archived cases/plans, existing active plans, and open intervention proposals, resets ILMP validation/submission to needs review, writes an internal note and `case_reopen_recovery` event, and supports either proposing a new intervention while leaving completed interventions untouched or reopening one completed/cancelled intervention for amendment.

## 2026-06-11
- Fix/Casework/DEV: Funding revision letter sends now recover when the selected intervention needs a Client Funding Agreement but no draft CFA exists yet. Intervention-scoped secure-message sends create the missing draft from the selected action plan before falling back to the application-assessment path, covering approved current amendments on records that were originally manually backloaded while keeping ordinary manual-backload edit/close operations silent.
- Fix/App Shell/DEV: Restored the Cloudscape side-navigation chevron hit target. The visible close control was wired correctly but sat below the SideNavigation `Homepage` header in the pointer stack, so real mouse clicks hit the header instead of collapsing the nav; the app shell now raises the AppLayout hide-navigation control above the header.
- Tooling/QA/DEV: Added `npm run smoke:app-shell-navigation:browser`, a Puppeteer smoke that loads representative shell routes and verifies side-nav close/open with real pointer clicks and `elementFromPoint` hit-target assertions.
- UX/Manual Intake/DEV: Reworked Manual Application Intake into a staff-assisted intake dashboard with a Cloudscape `Staff-Assisted Intake Wizard`. Staff can record identity/source details, search existing client/applicant-account rows, select an existing client/account to reuse its case context, choose an account handling plan, complete application details, review, and carry that decision into manual-intake metadata.
- UX/Manual Intake/DEV: Added a top `Staff-Assisted Intake Flow` widget that keeps the recommended process visible as boxed live checkpoints matching the wizard steps: identity, existing-account check, account handling, application details, and submit/follow-up.
- API/Manual Intake/DEV: Extended `/api/applications/manual-intake` to persist account-decision metadata, reuse a selected existing client when supplied, and optionally create a silent `Ready to invite` applicant account via the existing applicant-account service when staff choose that plan. Activation email remains a separate explicit workspace/User Management action.
- Tooling/QA/DEV: Added `npm run smoke:manual-intake:browser`, a Puppeteer smoke for `/iset/applications/intake` that verifies the flow widget, wizard navigation, applicant search/select flow, application-details/review steps, and submitted account-decision payload.

## 2026-06-10
- Ops/PROD/Release: Deployed corrected admin-only release `20260610-prod-modify-component-editor` after an admin-scoped maintenance warning and admin ALB fixed-response fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; PROD ASG refresh `c11b1376-fb62-4aa1-973e-c493d3062cba` completed successfully on replacement instance `i-05ca437b1f432c383`; fallback was cleared when ELB health needed normal forwarding, target health recovered to healthy on `i-05ca437b1f432c383:5001`, normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, deployed-source checks found release stamp `20260610-prod-modify-component-editor` and the matching `20260610` public release-note package on the instance, and the admin warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260610-prod-modify-component-editor--2026-06-10T22-02-10-615Z.json`.
- Ops/TEST/Release: Deployed corrected admin-only TEST release `20260610-test-modify-component-editor` after an admin-scoped Test and Training maintenance warning plus ALB fixed-response fallback. Schema, data, and portal deployment were skipped; admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260610-175526.zip` installed successfully on the cost-pruned single TEST app host `i-0a8be782ed8604211`; fallback was cleared, normal-routing admin target-group smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`, and the admin maintenance warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260610-test-modify-component-editor--2026-06-10T21-55-26-096Z.json`.
- Ops/TEST/Release: Deployed admin-only TEST release `20260610-test-application-workspace-review` after an admin-scoped Test and Training maintenance warning plus ALB fixed-response fallback. Schema, data, portal, and shared deployment were skipped; admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260610-144955.zip` installed successfully on the cost-pruned single TEST app host `i-0a8be782ed8604211`; fallback was cleared, normal-routing admin target-group smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001`, and the admin maintenance warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260610-test-application-workspace-review--2026-06-10T18-49-55-076Z.json`.
- Fix/Application Workspace/DEV: Fixed the Application Overview `Docs Requested` toggle so clearing an existing document-request flag on a decision-recorded application no longer shows a false "another user updated this application first" warning. The widget now sends the freshest selected-application row-version token, distinguishes row-version conflicts from business-rule `409`s, and the backend allows clearing stale document requests on terminal applications while still blocking new terminal document requests.
- UX/Application Workspace/DEV: Completed the first Application Workspace review chunk for the shell and core support widgets. Supporting Documents now has a document text filter over document metadata, Secure Messaging tables sort by Date/Time, From, Subject, Status, Forms, and Urgent, Case Notes refresh no longer self-dispatches a duplicate reload, Calendar fallbacks use `Not recorded` instead of replacement glyphs, generic load/error copy was tightened, and Application Assessment conflict-declaration wording now refers to the staff member working the application rather than "Client Case Manager".
- Tooling/QA/DEV: Added `npm run smoke:application-workspace:browser`, a Puppeteer smoke for the ISET Application Assessment workspace that checks default widget rendering, idle API settling, Supporting Documents search, Secure Messaging Date/Time sort toggling, Notes refresh single-call behavior, screenshots, and console warning capture.
- Tooling/QA/DEV: Added `npm run smoke:application-assessment:workflow:browser`, a Puppeteer workflow smoke for Application Assessment that drives conflict declaration, coordinator submission, NWAC approval, approval-letter send, and funding-documents completion with deterministic API fixtures. The smoke also guards against React render-phase parent updates; this caught and fixed row-version propagation so `CoordinatorAssessmentWidget` no longer calls the route parent updater from inside its child state updater.
- UX/Workflow Studio/DEV: Repaired the Manage Intake Steps / Manage Components dashboard board chrome. The page now uses the standard route-header Add widget and Reset layout actions, restores missing widgets through the shared split-panel palette, closes the empty palette after reset, and keeps Preview and Step JSON removable like other Cloudscape board items.
- UX/Workflow Studio/DEV: Upgraded the Intake Step Library table with sortable and resizable columns and stable full-list sorting before display. Selecting a step no longer refetches the step-library list, and the Preview widget iframe now flex-fills its board item without overflowing the tile.
- Tooling/QA/DEV: Added `npm run smoke:manage-components:browser`, a Puppeteer smoke for `/manage-components` that checks route-header controls, palette/reset behavior, table sorting/resizing, preview endpoint rendering, preview bounds, screenshots, and idle `/api/steps` request settling.
- UX/Workflow Studio/DEV: Tightened the Modify Intake Step editor. The editor now shows the current step name in its inner header, has a searchable component library, keeps cleanly loaded steps from appearing dirty after template enrichment, preserves repeated content-only components without Data Keys, and surfaces precise backend save validation messages in the alert.
- Fix/Workflow Studio/DEV: Step updates now preserve existing `step.ui_meta` unless a request explicitly sends replacement `ui_meta`, preventing ordinary component saves from clearing editor metadata.
- Tooling/QA/DEV: Added `npm run smoke:modify-component:browser`, a Puppeteer smoke for `/modify-component/132` that verifies editor layout, component-library search, repeated static text-block preservation, clean-load Save state, component selection/add/render, precise save-error handling, save payload sanitation, validation panel, screenshot capture, and idle API settling.
- UX/Casework/DEV: Improved Manage ISET Applications/Case Assignment work-queue navigation and server-backed filtering. Homepage work-queue links now use explicit `/case-assignment-dashboard?bucket=...` deep links, `/api/applications` supports those bucket filters before sorting/pagination, legacy `?status=` links resolve to server-backed views/search instead of filtering only the current page, and the stale `/api/case-assignment/unassigned-applications` route now returns `410 retired_endpoint`.
- Tooling/QA/DEV: Added `npm run smoke:case-assignment:browser`, a Puppeteer smoke for the Manage ISET Applications dashboard that checks standard configurable-board chrome, Applicant sorting, server-backed search behavior, legacy status-route mapping, work-queue bucket routing, screenshots, and the dashboard request-loop guard.
- UX/User Management/DEV: Reworked User Management from an early configurable-board layout into a stable table-first operations page. Staff access and Participant PATH accounts now have sortable/resizable/paginated Cloudscape tables with preferences; participant account search/status/sort/page requests are handled server-side before the row-limited page is returned. Removed placeholder failed-login metrics and added dashboard-specific help-panel/AI context.
- Maintenance/DEV: Completed a repo-wide admin React lint cleanup, reducing `eslint --ext .js,.jsx src` to 0 warnings. The cleanup stabilized hook dependencies and table fallback arrays, removed unused imports/dead code and unnecessary escapes, refactored conditional Jest expectations, restored the Caseworking portfolio table action column for assignment/reassignment actions, and removed the stale GOV.UK CSS source-map footer. Verification passed `git diff --check`, focused Jest coverage for applicant-account/auth/decision-letter helpers, and `CI=true npm run build`; the build still reports one third-party/runtime empty inline source-map warning.

## 2026-06-09
- UX/Workflow/DEV: Reworked the Manage Workflows `Workflow Preview` graph into a responsive React Flow surface with pan/zoom, fit-view controls, minimap, summarized branch labels, custom fixed-size step nodes, and vertical/horizontal layout options.
- Ops/PROD/Data cleanup: Cleared Bill's three System Administrator trial ILMP export-history groups from the live Recent ILMP exports widget without changing participant submission, client, case, application, action-plan, or intervention records. Guarded script `sql/ops/prod-clear-bill-trial-ilmp-export-history-20260609.sql` matched 128 `submitted` history rows across `esdc-participants-2026-06-08.xml`, `esdc-participants-2026-06-05.xml`, and `esdc-participants-2026-06-04.xml`, copied them to recovery table `prod_ilmp_trial_export_history_cleanup_20260609`, and deleted only those history rows; linked participant submission rows were already `pending` with `submitted_at = NULL`.
- Fix/ILMP/DEV: Recent ILMP exports now limits complete batch groups instead of limiting raw history rows before grouping. The `/api/esdc/participants/batches` route first selects recent batch IDs and then loads all history rows for those batches, preventing large export batches from hiding older batches or showing partial client counts.
- Fix/ILMP/DEV: Confirmed `Generate batch XML` is not capped by the participant queue table page size: the queue table uses `limit`/`offset` only for display, while `/api/esdc/participants/batch-prepare` and `/api/esdc/participants/batch-submit` call an uncapped backend collector. Also fixed the queue button gate so warning-only `needs_review` clients can still be exported when there are no fully green `ready` clients; blocked records remain excluded by backend validation.

## 2026-06-08
- Ops/TEST/Release: Deployed full TEST release `20260608-test-ilmp-validation-messages` after an all-surface Test and Training maintenance warning and ALB fixed-response fallback. Schema had `0` pending migrations; allowlisted `intake-release` data promotion was included; admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260608-212217.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260608-212535.zip` installed successfully on the cost-pruned single TEST app host `i-0a8be782ed8604211`; fallback was cleared before normal-routing target-group smoke; final smoke reported `nwac-test-admin-tg` healthy on `i-0a8be782ed8604211:5001` and `nwac-test-portal-tg` healthy on `i-0a8be782ed8604211:5000`; runtime maintenance announcements were cleared with `active_announcements = 0`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260608-test-ilmp-validation-messages--2026-06-09T01-21-41-386Z.json`.
- Ops/PROD/Release: Deployed full release `20260608-prod-ilmp-validation-messages` after an all-surface maintenance warning and ALB fixed-response fallback. Schema had `0` pending migrations; allowlisted `intake-release` data promotion ran after restore point `path-prod-20260608-prod-ilmp-validation-messages-20260609002509`; shared/admin/portal artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`; ASG refresh `9d63ac61-8801-4539-9feb-e621c35e690b` completed successfully on replacement instance `i-0427ea377137c09de`; fallback was cleared for ELB health evaluation; normal-routing smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; deployed-source marker checks found release id `20260608-prod-ilmp-validation-messages`, public release notes, and `src/lib/ilmpIssueMessages.js` on the PROD instance; the in-app warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260608-prod-ilmp-validation-messages--2026-06-09T00-25-09-405Z.json`.
- Tooling/Billing: Added an AwenTech billing invoice generator (`npm run billing:invoice`) with NWAC config, real AWS Cost Explorer lookup through the dedicated `awentech-billing-readonly` role, Bank of Canada monthly USD/CAD FX lookup, DOCX/PDF invoice production, audit notes, overwrite protection, and a Codex Agent usage guide.
- Ops/PROD/Cost: Added `docs/ops/runbooks/prod-aurora-provisioned-downsize.md` for safe provisioned Aurora downsizing. The documented path avoids modifying the only writer in place, trials `db.t4g.large` through a temporary reader/failover, keeps the existing `db.r6g.large` writer as rollback target, then reconciles back to Terraform-managed `nwac-prod-db-1` only after acceptance.
- Ops/TEST/Cost: Pruned the TEST AWS runtime from the earlier PROD-rehearsal shape to a low-cost layout. `nwac-test-asg` now runs one `t3.small` instance in `ca-central-1d`; all TEST private subnet egress routes use the single remaining `ca-central-1d` NAT gateway; two extra NAT gateways and their EIPs plus two idle unassociated EIPs were removed; the TEST ALB was reduced to two public subnets while keeping admin and portal target groups healthy. Terraform defaults, deployment runbooks, DB access guides, and persistent ops memory now preserve the single-NAT/single-app-instance TEST shape; post-prune target-group smoke and SQL-over-SSM checks passed against the single app host.
- UX/ILMP/DEV: ILMP validation issue text now uses a staff-facing format with the editable location, issue, ESDC Data Exchange Guide rule/source, and fix guidance. Visible warnings/blockers no longer expose raw action-plan/intervention database IDs, and PATH-only review checks are labelled as PATH-only instead of looking like gateway rules. Removed the unsupported `No funding received` childcare review warning because ESDC code `5` is valid and no separate justification field is validated.
- UX/Approvals/DEV: Intervention amendment review now shows a compact amendment scale for reviewers without changing approval workflow or generated PDFs. Pending Decision rows for proposed intervention changes include `Net change ... · Revised total ...` when the source intervention baseline is available, and the same line appears immediately above the decision control in Case Workspace `Review intervention change`.

## 2026-06-07
- Ops/PROD/Release: Deployed admin-only release `20260607-prod-tutorials-training-shorts` after an admin-scoped warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; Synthesia video `77a83dc0-284b-4978-abdb-15a2f8b3d8cf` was made public; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; PROD ASG refresh `86ed6e5c-61a3-4cc1-bdf8-7de7ebfbd5b4` completed successfully on replacement instance `i-0a84ae06c8ad88655`; fallback was cleared when ELB health needed normal forwarding; normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`; deployed-source marker checks found the new Synthesia video ID and release stamp in `/opt/nwac/admin-dashboard`; the admin warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260607-prod-tutorials-training-shorts--2026-06-07T20-42-37-187Z.json`.
- UX/Support: Extended Support > Tutorials into a shared training hub with a lean Training shorts section for Synthesia-hosted videos plus the existing Guided tours progress/reset controls. The Training shorts table is sortable/resizable with shorter row hint text and only shows Short, Length, and Action. The current shareable draft rows include `Withdrawing and reopening an application`, `ILMP submissions: preparing an export`, and `ILMP validation and repairs`; their Watch buttons open Synthesia embeds while MP4 files remain out of the React app.
- UX/Support: Backfilled the Training shorts table with the existing completed public `PATH Training Shorts - ...` videos from Synthesia while keeping long modules, release notes, private videos, and general introductions out of the staff-facing short list.
- Ops/TEST/Release: Deployed admin-only TEST release `20260607-test-tutorials-training-shorts` from an isolated worktree containing only the Tutorials source changes. The sequence was `warning -> wait -> ALB fallback -> admin-only deploy -> clear fallback -> normal-routing smoke -> clear warning`; schema, data, portal, and shared deployment were skipped. Normal-routing target-group smoke reported both TEST admin targets healthy on port `5001`. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260607-test-tutorials-training-shorts--2026-06-07T16-54-17-110Z.json`.
- Ops/Training Videos: Made the current withdrawal/reopen Synthesia draft public/shareable for review, deleted the obsolete duplicate/prototype Synthesia test videos, and tightened the video-production runbook so future work cleans up replaced renders and confirms actual embed playback.
- UX/Application Workspace: Updated Application Overview withdrawal guidance so `Withdraw application` covers both applicant-initiated withdrawal and staff-initiated withdrawal after a missed response deadline; related help text now points staff to reopen when review should continue.
- Docs/Guides: Added and revised the Synthesia training-video production runbook, recording the local `SYNTHESIA_API_KEY` env-var convention, read-only API smoke check, official API reference links, mandatory script/storyboard review checkpoint before rendering, production workflow, and stop conditions for future tutorial-video assistance without storing the secret value.

## 2026-06-06
- Ops/PROD/Release: Deployed full release `20260605-prod-ilmp-casework-batch` using the runbook sequence `warning -> wait -> ALB fallback -> deploy --skip-smoke -> clear fallback for ELB health -> normal-routing smoke -> clear warning`. Schema had `0` pending migrations; the normal allowlisted `intake-release` data promotion path ran after restore point `path-prod-20260605-prod-ilmp-casework-batch-20260606013006`; shared/admin/portal artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`; ASG refresh `94712e0d-db12-4ca4-9a3a-8f5b4bd74cf1` completed successfully on replacement instance `i-025190db80c95304b`; final normal-routing smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; deployed-source marker checks found the release stamp, public release-note package, raw application status fix, backloaded Participant Details seeding helper, and applicant-name resolver on the PROD instance; fallback and the in-app warning were cleared. Feedback reports `#132`, `#133`, `#134`, `#136`, and `#137` were updated in PROD with deploy notes and remain `in_progress` pending targeted workflow/artifact recheck. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260605-prod-ilmp-casework-batch--2026-06-06T01-30-06-618Z.json`.

## 2026-06-05
- Fix/Application Workspace/DEV: Application workflow decisions now preserve and prefer the raw persisted application status even when display labels collapse statuses such as `closure_notice` or `docs_requested` to `Awaiting Applicant`. This restores `Withdraw application` on closure-notice applications, keeps Secure Messaging resume-review checks tied to raw `docs_requested`, and leaves user-facing status labels unchanged.
- Fix/Casework/ILMP/DEV: New Action Plan ILMP prefills now use explicit value-to-code maps before label fallbacks for education and barriers, and labour-force prefill checks `unemployed`/`underemployed` before the broader `employed` match. This prevents canonical Participant Details values and unemployment text from being misread by label/string matching.
- Ops/TEST/Release: Deployed full TEST release `20260605-test-ilmp-casework-batch` using the runbook sequence `warning -> wait -> ALB fallback -> deploy -> clear fallback -> normal-routing smoke -> clear warning`. Schema had `0` pending migrations; `intake-release` data promotion was included; admin and portal artifacts were deployed to TEST instances `i-09fe8c219a4564040` and `i-0a8be782ed8604211`; normal-routing target-group smoke reported both admin targets healthy on port `5001` and both portal targets healthy on port `5000`; deployed-source marker checks passed for the ILMP EI-claimant wording, backloaded action-plan barrier guard, and release id; the warning and ALB fallback were cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260605-test-ilmp-casework-batch--2026-06-05T15-58-30-419Z.json`.
- Fix/ILMP/DEV: EI claimant validation now treats blank/null-like stored values as missing and uses staff-facing wording that tells users to select Claimant, Reach-back, or Non-insured instead of reporting a vague invalid-code message.
- Ops/PROD/Data repair: Repaired safe ILMP fields for Shayleen McNabb `ISET-20260410-78062A` after restore point `path-prod-shayleen-ilmp-safe-repair-20260605151744`. Guarded script `sql/ops/prod-repair-shayleen-mcnabb-ilmp-safe-fields-20260605.sql` filled root Participant Details fields directly supported by the application answers, moved action plan `53` start/activation from `2026-04-22` to `2026-04-21`, filled action-plan ILMP education/province/social-assistance codes, reset ESDC submission `115` for revalidation, wrote case event `195`, and stored before/after recovery data in `prod_shayleen_ilmp_safe_repair_audit_20260605`. Feedback report `#137` remains `in_progress`; barrier, EI claimant category, and previous employment NOC/version still need case-manager confirmation.
- Fix/Casework/ILMP/DEV: Add Existing Action Plan now loads application/Participant Details fallback context and requires Appendix A action-plan reporting facts before save, including social assistance recipient, EI claimant status, employment status at plan start, education level/province, barrier to employment, and conditional previous-employment NOC/version/schedule plus childcare funding. The backend action-plan create route now enforces the same core fields for historical/backloaded plans so incomplete records are blocked at entry instead of ILMP export time.
- Ops/PROD/Data repair: Seeded blank Participant Details fields from existing/backloaded action plan and intervention ILMP data for 30 PROD cases with guarded scripts `sql/ops/prod-preview-backload-participant-details-seed-20260605.sql` and `sql/ops/prod-repair-backload-participant-details-seed-20260605.sql`. The repair applied 229 field updates, wrote 30 `data_repair` case events, stored before/after row-level recovery data in `prod_participant_details_backload_seed_audit_20260605`, and post-repair preview returned 0 remaining candidates.
- Fix/Assessment/DEV: Decision-letter signing artifacts now resolve applicant names through a shared server-side helper and ignore generic `Applicant`/`Client` UI fallbacks when real participant identity is available from case context, application answers, intake payload, or client identity. Existing decision-letter templates continue to use `applicant_name`, now populated as a salutation so greetings render as `Dear Claire` instead of `Dear Applicant`.
- Fix/Casework/ILMP/DEV: Existing/backloaded action-plan saves now seed blank Participant Details fields from the plan's structured ILMP data without overwriting staff-entered Participant Details values. The seed covers Appendix A barrier codes, education/province, social assistance, EI claimant/insurance, mappable previous-employment status, childcare need/funding, and goals/summary; existing/backloaded intervention saves also seed blank program NOC/version from the intervention NOC. Participant Details barrier choices now expose the full Appendix A barrier set so copied historical barriers remain visible/editable.

## 2026-06-04
- Fix/ILMP/DEV: Participant validation and XML generation now fall back to saved action-plan `socialAssistanceRecipient` values when participant/application context is blank, and action-plan/API mappers preserve valid zero-valued yes/no codes instead of treating `0` as missing. This clears a false blocker on `CASE-2026-0000044` where the action plan correctly saved `socialAssistanceRecipient: "0"` (`No`) but validation still reported the Appendix A field as missing.
- Ops/PROD/Data repair: Repaired Sarah Froese case `CASE-2026-0000040` after restore point `path-prod-sarah-froese-plan6-planned-20260605022919`. Guarded script `sql/ops/prod-repair-sarah-froese-plan6-planned-20260605.sql` moved future-dated action plan `6` from `active` back to the planned/draft state, cleared its `activated_at`, moved future intervention `37` from `in_progress` back to approved/planned, reset the plan `6` ESDC submission readiness row, and wrote case event `132`. Post-repair verification found plan `5` as the only active Sarah Froese plan and zero PROD cases with more than one active non-archived action plan.
- Ops/PROD/Release: Deployed admin-only hotfix `20260605-prod-ilmp-social-assistance-hotfix` after a five-minute admin warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; PROD ASG refresh `a5096db8-b798-4159-916d-6f20a6081490` completed successfully on replacement instance `i-03a8dcdf26b433d7e`; the deploy-run smoke initially hit the active maintenance fallback and returned `503`, then fallback was cleared and normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`; deployed-source SSM checks found the release id plus `firstNonBlankValue`, `primaryDerivedPlan?.socialAssistanceRecipient`, `resolvedSocialAssistanceCode`, and the frontend `socialAssistanceRecipient` zero-preserving mapper in `/opt/nwac/admin-dashboard`; the admin warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260605-prod-ilmp-social-assistance-hotfix--2026-06-05T01-40-26-007Z.json`.
- Ops/PROD/Release: Deployed admin-only hotfix `20260604-prod-ilmp-barrier-context-hotfix` after a five-minute admin warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; PROD ASG refresh `688416aa-e9e8-45e2-8048-db22ac16765d` completed successfully on replacement instance `i-00122adebe94dbc5d`; fallback was cleared for ELB health evaluation when target health reported insufficient data; normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`; deployed-source SSM checks found the `BarrierToEmployment` validation-context fallback and the release id in `/opt/nwac/admin-dashboard`; the admin warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260604-prod-ilmp-barrier-context-hotfix--2026-06-05T00-30-37-423Z.json`.
- Ops/PROD/Release: Deployed admin-only release `20260604-prod-ilmp-appendix-a` after a five-minute admin warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; PROD ASG refresh `deb1933a-cdac-4982-9818-cf9abecc86f3` completed successfully on replacement instance `i-0bd804594be987639`; fallback was cleared for ELB health evaluation when target health initially reported insufficient data; normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`; deployed-source SSM checks found `education_level_required`, the action-plan start education message, and `hasIlmpOtherBarrierSupportingNotes` in `/opt/nwac/admin-dashboard/isetadminserver.js`; the admin warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260604-prod-ilmp-appendix-a--2026-06-04T23-53-56-543Z.json`.
- Fix/ILMP/PROD: Participant validation context now carries saved action-plan `BarrierToEmployment` codes from `esdc_action_plan_json` into the ILMP validator. The earlier fallback was present in the mapper/XML path but the validation context loader rebuilt action-plan objects without the barrier array, so PROD rows with saved plan-level barrier codes could still show `[barrier] Barrier to employment is required for ILMP action plan reporting.` until release `20260604-prod-ilmp-barrier-context-hotfix` was deployed; staff must rerun validation for stored readiness rows to refresh.
- Fix/ILMP/DEV: The participant validation warning for `Other barrier` now checks for supporting notes before flagging the record. Selecting `Other` with a saved Participant Details/application-answer note no longer leaves the participant in `Needs review` solely because `Other` was selected; blank other-barrier notes still produce the existing warning.
- Fix/ILMP/DEV: ILMP Submissions & Exports batch XML generation now excludes only participants with blocking ILMP issues. Warning-only participants are included in the generated/downloaded XML, and dashboard copy now distinguishes exportable participants from blocked records.
- Fix/ILMP/DEV: Participant validation now treats Appendix A in the ESDC Data Exchange Guide as authoritative for ISET exports and blocks records before XML generation when mandatory gateway fields would be empty. Coverage now includes `agreementHolderName`, core client identity/address fields, `maritalStatus`, `numberOfDependantChildren`, `languageSpoken`, `disability`, action-plan `agreementNumber`, `educationLevel`, `socialAssistanceRecipient`, `EIClaimant`, `barrierToEmployment`, `actionPlanPreviousEmployment`, `actionPlanStartDate`, required interventions, and Appendix A conditional NOC/result/closeout fields. Participant Details values are read directly for marital status, dependant children, language/education, social assistance/EI status, and barriers so stale application-answer blanks do not mask current case corrections; dependant-child counts derive from explicit No/count/ages data, counting only ages 18 or less, and stay blocking when PATH has no real value. The XML exporter no longer invents missing social-assistance, disability, agreement-number, EI, or result-education values.
- Fix/ILMP/DEV: Barrier-to-employment validation and XML export now use action-plan ESDC barrier codes when Participant Details/application barrier fields are blank. Backloaded action plans that already carry `BarrierToEmployment` in `esdc_action_plan_json` now validate/export those codes instead of being falsely blocked by empty participant-level barrier fields.
- Fix/Casework/ILMP/DEV: New and existing/backloaded action plan creation now requires the participant's education level and education province at action-plan start. PATH already exports these action-plan fields when present; the create/backload paths now block missing values earlier so staff cannot save a plan that will later fail Appendix A gateway validation for action-plan `educationLevel`.
- Fix/Casework/DEV: Participant Details now preserves checkbox-array answers such as application barriers when hydrating from the application form. Empty case-context barrier arrays no longer mask application barrier answers, and saved `Other barrier` text is read back from Participant Details case context instead of only from the original application answer.
- Ops/PROD/Data repair: Backfilled Participant Details checkbox/list values from the case primary application for 90 PROD cases after restore point `path-prod-participant-details-arrays-20260604144745`. Guarded scripts `sql/ops/prod-preview-participant-details-array-backfill-20260604.sql` and `sql/ops/prod-repair-participant-details-array-backfill-20260604.sql` preserved existing Participant Details values, appended missing application values, filled blank `Other` notes only, wrote 90 `data_repair` case events, and post-repair preview returned `0` remaining candidates. Katrina Woodgate case `MI-MNT3JPF0-5BFEF1` now has Participant Details barriers `["funding", "other"]`.

## 2026-06-03
- Workflow/Application Workspace/DEV: `Withdraw application` and `Reopen application` quick actions are now available to every role with normal file access when the application status is eligible. The backend now allows authorized staff to reopen closed/withdrawn/archived applications back to `In Review`, and withdrawal resolves open application escalations through the status-update transaction so staff do not need separate escalation-owner permission to finish the withdrawal.
- Fix/Assessment/DEV: Application Overview status and document-request updates now send the selected `applicationId` when saving through the case update route, and System/NWAC Administrators can use the status control to reopen a withdrawn application back to `In Review` as an administrative correction. Reopening clears the selected application's withdrawal-reporting context, the retired ESDC participant workspace link has been replaced with a Case Workspace correction link, and clean withdrawn records no longer show the extra reporting alert.
- UX/Casework/DEV: ISET Clients status badges now use the shared case-status display helper, so persisted `dormant` rows display as `No Active Plan` instead of `Dormant`.
- Fix/Reporting/DEV: Regional Snapshot Client Activity now uses one submitted-in-period application basis so regional tabs and the all-regions export no longer mix funded-client counts into an application-status block. Approved funded-client count remains available with the funding metrics, where it shares the Financial Reports approved-funding basis.
- Fix/Documents/DEV: Denied applications no longer show approval/funding checklist requirements after the denial letter completes and closes the file. The application checklist endpoint now reads `iset_application.decision_outcome`; denied outcomes route to the denial/no-funding checklist path even when the row is `completed`, the case is `closed`, or the assessment UI explicitly requests the approval/funding stage.

## 2026-06-02
- Ops/PROD/Release: Deployed admin-only release `20260601-prod-document-lineage-fix` after a five-minute admin warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`; PROD ASG refresh `06019adf-ddc9-4f80-8e2c-ae254a68b2f6` completed successfully on replacement instance `i-06dfaf4a1f010fe18`; fallback was cleared when ELB health needed normal forwarding; normal-routing smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; deployed-source SSM marker checks found `documentSourceRequiresApplicationLineage`, `preserveDocumentSourceLineage`, and both update-route call sites in `/opt/nwac/admin-dashboard/isetadminserver.js`; the PROD feedback report `#128` deploy note was added while leaving the report `in_progress` pending Danielle/staff recheck; the admin warning was cleared and `runtime/service.announcement` verified absent. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260601-prod-document-lineage-fix--2026-06-02T00-47-01-084Z.json`.

## 2026-06-01
- Ops/TEST/Release: Deployed admin-only release `20260601-test-document-lineage-fix` to TEST using the runbook sequence `warning -> wait -> ALB fallback -> deploy -> clear fallback -> normal-routing smoke -> clear warning`. Schema, data, portal, and shared deployment were skipped; TEST admin target-group smoke showed both admin instances healthy on port `5001`; deployed-source marker checks passed for `preserveDocumentSourceLineage` on both admin instances; fallback and the in-app warning were cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260601-test-document-lineage-fix--2026-06-01T20-05-31-772Z.json`.
- Ops/PROD/Feedback: Reopened live feedback report `#128` from `resolved` to `in_progress` after PROD read-only triage showed the May 28 Supporting Documents label fix missed the Edit document details path for application-submission `status_card` rows. Added internal note with role/scope findings, root cause, local fix evidence, and pending deploy/recheck boundary; no applicant/case/document records were mutated.
- Fix/Documents/DEV: Edit document details now preserves application-submission source lineage when saving client-scoped document types such as Status or Treaty Card. The May 28 fix sent case context for the modal, but the backend then normalized client-scoped storage by clearing `application_id`; PROD's `chk_iset_document_application_submission_scope` constraint requires submission-sourced rows to keep application scope, so the modal could still fail for assigned coordinators on current submission uploads. The update keeps the existing source-required `application_id` while still validating case/application access.

## 2026-05-29
- Ops/PROD/Release: Deployed full release `20260529-prod-case-lifecycle-reporting` after a five-minute all-surface warning and ALB maintenance fallback. Schema had `0` pending migrations; allowlisted `intake-release` data promotion ran for workflow `21` after automatic restore point `path-prod-20260529-prod-case-lifecycle-reporting-20260529221749`; shared/admin/portal artifacts uploaded to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`; ASG refresh `9c7a3b4e-eb57-481d-ae89-917d41dc0908` completed successfully on replacement instance `i-09fd5bf1ca75df6c4`; fallback was cleared when ELB health needed normal forwarding; public health smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, `https://iset.nwac.ca/healthz`, and `https://nwac-public.awentech.ca/healthz`; deployed-source marker checks passed for the release stamp, `No Active Plan` label, and application-scoped reporting markers; the PROD warning was cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260529-prod-case-lifecycle-reporting--2026-05-29T22-17-49-043Z.json`.
- Ops/TEST/Release: Deployed full TEST release `20260529-test-case-lifecycle-reporting` after a five-minute Test and Training warning and all-surface ALB maintenance fallback. Schema had `0` pending migrations; `intake-release` data plan/apply used runtime checksum `859812581515bfa5e19a8d1e8aef6369d7b1eee856cd92d03213cc6fc741250f`; admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260529-180301.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260529-180735.zip` installed successfully on `i-09fe8c219a4564040` and `i-0a8be782ed8604211`; normal-routing target-group smoke showed both admin targets healthy on port `5001` and both portal targets healthy on port `5000`; deployed-source marker checks passed on both admin instances; fallback and the in-app warning were cleared. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260529-test-case-lifecycle-reporting--2026-05-29T22-02-26-038Z.json`.
- UX/Wording/DEV: Renamed the staff-facing case lifecycle label `Dormant` to `No Active Plan` across case status badges, ISET Clients filters, case re-open copy, homepage queue descriptions, and related help guidance. Persisted values and API query values remain `dormant`.
- Fix/ILMP/DEV: Updated withdrawn-application reporting so withdrawing one application on a multi-application case creates application-scoped reporting artifacts without closing the whole case; selected-application reporting panels and ILMP validation now read the matching application/action-plan context.

## 2026-05-28
- Ops/PROD/Release: Deployed admin-only release `20260528-prod-evening-batch` after a five-minute admin warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, PROD ASG refresh `70388e81-c030-4234-a65c-a552f6e04925` completed successfully on replacement instance `i-03ab1b7f6a30c6fb6`, fallback was cleared when ELB health needed normal forwarding, normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, deployed-source SSM marker checks passed for release notes, Supporting Documents, Financial Reports, and ILMP validation markers, the admin warning was cleared, and live feedback report `#128` was updated with the deployed/recheck note. Manifest: `/home/bill/ISET/admin-dashboard/tmp/path-deploy/prod/20260528-prod-evening-batch--2026-05-28T23-28-20-183Z.json`.
- Ops/TEST/Release: Deployed replacement admin-only release `20260528-test-evening-batch-r2` after automated UI checks found and fixed the Financial Reports follow-up-state visibility gap in the first TEST candidate. The runbook sequence used a five-minute Test and Training admin warning and admin ALB maintenance fallback; schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260528-154928.zip`; TEST deploy manifest `/home/bill/ISET/admin-dashboard/tmp/path-deploy/test/20260528-test-evening-batch-r2--2026-05-28T19-49-28-045Z.json` completed successfully; normal-routing target-group smoke showed both TEST admin instances healthy on port `5001`; deployed-source SSM marker checks passed on both instances; and the fallback plus in-app warning were cleared. This supersedes the earlier `20260528-test-evening-batch` TEST artifact.
- Ops/PROD/Feedback: Updated live feedback report `#128` (`Uploaded Documents - Identity Documents`) from `submitted` to `in_progress` with the Supporting Documents inline-label root cause, local fix evidence, and pending PROD deploy/recheck boundary.
- Fix/Documents/DEV: Supporting Documents inline label edits now persist as rename-only updates. `PUT /api/documents/:id` no longer re-runs attachment scope resolution when the request only changes `label`, so older uploaded identity/status documents that are visible through application-submission payload matching can be renamed without failing on missing modern scope columns. The edit-details and duplicate-document modal saves now also send hidden case/application context for client-scoped document types such as identity/status documents, and the edit modal defaults older unscoped rows to the current workspace application/action-plan context when possible.
- UX/Help/ILMP/DEV: Updated ILMP Submissions & Exports help panels and admin AI guidance so staff guidance uses export/download/manual-upload language, explains the participant queue flow, clarifies Recent ILMP exports summary/clients/XML tabs, and states that the XML tab is the stored export snapshot rather than live regenerated XML.
- Fix/ILMP/DEV: Participant validation now maps the mixed-separator intake barrier key `lack_of_job-opportunities` to the ILMP marketable-skills barrier code, clearing a false blocker on case `ISET-20260508-0418E2`.
- Ops/PROD/Data repair: Reset Aaliyah Doxtator's application `52` / case `131` for reassessment after restore snapshot `path-prod-aaliyah-assessment-reset-20260528164743`. Guarded ops scripts `sql/ops/prod-reset-aaliyah-doxtator-assessment-20260528.sql` and metadata follow-up `sql/ops/prod-reset-aaliyah-doxtator-assessment-metadata-20260528.sql` moved the application back to `in_review`, returned the case to `intake`, cleared NWAC decision fields and application-scoped approval markers, archived action plan `29`, interventions `41`/`42`/`43`/`125`, proposals `111`/`216`, and approval-only generated documents, withdrew CFA versions `5`/`13`, and preserved applicant messages plus signed requests `14`/`15`/`16` as historical audit evidence.
- UX/ILMP/DEV: Simplified the optional Recent ILMP exports widget so it shows a compact export history table plus a flat selected-export area instead of nested detail containers. The widget now uses downloaded/export/manual-upload wording, keeps Summary/Clients exported/XML in tabs, shows recorded file path/name and downloader display name in the summary, removes the upload-status field, renames `Mark pending` to `Requeue`, and updates the batch XML modal to say `mark exported` so staff are not told PATH submitted data directly to ESDC.
- UX/Finance Reports/DEV: Renamed the Financial Reports Intervention detail all-rows selector from `All approved interventions` to `All reportable interventions`, clarified the hint text to say it includes zero-dollar rows including denied-application reporting records, and reset detail-table preferences so `PATH follow-up state` is visible by default instead of hiding or blurring PATH operational payment follow-up.
- Fix/Payments/DEV: Hid the old Payment Detail `Mark paid` line action again so submitted payment packets use the current follow-up workflow instead of the retired line-level paid shortcut.
- Fix/Payments/DEV: Disabled the legacy simple paid backend shortcut again by setting `SIMPLE_PAYMENT_WORKFLOW` to false. The rollback payments workflow smoke now verifies submitted payment lines stay out of the old paid shortcut path and leave no persistent fixture rows.
- Fix/Workflow/DEV: Workflow publishing and preview normalization no longer crashes when a step contains instruction-only/content-only components without explicit labels. Paragraph, inset-text, and warning-text fallback text now uses the normalized label/hint values and preserves bilingual fallback copy.
- Ops/PROD/Release: Deployed emergency admin-only release `20260528-prod-case-dashboard-loop-hotfix` after a five-minute admin warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, PROD ASG refresh `b3232241-978a-41ab-ba8c-1f6361a844a5` completed successfully on replacement instance `i-02059ca5bfca75b27`, fallback was cleared when ELB health needed normal forwarding, normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, and the admin warning was cleared.
- Fix/Casework/PROD: Stopped the Case Management dashboard `/api/cases` request loop by stabilizing the cases-data query dependencies. The loop was triggered by an inline sort object changing identity on each render, which recreated the fetch callback, aborted the in-flight request, and immediately refetched after result state changed. A scan for similar inline `sort: { ... }` calls found no other dashboard candidates.
- Ops/PROD/Protection: Added WAF rule `AdminCasesApiRateLimitPerIp` to `nwac-prod-alb-rate-guard`, blocking a source IP after more than 300 requests in 5 minutes when the host is `nwac-console.awentech.ca` and the URI path starts with `/api/cases`. The existing broader `AdminHostRateLimitPerIp` 2,000-request admin-host rule remains in place.

## 2026-05-27
- Ops/PROD/Release: Deployed admin-only release `20260527-prod-casework-ilmp-hotfix` after an admin-scoped warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact uploaded to `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, PROD ASG refresh `78e8b2be-9956-448d-92b6-a107a2906c94` completed successfully on replacement instance `i-029b4f596bf5180a8`, fallback was cleared when ELB health needed normal forwarding, normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`, and an extra full health smoke returned `200` for the admin host plus both public portal hosts. Live feedback reports `#126` and `#124` were updated with release notes; `#126` remains resolved and `#124` remains in progress pending targeted workflow recheck.
- Ops/PROD/Data repair: Resolved feedback report `#126` by repairing Candace Stone's applicant-account email typo on case `15` / client `15`. After restore point `path-prod-candace-email-repair-20260527-213107`, a corrected portal Cognito account was created for `candace.stone31@outlook.com`, client/local-user account records were repointed away from typo address `candance.stone31@outlook.com`, the old typo Cognito user was deleted, and a fresh PATH activation invitation was sent to the corrected address. Live verification showed `client.applicant_account_email`, client contact email, local `user.email`, and the latest invitation event recipient/finalRecipient all use the corrected address.
- Fix/Applicant Accounts/DEV: Case Header now has a `Change PATH account email` quick action for unactivated applicant accounts. The backend creates/reuses the corrected Cognito user, repoints the local portal `user` and `client` applicant-account fields, resets invitation state to `created`, updates client contact email JSON, attempts to delete the old typo Cognito user, and records an `account_email_changed` audit event before staff resend activation.
- Ops/PROD/Protection: Created and attached regional WAF Web ACL `nwac-prod-alb-rate-guard` to the PROD ALB. Rule `AdminHostRateLimitPerIp` blocks a source IP after more than 2,000 requests in 5 minutes when the request host is `nwac-console.awentech.ca`; default action remains allow. Post-association admin health smoke returned `200`.
- Ops/PROD/Observability: Enabled forward-looking PROD ALB access logs on `nwac-prod-alb` to bucket `s3://nwac-prod-alb-logs-468278742295-ca-central-1/prod/alb/AWSLogs/468278742295/`. The bucket was created in `ca-central-1`, public access was blocked, bucket-owner-enforced ownership and 90-day lifecycle expiry were configured, and ALB delivery was validated by `ELBAccessLogTestFile` at `2026-05-27T17:30:44Z`.
- Fix/Casework/DEV: Closed action plan detail edits now persist closeout fields from the details modal, including Action Plan Result Education Level, result code/date, future education, result NOC, outcome summary, and closure notes. The update endpoint also validates closed-plan result dates against the latest non-draft intervention end date, action-plan activation clears stale ESDC closeout JSON keys, and intervention close notes now update metadata before response mapping so reopen views do not show stale notes.
- Fix/ILMP/DEV: Denied-application reporting action plans now use a shared education-level normalizer that accepts both raw intake option keys and display labels before writing ESDC education codes. This keeps `highest-education` values such as `bachelors_degree` mapped to ILMP code `10` for both action-plan start education and result education.
- Ops/PROD/Data repair: Repaired denied-reporting action plan `55` on case `MI-MOD8SONV-D56651` with guarded script `sql/ops/prod-fix-denied-reporting-plan55-result-education-20260527.sql`, setting `actionPlanResultEducationLevel` to `10` to match the existing bachelor's-degree action-plan start education. Post-repair verification found 13 denied-reporting plans and 0 missing/invalid agreement, EI claimant, start education, or result education fields.
- Ops/PROD/Feedback: Updated live feedback report `#124` from `submitted` to `in_progress` with Kelly's added context, then added a correction note after live case inspection showed the saved case `120` intervention span is 365 inclusive days, not over 999. The report remains open pending exact reproduction or a confirmed PROD draft repair.
- Fix/Casework/DEV: Long intervention date ranges no longer block intervention revisions or saves through the hidden ILMP duration field. Frontend proposal/revision payloads and backend intervention create/edit/autoplan paths now cap stored/reportable duration at 999 days while preserving the real start and end dates.
- Fix/ILMP/DEV: The ILMP Participant submission queue now groups and sorts participant rows before applying page-size pagination, so the queue returns the real grouped total and Cloudscape pagination can move beyond the first page. The page can still display 10 participants at a time, and column sorting now applies to the full filtered queue instead of only the visible page.
- Fix/Casework/DEV: Intervention create/edit now treats outcome as closeout-only data. Open approved/in-progress/suspended interventions no longer persist synthetic outcome code `2`, the API rejects non-empty outcomes unless the intervention is being closed, and ordinary edits clear stale outcome values from open intervention rows/ESDC JSON. The close modal now starts with no selected outcome so staff must choose the final ESDC outcome deliberately. The intervention close endpoint now also blocks open-intervention mutation under closed or archived action plans, while keeping already-closed intervention close calls idempotent.
- Fix/Casework/DEV: Action plan details no longer validates an invisible agreement-number field, workspace intervention normalization no longer treats funding stream codes as budget pot IDs, and the action-plan details sections are controlled so validation can open the relevant collapsed section.
- Fix/Casework/DEV: Residence costs are now wired as a first-class intervention payment type in fallback payment mappings, payment requests, and backloaded intervention entry. Case Workspace and coordinator assessment cost-line modals can change the cost item while editing an existing line, and changing a recurring living-allowance line to an intervention-start payment type no longer carries the monthly installment schedule forward by accident.
- UX/ILMP/DEV: Combined the participant Validation Summary into the Participant submission queue widget. The queue now shows bucket-style ready/needs-review/blocked counts, owns the `Validate all` and `Generate batch XML` actions, links participant names on the main row, shows first validation detail on the parent row, and only exposes expandable child rows when a participant has multiple submission/action-plan rows. The batch modal now removes the XML preview and fake path field, using the browser's native save dialog where available and normal download fallback elsewhere. The ILMP Submissions dashboard default layout now uses one full-width queue widget; Recent ILMP exports remains available from the palette for audit/re-export work.

## 2026-05-26
- Ops/PROD/Release: Deployed full app PROD release `20260526-prod-snapshot-scope-guard` after a global warning and ALB maintenance fallback. Schema and data promotion were skipped; shared, admin, and portal artifacts were uploaded to `nwac-prod-artifacts`, ASG refresh `a2a28db3-e1d1-457e-97ca-24055c21987e` replaced the app instance with `i-09f0cff0a1bde8bac`, fallback was cleared when ELB health needed normal forwarding, and normal-routing smoke returned `200` for `nwac-console.awentech.ca`, `iset.nwac.ca`, and `nwac-public.awentech.ca`. Manifest: `tmp/path-deploy/prod/20260526-prod-snapshot-scope-guard--2026-05-27T00-50-56-082Z.json`; SSM stamp check `23cbf485-2c6d-42b9-949c-fa98de33b633` confirmed the deployed admin/portal build IDs.
- Fix/Reporting/DEV: Updated Regional Snapshot to use participant home province and Financial Reports' approved-funding basis for CRF/EI funding and funded-client totals, replacing the stale payment-packet/portfolio-region funding path that showed `$0.00` for BC FY 2026/27 in PROD.
- UX/Help/Finance/DEV: Tightened Financial Reports help and chatbot grounding so PATH-side approved-funding review is described as staff/program review, export, and handoff context rather than implying that NWAC Finance staff use PATH directly.
- UX/Help/Finance/DEV: Refactored Financial Reports guidance around report user jobs rather than recent troubleshooting context. The page and section info now emphasize report scope, approval-date basis, visible-row totals, funded clients/interventions, carry-over confidence, export scope, and first checks when a number looks wrong; explicit cross-dashboard comparison support lives in chatbot guidance instead of the primary help panel.
- UX/Finance/DEV: Updated the Financial Reports summary cards to use five evenly distributed buckets across the report width and renamed the client count from `Active clients` to `Funded clients`.
- Ops/PROD/Data repair: Repaired Katrina Woodgate's intervention `21` / proposal `127` amount persistence after restore point `path-prod-katrina-intervention-amount-repair-20260526130402`. The approved cost-line total `$10,005.28` is now reflected in `iset_case_intervention.budget_amount`, `intervention_cost`, ESDC/metadata cost fields, and `iset_intervention_proposal.proposed_cost`; the FY 2026/27 Financial Reports funded total remains `$402,591.91`.
- UX/Casework/DEV: Added a `Show All` page-size option (`9999`) to the Manage ISET Applications table and the ISET Clients table preferences. The `/api/cases` list cap now allows that page size so the Clients table option returns all rows instead of being clipped to 100.
- UX/Casework/DEV: Changed Manage ISET Applications and ISET Clients column sorting to run server-side before pagination. The table headers now send allowlisted sort/direction fields to `/api/applications` and `/api/cases`, so sorting applies to the full filtered result set rather than only the currently loaded page.

## 2026-05-25
- Ops/PROD/Release: Deployed admin-only PROD release `20260525-prod-bugcr-batch` after an admin-scoped warning and admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; admin artifact `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip` deployed through ASG refresh `17fab762-1f74-4a57-8e1d-6148cd9c9be4` on replacement instance `i-0738fae9b289634ee`; normal-routing admin smoke returned `200` / `{"status":"ok"}` after fallback clear, and the in-app warning was cleared.
- Ops/PROD/Feedback: Reconciled live feedback reports after PROD release `20260525-prod-bugcr-batch`: reports `#118` (`Propose intervention change`) and `#121` (`Template Editor`) were marked `resolved`; report `#120` (`Re-assessment Letter`) remains `in_progress` because the code fix is live but the affected funding-revision packet still needs deliberate resend/recheck before closeout.
- Testing/TEST: Completed focused post-deploy validation for feedback reports `#118`, `#120`, and `#121` after TEST release `20260525-test-bugcr-batch`. Local Jest coverage passed 3 suites / 16 tests, deployed-source assertions passed on both TEST admin instances, and the TEST runtime Access Control matrix was checked read-only for Template Editor / Notification Settings.
- Ops/TEST/Release: Deployed admin-only TEST release `20260525-test-bugcr-batch` after an admin-scoped warning and TEST admin ALB maintenance fallback. Schema, data, portal, and shared deployment were skipped; admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260525-142005.zip` deployed successfully to TEST instances `i-09fe8c219a4564040` and `i-0a8be782ed8604211`; normal-routing target-group smoke passed healthy for both admin instances after fallback clear.
- Fix/Casework/DEV: Other Funding in Application Assessment and Case Workspace intervention proposals now distinguishes confirmed, pending, denied, and unknown funders. Coverage is required only for confirmed other funding, amount is optional, pending/denied/unknown funders are kept as context without generating other-funder letters, and intervention revisions can seed missing other-funding context from the current application assessment.
- Ops/PROD/Feedback: Updated live feedback report `#118` (`Propose intervention change`) from `triaging` to `in_progress` with the local fix/test evidence and noted that it remains open until the planned PROD maintenance release and live recheck.
- Ops/PROD/Feedback: Closed live feedback report `#117` (`Krista Caspick - Activation Code Issue`) as support/deliverability rather than a PATH code defect. PROD evidence shows PATH sent activation links to the correct Gmail address and Cognito accepted multiple activation-code requests with email delivery details; repeated attempts also hit the daily reset-code limit.
- Fix/Access Control/DEV: Notification template/settings APIs now honor the runtime Access Control matrix instead of hardcoded System/NWAC administrator checks. Template authoring writes follow `/template-editor`, notification setting/sender writes follow `/manage-notifications`, and the shared template list can be read by either dashboard permission.
- Ops/PROD/Feedback: Reopened live feedback report `#121` (`Template Editor`) after Bill clarified that the Access Control matrix/dashboard is the source of truth. The earlier PROD config-only repair remains a temporary mitigation; the durable fix is prepared locally and awaits planned release/live recheck.
- Docs/Ops runbooks: Added the bug/CR release-planning boundary that prepared report fixes should be batched into the next planned PROD maintenance release unless Bill explicitly approves an emergency hotfix.
- Docs/Ops runbooks: Added a mandatory PROD bug/CR feedback reconciliation closeout gate to the deployment quick guide, PROD deployment guide, deploy-orchestrator reference, and agent entry point. Future PROD bug/CR deploys must update live report status, status history, and notes after smoke plus targeted recheck before the release is considered complete.
- Fix/Casework/DEV: Intervention approval and funding revision letter signing requests now render from the reviewed secure-message letter body instead of the application decision-letter draft. This keeps revised client-facing letter artifacts aligned with the revised CFA and current intervention amounts.
- Ops/PROD/Feedback: Updated live feedback report `#120` from `planned` to `in_progress` with root-cause notes and local test evidence for the funding revision letter packet fix. The report remains open until deployment and packet recheck/corrected-resend handling are complete.
- Docs/Agent guidance: Added the holistic bug/CR validation standard: workflow and client-facing-output reports should not be called fixed or marked resolved until the complete affected flow and generated/sent packet have been checked.

## 2026-05-24
- UX/Finance/DEV: Reworked the Financial Reports help content into staff-facing job-aid language, added section-level info links for report setup, summary, carry-over, region summary, and intervention detail, and seeded admin-AI guidance/eval coverage for the annual `ISET Advances and Active Clients` report.
- UX/Finance/DEV: Improved `Budgets and Finance > Financial Reports` intervention detail by defaulting to funded interventions only, adding an all-approved intervention override, hiding participant reference-number clutter while keeping references searchable, showing funding source and approved funding near the front of the table, and adding sortable/resizable column preferences.

## 2026-05-23
- Feature/Finance Settings/DEV: Added a read-only Finance packet email preview widget that renders sample payment-packet data through the backend finance email builder, including placeholder bundle-link wording, so admins can see the current outbound email shape without editing the template.
- Docs/Agent guidance: Clarified the exploratory-chat rule so agents avoid creating branching discussion trees in the linear chat interface, manage unresolved points themselves, and use lists only when list structure is genuinely the useful artifact.
- Docs/Agent guidance: Added an immediate-update trigger for durable corrections to Codex operating behavior, so future agents do not leave persistent process learning only in chat.
- Fix/Payments/DEV: Restored finance payment-packet bundle delivery as a signed seven-day PATH download link. The linked bundle is a simple zip of packet evidence files only, works through the admin backend instead of relying on a direct object-store presign, and avoids the audit-bundle PDF/manifest JSON extras.
- Config/Payments/DEV: Changed the payment-packet baseline evidence requirement to only the Client Funding Agreement (`FundingAgreement`) and signed EFT banking form (`SignedEftBankingForm`), with the EFT evidence type mapped to canonical document type `EFT_form`. Added ops SQL to update the runtime config while preserving per-payment-type evidence rules.

## 2026-05-22
- Ops/PROD/Release: Deployed admin-only release `20260522-prod-document-upload-scope` to PROD after a five-minute admin warning and admin ALB fixed-response fallback. The release used restore point `path-prod-document-upload-scope-20260522213235`, applied guarded data repair `sql/ops/prod-repair-document-upload-applicant-links-20260522.sql`, uploaded admin artifact `admin/admin-dashboard-latest.zip`, completed ASG refresh `6a740027-06b8-4680-bea4-b859bdbd13ca` on replacement instance `i-0f28b7f73b820f32b`, cleared fallback/warning, and confirmed normal-routing smoke returned `200` for `nwac-console.awentech.ca`, `iset.nwac.ca`, and `nwac-public.awentech.ca`.
- Ops/PROD/Data repair: Repaired the two active PROD applicant-account/client mismatches that caused Supporting Documents uploads to fail with `client_id_mismatch`: Katrina Woodgate case `88` / client `97` and Leah Plaited Hair case `90` / client `99`. The repair moved activated PATH account bindings from placeholder clients `134` and `179` onto the real case clients, moved account activation events, attached Katrina's orphan void-cheque document `1220` to case `88` / application `6`, and left only Molly Hink's already-archived duplicate application `76` as an expected historical mismatch.
- Ops/TEST/Release: Deployed admin-only release `20260522-test-document-upload-scope` to TEST using the required sequence: admin warning, wait window, admin ALB fixed-response fallback, deploy with schema/data/portal skipped, fallback clear, normal-routing smoke, and warning clear. Manifest `tmp/path-deploy/test/20260522-test-document-upload-scope--2026-05-22T21-15-29-017Z.json` reports success; TEST target-group smoke showed both admin instances and both portal instances healthy after normal routing was restored.
- Fix/Documents/DEV: Case Workspace Supporting Documents now treats application submission users that map to a different client than the case client as unsafe applicant context. Those files fall back to case-backed document mode, so staff uploads use `/api/cases/:id/documents/upload` and preserve the case/client scope instead of failing with `client_id_mismatch` through the applicant upload route.
- Ops/TEST/Release: Rehearsed admin-only release `20260522-admin-followup` to TEST using the runbook sequence: admin warning, admin ALB fixed-response fallback, deploy with schema/data/portal skipped, fallback clear, normal-routing admin smoke, and warning clear. Manifest `tmp/path-deploy/test/20260522-admin-followup--2026-05-22T17-49-16-374Z.json` shows both TEST admin instances updated successfully, and the admin target group reported healthy on both instances.
- Fix/Casework/DEV: Intervention planned-cost handling now accepts valid dollars-and-cents amounts up to two decimals in edit intervention, close intervention, and existing/backloaded intervention entry paths instead of requiring whole-dollar values.
- Fix/Application lifecycle/DEV: Sending an application assessment denial letter now completes the denied application from the backend secure-message send path, preserving `decision_outcome='denied'` while moving the application row to `completed` / `closed`. This removes the dependency on a client-side follow-up call and keeps sent-denial applications out of `Show Active Applications`.
- Ops/PROD/Data repair: Repaired 13 PROD applications whose denial letters had already been sent but whose application rows still showed `rejected` / `decision_recorded` / `denied`, including Tara Bear's application `53`. Guarded repair `sql/ops/prod-fix-denial-letter-completion-repair-20260522.sql` ran after restore point `path-prod-denial-letter-completion-repair-20260522170354`; all 13 now show `completed` / `closed` / `denied`, and the sent-denial-not-closed check returns `0`.
- Workflow/Reporting/DEV: `Withdraw application` now seeds reporting-only ILMP artifacts for future withdrawals: one closed action plan named `Actions leading to withdrawal` with completed `Career Research and Exploration` and `Employment Counselling` interventions. The generator records `withdrawn_reporting` metadata and the application form/overview wording now describes withdrawn reporting records without calling them denied applications.
- Fix/ILMP/DEV: Hardened the denied-application reporting generator so reporting-only denial action plans default missing EI status to CRF (`EIClaimant=3`, agreement `16535841`) and map raw intake highest-education option keys into ILMP education codes for both start and result education.
- Ops/PROD/Data repair: Repaired the May 20 denied-application reporting backfill action plans `38`-`47` after restore point `path-prod-denied-reporting-ilmp-fix-20260522151230`, filling CRF agreement/funding/EI fields plus start/result education values from the linked intake answers. Madison Murdock's plan `38` now has agreement `16535841` and education/result education code `5`.
- Ops/PROD/Data repair: Resolved feedback report `#113` (`Pending Completion`) with guarded PROD repair `sql/ops/prod-fix-feedback-113-erica-christian-backload-20260522.sql` after restore point `path-prod-fb113-erica-backload-20260522142039`. Erica Christian case `107` / intervention `15` had been persisted as approved new proposal `30` but was historical active caseload work, so the intervention and compatibility proposal were marked `source=manual_backload` / `entryMode=existing`; follow-up `sql/ops/prod-fix-feedback-113-erica-christian-backload-audit-metadata-20260522.sql` added the restore-point metadata to the repaired rows.
- Docs/Agent guidance: Added a standing dialog-mode directive for exploratory conversations with Bill: use natural English paragraphs, avoid formatting-heavy status-report style unless appropriate, and keep the thread focused on one main point at a time.
- Ops/PROD/Feedback: Resolved live feedback report `#110` (`Revised CFA`) after Bill sent workflow guidance to Emilie and copied Amanda. PROD evidence confirmed Shelly Van Loon CFA v2 was generated correctly, visible in Supporting Documents with the case-manager signature already applied as expected, and should be sent through `Prepare funding revision letter` / `Send client funding revision letter` so PATH includes the revised CFA for participant signature and completes the revision follow-up.
- Ops/PROD/Feedback: Triaged the live bug/change-request queue and updated PROD feedback reports `#110`-`#114` with internal evidence notes and status history. Reports `#110` and `#114` moved to `triaging`; `#111`, `#112`, and `#113` moved to `planned`; consultation-only reports `#96`, `#97`, and `#98` remain open in progress. Added guarded SQL artifact `sql/ops/prod-feedback-triage-20260522.sql`.

## 2026-05-21
- UX/Landing Page/Release notes: Updated the release-notes generator and landing-page rendering so the public panel uses the standard `What changed`, `Known Bugs`, and `What's Coming` sections, with `What changed` split into the three newest expandable release packages. Updated the TEST/PROD deployment runbooks and release-notes working log so future app deploys refresh those notes before release and do not reintroduce `Earlier changes`.
- Ops/PROD/Release: Deployed admin-only release `20260521-prod-admin-bugcr-packets` to PROD after an admin-scoped warning and ALB fixed-response fallback. Schema, data, portal, and shared deployment were skipped; admin artifact upload completed, ASG refresh `5378bcd6-26c5-4153-b91f-6038f033a38c` replaced instance `i-0317627e71b6bd57b`, normal-routing admin smoke returned `200`, all public health endpoints returned `{"status":"ok"}`, and the warning/fallback were cleared.
- Ops/PROD/Feedback: Resolved live feedback reports `#99`, `#101`, `#102`, `#103`, `#104`, and `#105` after PROD release `20260521-prod-admin-bugcr-packets` and targeted rechecks. Consultation-only reports `#96`, `#97`, and `#98` remain open.
- UX/Application approval/PROD: After an approver commits an application approval or denial, the Application Assessment widget now stays on `Approval and decision` with a recorded-decision confirmation and an explicit button for the separate approval/denial-letter follow-up. This prevents the decision save from feeling like it automatically launches letter writing while keeping post-decision work available.
- Ops/PROD/Data repair: Aligned Shayleen McNabb's already-withdrawn PROD application `12` / case `94` to the case lifecycle with guarded script `sql/ops/prod-align-shayleen-mcnabb-withdrawn-case-20260521.sql`. The repair changed the case to `closed` / `closure_reason='withdrawn'`, completed two stale withdrawal follow-up reminders, and added an internal case note clarifying that no denial-reporting interventions were created because withdrawal is not a denial.
- Ops/PROD/Feedback: Updated live feedback reports `#103` and `#105` from `triaging` to `in_progress` with Packet 2 implementation notes before release; both were resolved after the admin deploy/recheck.
- Fix/ILMP/DEV: Updated case-level ILMP submission lookup so archived action-plan submissions are not selected for validation/compliance and are not reset by case-level "needs review" updates. This keeps stale archived-plan validation rows from overriding the current reportable denial action plan.
- Fix/Case lifecycle/DEV: Withdrawn-only cases now recompute to `closed` with `closure_reason='withdrawn'` when the last/open application is withdrawn and there is no casework plan/intervention activity, so withdrawn files remain findable in Dormant/All client views instead of falling between intake and client filters.
- Ops/PROD/Feedback: Updated live feedback reports `#101`, `#102`, and `#104` from `planned` to `in_progress` with Packet 1 implementation notes, and added a matching note to closed duplicate `#106`; the canonical reports were resolved after the admin deploy/recheck.
- Fix/ILMP/DEV: Updated participant validation and XML context mapping so current intake/case barrier keys (`funding`, `location`, `lack-of-job-opportunities`, `other`, and health variants) normalize to ILMP barrier codes, intervention NOC lookup candidates include both snake_case and camelCase action-plan/intervention fields before checking `noc_code`, and future-dated active plans with linked planned/draft interventions report "no ILMP-reportable intervention yet" instead of incorrectly saying the action plan has no intervention.
- Ops/PROD/Data repair: Completed guarded duplicate-account merge for Molly Hink after restore point `path-prod-molly-hink-merge-20260521115556`; run `molly-hink-merge-20260521120204` kept Hotmail client `149` / user `189` / case `129` / application `50` as the survivor, archived duplicate Gmail application `76` with `closure_reason='duplicate'`, moved 22 Gmail-submission documents onto the survivor application, suspended old user `45`, retired case `42` as a detached `merged_duplicate`, expired stale duplicate-case notifications, inserted client/case merge audit rows, and cleared the temporary application locks plus admin maintenance warning after verification.

## 2026-05-20
- Ops/PROD/Data repair: Corrected Kaylan Lavallee application `ISET-20260422-B9C76E` DOB from the invalid intake value `2008-24-12` to `2008-12-24` using guarded SQL `sql/ops/prod-fix-kaylan-lavallee-dob-20260520.sql`. The repair updated the submitted intake payload, case context, client DOB, applicant user DOB, and application audit metadata so ILMP participant validation can read a valid date of birth.
- Fix/Public Portal/DEV: Added strict calendar validation for the public intake Date of Birth step and final submission guard so impossible values such as `2008-24-12` or invalid leap days are blocked before client creation/ILMP preparation. DEV workflow `21` was republished to `iset_runtime_config(scope='publish', k='workflow.schema.intake')`.
- Ops/PROD/Release: Deployed full release `20260520-prod-denial-reporting` to PROD after a 10-minute all-surface maintenance warning and ALB fixed-response fallback. The run captured restore point `path-prod-20260520-prod-denial-reporting-20260520222922`, applied migration `20260514_0001_rename_funding_overview_user_labels.sql`, uploaded shared/admin/portal artifacts, completed ASG refresh `d62c651a-09e1-4968-af56-750a3edb07c3`, cleared fallback for ELB health evaluation, passed normal-routing smoke for `nwac-console.awentech.ca`, `iset.nwac.ca`, and `nwac-public.awentech.ca`, and cleared the in-app maintenance warning.
- Ops/PROD/Data backfill: Applied PROD denied-application reporting backfill `sql/ops/prod-denied-application-reporting-backfill-20260520.sql` after the TEST rehearsal and final live preview. The backfill moved 11 sent-denial-letter cases into the `Actions leading to denial` reporting structure with completed `Career Research and Exploration` and `Employment Counselling` interventions, left ESDC participant rows `needs_review/pending`, and left the 2 watch-only denied cases without sent denial-letter markers untouched. Tanisha Saddleback's archived `auto_assessment` interventions were preserved and the denial-reporting pair was added separately.
- Ops/TEST/Release: Deployed TEST release `denial-reporting-test-rehearsal` with schema/data promotion skipped and admin plus portal included, using the TEST maintenance warning and ALB fallback sequence. Both TEST admin and portal target groups reported both instances healthy after normal routing was restored.
- Ops/TEST/Data rehearsal: Applied TEST-only denied-application reporting backfill `sql/ops/test-denied-application-reporting-backfill-20260520.sql`. Madison Murdock received a new `Actions leading to denial` plan plus the two completed reporting interventions; Violet Cross's legacy denial-reporting plan was upgraded and the missing `Employment Counselling` intervention was added. Both TEST ESDC participant rows were left `needs_review/pending` for validation follow-up.
- UX/Landing Page/Release: Refreshed the draft public release notes for the pending PROD denial-reporting release so the current What's New bullets focus on homepage item-list sorting, denied application completion after denial letters, and denial-reporting action plan/intervention creation plus the planned live-data back-load.
- Workflow/Reporting/DEV: New denial decisions now seed a reporting-only closed action plan named `Actions leading to denial` with completed `Career Research and Exploration` and `Employment Counselling` interventions dated to the denial decision day. The seed applies to all denial reasons going forward and keeps the files out of normal casework queues.
- Fix/Homepage/DEV: Sending an application denial letter now immediately completes the denied application, and Pending Completion no longer keeps denied application rows once their denial letter has been sent.
- Fix/Homepage/DEV: Restored client-side sorting in the shared Work Queue Items table so NWAC Administrator, Regional Manager, and ISET Coordinator item lists sort their visible queue and metric-result rows from the column headers, including the NWAC All Cases table.

## 2026-05-19
- Fix/Applicant Accounts/DEV: Admin applicant-account activation now repairs older imported Cognito users stuck in `FORCE_CHANGE_PASSWORD` before sending or resending PATH activation emails, so the public portal's activation-code/password setup flow can complete.
- Ops/PROD/Data repair: Repaired Molly Hink's imported Gmail applicant account and Jeannie Loring's applicant account in PROD Cognito by converting their stale temporary-password state from `FORCE_CHANGE_PASSWORD` to `CONFIRMED` with unknown permanent random passwords. PATH activation status remains invitation-sent until each applicant completes a normal portal sign-in.

## 2026-05-16
- Ops/TEST/Release: Deployed admin-only release `20260516-payments-packet-workflow-test` to TEST after an admin-scoped Test and Training maintenance warning plus ALB fallback. Schema, data, portal, and shared deployment were skipped; the admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260516-072032.zip` installed successfully on instances `i-09fe8c219a4564040` and `i-0a8be782ed8604211`, and normal-routing admin target-group smoke reported both targets healthy.
- Ops/PROD/Release: Deployed admin-only release `20260516-payments-packet-workflow-prod` to PROD after an admin-scoped maintenance warning plus ALB fallback. Schema, data, portal, and shared deployment were skipped, no DB restore point was captured because no DB mutation was planned, PROD ASG refresh `569d93b0-5587-41fc-98ac-fcac2454449d` completed successfully, fallback was cleared for ELB health evaluation, and normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`.

## 2026-05-15
- UX/Payments/DEV: Reworked the create payment packet modal so staff can add, edit, and remove multiple payment lines before creating a packet. The modal now uses the max width layout, aligns the approved-funding section with the rest of the form, shows draft payment lines in a table with a header add-line action after the first line, and submits all staged lines in one packet.
- UX/Payments/DEV: Added an `Amount remaining` column to the create payment packet modal's approved funding lines table, subtracting non-cancelled packeted amounts from each approved funding line.
- UX/Payments/DEV: Renamed the create payment packet modal's approved total copy to `Approved funding`, including related helper and validation text.
- Fix/Payments/DEV: Restored the finance email payment-packet bundle link and changed the generated email bundle to contain only linked evidence documents, excluding the packet summary PDF and JSON metadata. Finance email subjects now include the packet client name after the packet id.
- Fix/Payments/DEV: Removed the unapproved maker-checker blocker from payment packet submission, payment-line paid marking, and payment batch approval so DEV demos can use the payment workflow without same-user handoff failures.
- Fix/Payments/DEV: Updated payment packet evidence auto-attachment so supporting documents categorized with direct evidence-type names, such as `ClientApplicationSigned` or `FundingAgreement`, are recognized alongside lower-level document category codes. Backfilled the current DEV payment packet fixture links so baseline evidence no longer appears missing when matching placeholder evidence exists.
- Fix/PATH/DEV: Fixed Case Workspace action-plan budget pot mapping so intervention and action-plan displays use the resolved budget pot code/name instead of leaking the raw `iset_case_action_plan.budget_pot` storage value. Backend action-plan queries now join `budget_pot` by stored numeric id or code, and workspace UI fields prefer the mapped display label.
- Ops/PROD/Release: Deployed admin-only release `20260515-ilmp-draft-plan-readiness-prod` to PROD for the ILMP draft action-plan readiness fix. The run used scoped admin warning plus ALB fallback, skipped schema, data, portal, and shared deployment steps, skipped DB restore-point capture because no DB mutation was planned, cleared fallback for ELB health evaluation, completed ASG refresh `c3036341-ec7b-4bf4-888c-0f9e2e37d77d`, and normal-routing admin smoke returned `200` for `https://nwac-console.awentech.ca/healthz`.

## 2026-05-14
- Ops/TEST/Release: Deployed admin release `20260514-ilmp-draft-plan-readiness-test` to TEST after a scoped admin maintenance warning and ALB fallback. Both TEST admin targets reported healthy after the release, and Sarah Froese's participant submission row for `CASE-2026-0000040` revalidated to `ready` with no warnings.
- Fix/ILMP/DEV: Updated ILMP participant readiness so draft action plans are ignored when a participant also has an active/closed action plan. Draft plans are still excluded from ILMP XML, and a warning remains when no reportable action plan exists.
- Intake/Workflow/DEV-TEST-PROD: Updated workflow `21` Disability and Social Assistance intake configuration directly in DEV, TEST, and PROD so `has-disability` is mandatory in both workflow authoring and the published `workflow.schema.intake` runtime config. Applicants must now choose Yes or No before continuing, matching ILMP submission needs.
- Fix/ILMP/DEV: Fixed ILMP participant validation/XML context so application-backed case submissions with a missing `esdc_participant_submission.application_id` still resolve the action plan or case primary application, and seeded `case_context_json.applicationAnswers` are used as fallback intake answers before reporting fields such as Disability or address street missing. The ESDC validation requirements are unchanged.
- Workflow/Documents/DEV: Updated Financial Overview snapshot naming so formal applicant fields prefer full-name or first/middle/last application data before falling back to preferred-name fields. Added persistent project guidance that `preferredName` / `preferred-name` should be treated as a nickname/display preference, not as the participant's full name.
- UX/Workflow/DEV: Standardized the user-facing secure-message signature flow on Financial Overview terminology. The workflow label, generated version labels, generated PDF/display filenames, signed portal artifact label, public release-note copy, and project memory now use Financial Overview; internal `funding_overview_*` schema identifiers remain unchanged.
- Ops/PROD/Release: Deployed full release `20260514-prod-dev-alignment` to PROD after a 10-minute all-surface maintenance warning and ALB fixed-response fallback. The run archived `2,952` live `iset_event_outbox` rows to `iset_event_outbox_archive_20260514` before dropping the retired outbox table, captured restore point `path-prod-20260514-prod-dev-alignment-20260514004038`, applied migrations `20260510_0001_retire_event_outbox.sql`, `20260511_0001_add_payment_followup_model.sql`, and `20260512_0001_create_funding_overview_versioning.sql`, promoted workflow `21` intake runtime/config, uploaded shared/admin/portal artifacts, and completed PROD ASG refresh `1da35329-68ed-430c-8766-9de4d7dbb555`.
- Ops/PROD/Verification: During the PROD rollout, the ALB fallback was cleared when ELB health reported `Target.NotInUse` / insufficient data so target groups could evaluate real app health. Final normal-routing smoke returned `200` for `nwac-console.awentech.ca`, `iset.nwac.ca`, and `nwac-public.awentech.ca`; both admin and portal target groups reported the refreshed instance healthy, canonical schema plan reported `pendingCount: 0`, and workflow `52` (`Financial Overview`) was promoted to PROD and verified active.
- UX/Landing Page/Release: Updated the generated public What's New release notes for `20260514-prod-dev-alignment` with release date `14th May 2026` / `14 mai 2026`, covering the PROD DEV/TEST alignment bundle, intake financial-information update, Financial Overview signing, payment follow-up, Payments dashboard/communications, Denied wording, Withdraw application, and ISET Clients help update.

## 2026-05-13
- UX/Help/DEV: Updated the ISET Clients dashboard and Clients widget help panel copy to match the current client view filters. The help now explains Open, Funded, Dormant, Ineligible, and All client logic, grouped client rows, current table columns, search behavior, and role-scoped results.
- UX/Wording/DEV: Normalized PATH decision displays to use `Denied` instead of `Rejected` or `Not Approved` across application/intervention reporting drilldowns, supporting-document selectors, homepage metrics/work-queue descriptions, denial-letter labels/defaults, admin tutorial/help copy, and applicant decision-email template context. Internal raw status values such as `rejected` remain unchanged.
- Workflow/Application Workspace/DEV: Replaced the Application Overview `Close application` quick action with `Withdraw application`. The action now persists raw application status `withdrawn`, keeps closed lifecycle semantics, requires a note, resolves open escalations, and displays withdrawn files as `Withdrawn` in staff-facing application status surfaces.

## 2026-05-12
- Testing/Workflow/TEST: Ran an automated TEST Financial Overview signature smoke with temporary Cognito staff/applicant users and a synthetic case. The first pass caught a public-portal signing gap where the signing request became signed but the Financial Overview version stayed `sent`; follow-up portal release `20260512-test-funding-overview-portal-signing` fixed that path, and the rerun passed send/view/sign assertions, version status moved to `signed`, and the linked signed PDF used `Financial Overview v1 (signed)` plus a `signed` file name. Synthetic cases, users, Cognito accounts, DB rows, and S3 objects were cleaned up.
- Ops/Workflow/TEST: Deployed release `20260512-test-funding-overview-signatures` to TEST under all-surface warning plus ALB maintenance fallback. The release applied migration `20260512_0001_create_funding_overview_versioning.sql`, promoted workflow authoring data for workflow `52`, deployed current admin and portal artifacts, passed deploy target-group smoke on both TEST instances, and left TEST with 0 pending canonical migrations.
- Workflow/Documents/DEV: Added a CFA-style manual secure-message signing workflow for Financial Overview. DEV workflow `52` (`document_type='financial_overview'`) creates case-scoped Financial Overview versions, withdraws unsigned prior sends, renders full clean/redline financial overviews from Case Workspace / Application Details figures, and stores the client-signed PDF with `signed` in the document name.
- Intake/Workflow/DEV: Updated DEV workflow `21` authoring and republished `iset_runtime_config(scope='publish', k='workflow.schema.intake')` so Household Income, base Household Expenses, and amount-driven financial evidence uploads are no longer gated by selecting `Living allowance`; transportation, childcare, and other-support follow-ups remain support-specific.
- Ops/Intake/TEST: Rehearsed the `intake-release` promotion for workflow `21` into TEST without an app deploy or maintenance window. The TEST runtime row now matches version `2026-05-12T15:05:08.373Z#21`, and in-place portal/runtime assertions passed on both TEST app instances for the financial-step visibility and renderer-compatibility invariants.

## 2026-05-11
- UX/Payments/DEV: Began the Payments transformation for the NWAC email workflow. `/iset/payments` is now the cross-client operational dashboard using the shared payment queue/detail/communications widgets, Case Workspace manage-payments now includes Payment Communications, selected-packet communication loading works for scoped users, manual email logging opens a real modal instead of creating placeholder logs, and operational payment surfaces hide the experimental Intacct XML preview by default.
- API/Payments/DEV: Enabled line-level payment evidence attach. The payment document attach route now accepts line ids, verifies the line belongs to the packet, persists `payment_packet_document.payment_packet_line_id`, and the Payment Detail widget sends that line id when linking or uploading evidence.
- UX/Finance/DEV: Aligned budget/reporting labels with the PATH operational payment model. Financial Reports now prefer explicit payment follow-up state, exported paid/confirmed values are labelled as recorded paid, budget/case finance surfaces use recorded actual wording, and homepage metrics now show `Funds recorded actual`.
- Tooling/Payments/DEV: Added `scripts/payments-workflow-smoke.js` plus npm aliases for rollback DB, authenticated API, and Puppeteer UI payment workflow smokes. The rollback DEV DB smoke, authenticated API smoke, and authenticated Puppeteer smoke all passed with the documented WSL DB host; the browser pass verified the synthetic packet renders on both Case Workspace and `/iset/payments` with no console/page/API failures and cleanup verified.
- Auth/Frontend/TEST: Updated the authenticated admin `apiFetch` path to honor the existing runtime `window.__API_BASE__` override before the build-time API base. This keeps normal TEST/PROD routing unchanged while allowing deployed-bundle browser smokes to exercise the local on-instance API without CORS/rewrite failures.
- Ops/Payments/TEST: Completed the TEST rehearsal for the NWAC email payment workflow. Release `20260511-test-payments-workflow` applied the follow-up migration and deployed the payments transformation under admin warning plus ALB fallback; corrective release `20260511-test-payments-api-base` deployed the runtime API-base fix. TEST rollback DB smoke and authenticated API + Puppeteer smoke passed against the deployed admin bundle, with Finance email routing disabled and final SQL cleanup showing zero synthetic rows.

## 2026-05-10
- Data/Events/DEV: Retired the unfinished event outbox scaffold. Event emission now writes only the canonical `iset_event_entry` row and uses the existing in-process notification hook; migration `20260510_0001_retire_event_outbox.sql` drops unused `iset_event_outbox`, and reset/postload tooling no longer references it.

## 2026-05-09
- Ops/PROD/Release: Deployed admin-only release `20260509-admin-feedback-fixes` to TEST and PROD with schema/data/shared/portal skipped, preserving the existing generated What's New bullets while adding the four feedback fixes. TEST used admin warning plus ALB fallback and passed admin target-group smoke on both instances. PROD used admin warning plus ALB fallback, cleared fallback during ELB health evaluation, completed ASG refresh `9b6e5e40-f640-44c1-a785-667684687755`, passed normal-routing admin `/healthz` smoke, cleared the warning, and marked feedback reports `#87`-`#90` resolved.
- Ops/Assessment/PROD: Deployed combined admin + public portal release `20260509-prod-option-b-plus-portal` to PROD with all-surface warning and ALB fallback, schema/data promotion skipped except the additive application-assessment migration. Restore point `path-prod-20260509-prod-option-b-plus-portal-20260509093619` was captured, migration `20260508_0001_create_application_assessment.sql` applied successfully, ASG refresh `1e9442ae-7391-4177-8f84-86ec7ea9c467` completed on refreshed instance `i-0fa6a12dc69d110c0`, fallback and warning were cleared, and health smoke returned `200` for admin plus both public portal hostnames.
- Data/Assessment/PROD: Completed Option B PROD backfills from the refreshed admin artifact. The application-assessment table backfill copied `57` clear single-application legacy `iset_case_assessment` rows into `iset_application_assessment`, left duplicate application-assessment owners at `0`, and reran idempotently with `57` already migrated. The decision-letter context backfill moved `57` clear root-level workflow contexts under `applicationDecisionLetters[application_id]`, preserved `1` applicationless legacy case, and left exactly `1` intentional root workflow-key case.
- UX/Public portal/Release: Shipped the planned public portal dashboard/start-resume changes in the same PROD release. The landing-page What's New notes for `20260509-prod-option-b-plus-portal` include the repeat-application assessment fix and the applicant dashboard/start-resume update, with the prior repeat-application known issue removed.
- Docs/Testing: Added `docs/testing/browser-workflow-smoke-automation.md` and indexed Task History label `Overview approval workflows` so future threads can recover the Option B browser-smoke pattern: DB/API fixtures first, real-auth browser assertions, network/server-error capture, post-run database verification, and cleanup evidence.

## 2026-05-08
- Data/Assessment/DEV+TEST: Began the Option B containment fix for repeat applications by adding DEV migration `20260508_0001_create_application_assessment.sql`, creating application-scoped `iset_application_assessment`, adding dry-run/apply backfill classification tooling, and moving the first application assessment read/write surfaces to selected `applicationId` while preserving legacy `iset_case_assessment` for compatibility. DEV backfill classified one legacy row as ambiguous and copied zero rows; DB-level smoke proved Application 2 can start blank and save without mutating Application 1 or the legacy case assessment. TEST release `20260508-test-application-assessment-option-b` applied the schema/admin app under admin maintenance fallback, copied 45 clear single-application legacy assessments into `iset_application_assessment`, left 0 duplicate application-assessment owners, and passed a transactional repeat-application fixture smoke. Corrected follow-up TEST release `20260508-test-option-b-followup` fixed the `applicationJoinSql is not defined` regression and passed authenticated route-level EI save, repeat-application API isolation, and queue/decision API smoke. Later DEV walkthrough found another Option B blocker: Step 14 application approval-letter state still used case-scoped `iset_case.case_context_json` keys (`decisionLetterDrafts`, `decisionLetterPackDrafts`, `decisionLetterSent`), so a second approved application could inherit the first application's letters and read-only sent marker. DEV now scopes this decision-letter workflow state under `applicationDecisionLetters[application_id]`, repaired the local case `1` test data so Application `2` has no inherited sent marker, and the corrected TEST rehearsal plus weekend combined admin+portal PROD release plan are recorded in the application-assessment migration plan.
- Tooling/Assessment/DEV: Added `scripts/application-assessment-option-b-smoke.js` and `npm run assessment:option-b:smoke` for repeatable Option B smoke testing. The runner checks application-assessment ownership, app-scoped approval-letter/review context, root case-context leaks, duplicate assessment owners, and document application association; it supports read-only live-case checks, rollback fixture mode, and persistent fixture mode for browser testing. Current DEV persistent fixture is case `6`, Application A `11`, Application B `12`, with Application B approved/fresh for Step 14.
- Testing/Assessment/DEV: Ran authenticated DEV browser/API smoke for Option B with temporary Cognito staff users against fixture case `6`. The smoke verified selected-application workspace hydration for Applications `11` and `12`, an authenticated Application `12` assessment save that did not mutate Application `11` or legacy `iset_case_assessment`, healthy application-list routing, and the direct Step 14 approval-letter route for Application `12` opening blank/editable without Application `11` draft or sent/read-only state.
- Tooling/Data/DEV: Added `scripts/application-assessment-context-backfill.js` plus `assessment:context:plan/apply` to audit and migrate old root-level application-assessment workflow state in `iset_case.case_context_json` into `applicationDecisionLetters[application_id]` only where ownership is clear. DEV dry-run inspected two cases and found zero remaining root workflow-key leaks after local repair and fixture creation.
- Ops/Assessment/TEST: Deployed corrected admin release `20260508-test-option-b-letter-context` to TEST with schema/data/portal skipped, using admin warning plus ALB fallback. TEST schema plan reported 0 pending migrations, admin target-group smoke passed on both instances, context backfill applied 45 clear single-application root workflow-context rows and preserved one applicationless case, and repeat-application smoke created persistent TEST browser fixture case `136` / Application B `62` in fresh Step 14 state. Authenticated TEST API/browser smoke then passed with a temporary Cognito staff user: selected-application read/write isolation, Step 14 App 62 letter freshness/editability, Manage ISET Applications route smoke, queue/decision APIs, app-associated generated assessment document rows, and applicationless case `100` workspace all passed; SES send was intentionally not exercised in TEST. PROD remains paused until an approved maintenance window.
- UX/Landing Page/Release prep: Regenerated the public landing-page release notes for planned release `20260509-prod-option-b-plus-portal`. The current "What changed" bullets now include the repeat-application assessment fix and the public portal dashboard/start-resume change, and the old repeat-application known-issue warning has been removed.
- Ops/Assessment/PROD prep: Recorded the weekend PROD decision as GO only for a controlled combined admin+portal release `20260509-prod-option-b-plus-portal`. Read-only PROD deploy plan passed with exactly one pending schema migration (`20260508_0001_create_application_assessment.sql`), no data promotion, admin+portal app deploy, shared skipped, and three smoke targets; latest manifest is `tmp/path-deploy/prod/20260509-prod-option-b-plus-portal--2026-05-08T19-32-56-458Z.json`. Local syntax/diff checks and both production builds passed, with admin compiling under the existing warning set. The deploy orchestrator now includes the application-assessment support scripts in the admin artifact so PROD table/context backfills can run from `/opt/nwac/admin-dashboard`. Read-only PROD inventory found 57 legacy case-assessment rows, 0 repeat-application cases, 58 root workflow-context cases, and no existing `iset_application_assessment` table. No PROD mutation was performed.
- Ops/PROD/Release: Deployed shared + admin + portal release `20260507-prod-contact-retirement` to PROD from WSL after a 5-minute all-surface warning and ALB maintenance fallback. The run captured restore point `path-prod-20260507-prod-contact-retirement-20260508000234`, applied schema migration `20260507_0001_harden_admin_ai_guidance_schema.sql`, promoted the allowlisted `intake-release` dataset for workflow `21`, uploaded WSL-native artifacts to `s3://nwac-prod-artifacts/shared/shared-latest.zip`, `s3://nwac-prod-artifacts/admin/admin-dashboard-latest.zip`, and `s3://nwac-prod-artifacts/portal/portal-latest.zip`, completed ASG refresh `f323cb21-bc0c-4063-b0e8-017b40f31544` on replacement instance `i-00b00ebdff3f55dc5`, cleared fallback/warning, and confirmed final public smoke plus `/api/contact` returning `410 contact_retired`.
- Ops/Deploy: Ported and validated the PROD app branch in `scripts/path-deploy.js` to build/package/upload admin, portal, and shared artifacts from the WSL workspace and start/wait the PROD ASG refresh without invoking the legacy PowerShell component scripts. Updated the PROD, quick-start, orchestrator, and portal deployment runbooks to remove the temporary WSL app-rollout block and document the ALB fallback/ELB health interaction observed during the release.

## 2026-05-07
- Ops/Docs/PROD deploy: Superseded by the 2026-05-08 WSL-native PROD port. At this point in the 2026-05-07 migration review, the PROD runbooks were temporarily tightened so app rollout commands were consistently marked blocked from WSL until the PROD artifact path was ported/revalidated, partial deploy sections no longer presented legacy PowerShell component commands as active shortcuts, portal-specific docs pointed back to the corrected admin PROD guide, and `path:deploy` failed before any mutating PROD step when a WSL run selected app rollout.
- Ops/TEST/Release: Deployed corrective release `20260507-test-contact-retirement` to TEST from WSL with schema and data skipped. The first pass updated admin release notes plus portal Contact retirement using admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260507-162845.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260507-163116.zip`; portal-only follow-ups deployed `s3://nwac-test-artifacts/portal/portal-20260507-163840.zip` and `s3://nwac-test-artifacts/portal/portal-20260507-164535.zip` so `/api/contact` now returns `410 contact_retired` before auth and public AI-support copy no longer references Contact. TEST target-group smoke passed after fallback was cleared, and SSM verification on both instances confirmed the Contact-retirement note is present, the old duplicate-application release-note bullet is absent, stale Contact support copy is absent from the deployed portal server, and the retired contact endpoint returns 410.
- Ops/TEST/Release: Deployed admin + portal release `20260507-test-wsl-native` to the Test and Training environment using the new WSL-native TEST deploy runner after a 2-minute all-surface warning and ALB 503 fallback. Schema migration `20260507_0001_harden_admin_ai_guidance_schema.sql` applied successfully, `intake-release` for workflow `21` was promoted, admin artifact `s3://nwac-test-artifacts/admin-dashboard/admin-dashboard-20260507-160759.zip` and portal artifact `s3://nwac-test-artifacts/portal/portal-20260507-161030.zip` updated both TEST instances, fallback/warning were cleared, and normal-routing target-group smoke reported both admin and portal targets healthy.
- UX/Landing Page/Release prep: Refreshed the generated admin release notes for the May 7 PROD build, removed the application-table item from the current public What Changed list, called out the public-portal Contact-function retirement, moved still-relevant prior-patch notes into an Earlier Changes card, and added a public known-issue note warning that repeat applications can currently open/overwrite the previous application assessment until the planned weekend application-scoped assessment fix.
- UX/API/Application table/DEV: Changed the Manage ISET Applications list from one primary application per case to application-level rows, so Active still hides closed/archived applications but Closed and Show All Applications can show historical applications for the same client/case; opening a row now carries `applicationId` into the case workspace so historical application rows open the selected application record.
- Ops/Dev: Extended the WSL Puppeteer/Chrome runtime dependency workaround to the public portal backend as well as admin, setting `LD_LIBRARY_PATH=/home/bill/.local/chrome-deps/extract/usr/lib/x86_64-linux-gnu` for both backend tasks and documenting that portal signature/document PDFs need the same NSS/NSPR libraries.
- Ops/Dev: Added the WSL Puppeteer/Chrome runtime dependency workaround for admin PDF generation: user-local NSS/NSPR libraries under `/home/bill/.local/chrome-deps/extract/usr/lib/x86_64-linux-gnu` and `LD_LIBRARY_PATH` in the admin `.env` / `dev:all` backend task.
- Ops/PROD/Data repair: Corrected Tanisha Saddleback application `ISET-20260429-B2679D` / application `48` / case `127` from `Decision Recorded • Approved` to `Decision Recorded • Denied` after restore point `path-prod-tanisha-denial-20260507170032`. Guarded SQL `sql/ops/prod-fix-tanisha-saddleback-denial-20260507.sql` preserved the case manager `no_recommend` assessment and approver `agree` assurance, set the funding decision to `reject` / `denied`, closed the case, retired the auto-assessment approval plan/interventions, archived the active approval-only generated documents, withdrew the draft CFA version, and inserted correction audit events.
- UX/Application approval/DEV: The Application Assessment decision step now asks whether the decision-maker agrees with the case manager's recommendation before the funding outcome, defaults approve/deny from that answer, and blocks contradictory combinations such as agreeing with a no-funding recommendation while recording approval.
- UX/Intervention approvals/DEV: New intervention proposal and intervention change proposal decision steps now use context-specific wording for approving, denying, or requesting changes to the proposal/change, replacing misleading generic approval-decision copy.
- UX/Help/DEV: Added verified admin AI and Application Approval help for `Request Changes`, including the required note, Case Notes audit trail, and guardrails against treating it as denial or approval-letter workflow.
- UX/Help/DEV: Added verified admin AI and Supporting Documents guidance for missing required documents during Application Assessment, tying checklist review to Secure Messaging requests and Notes/Case Notes follow-up documentation.
- UX/Help/DEV: Added verified admin AI and Work Queue Items guidance for homepage `Pending Completion`, covering application post-decision follow-through plus approved intervention proposal/revision approval-letter follow-up.
- UX/Help/DEV: Added verified admin AI and help-panel guidance for approved new/revised intervention approval-letter follow-up from Case Workspace, including the `Prepare approval letters`, `Generate drafts`, and `Send client approval letter` path.
- API/AI/DEV: Hardened admin AI guidance retrieval with richer guidance-card/example metadata, managed migration coverage, and a help-panel no-match guardrail that tells the model not to invent PATH workflow controls when no verified guidance card matches.
- API/Diagnostics/DEV: Added debug-gated admin AI guidance diagnostics so System Administrators can see `_guidance` match/no-match metadata from `/api/ai/chat` when `ADMIN_AI_GUIDANCE_DEBUG=true`.
- Docs/Testing: Added the admin AI chatbot eval scaffold with broad drafted fixtures and a read-only `npm run ai:eval:check` fixture-shape validator.
- Docs/Tooling: Added read-only `npm run ai:inventory` to regenerate the admin AI route, help-panel, training-section, and docs-group inventory used by the chatbot coverage register.
- Docs/Planning: Added the admin AI chatbot coverage register with initial whole-system route, help-source, domain, training-section, cross-domain concept, and eval-backlog inventory for the knowledge-base overhaul.
- Docs/Planning: Added the admin `Ask the AI` knowledge-base transformation plan, defining the grounded PATH workflow assistant target, `/documentation` training-corpus role, no-grounding rule, knowledge-card model, eval expectations, and cross-thread runbook.
- Ops/Dev: Recorded the current WSL2 Ubuntu local-development workspace at `/home/bill/ISET/path-dev-wsl.code-workspace`, including `admin-dashboard`, `ISET-intake`, `shared`, and `intacct-mock-service`; added WSL-native VS Code `dev:all` tasks for the full local stack, documented the WSL-to-Windows DEV MySQL host (`DB_HOST=172.26.176.1`), and marked the older `/root/ISET/admin-dashboard` memory as superseded.
- Ops/Deploy: Ported the TEST app deploy path in `scripts/path-deploy.js` to build/package the WSL admin, portal, and shared trees directly, upload artifacts through WSL AWS CLI, and update TEST instances through SSM without the stale Windows checkout. Updated deployment memory to make `/home/bill/ISET` the TEST deploy source of truth and to block PROD app deploy from WSL until that separate app path is ported/revalidated.
- Docs/Ops: Superseded the earlier incomplete WSL/Windows deploy split guidance. TEST deploys now run from `/home/bill/ISET/admin-dashboard`; old `X:\ISET` / `/mnt/x/ISET` checkout instructions are stale for TEST and must not be used as a fallback.

## 2026-05-06
- Ops/PROD/Release: Deployed shared + admin + portal release `20260506-prod-dev-outstanding` to PROD after a 5-minute all-surface warning and ALB 503 fallback; restore point `path-prod-20260506-prod-dev-outstanding-20260507003106` was captured, schema had 0 pending migrations, `intake-release` data promotion applied, ASG refresh `7de0049e-782f-4a05-81c0-1d41c0f871a4` completed successfully, fallback/warning were cleared, and follow-up health smoke returned `200` for admin plus both public portal hostnames.
- Docs/Ops: Updated the deploy runbook with the PROD ASG-refresh caveat found during release `20260506-prod-dev-outstanding`: ALB fixed-response fallback can make target groups report `Target.NotInUse`, so operators may need to clear fallback before ASG ELB health evaluation can complete, then smoke normal routing before clearing the in-app warning.
- Ops/TEST/Release: Deployed admin + portal release `20260506-test-dev-outstanding` to the Test and Training environment after a 5-minute all-surface maintenance warning and ALB 503 fallback; schema had 0 pending migrations, the `intake-release` dataset was included, both admin and portal instances updated successfully, normal-routing smoke reported both admin and portal target groups healthy, and the fallback/warning were cleared.
- Docs/Ops: Tightened the TEST/PROD deploy runbooks so ALB fallback rollouts explicitly clear the fallback before the final normal-routing smoke and only then clear the in-app maintenance warning.
- UX/Approvals/DEV: Application approval and intervention proposal/revision decision steps now show the case manager recommendation and rationale above the decision controls, so reviewers do not need to open the generated assessment PDF just to see the proposal being approved or challenged.
- Fix/Public portal/DEV: Public intake final submission now merges the durable saved draft with the current submit-step payload and refuses final-step-only submissions that are missing core applicant facts, preventing upload/signature-only application records.
- UX/Casework/DEV: Generated approval-letter packs now let staff edit supporting institution, loan-provider, and other-funder letter bodies in the Application Assessment and Intervention Assessment letter tabs before saving, sending, or downloading.
- Ops/PROD/Config: Repaired the live document checklist runtime config for feedback reports `#78` and `#82`; income/expense evidence now uses conditional checklist IDs with active `evidence_income` / `evidence_expense` document types, and intervention Band/Nation decision-letter requirements now accept the merged `band_funding_decision` type.
- Ops/PROD/Data repair: Repaired application `ISET-20260505-4ED405` / case `138` after restore points `path-prod-kayla-app61-20260506132058` and `path-prod-kayla-payload-recovery-20260506142621`; display/routing facts were populated from linked client `160`, the case was assigned to the existing Alberta auto-assignment target, then a temporary PITR cluster restored to `2026-05-05T16:20:00Z` recovered the May 1 saved draft payload and merged it with the live May 5 upload/signature payload. The temporary `codex-kayla-*` RDS resources were deleted after verification.
- UX/DEV: Secure-message compose now opens in a workspace-owned floating non-modal panel so staff can continue using the Application or Case Workspace while drafting, including after quick-layout changes that remove the Secure Messaging widget from the board. Draft text is kept only in the open panel, not persisted, and the panel closes with a warning if the workspace changes to another applicant/case record.
- Fix/Casework/DEV: Case Workspace intervention row actions now open approval-letter follow-up through the durable approval deep-link route with `step=communication`, so `Prepare approval letters` no longer falls back to the blank new-proposal wizard.
- UX/Homepage/DEV: Added an application-level `On Hold` workflow bucket for parked applications. `on_hold` applications leave active assessment and decision queues, appear in the homepage `On Hold` queue for NWAC Administrators, Regional Managers, and ISET Coordinators, and no longer accrue an active application SLA stage while parked.
- UX/Casework/DEV: Added `Put on hold` to Application Overview quick actions. Staff select a hold reason and review date; the action persists application status `on_hold`, stores the hold reason in `awaiting_reason`, creates an application hold review reminder, and `Resume review` returns the file to `in_review`.

## 2026-05-05
- Ops/PROD/Release: Deployed shared + admin release `20260505-prod` to PROD after a 5-minute admin maintenance warning and admin ALB 503 fallback; schema/data/portal were skipped, ASG refresh `3603e0bf-dedb-4e72-8cb2-03d4635363fd` completed successfully, fallback and warning were cleared, and health smoke returned `200` for the admin console plus both public portal health endpoints.
- UX/Landing Page: Refreshed the public landing-page release notes for the May 5 release, with updated bilingual What's New bullets and empty Known Issues / Coming Next sections.
- Fix/Notifications/DEV: Intervention proposal decision bell alerts now display budget pot code/name instead of falling back to the raw `budget_pot.id`; new intervention decision events include `budget_pot_code` and `budget_pot_name`.
- Fix/Casework/DEV: Intervention proposal approval-letter sending now tolerates missing optional metadata fields, preventing a null `toLowerCase` server crash for approved new intervention proposals.
- UX/Homepage/DEV: Added an ISET Coordinator `My Clients` work-queue bucket in second position after `My Applications`, listing open client case files assigned directly to the signed-in coordinator.
- Fix/Casework/DEV: Changes-requested intervention proposals and revisions now keep their pushed-back status during save-progress edits and use a clear resubmission action that sends the same proposal row back to `submitted`.
- Fix/Homepage/DEV: ISET Coordinator `Awaiting Approval` now includes the coordinator's own submitted new/revised intervention proposals, not only application assessments; the shared intervention approval API is owner-scoped for coordinators.
- Fix/DEV tooling: Clear ISET test data now clears `iset_intervention_proposal`, and intervention proposal compatibility sync now overwrites submitted/reviewed timestamps so reused DEV rows cannot carry stale approval metadata into new proposal tests.
- Fix/Casework/DEV: Action-plan and intervention owner access checks now receive the canonical `assigned_staff_profile_id` from their backend row fetchers, preventing assigned ISET Coordinators from receiving a generic Forbidden response on case-owned intervention updates.
- Fix/Casework/DEV: Intervention activation now persists as a delivery-status transition instead of being mistaken for a proposal decision, so authorized case managers can activate approved interventions without NWAC/System Administrator decision permissions.
- Fix/Notifications/DEV: Approval-decision and intervention-proposal decision staff emails are now owner/watcher scoped; enabling role rows for these events supplies templates/settings but no longer broadcasts decision emails to every staff member in those roles.
- Fix/Casework/DEV: The Manage ISET Applications `Show Active Applications` filter now includes post-decision pending-completion files and excludes only closed/archived application lifecycle rows.
- UX/Homepage/DEV: Pending Completion application rows now open Application Workspace with explicit post-decision wizard intent: Approval letters before the approval letter is sent, then Funding forms and signatures after the approval letter is recorded.
- Fix/Homepage/DEV: Excluded `auto_assessment` interventions created by application approvals from intervention-proposal `Pending Completion` follow-up so an approved application assessment appears as one application completion item, not a duplicate intervention proposal item.
- Fix/Homepage/DEV: Reworked the `Pending Completion` homepage queue so ISET Coordinators, NWAC Administrators, and Regional Managers see post-decision application work until the application is completed/closed, and approved new/revised intervention proposal letter follow-ups until the approval letter is sent.
- Fix/Notifications/DEV: Added backend case-event emission for new intervention proposal and revised intervention proposal decisions, using six outcome-specific event keys so approved, denied, and changes-requested decisions flow through notification settings, bell/email dispatch, events panels, and audit history.
- Fix/Casework/DEV: Intervention proposal approval-letter follow-up now unlocks from persisted approved proposal/revision state instead of temporary frontend completion state; ordinary historical/backloaded approved interventions do not unlock the letter step, and intervention denial-letter paths remain hidden.
- Fix/Casework/DEV: Approved new/revised intervention proposals now remain an in-progress proposal workflow until the intervention-scoped approval letter is sent; sending the letter persists an `approvalLetterFollowUp` marker on the durable intervention row so quick actions, widget headers, and proposal/revision blocking clear across sessions only after the letter follow-up is complete.
- API/Messaging/DEV: Secure-message approval-letter attachments can now be sent for an approved intervention proposal or approved applied revision that belongs to the case, while intervention-scoped denial-letter attachments remain blocked.
- Docs/Ops: Tightened the agent entry point and deployment quick guide so TEST/PROD app deploys that can restart targets or expose raw `502 Bad Gateway` must use the ALB fixed-response `503` maintenance fallback before `path:deploy`, not just an in-app warning.

## 2026-05-04
- Security/Public portal/DEV: Hardened staff-to-applicant secure-message/signing recipient resolution so portal-visible applicant recipients resolve only from submission ownership or the client's Cognito-sub-linked applicant account, and conflicting strong applicant links fail closed instead of falling back to email or payload user IDs.
- Security/Public portal/DEV: Hardened applicant upload finalization so S3 keys and local-direct file paths must match the same user's pending upload provenance before document rows are created.
- Ops/TEST/Release: Deployed admin + portal release `public-portal-exposure-hardening-test` to the Test and Training environment after an all-surface maintenance warning; schema had 0 pending migrations, both TEST admin and portal instances updated successfully, target-group smoke reported both admin and portal targets healthy, and on-instance verification confirmed the deployed hardening markers.
- API/ILMP: Changed ILMP action-plan/intervention close-out validation to be status-driven instead of date-driven, so planned intervention end dates no longer require outcomes or emit close-out XML unless the intervention is completed/cancelled.
- Ops/PROD/Data repair: Completed guarded duplicate-record merge for Jodie Stephens after restore point `path-prod-jodie-stephens-merge-20260504122704`; run `jodie-stephens-merge-20260504123533` kept public-portal client `156` / user `199` / case `134` / application `56` as the survivor, retired imported client `72` / case `72`, suspended old user `75`, seeded a draft assessment from proposal `84`, moved 25 manual-upload documents with source metadata preserved, used an application lock plus admin maintenance warning during the repair, and made no hard deletes.
- Docs/Meta: Added a PROD duplicate applicant identity merge guide and indexed the Task History title `Locate PROD case merge thread` so future chats can recover the detection and guarded repair pattern for imported-client/public-portal duplicate identity cases.

## 2026-05-03
- UX/Casework/DEV: Split Application and Case Workspace header menus into `Quick layouts` for board/view presets and `Quick actions` for mutating workflow actions.
- UX/API/Casework/DEV: Widened Case Header historical-entry actions (`Add existing action plan`, `Add existing intervention`, `Upload existing documents`) to non-archived application-backed cases for System Administrators, NWAC Administrators, and Regional Managers, with a historical-record warning and backend role enforcement for manual backload submissions.
- UX/Casework/DEV: Added a Case Management-style header selector to the Manage ISET Applications table so staff can filter the server-paginated application list by active, new, assessment, pending-decision, decision-recorded, approved, denied, closed, flagged, or all applications.
- UX/Homepage/DEV: Cleaned up Work Queue inline row actions so item names are the workspace link, redundant `Open workspace` actions are removed, the `Actions` column hides when no secondary row actions exist, rows expose at most two inline actions, and assignable rows use owner-aware `Assign` / `Reassign` labels.

## 2026-05-02
- UX/API/Auth/DEV: Administrative Users profile details now return and edit DB-backed staff `name` and `display_name` values from the selected user Profile tab, using a guarded profile-update endpoint instead of Cognito custom attributes.
- Notifications/DEV: Split secure-message notification events by direction. Public-portal applicant-to-staff messages now emit `applicant_secure_message_received`, while admin case-workspace staff-to-applicant messages emit `staff_secure_message_sent`; DEV notification settings were moved so staff templates fire only for inbound applicant messages and applicant templates fire only for outbound staff messages.
- Notifications/DEV: Made inbound `Applicant secure message received` bell/email delivery owner-scoped. The assigned owner receives the alert through their actual staff-role row, case watchers receive it through the `ISET Coordinator` row, and broad admin role rows no longer broadcast applicant secure-message alerts to every user in that role.
- Notifications/DEV: Added the `New secure message from applicant` notification template for staff-facing applicant secure-message alerts, with copy that identifies the applicant, received timestamp, tracking ID, and PATH link without including message subject or body content.
- UX/Notifications/DEV: Added explicit event timestamp placeholders (`{event_datetime}` and `{message_received_at}`) to the staff notification renderer context and Template Editor field catalog so secure-message staff emails can show when the message was received.
- UX/Auth/Public portal: Updated the registration collision message for existing confirmed applicant accounts so users are told an NWAC case manager may have already set up the account and can use **Reset or set password** to activate access.
- Ops/TEST/Public portal: Deployed portal-only hotfix `20260502-portal-register-collision-copy-test` after a scoped Test and Training portal maintenance warning; both TEST portal targets reported healthy.
- Ops/PROD/Public portal: Deployed portal-only hotfix `20260502-portal-register-collision-copy-prod` after a scoped portal maintenance warning; ASG refresh `b7c78445-359f-4dc5-8d67-00afd0fb3a30` completed and public portal health smoke returned `200` for `https://iset.nwac.ca/healthz` and `https://nwac-public.awentech.ca/healthz`.
- Ops/TEST/Release: Deployed full release `20260502-001919` to the Test and Training environment after a 5-minute admin + portal maintenance warning; schema had 0 pending migrations, `intake-release` workflow/data promotion applied for workflow `21`, admin and portal artifacts deployed from commit `d299e7f0`, both TEST target groups reported healthy, and the maintenance warning was cleared.
- Ops/PROD/Release: Deployed full release `20260502-003615` to PROD after a 5-minute admin + portal maintenance warning; captured restore point `path-prod-20260502-003615-20260502003633`, schema had 0 pending migrations, `intake-release` workflow/data promotion applied for workflow `21`, shared/admin/portal artifacts deployed, ASG refresh `93cc3a8c-6a4c-4df5-8ac7-11b1ab1d9bce` completed, admin and portal health smoke returned `200`, and the maintenance warning was cleared.

## 2026-05-01
- Fix/Application workflow/DEV: Hardened case-to-application selection so active/in-review applications win over closed/completed historical applications, even when old rows receive later client-file updates; case saves now refuse to move terminal applications back into review or document-request queues while still allowing the approved-to-completed finish step.
- Ops/TEST/Release: Replaced the earlier landing-page-only TEST rollout with full release `20260501-dev-sync-test`; schema had 0 pending migrations, `intake-release` workflow/data promotion applied for workflow `21`, admin and portal apps deployed from the current DEV heads, both TEST target groups are healthy, the maintenance notice was cleared, and on-instance verification confirmed the new admin/portal bundles plus the Template Editor refactor strings in the deployed admin bundle.
- Notifications/DEV: Added generic staff SES delivery for non-assignment notification events. Enabled `notification_setting` staff rows with `email_alert=1` and an assigned template now resolve role/case/watch recipients, render through `notification_template`, and send once per deduped staff recipient; assignment-family emails keep their existing assignee/watcher path.
- Notifications/DEV: Hardened duplicate notification settings by ordering dispatcher and renderer lookups toward enabled email rows with assigned templates, so stale duplicate rows do not suppress active configuration or create duplicate sends.
- Notifications/DEV: Split NWAC review notification routing into outcome-specific event keys for approved, denied, and changes-requested reviews so the Manage Notifications matrix can configure each outcome separately.
- Ops/Safety: Added a SES runtime guard that suppresses notification emails when the app is running against TEST environment markers or the TEST DB host, preserving the Test and Training environment blocker while allowing DEV SES sends.
- UX/Notifications/DEV: Overhauled the Template Editor dashboard with a grouped notification field catalog, subject-safe field insertion, scenario-based subject/body preview, unknown-field warnings, scenario-fit guidance, and updated help-panel copy for authoring staff and applicant notification templates.

## 2026-04-30
- Ops/PROD/Release: Deployed admin-only release `20260430-admin-whatsnew-prod` to PROD after a 10-minute admin + public portal runtime maintenance countdown; schema/data/shared/portal deploy steps and DB restore point were skipped, ASG refresh `0ac8bc30-2a15-494a-86d1-84cedaa9c6cb` completed, admin and portal health smoke returned `200`, the runtime maintenance warning was cleared, and ALB fallback routing stayed on normal forwarding.
- Ops/TEST/Data: Refreshed TEST from a sanitized current PROD database dump for application/case experimentation, preserving TEST Cognito pools, rebinding `bill@sillery.co.uk` and `program.admin@awentech.ca` through the TEST identity overlay, leaving PROD supporting-document S3 objects uncopied, and clearing maintenance after both TEST admin and portal target groups were healthy.
- Ops/Tooling: Tightened the TEST DB restore helper's `DEFINER=` stripping command so future restores do not emit misleading local shell warnings while building the remote restore command.
- Fix/Auth: Stopped admin staff authentication from hydrating staff scope or identity from legacy Cognito `region_id`, `custom:region_id`, `user_id`, or `custom:user_id` claims; staff region access now stays DB-backed through `staff_profiles` / `staff_region` and missing assignments require explicit data/user-management repair.
- Ops/Auth: Added a dry-run-first staff Cognito legacy attribute cleanup helper and guide for auditing or clearing per-user `custom:region_id` / `custom:user_id` values after DB-backed assignments are verified.
- Ops/DEV/Auth: Cleared remaining per-user `custom:region_id` values from the DEV staff Cognito pool after code stopped relying on those legacy custom claims; TEST and PROD Cognito attributes were not changed.
- Docs/Ops: Strengthened TEST deployment guidance so TEST rehearses PROD maintenance behavior: planned TEST deploys that can interrupt service or expose raw `502 Bad Gateway` responses must use a scoped maintenance warning or ALB maintenance page.
- Docs/Ops: Added a TEST maintenance-copy rule requiring warnings to use the user-facing name `Test and Training environment` and explicitly state that Production is not affected.
- Ops/Tooling: Updated `path:maintenance` so operator-supplied `--title` / `--message` copy is written into the service-announcement payload and unknown options fail loudly instead of being ignored.
- Ops/Tooling: Fixed `path:deploy:smoke` output for TEST target-group health checks so it prints each target and health state instead of `undefined undefined`.
- Ops/TEST/Auth: Deployed admin-only release `20260430-auth-region-db-test` to the Test and Training environment with no schema/data/portal changes and no Cognito attribute cleanup; rollout used an admin-scoped maintenance warning plus ALB maintenance page, then cleared both after target health and on-instance `/healthz` checks passed.
- Ops/TEST/Release notes: Deployed admin-only release `20260430-admin-whatsnew-test` to the Test and Training environment with no schema/data/portal/shared changes; the landing-page What's New list now contains only the requested New Applications queue, Bugs and Change Requests dashboard, generated assessment PDF repair, and Regional Manager All documents load-error bullets.
- Ops/Data: Repaired Sarah Froese's PROD intervention-proposal assessment documents by backfilling `Case manager assessment v2` and `Case manager assessment redline v2` for case `40` / proposal `2` / intervention `11` after a TEST rehearsal, preserving Amanda Curtis's original `2026-04-17 12:20:49 UTC` submission signature.
- UX/Homepage/DEV: New Applications queue rows now use the applicant name as the workspace link, hide the redundant inline `Open workspace` action, and show `Assign` or `Reassign` based on whether the file already has an owner.
- UX/Support/DEV: Added a dedicated `Support > Bugs and Change Requests` dashboard that reuses the existing feedback triage widget and defaults access to System Administrators, NWAC Administrators, and Regional Managers.

## 2026-04-29
- Docs/Meta: Added a root `AGENTS.md` entry point, refreshed the project-memory standing directive, linked the current documentation audit from `docs/AGENTS.md`, and recorded the first-pass docs inventory and cleanup queue in `docs/meta/documentation-audit-2026-04-29.md`.
- Docs/Meta: Replaced the stale Create React App root README and refreshed verified parts of the project map, including homepage structure, signature-ack macro infrastructure, cross-cutting session-state wording, and the legacy development-tracker caveat.
- Docs/Meta: Superseded the older `docs/meta/codex-crib-sheet.md` quick-start so future searches redirect to the maintained entry path instead of stale onboarding notes.
- Docs/Meta: Marked `docs/meta/level0-document-checklist.md` as historical and pointed future document-model work to the current unified documents model and canonical document-type review.
- Docs/Meta: Added `docs/planning/README.md` and `docs/change-requests/README.md` as directory gates for mixed current/historical planning and CR source material.
- Docs/Meta: Added `docs/data/README.md`, `docs/requirements/README.md`, and `docs/training/README.md` as directory gates for generated dumps, source artifacts, reference standards, and training material.
- Docs/Meta: Added `docs/ops/README.md` and `docs/guides/README.md` as directory gates for operational runbooks and how-to guides, requiring command/path verification before acting.
- Docs/Meta: Added `docs/README.md` plus README gates for the remaining top-level docs directories, including `docs/meta`, so every major docs area now advertises its status and verification requirements.
- Docs/Data/Security: Corrected stale document-model, public-portal security, and application-assessment dashboard notes that referenced future document scoping, missing env/auth docs, or retired `iset_case.application_id` behavior as current.
- Docs/Meta: Corrected stale local and cross-repo documentation references in database docs, public-portal security, file uploads, intake authoring, AWS TEST environment, and input-JSON CR notes; marked the nForm extraction plan and scope note as historical/planned so missing phase deliverables are not mistaken for lost files.
- Docs/Components: Updated the `signature-ack` component pattern and file-upload conditional-rules plan so their status matches the implemented source anchors and remaining grouped-logic/null-semantics work.
- Docs/Meta: Cleaned up additional broken doc references found by a broader scan, including intake-authoring runtime links, finance CR paths, ESDC gap-analysis evidence paths, a PROD Terraform runbook link, and an older runtime-config changelog path.
- Docs/Tooling: Added `scripts/check-doc-links.py`, a read-only local Markdown reference checker for the docs tree, and documented it in the project-memory maintenance guidance.
- Docs/Ops: Moved DB/TEST/PROD/AWS profile command detail from `docs/AGENTS.md` into `docs/ops/agent-operational-access.md` so the agent entry point stays shorter while preserving operational access notes.
- Docs/Data: Added a README gate to `docs/data/temp/` to classify tracked binary source artifacts and defer any keep/archive/delete decision to a focused cleanup.
- Docs/Meta: Added `docs/meta/documentation-cleanup-plan-2026-04-29.md` as the active progress tracker for the broader docbase cleanup effort, including completion criteria and the next work queue.
- Docs/Meta: Expanded the documentation cleanup plan scope to include the sibling public portal docbase at `../ISET-intake/docs` as part of the same cross-app Codex memory base.
- Docs/Meta: Extended `scripts/check-doc-links.py` to scan both admin and public-portal docbases, added README gates across `../ISET-intake/docs`, and recorded the portal first-pass inventory in the cleanup plan.
- Docs/Meta: Compacted `docs/AGENTS.md` by replacing duplicated subsystem status blocks with canonical dashboard, feature, data, and ops doc pointers.
- Docs/Ops: Added `Status` and `Last reviewed` metadata to every admin and portal ops Markdown doc, checked documented deploy/data/migration command names against current package scripts, and marked historical TEST/prod environment notes so they are not mistaken for live runbooks.
- Docs/Security: Redacted literal DB password values from historical docs and strengthened the standing directive so future doc cleanup redacts credentials/tokens/secrets instead of preserving them.
- Docs/Meta: Added `docs/meta/planning-cr-archive-triage-2026-04-29.md` as the first-pass classification index for planning docs, change requests, DOCX source artifacts, and portal archive docs, with initial delete/archive candidates recorded but not deleted.
- Docs/Data: Added `docs/meta/data-artifact-retention-2026-04-29.md` and `docs/data/DB-Structure-Dump/README.md` to classify generated schema dumps and tracked temp binaries as generated/source artifacts, not maintained agent guidance; the dump directory ignore rule now permits the README gate while keeping dump files ignored.
- Docs/Meta: Added `docs/meta/meta-log-retention-2026-04-29.md` to define search, update, retention, and future split rules for large admin and portal meta logs.
- Docs/Planning: Replaced superseded Query Editor, document-model ERM, and public-intake renderer planning notes with redirect stubs pointing to current docs/source; the public-intake renderer stub also removes the old file's invalid text encoding.
- Docs/Meta: Marked the 2026-04-29 cross-app documentation cleanup pass complete in `docs/meta/documentation-cleanup-plan-2026-04-29.md`, with remaining work framed as ongoing maintenance rather than unfinished cleanup blockers.
- Docs/Meta: Indexed this cleanup/context-persistence thread in `docs/meta/codex-thread-index.md` under the exact Task History title `Clarify thread context persistence`.

## 2026-04-28
- Fix/Application workspace: Stabilized the application workspace Secure Messaging widget so global maintenance-announcement countdown renders do not retrigger message reloads every second.
- Ops/TEST rehearsal: Completed the second PROD-like privacy ERM rehearsal in TEST, including duplicate-case consolidation. Measured app-stop-to-healthy-targets downtime was about 38m41s; canonical migrations took 10m37s and app build/deploy took 12m29s.
- Ops/SQL: Made the message-item cleanup preview/apply SQL schema-adaptive so it works before canonical migrations with legacy `messages.sender_id` / `recipient_id` and after migrations with typed `sender_user_id` / `recipient_user_id`.
- Ops/Data rehearsal: Added duplicate-case consolidation preview/apply SQL for the one-client/one-case model after the PROD-like TEST privacy ERM rehearsal exposed four duplicate client case groups. The rollback-only TEST validation reported four merge pairs, zero blockers, and zero remaining duplicate groups or dangling case references after the scripted consolidation.

## 2026-04-27
- Fix/Portal/DEV: Pre-submission public-portal document finalization now records uploads in `iset_application_file` plus intake `doc_refs` until application/case scope exists, then materializes them into `iset_document` during intake completion so `chk_iset_document_application_submission_scope` stays strict.
- Fix/Portal/DEV: Public-portal intake-state saves now persist "no client linked yet" as `NULL` instead of `0` for `input_json_state.client_id`, preserving the hardened FK while allowing early applicant workflow saves before client creation.
- Ops/DEV: Removed the synthetic privacy-denial live-smoke fixture graph and ignored local `tmp/privacy-denial-*.env` files after verification, leaving the live denial harness ready for fresh-token reseeding when needed.
- Security/API/DEV: Fixed payment-packet case-scope evaluation so rows using the legacy `assigned_to_user_id` compatibility alias are still treated as assigned staff-profile rows; seeded live denial checks now reject out-of-region Regional Manager payment packet reads/PDFs.
- Dev tooling/Security: Added `npm run seed:privacy-denials`, an idempotent DEV-only fixture seeder for the real-token privacy route-denial smoke harness.
- Ops/Security: Updated message-item cleanup preview/apply SQL to use typed message participant columns after physical retirement of `messages.sender_id` / `recipient_id`; applied the guarded DEV cleanup for 11 stale mailbox rows.
- Data/Schema/DEV: Hardened internal notification identity by adding typed staff-profile/applicant-user audience and dismissal viewer columns with FKs and CHECK constraints, initially keeping legacy `audience_user_id` / dismissal `user_id` as compatibility shadows.
- Data/Schema/DEV: Constrained `pending_uploads.user_id` to shared `user(id)` and `application_lock.application_id` to `iset_application(id)`, while documenting lock owner, event receipt, and session audit IDs as opaque actor identifiers rather than shared-user FKs.
- Security/Dev tooling: Extended the privacy ERM audit with notification audience/viewer counts, pending-upload ownership counts, and an opaque actor identifier inventory.
- Data/Schema/DEV: Added a staff-profile FK to legacy `iset_case.assigned_to_user_id`, keeping it aligned as a compatibility shadow of `assigned_staff_profile_id` before physical retirement.
- Security/Dev tooling: Classified user-like audit rows as legacy staff-profile shadows, typed notification shadows, opaque principals, or retired legacy identities so the remaining inventory is less misleading.
- Data/Schema/DEV: Retired the confirmed-empty `jordan_application` and `jordan_application_draft` experiment tables through a fail-closed migration that records zero-row retirement counts and refuses non-empty TEST/PROD drops.
- Data/Schema/DEV: Retired five broken appointment/queue stored procedures after confirming their backing tables are absent and no live code `CALL` sites remain.
- Data/Schema/DEV: Added and hardened typed staff-profile/applicant-user actor references on `iset_event_entry`, backfilled deterministic legacy event actors, and updated shared event writes/reads to avoid raw `actor_id` authorization assumptions.
- Data/Schema/DEV: Added typed staff-profile/local-user author references to `iset_application_version`, updated version writes/responses to carry explicit author IDs, and retired the free-form `created_by_id` shadow in DEV.
- Data/Schema/DEV: Hardened application submission/version lineage and CFA case/version/document/participant relationships with FKs plus audit coverage for CFA document case/client mismatches.
- Data/Schema/DEV: Preserved and deleted 40 orphan client-account event rows, then hardened remaining deterministic relationship FKs for client account events, input-state clients, assessment budget pots, reminder action plans, and staff-profile regions through migration `20260427_0018`.
- Data/Schema/DEV: Retired the empty `zzz_legacy_documents` experiment table through a fail-closed migration and classified the remaining no-FK ID-like columns as runtime keys, external references, audit principals, upload tokens, tutorial keys, or lookup keys.
- Dev tooling/Security: Added `npm run smoke:privacy-erm`, a read-only smoke checker for the cleaned privacy ERM model covering retired tables/columns, required FKs/CHECK constraints, message/document/application/client-account invariants, and informational workflow string-key rows.
- Security/API/DEV: Hardened finance allocation evidence upload/delete/presign routes so raw object keys require finance-role access plus allocation/pot metadata provenance or current-user pending-upload ownership.
- Dev tooling/Security: Added `npm run smoke:privacy-routes`, a static route-scope guard tripwire for high-risk document, message, signing, event, and finance evidence routes.
- Data/Schema/DEV: Added typed staff-profile/applicant-user viewer references plus a single-viewer CHECK to `iset_event_receipt`, and updated event feed/read-state paths so legacy `recipient_id` is only a compatibility shadow.
- Fix/API/Documents/DEV: Manual supporting-document uploads now preserve or resolve the real `case_id` for application, action-plan, and client-scoped inserts, keeping the hardened `chk_iset_document_manual_upload_scope` privacy guard intact.
- Data/Schema/Documents/DEV: Backfilled the remaining application-linked system-generated document with applicant scope, tightened `chk_iset_document_system_generated_scope`, and kept global payment batch exports out of `iset_document`.
- Security/Messaging/DEV: Secure-message admin and portal responses now expose canonical typed actor/thread objects, the main admin widgets no longer fall back to raw `sender_id` / `recipient_id` for applicant direction, and secure-message events include typed actor payload fields.
- Security/Dev tooling: Added a legacy compatibility-shadow retirement inventory to the privacy ERM audit and documented physical retirement gates for message participant, assignment, notification, event receipt, application-version, and case/application shadow fields.
- Data/Schema/Messaging/DEV: Physically retired `messages.sender_id` and `messages.recipient_id` after removing secure-message runtime reads/writes and recording aggregate 0-drift counts in a retirement audit table.
- Data/Schema/Assignment/DEV: Physically retired `iset_case.assigned_to_user_id` after removing runtime reads/writes and recording aggregate 0-drift counts in a retirement audit table.
- Data/Schema/Notifications/DEV: Physically retired `iset_internal_notification.audience_user_id` and `iset_internal_notification_dismissal.user_id` after removing runtime reads/writes and recording aggregate 0-drift counts in a retirement audit table.
- Data/Schema/Events/DEV: Physically retired `iset_event_receipt.recipient_id` after moving read-state joins/writes to typed viewer keys, adding typed viewer unique keys, and recording aggregate 0-drift counts in a retirement audit table.
- Security/API/DEV: Limited Query Editor server export to the active environment PATH database and added route-smoke guards so visible non-PATH schemas cannot be selected for dump export.
- Security/API/DEV: Generated consent, authorization-release, client-acknowledgement, Indigenous-declaration, and conflict-declaration PDFs now validate application visibility before rendering from a request body `applicationId`.
- Docs/Ops: Added the privacy ERM grand cleanup rehearsal runbook covering DEV gates, TEST rehearsal order, PROD preflight, stop conditions, rollback expectations, and the no-constraint-weakening rule.

## 2026-04-26
- Docs/Security: Added the privacy ERM cleanup grand-release plan, setting the DEV-first strategy for fixing secure-message identity-domain confusion, case/application/client scope, document/message attachment relationships, stale backend experiments, and the eventual rehearsed PROD data migration.
- Security/Dev tooling: Added a read-only privacy ERM audit script and first DEV report covering ID-domain confusion, unconstrained message/document relationships, stale `message_item` rows, dead experiment tables/routes, and old stored procedures.
- Security/Messaging: Stopped admin case secure-message reads from seeding nonparticipant `message_item` rows for staff case viewers, and blocked admin mailbox-state mutations for users who are not the message sender or recipient.
- Data/DEV cleanup: Added preview/apply SQL for unsafe `message_item` rows and applied it in DEV, preserving 38 deleted rows in `privacy_erm_message_item_cleanup_audit` and leaving only sender/recipient mailbox rows.
- Security/Portal: Public-portal secure-message attachments now persist `message_attachment.case_id` at insert time, reducing later case-scope inference during admin attachment adoption.
- Security/API cleanup: Retired obsolete admin GOV.UK component routes, old case-based application-version routes, and the direct application-answer patch route so they return `410` instead of running missing-table or mismatched-column legacy SQL.
- Data/DEV cleanup: Added preview/apply SQL for document scope and applied it in DEV, preserving old/new values while backfilling missing document client/case/application scope and clearing invalid document `user_id` values.
- Data/Schema/DEV: Added `iset_case.assigned_staff_profile_id` with an FK to `staff_profiles(id)`, backfilled it from valid legacy assignments, normalized invalid legacy assignment values to unassigned, and updated admin/portal assignment writes plus the privacy audit to track drift during cutover.
- Security/Case access: Cut high-risk admin/shared case-assignment reads over to explicit staff-profile semantics, including case-access helpers, RBAC predicates, coordinator/regional filters, staff joins, reporting filters, and owner-resolution comparisons.
- Data/Schema/DEV: Added typed secure-message actor-domain columns with FKs to `user(id)` and `staff_profiles(id)`, backfilled existing DEV messages, and updated admin/portal message writes plus the privacy audit to track actor-domain gaps.
- Data/Schema/DEV: Hardened secure-message attachment scope with `client_id`, corrected `application_id` typing, FKs to message/case/application/client/user, public-portal client-scope writes, and admin adoption mismatch checks.
- Data/Schema/DEV: Added referential constraints for legacy secure-message sender/recipient, message case/application scope, and `message_item` message/owner rows after DEV cleanup proved those relationships clean.
- Security/Messaging: Cut public-portal applicant secure-message reads and reply targeting over to typed actor plus case/application scope, and moved admin mailbox-state authority from legacy sender/recipient IDs to typed sender/recipient user fields.
- Data/Schema/DEV: Hardened `iset_document` scope references by normalizing document user/message ID column types and adding FKs for user, applicant user, case, application, and origin message relationships.
- Security/API cleanup: Retired the unscoped `POST /api/applications/ingest-from-submission` admin endpoint so applications are no longer created without client/case scope through that legacy path.
- Security/Messaging: Updated admin secure-message widgets and the public-portal reply composer so message direction and reply targeting no longer depend on legacy `sender_id` / `recipient_id` response fields when typed actor data is available.

## 2026-04-25
- Docs/Security: Added the public-portal legacy fallback security review, documenting remaining high-risk applicant-portal identity/linking fallbacks after the secure-message breach and the recommended next hardening pass.
- Security/Portal: Hardened the current deployed public portal repo (`../ISET-intake`) against legacy identity fallback risks: Cognito/local-user linking no longer returns already-bound email matches, applicant data routes require the primary applicant portal Cognito client and reject staff/admin roles, client linking no longer claims existing clients by SIN/email/name fallback or overwrites another subject, staff message recipients no longer resolve by arbitrary email fallback, and legacy `POST /api/applications` now returns `410`.
- Ops/Portal: Deployed the portal hardening to TEST, then followed up with release `portal-security-hardening-message-20260425-test` so staff/admin or wrong-client public-portal sign-ins show an explicit applicant-account-required message instead of a generic workflow-schema load error.
- Fix/Portal: TEST validation with `jack@sillery.co.uk` showed a false applicant-account-required block where the token role was `Applicant` but the client match failed. Portal auth now prefers the HttpOnly portal access cookie over any bearer header, preventing stale bearer tokens from shadowing the applicant portal session.

## 2026-04-23
- Fix/Application Assessment: Step 13/14 in the application workspace now trusts the canonical recorded application decision before the older assurance-only fallback, so denied files cannot reopen the approval branch just because `assessment_nwac_review` still says `agree`.
- Ops/Data repair: Applied a guarded one-off PROD correction for `ISET-20260409-123477`, forcing the persisted assessment review fallback onto the denied path and bumping the application row version so step 13 / step 14 in the application workspace hydrate consistently as a denial.
- Ops/Maintenance: Shortened the default ALB fallback maintenance-page body copy to `PATH is temporarily unavailable while maintenance is in progress.` so incident holds do not imply an immediate reopen.
- UX/Homepage: Reworked the NWAC Administrator and Regional Manager Work Queue into a clearer application pipeline with `New Applications`, `In Assessment`, `Pending Decision`, and `Pending Completion`, while keeping all decision-stage application and intervention work combined in the final decision queue.
- UX/Homepage: Removed the redundant NWAC Administrator `All Applications` queue card now that the homepage application pipeline is represented directly by the four stage buckets.
- UX/Homepage: Narrowed `Pending Assessment` to submitted assigned files still waiting for EI verification, and expanded `New Applications` to also include EI-verified submitted files that have not yet entered active assessment.
- UX/Homepage: NWAC Administrators no longer see the narrower EI-pending queue separately; those files are folded into `New Applications`, while Regional Managers keep the same stage under the shorter label `EI Check Needed`.
- UX/Homepage: Added `Pending Completion` across the role homepages as the post-decision application stage for files that still need letters, funding-form/signature follow-through, or other closeout work before the application workflow is complete, and aligned the coordinator's former funding-agreement queue to the same concept.

## 2026-04-22
- Docs/Ops: Clarified the standing deploy-intent rule in `docs/AGENTS.md`: when Bill asks to deploy to TEST or PROD, Codex should assume the full current awaiting-release checkout state, including relevant code/config/runtime/schema promotion work, unless he explicitly narrows the release. Data resets or one-off live data mutation still require explicit intent.
- UX/Finance/Email: Finance payment-packet emails now use a lean AP-style body with `Payee`, `Payment Instructions`, and `Coding` sections, rendering the payment details as a compact line table and only showing optional PATH-entered references when staff provided them.

## 2026-04-21
- UX/Intake/Uploads: Applicant file-upload fields in the legacy public portal now use one visible `Upload` action that opens a `Take photo` / `Choose file` chooser on likely camera-capable mobile devices, while keeping the authored broad document/image accept lists for normal file picking.
- UX/Workflow/Preview: Admin Workflow Preview and the intake-step editor now show a static upload-preview callout explaining the mobile camera chooser behavior so authoring screens stay aligned with the live applicant runtime.
- Fix/Notifications/Approvals: Approval-decision bell alerts now show the actual approver action for application reviews, with `Request Changes` and denials no longer reusing a generic green `Assessment submitted` notification and request-changes alerts now surfacing as yellow warnings.
- UX/Landing Page: Public landing-page release notes are now generated from the maintained release-notes log during build, stamped with the deployed release ID/date, and no longer depend on a separate hardcoded version label that can drift from the live build.
- UX/Admin shell: The visible admin build/version line now uses the deployed release/build identity instead of the stale package semver, keeping the footer build stamp aligned with the published landing-page release notes.
- Fix/Approvals/Wizards: Queue-launched application and intervention approvals now keep the explicit `decision` entry step instead of letting local wizard-step restore or the old Cloudscape navigation-priming workaround bounce approvers back to step 1 or another stale step.
- Fix/Approvals/Workspaces: Queue-launched application and intervention approvals now start on their approval-review board layouts without overwriting the user's saved normal workspace layout, and board quick actions/reset work again after launch.
- UX/Workflow/Wizards: Application Assessment and Intervention Assessment now switch their widget heading and lead-in copy to match the current phase, so drafting, approval, and follow-up states no longer reuse assessment-era labels.

## 2026-04-20
- Fix/Agreements/CFA: Client Funding Agreement signature prefill now prefers the assigned case manager on the case and stores that signer name in the CFA version snapshot so later sends/regenerations do not drift to the staff member who happened to click Generate or Send.
- Fix/Documents/PDF: Case manager assessment signature blocks now prefer staff display names from PATH profiles and read approval/submission signatures from the current shared event store before falling back to legacy case-event rows, avoiding raw email addresses on newly generated assessment PDFs.
- Fix/Workflow/Applications: Application Assessment request-changes decisions now refresh Notes and Timeline immediately after commit, and the auto-created request-changes case note is now written in the main case-update transaction with the normal note-added audit event.
- Fix/Workflow/Documents: Assessment submission now warns before replacing uploaded `Application form` or `Financial overview` files and lets staff retain those current uploads while still generating the case manager assessment PDF.
- Fix/Documents/PDF: Case manager assessment PDF redlines now stay clean on version 1 and compare later versions against the immediately previous submitted assessment so changed text fields render prior values in red strikethrough above the new green replacement text.

## 2026-04-19
- Fix/UX/Casework: Case Workspace now prefers the action plan that owns the latest open intervention proposal when a case is reopened, and the `Proposal in progress` warning now offers a direct jump to that draft proposal instead of leaving staff on the wrong plan tab.
- UX/Notifications/Admin+Portal: PATH-generated emails now support a configurable sender display name and `Reply-To` address in addition to the shared sender email, and applicant-facing mailers now use those settings consistently across notifications and account invitations.

## 2026-04-17
- Docs/Ops: Expanded the durable bug/change triage guidance so Codex now treats triage requests as full queue review plus internal notes, intentional status updates, duplicate/info-gap handling, and a prioritized planning analysis for Bill, while keeping autonomous implementation/deployment out of scope unless explicitly requested.
- Docs/Ops: Added a standing weekly-summary instruction so user-facing "last week" fix/change bullets must review both the release-notes working log and the thread index, catching relevant non-bug/non-CR changes instead of relying only on the feedback queue.

## 2026-04-18
- Fix/Notifications/Admin+Portal+Events: `Auto assigned`, `Case assigned`, and `Case reassigned` now act as separate configurable SES notification events from the Manage Notifications matrix. System auto-assignment sends `Auto assigned`, manual first assignment sends `Case assigned`, and manual reassignment sends `Case reassigned`, all to the actual assignee plus any case watchers using the configured role rows and shared runtime sender email, with the portal-side pre-case `auto_assigned` event suppressed from email delivery so auto-assignment workflows do not double-send.
- UX/Workflow/Applications: Moved the Application Assessment `Deny Funding` shortcut back onto `What is being proposed?`, so staff capture proposal context before taking the denial path.
- UX/Configuration/Admin shell: Demo-toolbar visibility is now stored in shared runtime config instead of browser-local storage, with hidden-by-default fallback and a single System Administrator control that applies consistently across sessions.
- Docs/Ops: Tightened the TEST deploy runbooks to spell out the app-coupling rule for admin changes that depend on sibling `shared` or `ISET-intake` runtime modules, so operators do not incorrectly use an admin-only TEST rollout for coupled server paths such as notification email rendering.

## 2026-04-15
- Security/Auth/Portal: Public password-reset requests now write a durable audit trail keyed by normalized email, including page route, request flow, source IP, user agent, outcome, and small Cognito delivery/error metadata so future investigations can distinguish `/forgot-password`, `/activate-account`, and `/reset-password` resend traffic.
- Fix/Workflow/Applications: Application approvals now move `iset_application.status` to real decision outcomes at commit time, using `approved` or `rejected` immediately instead of the placeholder `decision_ready`, while keeping the approval path open until letters and any required funding forms are finished.
- UX/Approvals/Workspaces: Opening a row from the homepage `Approvals` queue now launches the target workspace in an approval-focused board layout and lands the relevant wizard on its decision step, including selecting the correct intervention proposal in case workspace.
- UX/Approvals/Casework: `Proposed new intervention` now uses `Approved`, `Denied`, and `Request Changes` as the intervention-decision labels, commits the approval at `Record of decision`, and keeps decision-letter preparation as a separate post-decision follow-up instead of another wizard step.
- UX/Home/Docs: Updated the homepage approvals queue messaging, help-panel content, and durable docs to reflect the current `Approvals Items` layout, including province, EI status, compact timeline badges, and workspace-only approval actions.

## 2026-04-14
- Docs/Ops: Updated the standing handoff docs to require live PROD feedback-log updates during Codex bug/change triage, documented the short outcome-first hotfix-note wording style, and captured the later same-thread PROD Regional Manager case-access and feedback-log follow-up under the exact Codex task-history title.
- Fix/API/Casework: Regional Manager case-workspace access now treats direct assignment as a first-class grant across the `/cases/:id` workspace family and the shared action-plan/intervention validators, so directly assigned out-of-region files no longer fail with a region-scope `403`.
- UX/Workflow: Intake step authoring and Workflow Preview now expose the same runtime-backed checkbox-array conditional-visibility operators and whole-step skip behavior as the public portal, bringing the admin editor suite back in line for renderable intake content.
- Ops/Workflow: Verified that DEV workflow `21` authoring rows now regenerate the published intake runtime payload, so the step library, workflow library, and `publish/workflow.schema.intake` row are back in sync in DEV.
- UX/Applications: Application Overview now exposes the manual status selector to `NWAC Administrator` users as well as `System Administrator`, while keeping the existing role-scoped transition checks and finalized-status confirmation flow.
- UX/Home: Reordered the homepage `Work Queue` for `NWAC Administrator` and `Regional Manager` so `Approvals` now sits directly below the case queue in both the card layout and the `Work queue preferences` list.
- UX/Workflow/Manual Intake: Manual Application Intake now uses the same shared conditional-visibility operators and visible-step skipping logic as the public-intake-aligned admin preview/editor path, so support-driven follow-up steps like the Step 19 checkbox flow stay navigable in the admin console without stale hidden answers leaking into submission.
- Fix/UX/Casework: Case Workspace `Intervention Assessment` Step 6 now loads payment-type mapping from the existing payments read endpoint used by the coordinator assessment flow, so `Regional Manager` and `ISET Coordinator` users no longer lose all `Add cost item` options because of a finance-config `403`.
- Fix/UX/Applications: `ISET Application Form` financial totals and amount displays now use the shared two-decimal CAD currency helper instead of a one-off formatter that could show inconsistent decimal precision.
- Fix/Messaging/Portal: Applicant message replies now stay allowed when the replied thread belongs to the applicant's current case and application but the original staff sender is no longer the current case assignee, preventing false `recipient_not_allowed` reply failures after reassignment.

## 2026-04-11
- Fix/Security/Casework: Hardened Word supporting-document viewing so the internal preview path now falls back to a cached self-contained HTML preview when a deployed host cannot launch Chromium for PDF rendering, instead of failing the document view outright.
- Workflow/UX/Configuration: Added a configurable `EI Status Verification` timing target between Assignment and Assessment, seeded through `sla_stage_target`, and exposed it in `Configuration > Workflow timing targets`.
- Fix/UX/API/Applications: Unified application due/overdue stage selection across the assessment dashboard, application overview, homepage queues, and server-side work-queue counts so assigned files with blank `assessment_esdc_eligibility` now report against the EI Status Verification target before moving into Assessment.
- Ops/UX/Admin shell: Added a runtime-config-driven maintenance announcement rail to the admin shell, polling `/api/service-announcement/current` every 15 seconds and rendering a non-dismissible Cloudscape `Flashbar` warning with a live local countdown.
- Ops/Deployments: Added the `path:maintenance` operator command so Codex can set or clear planned or unscheduled maintenance warnings in `iset_runtime_config(scope='runtime', k='service.announcement')` as part of deploy or incident workflows.
- Ops/Deployments: Added the `path:maintenance:fallback` operator command so TEST/PROD HTTPS ALB host rules can be switched to a static HTML `503` maintenance page and later restored, giving users a deliberate maintenance message instead of a generic browser error during hard downtime.

## 2026-04-10
- Security/UX/Casework: Supporting Documents now exposes a separate `Download` action for `System Administrator` and `NWAC Administrator` only, gated by a privacy-risk confirmation and backed by a server-enforced original-file attachment path.
- Fix/Security/Casework: Supporting Documents and other shared document-view actions now render `.doc` and `.docx` through an internal cached preview artifact instead of exposing the raw Word object to browser/Office Online handling, and admin-side manual document uploads now accept Word files in the same workflow.

## 2026-04-07
- UX/AI/Casework: Expanded the Case Workspace help panels and embedded AI help context so staff get explicit guidance for backloading historical action plans, interventions, and supporting documents on imported/application-less cases.
- Fix/UX/Casework: Supporting Documents now keeps imported/application-less cases in case-based document mode whenever the case has no linked application, even if the client already has a PATH account, so `Upload existing documents` no longer asks staff to attach application-scoped document types to a nonexistent application.
- Fix/API/Admin users: Replaced the `Administrative Users > Resend invite` placeholder with a real Cognito `RESEND` flow for staff accounts still in `FORCE_CHANGE_PASSWORD`, and now return clear guidance to use `Force reset` only for active accounts.
- Fix/Security/Admin users: Hardened admin-user management routes so they resolve the target user's actual Cognito admin group server-side instead of trusting role labels sent from the browser, closing gaps across disable, enable, role change, role removal, region update, resend invite, and force-reset actions.
- UX/Admin users: Scoped the Administrative Users dashboard to the roles the current actor is allowed to manage, tightened toolbar button enablement to match account state, and surfaced route `detail` messages in flash errors instead of generic HTTP failures.

## 2026-04-06
- Workflow/API/Finance: Stopped auto-creating payment packets from intervention approval or auto-generated assessment interventions, and now treat approved intervention funding as authorization only until staff create a packet for a specific claim period or receipt.
- Fix/API/Finance: Payment packet creation now allows multiple packets over time for the same intervention, while validating the initial line against remaining authorized funding and adding duplicate warnings at create time.
- UX/Finance: Updated the Payments queue, detail help, and create-packet form to reflect period-based claims, including separate-packet guidance and amount entry against the approved funding ceiling instead of defaulting to the full approved total.
- UX/API/Finance: Added an `Include carry-over` option to `Budgets and Finance > Financial Reports`, surfacing best-effort carry-in/carry-out estimates plus row-level carry-over adjustments derived from payment-line dates when available and intervention schedules otherwise.
- UX/API/Finance: Replaced the old Financial Reports demo dashboard with a live annual `Budgets and Finance > Financial Reports` page for CRF/EI approved intervention funding, including province/territory and case-manager slice-and-dice filters, province totals, intervention-level detail, and finance follow-up on each intervention row.
- UX/Export/Finance: Added workbook-style Excel export for the Financial Reports page, producing a summary worksheet plus separate CRF and EI detail worksheets from the current filtered report view, with finance follow-up fields beside the approved funding amounts.
- UX/Finance: Simplified the top controls on `Budgets and Finance > Financial Reports` by renaming the page to `ISET Advances and Active Clients`, keeping only fiscal-year and region filters in the header, and moving `Reset Filters` into the header actions beside Excel export.
- Fix/API/Finance: Retired the legacy intervention-ledger shortcut so approved interventions no longer create live `finance_transaction` rows. Live commitments now start when a payment packet is sent to finance, and actuals start only when PATH records a posted/confirmed payment.
- Workflow/Schema/Finance: Simplified the payment workflow to a packet-first canonical status set: packet statuses are now `Draft`, `Ready to send`, `Sent to finance`, `Payment confirmed`, and `Cancelled`; line statuses are `Needs evidence`, `Ready to send`, `Sent to finance`, `Paid`, `Held`, and `Cancelled`; optional finance batches no longer act as packet/line statuses.
- UX/API/Configuration: Added the restricted `Configuration > Applicant Watchlist` manager page so NWAC and System Administrators can search, view, add, edit, deactivate, and reactivate SIN-based watchlist entries directly.
- Data/Schema/Watchlist: Rebuilt `iset_applicant_watchlist` to the clean active/inactive model with canonical 9-digit SIN storage plus update/deactivation audit fields, intentionally using a destructive migration because the feature was not yet live in production.
- Events/Admin+Portal: Added shared applicant-watchlist lifecycle events (`added`, `updated`, `removed`, `hit`) and now emit watchlist-hit events from both admin manual intake and the public intake completion path using masked SIN payloads only.
- Security/Home: Homepage watchlist-hit matching now ignores inactive entries, and generic event-feed responses now filter watchlist events for users who do not have access to the Applicant Watchlist dashboard.

## 2026-04-05
- UX/API/Admin shell: Added in-app bug reporting and change requests from the admin-console top header. Staff can now open a dedicated help panel, launch a floating non-modal report window, capture severity plus description, and attach supporting files without leaving PATH.
- Data/Storage/Admin shell: Added the dedicated `admin_feedback_report` and `admin_feedback_attachment` schema plus `POST /api/admin/feedback-reports`, keeping internal bug/change evidence out of `iset_document` while reusing the shared object-store upload path.
- UX/API/Home: System Administrators now see a new `Bug & Change Requests` widget on the homepage, with live filters/search plus a floating review panel for internal bug/change triage without leaving the shell.
- Data/Storage/Admin shell: Added dedicated feedback-management routes plus `admin_feedback_status_history` and `admin_feedback_note` so System Administrators can persist status changes and internal notes on internal feedback reports.
- Fix/API/Finance: `manual_backload` interventions now write historical posted finance ledger entries from their `actual amount`, stay blocked from payment-packet creation, and appear in the payment-ledger export without entering the live finance submission workflow.
- Fix/API/Casework: Backloaded existing interventions now enforce plan-lifecycle compatibility, blocking archived-plan placement, limiting closed plans to completed/cancelled interventions, requiring an active plan for in-progress or suspended interventions, and suppressing retroactive finance/CFA/payment side effects when `manual_backload` interventions are later edited or closed.
- Fix/Data/Casework: Backloaded action plans and closed interventions now seed their lifecycle timestamps from the entered historical dates so `activated_at`, `closed_at`, result dates, and end dates stay internally consistent.
- Docs/Casework: Updated the client-file import and case-header references plus `docs/AGENTS.md` to document the enforced backload rules for imported/application-less cases.

## 2026-04-04
- Ops/Deployments: Established `sql/migrations/` as the canonical PATH shared-schema migration path, moved one-off SQL into `sql/ops/`, added the explicit `path-schema-migrate` CLI (`db:migrate:inventory|plan|apply`), and updated deployed portal paths to force `AUTO_MIGRATE=false`.
- Ops/Deployments: Added an allowlisted PATH data-sync CLI (`data:sync:*`) and prod/test SSM SQL helpers so intake runtime config and workflow-authoring graph promotion can be planned, bundled, and applied without ad hoc copy/paste SQL.
- Ops/Deployments: Added a non-destructive TEST DB refresh planner command (`test:db:refresh:plan`) so Codex can validate the current test account/ASG/instance state before the full refresh implementation lands.
- Ops/Deployments: Added the `path:deploy` control-plane command and runbook so PATH deploys now have a single orchestrated entry point for AWS identity preflight, remote canonical schema apply, optional allowlisted data sync, app rollout, TEST target-group smoke checks, and local release-manifest capture.
- Ops/Deployments: Hardened deployed schema ownership by forcing `DISABLE_AUTO_MIGRATIONS=true` in deployed admin env render paths, standardised the Codex/operator prod profile alias to `nwac-prod`, and aligned the prod-facing deploy helpers/docs to that explicit profile name.
- Ops/Deployments: Verified the prod control plane end to end for read-only preflight, hardened AWS-backed Node/npm operator scripts to route through bash/WSL so they use the Codex-managed AWS profiles correctly, and fixed the prod SSM SQL helper to use explicit DB host/name/port defaults because the prod secret currently holds credentials only.
- Ops/Deployments: Replaced the TEST DB refresh planner-only gap with an executable `test:db:refresh` operator command that uploads or references a scrubbed dump, restores it over SSM on a live TEST app host, reapplies canonical schema, runs TEST smoke, and records a local manifest.
- Ops/Deployments: Prod deploy plans/runs now model restore points explicitly; DB-affecting prod runs auto-capture an Aurora cluster snapshot restore point for `nwac-prod-db` before schema/data mutation begins.
- Ops/Deployments: Closed the last manual TEST-reset gap by teaching `test:db:refresh` to generate its own DEV-derived baseline snapshot (full schema plus allowlisted safe/reference data only) and by adding `--refresh-test-db` to `path:deploy` so TEST can be reset and redeployed from one command with no manual dump step.

## 2026-04-03
- UX/API/Home: System Administrators now see a new `Operations Snapshot` widget on the homepage with live counts for ILMP submission blockers, applicant activation backlog, and staff access follow-up, replacing the default Development Tracker tile.
- UX/API/Home: The new System Administrator snapshot links now open filtered `ILMP Submissions & Exports` and `Manage Users` views, and the ESDC participant queue now honors `readiness` / legacy `filter` URL params so blocked and needs-review links land on the correct queue state.
- UX/API/Home: System Administrators now see a role-aware `Recent Admin Activity` widget that mixes workflow publishes, upload-config changes, event-capture updates, and relevant admin/system case events without a schema change.
- Fix/Home: Homepage activity links now open case workspace records via `/cases/:caseId` instead of the stale `/case/:id` path.
- UX/API/Home: Added a new full-width `AWS Environment Status` widget for System Administrators, showing live read-only checks for staff Cognito, applicant Cognito, and SES mail in the active environment with direct links into User Management and Notification Settings.
- UX/API/Home: Added a new full-width `Users & Access Alerts` widget for System Administrators, showing staff MFA/reset/disabled-account follow-up plus applicant activation backlog with direct filtered links into User Management.
- UX/Home: System Administrator homepage layout storage now rolls to `admin-home-layout-v9` so the new `AWS Environment Status` widget is included by default without resetting other roles' saved homepage layouts.

## 2026-04-02
- Fix/Notifications: Applicant secure-message and reminder emails now recover the public-portal link from standard portal host env vars (`REACT_APP_PORTAL_URL`, `REACT_APP_API_BASE_URL`, `PORTAL_DOMAIN`) when explicit `APPLICANT_PORTAL_*` settings are missing, preventing TEST emails from degrading the sign-in call to plain text with no hyperlink.
- Docs/Ops: Updated the TEST portal env template, runtime notification doc, and environment config map to make the applicant-portal email-link dependency explicit for future deployments.
- Docs/Auth: Added a TEST staff Cognito recovery guide and thread-index entry covering `staff_profiles.cognito_sub` drift, why `Administrative Users` can fall back to primary-region-only display, and the correct recovery paths for legacy `FORCE_CHANGE_PASSWORD` staff accounts when Cognito mail is missing or fails.

## 2026-04-01
- Intake/PDF: Updated the signed intake declaration PDFs and the generated application-form PDF so their English record output matches the current published workflow-21 intake wording for consent/declarations, gender, social-assistance follow-up, target-program options, and support/student-aid sections.
- Intake/Workflow: Removed the generic Step 24 `Other support documents` upload from DEV workflow `21` and republished the intake so selecting `Other` support no longer creates a mandatory catch-all document blocker at submission time.
- Intake/Portal: Completed a French-copy pass on the current DEV intake publish for recently edited consent/funding forms, the new social-assistance and childcare follow-up questions, and the Step 24 support-driven upload requests so the public intake no longer falls back to placeholder or English-only text in those updated areas.
- Portal/Landing Page: Updated the public ISET landing-page French copy to mirror the latest English eligibility/support wording, including the publicly funded institution bullet, the moved support note, and the revised NWAC ISET case-manager wording. This code change still requires a portal deploy to become visible.
- Intake/Portal: Added public-portal checkbox-array conditional operators (`contains`, `notContains`, `containsAny`, `notContainsAny`, `containsAll`) so Step 19 support selections can drive later workflow-21 questions and document uploads without refactoring `Supports Requested` into separate yes/no fields.
- Intake/Portal: Public intake runtime now auto-skips steps whose authored components all hide after conditional evaluation and renumbers visible progress accordingly, allowing irrelevant Step 21/22 support-driven steps to disappear entirely instead of showing placeholder notices.
- Intake/Workflow: Updated DEV workflow `21` authoring so Step 19 `requested-supports` now drives Living Allowance income/expense follow-up, Transportation/Childcare/Other expense questions, and new support-specific Step 24 upload requests for tuition statements, books/materials proof, transportation proof, childcare cost proof, and other support evidence.
- Intake/Workflow: Split workflow `21` Step 19 into two branched variants after Step `93`, so applicants who answered `No` to `dependent-children` are routed to a no-childcare copy of `Financial Supports Requested` and are no longer offered `Childcare` in the public intake.
- Docs/Meta: Added `docs/meta/codex-thread-index.md` as the searchable cross-thread recovery index for durable handoff notes and prior-thread findings, and updated `docs/AGENTS.md` to require future chats to use and maintain it.
- Docs/Ops: Updated the TEST DB access guide and thread index with the 2026-04-01 intake-import finding that large JSON exports through SSM stdout truncate, so future TEST form pulls should export large authoring rows component-by-component with base64 wrapping before reconstructing them locally.
- Docs/Planning: Added `docs/planning/step19-checkbox-conditionality-followup.md` to capture the remaining parity work for Manual Intake, Workflow Preview, and the intake-step editor after the new public-portal-only Step 19 checkbox conditionality landed.

## 2026-03-31
- UX/Casework: Case Workspace now includes an `Events timeline` widget on the default board and a new `View audit trail` quick action that switches to a focused audit layout using the same case-event feed as Application Workspace.
- UX/Landing Page: Reframed the public PATH landing page as a staff access/support entry point, removed development/marketing-oriented content from the default view, and moved release notes behind an optional expandable section so sign-in/help content leads the page.
- Docs/Landing Page: Updated the landing-page feature doc and `docs/AGENTS.md` to reflect that the public landing page is pre-sign-in, not role-aware, and that role-specific home dashboards begin after authentication.
- Fix/API/Casework: Case Workspace secure messaging now works for imported application-less client files when the case is linked to a participant PATH account, resolving the recipient through the client applicant account when no application submission exists.
- UX/Casework: The shared Secure Messaging widget now shows a disabled/unavailable state for cases that do not yet have a linked participant account instead of surfacing a generic load failure.
- Fix/API/Finance: Payment packet list/detail hydration now resolves applicant user IDs for imported application-less cases through the linked client PATH account when no application submission exists, preventing imported-case finance evidence flows from losing applicant document access.

## 2026-03-30
- UX/Integration: Replaced the broken in-page Job Bank iframe with an explicit launch panel because Job Bank now returns `X-Frame-Options: SAMEORIGIN` on the search and summary URLs PATH uses, so browsers refuse to display those pages inside PATH.
- UX/Help: Updated Job Bank Search guidance to explain that PATH still builds the correct Job Bank destination URL, but staff now continue on Job Bank in a separate tab instead of an embedded frame.
- Docs/UX/Casework: Rewrote the coordinator-facing PATH help panels and AI chat context for Home, Manage ISET Applications, Manual Application Intake, Application Workspace, and Case Workspace to align guidance with NWAC staff training expectations around acknowledgement, documentation, assessment, active case management, and closure.
- UX/AI/Casework: Tightened the shared help-panel AI system prompt so coordinator-facing PATH chats answer more like a staff job aid, including direct yes/no answers when appropriate, stronger PATH-specific next-step guidance, and explicit reminders around financial evidence for living allowance and post-intervention follow-up before closure.
- UX/Tutorials/Casework: Reworked the coordinator-facing PATH tutorials and first-run tutorial prompts so they explain how staff should actually use PATH in their daily work, not just where widgets are. The home intro, application workspace, case workspace, and NWAC decision walkthroughs now emphasize choosing priorities, opening the right file, recording follow-up, supporting recommendations with evidence, and not closing cases before required post-intervention follow-up.
- UX/Tutorials/Casework: Versioned the coordinator home intro to `iset-coordinator-intro-v2` and the application/case workspace walkthroughs to `application-workspace-overview-v3` and `case-workspace-overview-v3` so the rewritten PATH quick-start guidance can reprompt for staff who had already completed the older generic tours.
- UX/Tutorials/Home: Added the missing homepage `home-info-link` tutorial hotspot to the header `Info` link so tutorial steps can point directly at the help-panel entry point for contextual guidance and AI support.
- UX/Casework: Added missing widget-level `Info` access for the main ISET Applications table and the Manual Intake form, and fixed the ISET Application Form widget so its help panel now opens with its own AI context instead of the generic fallback.
- Ops/Dev/Home: Restored local-dev `Clear ISET test data` behavior by starting the Admin Backend from `start-dev.ps1` with `ENABLE_UNSAFE_ADMIN_DEBUG_ROUTES=true`, and updated the Demo Controls error copy to explain the missing env flag instead of showing a misleading bare `not_found`.
- UX/API/Home: NWAC Administrators now see new first-position `All Applications` and second-position `All Cases` cards in the homepage Work Queue, exposing the full non-terminal application portfolio and the full open case portfolio from the same widget.
- UX/API/Home: Regional Managers now see a new first-position `Applications in My Region` card in the homepage Work Queue, showing all non-terminal applications in their assigned provinces and territories while leaving `My Applications` as a separate queue.
- UX/API/Home: Regional Managers now also see a new second-position `Clients in My Region` card in the homepage Work Queue, counting open case files in their regional portfolio while keeping `dormant` and `ready_to_close` in scope and excluding only `closed`/`archived`.
- Fix/API/Home: `/api/applications` now supports `excludeTerminal=1` for staff-scoped open-application queues, and terminal-application normalization was tightened to treat withdrawn/denied-style terminal values consistently.
- Docs/Home: Updated the homepage Work Queue reference, help-panel copy, and `docs/AGENTS.md` with the live Regional Manager queue order, regional scoping rules, and the new case-based `Clients in My Region` semantics.

## 2026-03-29
- UX/Notifications: The admin-shell `Notifications` footer control is now visible to all signed-in roles and continues to refresh bell alerts in place instead of acting like a Notification Settings permission-gated link.
- UX/API/Reminders: Reminder due/overdue events and case reminder badges now classify by the PATH business day in `America/Toronto` instead of UTC/browser-local day boundaries, and reminder reschedules/reopens now clear prior due/overdue emit flags so future reminder bells can fire again.
- UX/Notifications: Staff bell alerts now append a date/time in the heading, using the notification delivery/creation timestamp rendered in the viewer browser timezone with an `America/Toronto` fallback.
- Docs/Notifications: Updated the notification dashboard reference, reminder runtime notes, help-panel copy, and `docs/AGENTS.md` with the current bell-alert timestamp rule, the reminder business-day rule, and the fact that PATH does not yet persist per-user timezone preferences.

## 2026-03-25
- UX/API/Configuration: Query Editor now includes a `Server Export` tab with a MySQL Workbench-style object-selection flow for choosing one database, selecting the tables to include, and writing a self-contained SQL dump file directly on the admin server.
- Docs/Configuration: Updated the live Query Editor dashboard reference, help-panel copy, and `docs/AGENTS.md` for the new server export flow, hardwired dump options, and Windows/WSL dump-path behavior.

## 2026-03-26
- UX/API/Auth: `Manage Users` now includes a new `Applicant Accounts` tab for imported participant accounts, with PATH-managed statuses, silent account creation, and manual `Send activation` / `Resend activation` actions.
- API/Auth/Import: Client-file import now silently creates or links applicant Cognito accounts only when the imported row resolves to one clean email value, suppresses all account emails during import, and skips account creation for missing, invalid, or ambiguous email rows.
- UX/Auth/Portal: Added a dedicated public-portal `Activate your account` page that wraps Cognito’s forgot-password mechanics in first-time activation wording, and the portal now marks linked applicant accounts as activated on first successful sign-in.
- UX/API/Casework: Case Workspace `Case header` now shows `PATH Account Status` and includes a quick action to activate or resend activation for the participant account directly from the case.
- Docs/Auth/Data: Added applicant-account activation workflow documentation, updated the user-management feature doc, and expanded `docs/AGENTS.md` with the client-anchored invitation/activation model and import rules.
- Feature: Notification Settings now includes a configurable PATH sender email stored in `iset_runtime_config`, and PATH-generated SES mail from both the admin dashboard and portal reads that shared runtime value.

## 2026-03-24
- UX/API/Reporting: Added inline drilldown on non-zero `Intake and Assessment` and `Interventions` values in `Reporting > Data and Results`, showing the contributing records directly beneath the clicked row with linked applicant/participant names that open the related application or case workspace.
- Docs/Reporting: Updated the live `Data and Results` dashboard reference, help-panel copy, and `docs/AGENTS.md` with the new inline drilldown behavior, fiscal-window rules for monthly vs cumulative clicks, and the current demo-mode limitation.
- UX/API/Reporting: Added a new `Intake and Assessment` section to `Reporting > Data and Results`, placed above `Interventions` in the default board layout, with participant home province/territory rows, month columns, a `Show` selector for new/approved/denied applications, and a local province filter input.
- Docs/Reporting: Added a live `Data and Results` dashboard reference, updated the dashboard help content, expanded `docs/AGENTS.md` with the new section and date-bucketing guardrails, and noted that the board layout storage key moved to `reporting-data-and-results-layout.v2`.
- UX/API/Home: Homepage count metrics now open the matching records in the shared `Work Queue Items` widget, which has a dedicated metric-results mode with neutral columns, a `Back to work queue` action, and automatic restore if the Items widget had been removed from the board.
- Fix/API/Home: Homepage metrics scope now honors all resolved Regional Coordinator `regionIds`, so multi-region summary counts and metric drilldowns reconcile correctly instead of silently falling back to a single region.
- Docs/Home: Added a live homepage Metrics dashboard reference, updated the homepage help-panel copy for metric drilldown behavior, refreshed the planning note, and expanded `docs/AGENTS.md` with homepage metrics/items pointers and guardrails.
- Fix/Auth: Removed the remaining admin dev-bypass and simulated-header auth paths, centralized frontend auth/current-user state in `AuthContext`, isolated `/auth/callback` from the main app shell, removed the stale placeholder `src/auth/AuthProvider.js`, and cleaned auth/config UI so sign-in now runs only through real Cognito/IAM.
- Fix/Auth: Removed the last auth-disabled/mock runtime branches from the server middleware and admin-user routes, stopped `/api/tasks` from falling back to a hardcoded user, removed duplicate admin-route mounting, and updated the docs base to describe Cognito-only admin auth.
- UX/Casework: Submitted intervention proposals and submitted revisions can now still be edited by all casework roles with case access, including cost-line changes, while final decision recording remains limited to approver roles.

## 2026-03-23
- Fix/Auth: Hardened the post-login callback path so AppContent no longer crashes if the real Cognito role is briefly null while the session claims are still resolving; role normalization now treats missing role state as empty instead of dereferencing `null`.
- Auth/UX: Removed the old local IAM toggle and simulated-user mode from the admin dashboard; the client now always uses real Cognito/IAM identity, assignment no longer offers placeholder staff identities, and the auth middleware no longer honors dev-bypass headers.
- UX/Letters: Denial-letter generation now rewrites internal decision notes into applicant-facing prose instead of pasting raw assessor wording like `Person is...` directly into the letter body, improving flow for both coordinator funding denials and intervention denial letters.
- UX/API/Casework: `Add existing action plan` now mirrors standard action-plan closeout requirements for closed plans, including result education, future education for `Returned to school`, and NOC version/code for `Employed`, and persists those values into the real action-plan closeout payload.
- UX/API/Casework: Imported/application-less client files now have explicit Case Workspace backload actions for `Add existing action plan`, `Add existing intervention`, and `Upload existing documents`; the new action-plan/intervention backload paths create real records silently without approval routing, checklist progression, or applicant notifications.
- UX/API/Casework: Supporting Documents now has a case-based mode for imported/application-less client files, using `GET/POST /api/cases/:id/documents*` so staff can upload case documents without an applicant account, including application-type documents that fall back to action-plan or case storage when no real application exists; the checklist tab is intentionally hidden in that mode.
- UX/Casework: Supporting Documents in Case Workspace now uses clearer relevance-filter language (`Show documents relevant to`) and a dismissible case-mode notice so the widget no longer implies that intervention is the primary attachment scope for every document.
- UX/Casework: Supporting Documents now places the application/intervention relevance filter below the tabbed document area instead of above it, reducing top-of-widget clutter in Case Workspace.
- Fix/Casework: Supporting Documents case-based uploads now preserve client resolution for client-scoped documents instead of dropping the case context before `client_id` is derived, and the widget now surfaces `client_id_required` as a specific upload error.
- Fix/Casework: The `ISET Clients` widget now paginates grouped client rows correctly by grouping the full filtered case list first and then paging the grouped client result, so the Cloudscape page control can move beyond page 1.
- UX/Auth: Removed the legacy `Developer bypass mode` top-navigation account item when IAM is off; local auth simulation still works through the existing dev IAM toggle without surfacing that early-dev label in the UI.
- Fix/Auth: Restored the top-navigation account dropdown in local auth simulation mode so the signed-in identity remains visible and `Sign Out` works again even when IAM is off.
- UX/API/Casework: Client Batch Import no longer blocks on malformed or checksum-failing SIN values; dry run now warns, imports the raw digits for later case-management correction, and stores raw SIN values in the imported client/case profile payloads instead of import-side hashing.
- UX/API/Casework: Client Batch Import dry-run now auto-detects the real header row, skips leading guidance rows before the first participant row, and lets staff override the first data row explicitly for spreadsheets with extended headers or setup rows.
- UX/Configuration: Moved `/iset/imports/client-files` into `Configuration` and renamed the navigation entry to `Client Batch Import`.
- UX/API/Casework: Added `Client Batch Import` at `/iset/imports/client-files`, with spreadsheet dry-run preview, duplicate/client matching, and transactional commit into real `client` + application-less `iset_case` records.
- Docs/Casework: Added a live Client Batch Import dashboard reference and updated the client-file import guide/gateway docs to reflect the implemented import workflow and its non-goals.
- UX/Configuration: Query Editor now supports loading a single `.sql` or `.txt` file into the SQL editor before running it through the existing multi-statement query execution flow, with a 900 KB client-side upload limit to stay within the server's 1 MB JSON body limit.
- Docs/Operations: Added a live Query Editor dashboard reference doc and corrected the gateway docs to reflect current behavior, including SQL file upload support and the shared execution path.
- Docs/Architecture: Added a client-file import guide clarifying that the schema supports application-less cases, documenting the new core case support, and calling out the remaining participant-account-dependent caveats around secure messaging and applicant-scoped documents.
- API/Casework: `POST /api/cases`, `PUT /api/cases/:id`, and `GET /api/cases` now support true client-file cases with no linked application, and the Case Workspace secure-messaging widget now suppresses message actions when no participant account is linked.

## 2026-03-22
- UX/API/Reporting: Wired `Regional Snapshot` coordinator salary values to the new `Budgets and Finance > Salaries` data so monthly, quarterly, and annual snapshots now derive salary from the selected region's annual salary entry instead of storing a separate manual amount.
- UX/API/Finance: Added `Budgets and Finance > Salaries`, a new standard board-based dashboard for annual salary tracking by province or territory, with a fiscal-year control, explicit budget-pot assignment, annual salary entry, and derived monthly values for review.
- Data/Finance: Added the `finance_regional_salary_entry` table for annual regional salary totals keyed by `region_code + fiscal_year_start`, and seeded the current dev fiscal year with logical regional salary-pot assignments.

## 2026-03-20
- API/UX/Reporting: Wired Regional Snapshot `C. Funding` and the matching Excel export to live PATH client funding by summing scheduled payment lines for the selected region and period, split by `CRF` and `EI`, and removed manual editing of those two client-funding fields from the snapshot editor.
- Fix/API/Reporting: Refactored Regional Snapshot client activity to use the case-level `portfolio_region_id` as the canonical reporting region, defaulted from applicant/client province, and backfilled current dev cases so regional counts no longer depend on staff assignment or finance records.
- UX/API/Reporting: Corrected the Regional Snapshot funding labels from OCR-derived `ER` / `IF` to `CRF` / `EI`, including the saved snapshot schema, edit form, on-screen report, and Excel export.
- UX/Reporting: Added Excel export to `Reporting > Regional Snapshot`, including `Download Excel` for the current region and `Download all Excel` for the selected period with a summary tab followed by one worksheet per region.
- UX/Reporting: Added `Download CSV` to the Data and Results board-item menus for each data section, exporting the exact filtered/demo/monthly-or-cumulative view currently shown on screen.
- UX/API/Home: Reworked the homepage Metrics widget into a configurable KPI widget with cleaner defaults (`New applications`, `Applications approved`, `Applications denied`, `Active cases`, `Employed`, `Returned to school`) and added metric selection from a longer list of application, outcome, case, intervention, and funding measures.
- API/Home: Redefined homepage `Funds committed` to sum approved intervention value in the selected period, and started stamping intervention review timestamps when proposals are approved or otherwise decisioned so commitment reporting aligns with approvals rather than downstream finance transactions.
- UX/Home: Simplified the NWAC homepage Work Queue by merging application approvals and intervention approvals into a single `Approvals` queue so the summary card and item table reflect one combined approval workload.
- UX/Home: Moved the Metrics widget period range from the header into the widget body below the metric tiles, keeping the exact applied date range visible without crowding the header actions.
- Data/Reporting: Added the `iset_regional_snapshot_report` schema for saved regional Board-style reporting snapshots by region and reporting period, including manual funding/admin fields, compliance flag, comments, and authoring metadata.

## 2026-03-19
- UX/Reporting: Renamed the reporting side-navigation sections to `ILMP Submissions` and `Reporting`, and added a new `Reporting > Data and Results` dashboard scaffold using the standard Cloudscape board pattern. Default access is enabled for System Administrators and NWAC Administrators through the route access matrix.
- UX/Reporting: Refined `Reporting > Data and Results` into a fixed workbook-aligned reporting page with the report sections ordered to match the NWAC spreadsheet and a shared province/territory multi-select filter bar for future slice-and-dice controls.
- UX/Reporting: Added a `Demo mode` toggle to `Reporting > Data and Results` that fills the workbook-aligned sections with in-page development/demo data and applies the existing province/territory filter to those demo values.
- API/Reporting: Connected `Reporting > Data and Results` section `Quarterly Data Uploads` to a live backend endpoint backed by PATH reporting-package records, while preserving workbook-aligned quarter due dates and showing agreement-wide schedule status when no package rows exist yet.
- API/Reporting: Wired the remaining `Reporting > Data and Results` workbook sections to live cumulative PATH reporting aggregates, including year-end results, intervention completions, client results, data-upload outcomes, and action-plan status snapshots, with optional AOP targets loaded from reporting runtime config when available.
- UX/API/Reporting: Added an admin-editable `Edit targets` flow on `Reporting > Data and Results` so the three AOP target values can be maintained directly from the dashboard and stored in runtime config.
- UX/API/Reporting: Added an admin-editable `Edit comments` flow on `Reporting > Data and Results` so `Additional Comments` is now a saved fiscal-year narrative note stored in runtime config and shown read-only in the report.
- UX/Reporting: Clarified the shared geography filter label in `Reporting > Data and Results` to explicitly mean participant home province/territory, matching the live backend filter behavior.
- UX/Reporting: Simplified the `Reporting > Data and Results` control bar by removing redundant `Current geography` and `Data source` summary tiles; the active geography is already visible in the filter control and demo/live state is already conveyed elsewhere on the page.
- UX/API/Reporting: Expanded `Reporting > Data and Results` controls to a 3-column layout with top-row context (`ISP Name`, `Portfolio`, `Demo mode`) and second-row filters (`Participant home province / territory`, `Case manager`, `Fiscal year`), and wired the live report aggregates to respect the new case-manager filter.
- UX/API/Reporting: Scoped `Reporting > Data and Results` AOP targets by fiscal year so the new fiscal-year control drives workbook sections, saved comments, quarterly uploads, and target editing consistently.
- Fix/Reporting: Repaired the `Reporting > Data and Results` live-report fetch after the new case-manager filter wiring introduced an ambiguous participant-submission query, and stopped target-save success states from being visually wiped by a follow-up live-report reload failure.
- UX/Reporting: Rewrote the `Reporting > Data and Results` page copy, status text, section descriptions, and help content so they read as end-user reporting guidance rather than development-oriented implementation notes.
- Fix/Reporting: Corrected the `Reporting > Data and Results` route wiring so the internal help-panel AI context is no longer rendered in the page header description.
- UX/Reporting: Made the top `Sample Data View` banner dismissible so demo mode does not keep an extra persistent alert on screen unless the user wants it.
- UX/Reporting: Enabled striped rows on the embedded Cloudscape tables in `Reporting > Data and Results` and emphasized the `TOTAL` row in the Interventions matrix for easier scanning.
- UX/Reporting: Narrowed row striping in `Reporting > Data and Results` so only the Interventions matrix is striped; the Overall Results and Quarterly Data Uploads tables now render unstriped while the Interventions `TOTAL` row remains emphasized.
- UX/Reporting: Moved `Demo mode` into the Report Controls header actions and replaced the in-content control slot with a `Results view` segmented control that switches the matrix sections between cumulative and monthly values while keeping `Final (p14)` as the year-end total.
- UX/Reporting: Replaced passive reporting guidance on `Reporting > Data and Results` with contextual popovers for demo mode, filter behavior, and quarterly upload behavior, while keeping alerts for actual empty/error states and save confirmations.
- Fix/Reporting: Stopped demo-mode AOP target figures in `Reporting > Data and Results` from changing with case-manager or province/territory filter selections; demo targets now stay agreement-level while demo results continue to respond to the selected filters.
- Fix/Reporting: Reworked the `Reporting > Data and Results` demo dataset so the sample sections reconcile with each other, including matching `Clients Served` totals across Overall Results, Client Results, and the Interventions `TOTAL` row.
- UX/API/Reporting: Added Interventions-only header controls on `Reporting > Data and Results` so that table can be viewed by `Completed`, `Planned`, `Active`, or `Cancelled` interventions and grouped by `By start date` or `By end date`, with the default workbook-aligned view set to completed interventions by end date.
- UX/API/Reporting: Expanded the `Reporting > Data and Results` Interventions section with a `Show` selector for `Count` or `Cost`; `Cost` uses payment-month allocation, and completed interventions use actual cost when available before falling back to planned cost.
- UX/Reporting: Converted `Reporting > Data and Results` workbook sections into removable Cloudscape board items with standard `Add section` / `Reset layout` controls, and moved `Interventions` to the top of the default layout under the report filters.
- Fix/Reporting: Corrected a Cloudscape board runaway-loop regression on `Reporting > Data and Results` by aligning the new board palette synchronization with the repo’s known-good dashboard pattern, preventing initial palette sync from repeatedly triggering upstream re-renders.
- UX/Release Notes: Published landing-page release notes as `v0.5.6` dated `19th March 2026`, and promoted the new reporting dashboard from `Coming Soon` to the top of `What’s New` in both English and French.
- UX/API/Reporting: Renamed the reporting section to `ILMP Data Uploads` and reduced it to the supported `Submitted` row only, removing implied gateway outcome rows that PATH cannot currently source.

## 2026-03-18
- Fix/Agreements: Participant-facing redline CFAs now render the intervention update badge correctly and apply explicit strike/add styling to redlined details cells, so removed funding lines no longer appear as plain rows and badge HTML no longer leaks into the document.
- UX/Layout: Starting or resuming intervention proposal/revision work from Case Workspace now switches the dashboard into an intervention-focused layout with `Case header`, `Action plans`, `Interventions`, and the intervention workflow widget visible together.
- Messaging/Workflow: Approval-letter funding packages no longer auto-attach `Client Acknowledgement of Funding Source`, because that form is now collected during the application process.
- Fix/Messaging: Case Workspace proposed-intervention approval letters now attach the funding package (`Client Funding Agreement`, `EFT/Wire form`) when the approved intervention being sent includes funded cost lines, even if the original assessment on the case had no funding.
- UX/Workflow: Case Workspace proposed-intervention approval now exposes inline parent Action Plan funding settings (`Funding stream`, `Budget pot`, `Paid from`) in the decision step when approval needs them, instead of forcing staff to leave the wizard to repair the plan first.
- UX/Lettering: Application Workspace approval letters now switch to intervention-focused wording when the approved assessment has no funded cost lines, emphasizing the approved intervention(s) and dates instead of funding-disbursement language.
- Messaging/Workflow: Sending an approval letter from Application Workspace now skips the auto-attached funding package (`Client Funding Agreement`, `EFT/Wire form`) when the approved assessment contains no funded cost lines.
- UX/Agreements: Revised Client Funding Agreements now generate and send as redline revisions against the prior signed CFA, with amended funding rows and totals shown inline using strikeout/added markup instead of only storing a separate coarse diff PDF.
- UX/Agreements: Client Funding Agreement generation now reflects already-paid intervention cost lines in the detail text, for example switching one-time items from `payable on ...` to `paid on ...` and showing partial-payment wording for recurring lines with historical paid amounts.
- Refactor: Intervention status handling now uses a canonical set only: `draft`, `submitted`, `in_review`, `changes_requested`, `approved`, `rejected`, `in_progress`, `suspended`, `completed`, `cancelled`.
- UX: Pre-start approved interventions now display as `Approved` across Case Workspace and finance flows instead of using the legacy `planned` state.
- API: Auto-created and newly approved interventions now persist `approved` as the pre-start status, and activation flows transition only from `approved` to `in_progress`.
- Ops: Added `sql/20260318_0001_cleanup_intervention_statuses.sql` to normalize old intervention statuses and change the table default to `draft`.
- Fix/Payments: Auto-generated payment packets now create their line items transactionally, and payee-type storage was widened to fit the configured detailed payee codes used by approved cost lines.
- Fix/Payments: Aligned the `payment_packet.status` DB enum with the live scheduling workflow so `awaiting_trigger` and `released` are now valid persisted packet statuses, matching the existing server/UI behavior.

## 2026-03-17
- UX/Lettering: Approval-letter packs in Application Assessment and Case Workspace proposed interventions now generate dedicated `Loan Provider` letters for funded `Student Loan Repayment` lines, grouped by provider/account and available as a separate preview/download tab.
- UX/Assessment: Cost-line modals now relabel the payee fields for `Student Loan Repayment` to `Loan provider / servicer name` and `Loan account number`, making the approval-letter data entry explicit at assessment time.
- UX/Data: Payee-type selectors in Application Assessment, Case Workspace, and Finance payment modals now load from a runtime-config payee-type catalog instead of a hardcoded frontend list; seeded the catalog in runtime config and added `Student Loan Provider / Servicer` for `Student Loan Repayment` lines.
- Fix/Workflow: Eligibility-denial reporting seeding now keys off a persisted structured denial reason code from the denial-letter workflow instead of the free-text assessment note, so `eligibility_not_met` denials reliably create the reporting-only downstream records.
- UX/Wording: Application-status labels now display `Not Approved` instead of `Rejected` across application-facing admin UI surfaces while keeping the underlying system status code as `rejected`.
- Workflow/Reporting: Eligibility denials (`eligibility_not_met`) now auto-seed reporting-only downstream records: ensure client, create a closed action plan, create one completed `Career Research and Exploration` intervention, and initialize ESDC participant validation without sending the record into normal casework queues.
- Workflow/Reporting: Denied-ineligible records now stay editable in Application Workspace for ILMP corrections after rejection, with automatic downstream resync/revalidation and clear Application Overview status messaging for blocked vs ready ESDC reporting state.
- Reporting/Batching: ESDC batch prepare/submit now include only `ready` participants and automatically exclude `blocked` / `needs_review` records instead of failing the whole batch; the batch widget now shows ready/review/blocked counts and excluded-record details.

## 2026-03-16
- Workflow/Content: Added `scripts/update-workflow21-trauma-copy.js` and revised workflow `21` intake step-library copy in the dev database for trauma-informed, bilingual applicant-facing language, including summary-page label snapshots and document-upload/legal declaration text cleanup.
- Workflow/Content: Simplified workflow `21` step `76` consent copy further into plain-language informed permission, removing statute references from that step while leaving the later legal-submission consent block unchanged.
- Docs/Planning: Added `docs/planning/intacct-mock-dashboard-design.md` as the durable handoff and design baseline for the separate mock Sage Intacct dashboard, PATH bill-splitting correction, phased MVP plan, and future reconciliation sync work.

## 2026-03-13
- UX/Integration: Refactored the Job Bank Search dashboard into two tabs: `Find a Job` retains the original posting-search flow, while `Explore a Profession` adds a PATH 2021-NOC autosuggest plus location input that resolves to the matching Job Bank profession summary page in the lower embedded frame.
- API/Integration: Added a Job Bank profession-summary resolver endpoint that translates PATH profession/location inputs into Job Bank's own occupation and location identifiers before building the final summary-page URL.
- UX/Help: Updated the Job Bank Search help panel to explain the new tabbed flow, the 2021-NOC profession picker, and the Job Bank summary-page resolver behavior.

## 2026-03-12
- Messaging/Workflow: Approval letters now carry required funding-signature forms as attachments in the same secure message (`Client Funding Agreement`, `Client Acknowledgement of Funding Source`, `EFT/Wire form`) instead of sending a separate follow-up message.
- Messaging/Workflow: Sending an approval letter now triggers docs-requested/reminder automation through the same secure-message path as manual form requests because non-letter signing attachments are included with the letter send.
- UX/Lettering: Approval draft generation now uses a single privacy-safe AI copy-edit pass with placeholder tokens (no applicant personal data sent), then deterministically injects case values locally and preserves fixed funding/forms paragraphs.
- Fix/Checklist: Approval-letter auto-attachments now stamp EFT signing requests with canonical `EFT_form`, so signed EFT submissions correctly clear the funding forms checklist item.
- UX/Lettering: Approved communication now behaves as an admin letter pack: the client approval letter remains editable, while institution and other funding source letters appear as separate read-only tabs with download actions for admin-side use.

## 2026-03-11
- UX/Assessment: Cost-item choices in `What will it cost?` now follow the wizard `Childcare Need` answer (yes/no) so childcare cost items are presented or hidden accordingly.
- Fix/Assessment: Application Assessment default-intervention auto-seeding now waits for the payment-intervention mapping fetch to complete before running, preventing false “seeded” state on first render.
- Security/Messaging: Hardened secure-message isolation so applicants only see messages where they are sender/recipient (`/api/messages`), with defensive cleanup of stale invalid mailbox rows; case-thread fetch (`/api/cases/:id/messages`) now blocks applicant access to other applicants' cases.
- UX/Help: Finance Settings `Payment type mapping` now shows a widget `Info` link with dedicated help-panel guidance, and the intervention default column label was shortened to `Auto-add?`.
- Workflow/UX: Renamed approval follow-up stage from `Complete funding documentation` to `Funding forms and signatures`; final action now reads `Mark application complete`, and step guidance now explicitly requires all required checklist items to be `Complete` before completion.
- Messaging/Workflow: Sending secure messages with signing-form attachments now sets docs-requested state/reminders server-side (`docs_requested_active=1`, source `secure_message`, reminder creation, and event capture) even for system-triggered sends.
- Messaging/Workflow: Applicant signing completion now clears docs-requested state/reminders when all pending non-letter signing requests for the case are complete.
- Messaging/Workflow: Approved communications now include attachments for `Client Funding Agreement`, `Client Acknowledgement of Funding Source`, and `EFT & Wire Transfer Direct Debit` in the approval-letter send flow (no separate funding-forms message).
- API/Checklist: Gate 6 funding-document requirements now enforce `assessment total cost > 0` for funding package artifacts (`funding_agreement`, `client_acknowledgement`, `EFT_form`/wire transfer form, and `voided_cheque`), so zero-cost approved assessments no longer block on those documents.
- Intake/Docs: Intake submission now auto-generates and stores `iset_client_info_release` (Authorization for Release of ISET Client Information) as a signed PDF, and the Application Form widget now includes a dedicated link/modal + PDF download action for that signed form.

## 2026-03-10
- UX/Data: Refactored `Other funding` in both Application Assessment and Case Workspace Proposed Interventions wizards to a structured flow (`involved?`, repeatable non-NWAC funders, NWAC coverage, notes) while retaining backward-compatible summary text persistence for existing records.
- Help: Updated Application Assessment and Case Workspace Proposed Interventions help-panel guidance/AI context to match the new structured Other funding step behavior.
- Refactor/UX: Intervention proposal cost-line modals in both Application Workspace and Case Workspace now support early payee capture (`payee type`, `payee name`, optional `reference`) without adding a new costing-table column.
- API/Data: `assessment_proposed_interventions` cost-line normalization/serialization now supports optional payee payloads so early payee values persist with proposed interventions.
- Payments/API: Auto-generated payment packet lines now prefer payee values from proposal cost lines (with existing fallback derivation retained) and now forward `payee_reference` when present.
- Validation: Payment packet validation now blocks submission with explicit `payee_missing` policy errors when line payee details are incomplete.
- UX: Payment packet detail table now shows line-level `Payee missing` indicators after validation, complementing top-level validation-block messaging.
- Docs: Added planning tracker `docs/planning/vendor-payee-early-capture-refactor.md` and updated related help-panel guidance for Application Assessment, Case Workspace Proposed Interventions, and Finance Payment Detail.
- Docs: Expanded `docs/AGENTS.md` interview directives (single-question interview flow, avoid preference-boundary probing, Codex-owned code/data decisions, and minimal-question policy).

## 2026-03-09
- Ops/Storage: `POST /api/clear-iset-test-data` now also purges object-store files linked to records being cleared (collects object keys before DB delete, then deletes keys after commit), returning `objectPurge` and `objectKeySources` in the response for audit visibility.
- Safety/Ops: Clear-test object purge now blocks deletion when `OBJECT_BUCKET` appears production-like (`prod`) to prevent accidental production bucket removal.

## 2026-03-06
- Feature: Added a new `Application Intake` dashboard route (`/iset/applications/intake`) under New ISET Applications for manual staff-entered intake.
- UX: Added `Manual Application Intake` page scaffold with frontend-held working state, session autosave, field-level validation, and `Create Application` gating until required fields are valid.
- API: Added `POST /api/applications/manual-intake` to create manual-origin records transactionally (`user` -> `iset_application_submission` -> `iset_application` -> `client` -> `iset_case`).
- Events: Manual intake create now emits `application_submitted` via shared case event service with manual-origin metadata (`origin_channel=admin_manual`, actor/timestamp details) while preserving baseline submission payload keys.
- UX: Successful manual create now redirects directly to the new application workspace (`/application-case/:id`) with a success flash banner.
- Access: Enabled `Application Intake` route access for System Administrator, Program Administrator, Regional Coordinator, and Application Assessor in role matrix/navigation.

## 2026-03-05
- Docs: Rewrote `docs/AGENTS.md` as a thread-handoff/quick-onboarding guide focused on durable codebase/docbase/database context, critical conventions, known pitfalls, and location-first references.
- Docs: Clarified `docs/AGENTS.md` to explicitly frame assistant/user collaboration as a design dialog (challenge assumptions, discuss tradeoffs before high-risk implementation, avoid literal blind execution).
- UX: Home work-queue conflict rows now show inline actions `Open workspace` and `Reassign` (falling back to `Assign` if no current owner), while hiding the inline `Resolve` action; underlying resolve code path remains in place for potential reinstatement.
- Docs/UX: Updated homepage help-panel content (`Home dashboard`, `Work Queue`, `Work Queue Items`, `Metrics`, `My Tagged Applications`, `Recent Activity`, and `Development Tracker`) to align guidance with current role-based queues, inline actions, tagging behavior, and widget controls.
- Docs: Added `docs/meta/next-release-notes-log.md` as the standing running log for next-release "What's New" drafting (version-tagged entries), and updated `docs/AGENTS.md` to require maintaining it in future threads.
- UX: Assessment wizard Step 1 is now titled `Assess Eligibility`, with simplified role guidance and clearer copy for EI status ownership/eligibility lock messaging.
- UX: Assessment wizard `Cancel` now exits edit mode via confirmation instead of appearing to discard changes with no navigation effect.
- UX: EI verification UX in assessment Step 1 now surfaces current documents inline, flags the latest document as current, and updates immediately when a new report is selected.
- UX/Validation: ISET Application Form SIN handling now combines strict edit-mode validation (9-digit + checksum), numeric/length input constraints, and grouped read-only display formatting (`XXX XXX XXX`).
- UX/Data: Version history modal refreshed for readability (smaller footprint, tighter columns, inline-link actions), `Saved by` now resolves from `staff_profiles.display_name` (fallback `email`), and `View` now shows a human-readable field-diff view (`View changes`) instead of raw JSON.
- UX: Successful version restore now closes the Version History modal automatically.
- Fix/Storage: Secure Messaging Message Details attachments now resolve to presigned S3 download URLs instead of local `/uploads` links.
- Ops/Storage: Removed legacy static `/uploads` delivery and remaining local-direct document download fallbacks; admin document/evidence download flows now operate as S3-only.
- Workflow: Secure messaging (applicant portal + case widgets) now uses per-user mailbox items for folder state, so delete/purge operations only affect the current user’s folders and no longer remove messages for other participants.
- Fix: Applicant portal replies now inherit case/application linkage from the replied message, restoring visibility of applicant replies in case-scoped secure messaging widgets.
- Fix: Assessment cost-line recurrence policy now resolves payment-type aliases/legacy labels (including wage subsidy variants), so `optional` recurrence settings correctly enable installments in coordinator and case-workspace editors.

## 2026-03-03
- UX: Removed the `Supporting documents` section from the ISET Application Form widget; document review/management remains in the dedicated Supporting Documents widget.
- UX: Moved the Application Assessment `Deny Funding` shortcut from Step 2 (`What is being proposed?`) to Step 1 (`EI Eligibility Check`) as a step-header action.
- UX: Application Assessment Step 2 (`What is being proposed?`) now promotes `Add intervention` as the only primary action until at least one intervention exists; once present, wizard `Next` returns as the primary action.
- API: Conflict-of-interest declaration updates in `PUT /api/cases/:id` now consistently resolve the active `staff_profile_id` from request context for both write and response readback, preventing stale conflict state when auth identity fields differ.

## 2026-03-02
- Payments: Auto-generated intervention payment packets are now schedule-driven and grouped by intervention + scheduled date; recurring occurrences create separate dated packets, manual-trigger groups are created as `awaiting_trigger`, packet queue ordering now prioritizes scheduled dates (`due_by`), and queue rows now show due/overdue/upcoming schedule indicators.
- UX: Batch payments queue now defaults to visible columns ordered as Packet, Client, Schedule, Status, Amount, and Blocking (others hidden by default), with sorting enabled on key operational columns (including Packet, Client, Intervention, Schedule, Status, Amount, Reporting unit, Submitted, and Age).
- Docs: Added `docs/planning/thread-handoff-2026-03-02.md` as a self-contained conversation handoff capturing locked payment scheduling decisions, related intervention-widget decisions, deferred scope, and execution order for continuation in a fresh thread.
- Docs: Added and expanded `docs/planning/payment-packet-scheduling-design.md` to capture full payment packet scheduling decisions, including canonical packet status model and transition rules for implementation handoff.
- Fix: Case workspace `Propose new intervention` wizard now deletes interventions reliably from the framing-step table even when hydrated draft IDs differ in type (string vs number).
- UX/Validation: Intervention NOC requirements now apply to codes `6–13` (and existing employer-type `17`) in both case workspace and coordinator assessment intervention flows, with matching NOC field visibility and required-field enforcement.
- API/Validation: Intervention create/update no longer treats proposal-stage end dates as closure; outcome-required closeout validation now applies only when status is `completed` or `cancelled`.
- UX/API: Finance Settings Payment Type Mapping now supports per-payment-type submission timing (`intervention start`, `intervention end`, `recurrence schedule`, `manual trigger`) with recommended defaults persisted in runtime config.
- UX/API: Finance Settings `Payment type mapping` widget now includes a required-evidence multiselect per payment type and saves those rules via runtime config (`finance:payment.evidence.rules`) through `/api/config/runtime/payment-type-mapping`.
- API: Payment type mapping runtime payload now returns `paymentEvidence`, `paymentEvidenceUpdatedAt`, and `evidenceTypes` so the widget can manage line-level evidence rules without hardcoded UI lists.
- UX/API: Payment recurrence is now configured per payment type (required/optional/not allowed) in Finance Settings and persisted in assessment costing runtime config (`assessment:coordinator.costing.line_item_defaults`).
- API: Payment validation, recurring-line creation, and auto-generated payment lines now apply recurrence policy from runtime config instead of hardcoded payment-type rules.

## 2026-02-27
- UX: Added a second Demo Controls action, `Create Case + Payments Data`, with a modal for client count, interventions per client, intervention-type selection, and optional prompt guidance.
- API: Added `POST /api/ai/create-dummy-case-payments` (with optional `?stream=1`) to generate coherent client/case/assessment/action-plan/intervention records plus draft payment packets and lines for finance workflow testing.

## 2026-02-26
- UX: Renamed Finance Payments user-facing labels to Batch Payments (route title/breadcrumbs, queue/detail/comms widget titles, and help panel wording) to reflect the batch-submission workflow.
- UX: Removed draft delete actions from the Finance Payments submission queue to keep the dashboard focused on batch submission.
- UX: Removed Export ledger from the Finance Payments submission queue header so the widget stays focused on due-for-submission packet actions.
- UX: Finance Payments queue now scopes to packets due for submission only (draft stage), excluding already submitted/cancelled packets from this widget.
- UX: Simplified Finance Payments queue header controls by replacing the Ready filter button with a true toggle and keeping selection/submission actions focused on due-for-submission work.
- API: Hardened AI dummy-draft generation to enforce published intake schema conformance before save (drop unknown keys and coerce values to expected scalar/multi/signature/file shapes).
- API: Added defensive coercion for identity spillover objects (e.g., `{ first_nations_band, registration_number }`) so text fields never persist raw objects that can crash workspace rendering.

## 2026-02-23
- ILMP: Added backend validation that first/last names cannot be numeric-only, matching ESDC ILMP guide rules.
- ILMP: Tightened intervention outcome enforcement so outcome is required when an action plan result date is present (queue validation + action-plan close endpoint).
- ILMP: Added strict NOC validity checks (2016/2021) against `noc_code` during action-plan/intervention save and close flows, with queue validation parity and aligned NOC-version allow-lists.
- ILMP: Locked action plan identity fields after ESDC submission by blocking post-submission changes to action plan start date and agreement number.
- ILMP: Fixed grouped batch XML duplication by deduplicating action plans when a client has multiple participant-submission rows.
- ESDC Dashboard: Participant queue now requests grouped-by-client rows so each client appears once with child rows for related submissions/plans.

## 2026-02-20
- Messaging: Internal `/messages` now supports explicit `Forward` compose mode with `Fwd:` subject handling and forwarded body prefill.
- Messaging: Reply delivery semantics now honor explicitly selected recipients instead of auto-notifying all historical thread participants.
- Messaging: Message list recipient/participant metadata is now derived per-message (mailbox-item based), enabling correct `Reply all` defaults.
- Messaging: Compose now allows empty subjects for new messages (display fallback remains `(No subject)`).
- Messaging: Added `Mark unread` action and backend endpoint (`PATCH /api/me/staff-messages/:itemId/unread`).
- Messaging: Read/delete/restore/permanent-delete flows now emit refresh events so side-nav unread counts stay synchronized.
- API: Removed staff-messaging missing-table compatibility fallbacks in dev to keep internal messaging code path clean.
- API: Existing-thread sends now require sender membership in the thread (`thread_access_denied` when not a participant).

## 2026-02-16
- API: `/api/admin/upload-config` now uses admin-local runtime config storage (`iset_runtime_config`) by default, removing the implicit intake proxy dependency; legacy proxy behavior is opt-in via `UPLOAD_CONFIG_PROXY=true`.

## 2026-02-18
- UX: Summary List "Summary Source" field picker in Modify Intake Step now includes `character-count` and `signature-ack` inputs (plus legacy input aliases), fixing missing data keys like `long-term-goal`.

## 2026-02-15
- UX: Notifications in the Case and Application workspaces now show only notifications tied to the current case/application (matching notification metadata), instead of the user's full notification list.

## 2026-02-13
- UX: Moved the assessment "Deny Funding" shortcut into the Proposed Interventions table header actions (next to Add intervention) and renamed the section to "Propose Intervention(s)".

## 2026-02-11
- Feature: Added a new `Case workspace overview` hands-on tutorial in the centralized platform, with first-run prompt on `/cases/:id`, help-panel start/restart controls, and role-consistent tutorial lifecycle handling.
- Fix: Case workspace tutorial startup now resets case workspace layout before launch so required hotspot widgets are present and step progression remains stable.
- Fix: Application workspace and NWAC tutorials now force-reset the application dashboard layout before starting, preventing `Next` dead-ends when required hotspot widgets were removed from a customized board layout.
- UX: Rewrote all `Application workspace overview` tutorial steps to reflect real widget behavior and first-run workflow guidance (orientation, quick layouts/actions, assessment progression, documents/messaging interplay, notes/tasks, calendar, and audit trail).
- Fix: Application workspace tutorial first-run prompt now recognizes role aliases (`ISET Coordinator`, `Program Admin`, `Regional Manager`, etc.) and no longer relies on only `Application Assessor`.
- UX: On application-case pages, NWAC tutorial prompt is now evaluated first for NWAC reviewers on `pending_approval`; otherwise the workspace overview prompt can still appear.
- UX: ISET Application Assessment help panel now includes a direct tutorial start/restart card (aligned with the homepage help-panel tutorial pattern).
- Docs: Updated `docs/AGENTS.md` with a standing rule to keep dashboard/widget help panel content in sync with refactors in the same change.
- UX: Refreshed homepage Work Queue help content (`Work Queue`, `Work Queue (ISET Coordinator)`, `Work Queue Items`, and tagged-items guidance) to match current widget behavior, bucket preferences, and terminology.
- Refactor: Replaced ad-hoc tutorial definitions with a centralized tutorial platform (`src/tutorials/tutorialPlatform.js`) and converted legacy tutorial files into thin category wrappers.
- UX: Home intro hotspots now anchor to stable homepage controls (`home-overview`, `home-layout-controls`, `home-info-link`) to reduce blocked `Next` transitions.
- UX: Added tutorial help-panel actions (`Restart tour`, `End`) for homepage/workspace/NWAC tutorial contexts.
- Fix: Resetting tutorial progress from Tutorials dashboard now clears in-memory prompt guards so auto-prompts can reappear in the same session.
- UX: Homepage tutorial/help copy now uses `Tag/Tagged` terminology for personal follow-up items (separate from Watchlist Hits).
- Docs: Added `docs/features/tutorial-platform.md` as the canonical tutorial architecture/runbook and updated `docs/data/tutorial-progress.md`.

## 2026-02-10
- Feature: Added an ISET Coordinator "Take a tour" intro hands-on tutorial with a one-time sign-in prompt (Start tour / Not now).
- Data: Tutorial completion/dismissal is now persisted per staff in MySQL (`staff_tutorial_progress`) instead of browser-only localStorage.
- API: Added tutorial progress endpoints (`/api/me/tutorial-progress`) plus a localStorage-to-DB migration helper (`/api/me/tutorial-progress/bulk-complete`).
- Ops: Admin deploy scripts now stage `sql/` so the server migration runner can apply new migrations.

## 2026-02-05
- Docs: Added database documentation index and overview, including demo-data guidance and schema dump pointers.
- UX: Application assessment now includes a Deny Funding shortcut on the framing step that routes to the Review step before submitting to Pending Approval and jumping to the decision step; denial letters now mark the application as rejected after sending.
- UX: Assessment wizard action buttons now hide once the application is finalized to avoid inert controls.
- UX: Decision letter editor now locks after sending to prevent duplicate letters.
- UX: Funding documentation step now preserves the primary action button label even when the checklist is incomplete.
- UX: Added guidance above the funding documentation checklist about uploading files or sending forms via Secure Messaging.
- Feature: Case workspace intervention approvals now auto-create draft payment packets from the proposed cost lines (no assessment fallback).
- UX: Planned interventions are now eligible for manual payment packet creation.
- API: Payment packet creation now blocks duplicates for interventions that already have a non-cancelled packet.
- API: Payment initiation no longer blocks interventions in planned status.
- API: Case workspace intervention approvals now require/derive an action plan budget pot so finance transactions can be created.
- API: Intervention-level finance transactions now record one entry per cost line instead of a single total.
- API: Payment packet submission now creates line-level finance transactions and posts them on confirmation.

## 2026-02-03
- UI: Authentication widget now exposes separate applicant inactivity timing fields (warning trigger + countdown duration).
- UX: Reconciliation dashboard copy, hints, and help panel guidance now clarify the exception workflow and data source.
- Feature: Reconciliation dashboard now loads live finance transactions and persists request/resolve actions in transaction metadata.

## 2026-02-04
- UX: Reconciliation dashboard now focuses on Sage Intacct REST submission outcomes with a packet-level submission queue and detail view.
- API: Added Intacct submission listing endpoint for packet-level REST attempt history.
- Data: Intacct REST submission attempts now record outcome + reason metadata for queue filtering.
- UX: Payment packet queue now surfaces Sage Intacct submission outcomes as intelligent status labels.
- UX: Payment packet detail now allows reopening failed/partial Sage submissions for resubmission, while blocking duplicates for accepted packets.
- UX: Finance Overview now defaults to only the Spend trend widget; other tiles start in the palette.

## 2026-02-02
- UX: Added Query Editor configuration dashboard scaffold with System Administrator-only access.
- Feature: Query Editor now includes SQL input, results widgets, and admin-only query execution endpoint (100-row cap).
- UX: Query Editor input now uses the Code Editor component for SQL entry.
- UX: Query Editor results now render in Code View with copy support.
- UX: Query Editor results now use tabs for Table, JSON, and CSV views.
- UX: Query Editor results now default to CSV and use Code View for CSV output.
- Feature: Query Editor now supports multiple SQL statements per run with per-statement selection.

## 2026-02-01
- Fix: Published workflow schema metadata now includes workflow type for runtime consumers.
- Fix: Workflow publish now blocks non-main-intake types and the editor shows type labels instead of raw values.

## 2026-01-27
- Fix: Admin user MFA status now reflects Cognito software token MFA via AdminGetUser enrichment.
- Ops: Deployment scripts now build POSIX-path zip archives to avoid Linux unzip failures.
- UX: Proposed Interventions widget now includes an info link with dedicated help panel guidance and AI context.
- UX: Proposed Interventions status badge now sits in the header actions next to Save Progress.
- Docs: Rewrote Proposed Interventions help content to align with PATH case management guidance.
- Fix: Proposed Interventions wizard now clears draft data after approvals or rejections so the next proposal starts clean.
- Fix: Rejected interventions can now be deleted from the case workspace.
- Feature: Regional Managers now support multi-region scoping in dev via `staff_region`, backend scoping updates, and admin user management changes.
- Ops: Added `scripts/run-prod-sql.ps1` helper for running ad-hoc SQL against prod via SSM.

## 2026-01-28
- Ops: Test deploy now falls back to tar/Compress-Archive if ZipArchive types are unavailable.
- Fix: Initial CFA drafts now generate from assessment data when no action plan exists, keeping secure message CFA attachments working before completion.
- Fix: CFA draft generation now uses application submission ownership fields to match the current schema.
- Fix: CFA draft generation now selects the intervention funding stream from the current schema to avoid SQL errors and allow plan-based CFA drafts to generate.

## 2026-04-10
- Fix: Staff/admin user management no longer writes legacy Cognito `custom:region_id` / `custom:user_id` attributes for staff-region changes and now persists region access through `staff_profiles` / `staff_region`.
- Fix: Staff auth enrichment now preserves DB-backed region assignments on sign-in and resolves effective `userId`, `regionId`, and `regionIds` from the staff profile model instead of depending on legacy token claims.

## 2026-01-29
- Fix: Application assessment no longer blocks submissions with a "Reason for not approving" error when the recommendation is not "Do not recommend funding".
- Fix: Case workspace cost item installment counts now handle dates entered with slashes.
- Fix: Case workspace cost item modal now auto-calculates installments when start/end dates are already set.
- UX: Payment line validation errors now return clearer, actionable messages instead of generic codes.
- Fix: Case scoping now uses `portfolio_region_id` so region-filtered application queries stop failing in dev.
- Fix: Funding authorization now recognizes payment-type labels in funding breakdowns when deriving category caps.
- UX: Payment packet detail now adds a Validate action and only shows Submit once validation passes; edits reset validation.

## 2026-01-30
- UX: Payment packet detail now includes an Intacct XML (Draft) preview tab with copy/download actions and missing-field flags for demo use.
- UX: Finance Settings now includes a Sage Intacct integration widget to capture XML Web Services credentials and defaults.

## 2026-01-26
- Assessment: Coordinator assessment now supports multiple proposed interventions with per-intervention cost tables, inline amount edits, and line-item modals.
- Assessment: Proposed interventions step now uses an embedded table with modal-based editing for intervention details and delete-only row actions.
- Assessment: Costing tables are now embedded Cloudscape tables with visible inline delete actions for cost lines.
- Assessment: Removed the duplicate top-level total from the costing step; totals remain in each table footer.
- Assessment: Proposed interventions and costing tables now allow column resizing.
- Assessment: Installments column now displays text ("in X installments") instead of icons.
- Assessment: Removed per-intervention header totals so only the table footer total is shown.
- Assessment: Restored the overall total at the top of the costing step.
- Assessment: Cost line modal now recalculates installments/amounts when dates or installment counts change, using intervention dates as defaults.
- Data: Assessment submissions now persist proposed interventions + cost lines in `assessment_proposed_interventions` with runtime-config defaults for suggested items.
- API: Added runtime config endpoints for coordinator assessment costing defaults.
- Data: Removed legacy intervention type references from schema/mapping sources.
- Ops: Added production Terraform environment scaffolding under `infra/terraform/environments/prod` to keep test and prod isolated.
- Ops: Parameterized Terraform module log group and IAM path prefixes so prod can be applied without test hard-coding.
- Docs: Added production Terraform runbook for baseline apply steps.
- Ops: Added Terraform-managed artifacts bucket and prod-safe bootstrap configuration.
- Ops: Added ASG capacity controls to the compute module and staged prod to start at zero capacity until env parameters are ready.
- Ops: Enabled S3 backend configuration blocks in Terraform env roots for remote state usage.
- Ops: Added explicit S3 backend state keys for test/prod to avoid interactive init prompts.
- Ops: Aligned backend lock table names with bootstrap naming convention.
- Ops: Resolved Terraform plan-time unknown count issues in compute listener rules and artifacts bucket encryption.

## 2026-01-17
- Feature: Funding agreements now generate versioned CFA PDFs (CFA vN) per plan when approved interventions change.
- API: Added CFA version list/create endpoints and automated sent/signed status updates through secure messaging.

## 2026-01-16
- UX: Finance Settings now uses a configurable dashboard layout with widget palette controls.
- UX: Added a Payment type mapping widget to manage intervention payment type rules.
- API: Added runtime config endpoints for payment intervention type mapping.
- UX: Docs requested thresholds now create case calendar reminders and clear them when the request is removed.

## 2026-01-15
- Assessment: Case workspace "Proposed Interventions" wizard rebuilt to support multi-intervention proposals with action plan selection, costing, and simplified documents.
- Assessment: Decision step now captures approve/request changes/reject outcomes with EI verification upload required for approvals and case-note logging for changes/rejections.
- API: Added endpoint to link EI verification documents to approved interventions.
- UX: Proposed Interventions wizard blocks navigation past the action plan step until a plan exists.
- UX: Proposed Interventions cost item modal now mirrors coordinator assessment behavior, including editable amount inputs and installment controls.
- UX: Proposed Interventions wizard auto-saves draft progress when navigating between steps.
- Fix: Proposed Interventions NOC autosuggest now matches coordinator search behavior and returns suggestions.
- Fix: Proposed Interventions draft data now restores when returning to an incomplete proposal without a saved draft.
- Fix: Proposed Interventions wizard validates framing data before moving forward and only auto-saves when a draft can be created.
- Fix: Proposed Interventions wizard now shows field-level validation errors across framing, type, cost, and decision steps.
- Fix: Proposed Interventions wizard restores submitted proposals on workspace load instead of blocking new navigation.
- Fix: Create payment packet modal now excludes rejected interventions from the eligible list.
- UX: Interventions widget status filter now includes Rejected.
- UX: Interventions widget status filter moved into the header as a select control.
- Fix: Proposed Interventions wizard defers auto-save until required NOC fields are available for NOC-required codes.
- UX: Proposed Interventions wizard now captures delivery details (NOC, partner, ITP, wage subsidy) in the add/edit modal instead of a separate step.
- UX: Proposed Interventions wizard disables Next on the framing step until at least one intervention is added.
- Fix: Proposed Interventions wizard no longer errors on load when checking framing readiness.
- Fix: Decision step navigation no longer blocked by draft auto-save logic on submitted proposals.

## 2026-01-14
- UX: Case portfolio Cases widget no longer shows the "New Case" action button.
- UX: Case portfolio ISET Cases search filter now renders inside the table header.
- UX: Case portfolio headings now use "Client" wording (ISET Clients dashboard, Clients widget, ISET Clients table).
- UX: Case portfolio Open Interventions badge uses the dormant status grey when the client is dormant.
- UX: Case portfolio Next action due now uses the next open case reminder date with overdue severity colors.
- UX: Case workspace Interventions table removed Duration and ESDC Outcome columns; Cost now follows Type.
- UX: Case workspace Interventions table Start - End shows a single date when no end date or same-day range.
- Fix: Auto-created action plans now map application childcare support status into the childcare funding code.
- UX: Intervention edit modal no longer asks for a title and the close hint now reads "Required to close".
- UX: Intervention edit modal now opens in a view state with an Edit toggle; close quick actions keep only closeout fields editable.
- UX: Closure status now shows a required hint in the intervention closeout section.
- UX: Action plan details modal now opens in view mode with Edit and closeout flows matching the intervention modal.
- UX: Payment packet queue now shows packet labels that include the case number.
- Data: Case creation now sets `portfolio_region_id` from the client's province to populate reporting unit data downstream.
- Fix: Band funding decision documents now satisfy band funding evidence and checklist requirements.
- UX: Case header quick actions now use client wording, updated labels, and the new order.
- UX: Payment packet "Submit to finance" now shows a loading spinner and submitting label while the email is generated.
- UX: Payment packet detail alerts are now dismissible.
- UX: Removed Program Payments from the Current ISET Clients navigation group.
- Fix: Action plan result date validation now compares date-only values so same-day closeouts pass.
- UX: Action plan closeout education level options now start at the plan's education level.

## 2026-01-12
- UX: Landing page release notes updated to v0.5.1 with application assessment fixes.
- Feature: Document request tracking is now stored independently of application status with new `docs_requested_*` fields and event emission on set/clear.
- UX: Application Overview and work queues now show a Docs Requested badge alongside the application status, with a manual toggle to start/clear the timer.
- Config: SLA settings include document-request reminder/closure thresholds for future event-triggered automation.
- UX: Role labels now consistently display System Administrators, NWAC Administrators, Regional Managers, and ISET Coordinators across the admin UI.
- Policy: Historical role-based approval thresholds were added here; this was superseded on 2026-06-19 by the single rule that only Shelley Stacey (`sstacey@nwac.ca`) can approve funding of `$20,000` or above.
- UX: Program Admin work queue labels now read "Application Assessments" and "New Interventions" with updated hint text.
- Feature: Added applicant watchlist quick actions in Application Overview and Case Header, backed by a new applicant watchlist table and API endpoint.
- UX: Renamed the homepage My Watchlist widget to My Flagged Applications.
- UX: Homepage work queue now surfaces watchlist hits (applications with watchlisted SINs) in place of ILMP issues.
- UX: Homepage work queue now loads "Marked for Closure" applications in the queue and items table.

## 2026-01-11
- Assessment: Wizard navigation now auto-saves assessment progress on Next/Previous to preserve cost line edits without manual saves.
- Assessment: Empty proposed intervention shells are filtered on load/save to prevent blank rows.
- Fix: Eligibility step no longer warns about concurrent updates when auto-save runs before Next.
- Fix: Do not persist zero-cost legacy totals when no interventions exist, avoiding blank proposed rows on new assessments.
- Fix: Assessment submit validation alerts now flatten nested error objects to avoid React child rendering crashes.
- UX: Decision communication step no longer shows the introductory info alert.
- UX: Denial letter drafting omits Next steps unless a clear remedy exists and avoids carrying generic steps.
- UX: Denial letter prompt now paraphrases assessor input into applicant-facing language instead of repeating labels or form wording.
- UX: Denial letters no longer include the worthiness/judgment reassurance line.
- UX: Denial letter prompt now references applicant-requested program/supports instead of assessor-proposed interventions.
- UX: Denial letters now use narrative paragraphs (no Decision/Reason labels) and focus on requested supports in the opening.
- UX: Communication step now sends letters on completion, hides checklist for denials, and simplifies the letter editor header text.
- UX: Decision letters now start blank unless a draft exists, and denial letters reference requested supports in lower-case phrasing.
- UX: Denial letter drafts now retain assessor-provided suggestions from the denial reason modal.
- UX: Communication step title switches to "Send denial letter" when the decision is not approved.
- UX: Approval letters now use a "Send approval letter" step and the funding checklist moved into a "Complete funding documentation" step that finalizes approved applications.
- UX: Approval letter drafting now lists funded supports with plain-English payment wording and removes label-style formatting.
- UX: Approval letters now aggregate supports across all interventions and paraphrase justification text instead of quoting it.
- UX: Approval letters now always add a second paragraph for authority, payment explanations, and missing-document requirements.
- UX: Approval letter drafts now use a fixed three-paragraph structure with the submission reference/date and per-intervention cost line amounts plus payment methods.
- UX: Decision letter attachments now render funding lists with proper bullet formatting in the portal and PDFs.
- UX: Secure messages now include the full decision letter body in the message text.

## 2026-01-10
- Fix: Assessment intervention total now parses currency values correctly to avoid inflating approval thresholds or dashboard totals.

## 2026-01-09
- UX: Denial letter drafting now collects a single program-level denial reason with a short explanation before generating the AI draft.
- UX: Denial letter AI prompt now enforces authority, non-judgment language, and options-forward requirements without introducing new reasons.

## 2026-01-08
- Ops: Intake uploads now ensure a client record exists pre-upload and pin `client_id` for the session.
- Data: Added `client.applicant_cognito_sub` and `iset_document.client_id` to anchor intake documents to clients.
- Ops: Intake-generated PDFs now attach to the resolved client record.
- Data: Added `iset_document.action_plan_id` + `iset_document_intervention`, expanded `document_type.scope`, and removed `linked_intervention_id`.
- Fix: Aligned `client.applicant_cognito_sub` collation to match `user.cognito_sub` to avoid ER_CANT_AGGREGATE_2COLLATIONS during document uploads.
- UX: Supporting Documents widget now supports action-plan scoping with optional multi-intervention links and updated scope labels.
- Payments: Evidence links now attach at the packet level and require client ID matches when attaching documents.
- UX: Sending decision letters now generates a PDF supporting document tied to the client/application and refreshes the decision checklist.
- Fix: Communication step now loads Gate 6 checklist items and blocks completion until required agreements are present.
- Fix: Checklist progression now skips Gate 1 in admin, enforces Gate 2 on the eligibility step, and enforces Gate 3 for assessment submission before switching to Gate 6.
- Authoring: File-upload components now expose the validation panel and persist rules into published workflows.

## 2026-01-06
- UX: Regional Manager work queue now includes a My Applications bucket for assigned files.
- Fix: Conflict of interest signing now routes no-conflict submissions to step 1 and blocks progress with a modal when a conflict is declared.
- Fix: Work queue escalation actions now open a modal and submit to the escalation API.
- UX: Messaging recipient list now shows region-coded role labels for ISET Coordinators and Regional Managers.
- Fix: Escalation action notes now create case notes automatically.

## 2026-01-03
- Docs: Added Payments module user manual (`docs/guides/payments-module-user-manual.md`).

## 2026-01-04
- UX: Added Case Workspace quick action for managing payments (payments queue + detail above full-width interventions/action plans).
- UX: Program Payments widgets now live in the Case Workspace (case-scoped queue + packet detail).
- UX: Manage Payments quick action now focuses the first intervention with a draft/returned payment packet.
- UX: Payment packet creation in the Case Workspace now derives reporting unit, pot, and amount from the intervention and supports partial payments.
- UX: Service period fields now show only for living allowance and wage subsidy payment types in payment packet/line modals.
- API: Blocked payment initiation for draft/planned/submitted/in_review/changes_requested/cancelled interventions.
- Payments: Payment type options now filter by intervention code via runtime config and the API blocks mismatched types.
- UX: Refreshed payment packet detail summary layout for clearer grouping and readability.
- UX: Payment packet detail now starts with payment lines; summary cards removed.
- UX: Payment packet queue amount column now shows stream total badges; removed payment type column.
- UX: Add payment line modal now filters budget pots to the packet reporting unit region (retains existing pot on edit).
- UX: Add payment line modal now surfaces detailed validation errors from the server.
- UX: Payment line evidence column now distinguishes between no evidence required and missing baseline evidence.
- Fix: Supporting documents now auto-move from application to the auto-created intervention on approval.
- Fix: Evidence checklist items now keep their payment-document IDs so verification works in Finance view.
- UX: Draft payment packets can be deleted from the payment packet queue.
- Payments: Supporting documents now auto-attach to new payment packets/lines based on evidence rules.
- Payments: Initial interventions created on application approval now auto-generate draft payment packets.
- Feature: Assessment submissions now generate an application-form PDF alongside the assessment PDF (stored as `application_form` documents).
- Feature: Assessment submissions now generate a financial overview PDF alongside the assessment PDF (stored as `financial_overview` documents).
- UX: Case manager assessment PDF layout now matches the application and financial overview PDF styling.
- Fix: Case manager assessment PDF now includes intervention framing, childcare, and cost schedule fields captured in the assessment wizard.
- Payments: Simplified the workflow to Draft -> Submitted only; removed verification/approval/batching/mark-paid steps and locked packets after submission.
- Payments: Submission now emails finance from the status update and evidence gates use received evidence instead of verification.
- Docs: Updated payments requirements, user manual, and help copy to reflect the simplified workflow.

## 2026-01-05
- UX: Combined the payment packet evidence checklist and documents list into a single table in the detail widget.
- UX: Evidence table now lists all packet evidence requirements across lines and shows all attached documents per requirement (no separate document rows).
- UX: Removed the notes section from the payment packet detail widget.
- UX: Evidence table now uses row-level actions to view, link, upload, replace, or unlink supporting documents.
- Payments: Finance submission email now lists document names and includes a 7-day packet bundle download link.
- Payments: Auto-generated draft packets now prefill line items from assessment cost breakdowns, including recurrence and payee inference.
- Fix: Auto-generated payment packets now resolve requester user IDs to avoid FK insert failures.
- UX: Submit-to-finance alerts now summarize policy blockers with line ranges.
- Fix: Payment packet bundle generation now handles typed-array buffers to avoid archiver crashes.
- Access: Archived applications now only appear to System Administrators in application lists and counts.

## 2026-01-24
- Payments: Auto-generate draft payment packets from approved interventions.
- Payments: Evidence verification required before approvals; verify/unverify controls added.
- Payments: Mark Paid now uploads proof-of-payment and enforces proof requirement.
- Payments: Added Annual Report ledger extract export from Payments queue.
- Payments: Override modal captures reason for evidence/duplicate/threshold gates.
- Payments: Added internal notes thread for program ↔ finance collaboration.

## 2025-12-31
- UX: Homepage work queue items table now supports flagging/unflagging and no longer shows the row-selection radio.
- UX: Homepage work queue items table now shows province codes instead of full names.
- UX: Homepage removed the legacy Application Work Queue, Case Work Queue, Conflict Declarations, and Program Admin Work Queue Items widgets.
- Fix: Watchlist applicant names now pull from intake payload fields so names render consistently.
- UX: Homepage now includes a Metrics widget with period-based totals for applications, decisions, active cases, and funding.
- API: Added `/api/dashboard/metrics` to serve periodized homepage metrics.
- UX: Updated Program Admin work queue bucket descriptions for conflicts, eligibility, escalations, and approvals.
- UX: Homepage watchlist now refreshes automatically when queue items are flagged or unflagged.
- Docs: Updated NWAC ISET homepage help panel copy to reflect the current widget set.
- UX: Added info links and placeholder help panels for NWAC ISET homepage widgets.

## 2025-12-26
- UX: Application assessment cost step now supports recurring cost scheduling (period, amount, occurrences) tied to the total cost input.
- Fix: Case detail payload now includes case context so assessment delivery mode persists after save/refresh.
- UX: Removed the intervention duration input from the application assessment cost step.
- UX: Reduced redundant section headings inside the application assessment wizard steps.
- UX: Moved budget pot selection into the approval/decision step for application assessments.
- UX: Application assessment checklist step now supports checklist-driven uploads, matching the proposed intervention workflow.
- UX: Application assessment wizard now blocks advancement past the checklist step until required items are complete for draft assessments.
- UX: Approval/decision step now reveals budget pot fields only when approved with a non-zero cost and clears them otherwise.
- UX: Application assessment quick actions now include layout presets for review, documents/messages, and notes/calendar views.
- UX: Application assessment quick actions now include a View audit trail layout preset.
- UX: Application overview key/value layout now supports up to six columns.
- UX: Application overview now shows province/territory, document checklist completeness, and lock owner/expiry.

## 2026-01-20
- Feature: Configuration Settings now includes a Document Checklists widget to edit required documents per status gate for applications and interventions.
- API: Checklist configuration can be persisted to runtime config for both application and intervention scopes.

## 2025-01-05
- Authoring: Default Value fields now accept `{data_key}` placeholders to prefill from another field in the same workflow.

## 2025-11-24
- Feature: Manage ISET Applications dashboard now includes the Application Work Queue summary widget alongside the ISET Applications table; help content updated.

## 2025-10-22
- Docs: Normalized the admin library layout (meta/, components/, features/, ops/), renamed file-upload conditional notes, and introduced a docs README for quick orientation.

## 2025-09-25
- Fix: Admin secure messages now persist case/application IDs so applicant booking references render consistently.
- Feature: Portal message view surfaces the booking reference for case-linked threads.
- Docs: Added secure messaging notes and refreshed widget catalog to reflect case-scoped behaviour.
## 2025-09-22
- Feature: Access Control matrix widget now supports in-place role toggles with instant persistence.
- UX: Navigation and route guards consume the shared RBAC matrix and hide empty sections per role.
- Docs: Refreshed RBAC notes to reflect self-service configuration flow.

## 2025-09-21
- Feature: Restored Secure Messaging widget with inbox/sent/deleted tabs, Cloudscape tables, modal compose, and attachment adoption triggers.
- Feature: Supporting Documents widget gains refresh button, auto-refresh event listener, simplified columns.
- Fix: Attachment adoption back-fills applicant/application/user metadata when re-opened.
- Docs: Updated widget catalog and documents model notes.

## 2025-09-18
- Feature: AI settings widget now persists to shared DB (`iset_runtime_config`) so the public portal respects admin-chosen model/params/fallbacks.
- Fix: Corrected SQL for fallbacks upsert (JSON array via CAST) and idempotent table creation.
- Docs: Added `docs/data/runtime-config/ai-runtime-config.md` and updated project map notes (cross-app config flow).

## 2025-12-03
- Admin application form: collapsed intake registration number variants (sfn/nsfn/metis/inuit) into a single Registration number field and ignore the UI-only key in diffs so saving updates the correct stored key.
- Case workspace: Participant Details now reads/writes registration number across all variant keys, collapsing to a single value post-intake.

## 2025-12-04
- Docs: Added auto-assignment notes (config in admin, execution in portal ingest) and clarified province sourcing from submission payload.

## 2025-12-31
- UX: User Management dashboard now shows region codes (not numeric IDs) and uses a region code selector when inviting admin users.
- UX: Administrative Users table renders as embedded content within its tab panel.
- API: `/api/regions/canada` now includes `regionId` alongside code/name for region lookups.
- Auth: Mapped new Cognito group names (System_Administrator, NWAC_Administrator, Regional_Manager, ISET_Coordinator) to canonical admin roles.
- Ops: Updated local admin `.env` to the new Cognito user pool, client, and Hosted UI domain.

## 2025-12-23
- Feature: Case workspace intervention assessment now supports submit-for-approval from the proposal wizard, and the interventions table surfaces submitted status with a status filter.
- Fix: Intervention proposals block new wizard creation when a draft/submitted proposal exists and allow read-only viewing for non-draft statuses.
- Feature: Regional Manager work queue now pulls intervention approval items/counts from the dashboard endpoint.
- Fix: Intervention approval queue now reads from the correct intervention columns in `iset_case_intervention`.
- Fix: Intervention approval queue items now open the case workspace instead of the application assessment view.
- UX: Proposed Intervention widget now uses the wizard for draft/submitted interventions and a read-only form for other statuses.
- Feature: Submitted intervention proposals remain in the wizard for RM/PA/SA review, with EI verification and decision steps captured in review metadata.
## 2026-01-19
- Fix: Align MySQL connection collation with event tables to prevent doc-request threshold poll collation errors.

## 2026-01-27
- Cleanup: Removed legacy evaluator/PTMA assignment APIs and UI (intake-officers, PTMA evaluators, assigned evaluator display).
- UX: Secure messaging now relies on sender/recipient names without evaluator lookups.
- DB: Added migration to drop legacy `iset_evaluators` and `iset_evaluator_ptma` tables if present.

## 2026-02-04
- UX: Intacct XML draft preview no longer flags Bill date/Due date as missing while a packet remains in draft status.
- API: Payment packet validation now enforces Intacct REST submission requirements (vendor, GL account, required dimensions) and REST payload includes bill/due dates plus Intacct line fields.
- API: Payment packet validation now syncs packet evidence document IDs into finance transactions for reconciliation.

## 2026-02-10
- Feature: Implemented Tutorials dashboard (`/tutorials-dashboard`) under Support to run hands-on tutorials and view per-staff completion state.
- Feature: Added self-service reset endpoint to clear tutorial completion/dismissal state (`POST /api/me/tutorial-progress/reset`), used by the Tutorials dashboard Actions widget.

## 2026-03-27
- Fix: Case Calendar weekday headers now align with the actual calendar grid in Canadian time zones.
- Fix: Case and application workspace calendar events now keep `YYYY-MM-DD` reminder/action-plan/intervention dates on the intended local calendar day instead of drifting through UTC parsing.
- Fix: Template editor now offers a dedicated environment-aware portal sign-in link insert option instead of only the raw `{portal_dashboard_url}` token, reducing hardcoded portal URL mistakes in applicant notification templates.
- Ops: Reduced the default prod ASG instance-refresh warmup from 900 seconds to 180 seconds in the refresh script and deployment docs so normal rollouts do not pause for an unnecessary 15-minute warmup.

## 2026-02-11
- Fix: Versioned case workspace tutorial to `case-workspace-overview-v2` so updated hotspot mappings (including final step return-to-header) are applied cleanly after prior persisted state.
- Fix: Home intro tour restart now always re-enters through the canonical `tutorials:start` event path, avoiding stale in-memory tutorial state.
- Fix: Home intro role mapping now tolerates underscore/hyphen role keys (for example `ISET_Coordinator`, `Program_Administrator`, `Regional_Manager`).
- Fix: Tutorials now start from a fresh runtime clone (`completed: false`, cloned tasks/steps) so Restart works reliably even after completion state is saved.
- UX copy: Replaced user-facing “bucket(s)” language with “queue(s)” across home intro tutorials, home/work-queue help content, and work-queue widget preferences/empty states.
- Feature: Tutorials dashboard now shows role-relevant tutorials in a table (one row per tutorial) with per-row completion toggles.
- Feature: Tutorial toggle OFF now resets that tutorial progress via single-tutorial reset, while toggle ON marks it completed.
- UX: Refactored tutorials reset action to a dedicated `Reset all tutorial progress` control.
- Docs: Added initial non-System-Administrator workflow inventory tranche (Application Assessment, Case Management, ILMP Reporting, Payments AP Integrations) with workflow docs and widget-level documentation index/files.
## 2026-04-14
- Finance semantics: Re-aligned PATH summaries to treat `Approved` as intervention approval in PATH, `Committed` as payment packets submitted to finance, and recorded actuals as PATH-recorded paid spend.
- Home: Metrics widget now reports `Funds approved`, `Funds committed`, and `Funds recorded actual` with those meanings.
- Case workspace: Funding header and Finance panel now separate approved, committed, and recorded-actual amounts, and remaining balances subtract both committed and recorded actuals.
- Finance budgets: Pot rollups and remaining-balance calculations now derive committed totals from submitted finance transactions rather than approved intervention amounts.

## 2026-04-21
- UX: Batch Payments now behaves as an oversight dashboard, not a second send/edit workbench.
- UX: Finance queue selection is now single-select and no longer auto-loads the first packet when nothing is selected.
- UX: Batch Payments detail is now inspection-only; create/edit/validate/send flows stay in the program workspace.
- UX: Batch Payments communications now clearly switch between active-packet and all-packets views, and the finance page hides manual-log actions.
- UX: SLA snapshot now starts in the widget palette instead of the default Batch Payments layout.

## 2026-04-23
- Security: Fixed public-portal applicant secure-message routing so assigned case managers resolve through staff user accounts instead of overlapping `staff_profiles.id` values, preventing applicant-origin messages from landing in unrelated applicant inboxes.
- Ops: Hardened the in-place TEST admin/portal deploy scripts to remove deployed `node_modules` before remote `npm ci/install`, and updated the deploy docs to treat that as the standard dependency-reinstall rule alongside the already-safe PROD bootstrap path.
- Ops: Added guarded PROD repair SQL to reassign existing misrouted applicant secure messages to the correct staff recipients and remove the wrong mailbox rows before reopening the portal.
- Ops: Restored the PROD public portal after final live verification that the five repaired messages had no remaining unauthorized applicant mailbox rows in any folder state and that both public portal hosts were returning healthy `200 {"status":"ok"}` responses.
- Access control: Regional Managers can no longer record application decisions or intervention proposal/revision decisions; those approval/denial paths are now restricted to NWAC Administrators and System Administrators in both frontend gating and backend enforcement.
- Security: Removed the unused legacy portal `POST /api/case-events` endpoint after confirming the current portal source/build no longer calls it; current intake event capture remains on `/api/draft`, `/api/intake/complete`, `/api/applications`, and `/api/upload-application-file`.
- Security: Removed the unused legacy portal upload-delete alias and the Jordan experiment draft/application lookup endpoints after confirming the current portal source/build no longer calls them; current upload deletion is `DELETE /api/uploads/remove`.
- Maintenance: Removed stale current-portal references to old slot-search/save-draft endpoint names; the dashboard start flow and `DynamicTest` fallback now use only the current draft/intake endpoints.
- Maintenance: Removed orphan current-source leftovers that were not mounted in the live portal server, including the unused test-notification helper, the dead admin-auth-metrics router/snippet pair, and the stale `/api/get-profile` test mock.
- Security: Hardened finance payment routes so audit-owner user IDs now resolve from authenticated staff context on the server and no longer trust caller-supplied actor/requester/verifier ID fields.
- Security: Hardened admin note/reminder/escalation/document audit writes so staff requests now resolve to the real local `user.id` by Cognito subject instead of falling back to `staff_profiles.id` or other raw auth numeric IDs.
- Maintenance: Clarified admin identity semantics by exposing explicit `staffProfileId` values for staff-profile comparisons in backend requester identity and frontend current-user state, while leaving application-lock ownership on its existing opaque actor/subject identity.
- Maintenance: Staff auth hydration now writes an explicit `staffProfileId` field on admin auth state, and shared RBAC/authz helpers now prefer that field for staff assignment scope checks.

## 2026-04-24
- Ops: PROD release `20260424-094930` completed successfully after a failed first attempt exposed an IAM gap in the reduced operator role: `path:deploy` could not finish automatic restore-point capture because `CreateDBClusterSnapshot` also required `rds:AddTagsToResource` on the snapshot resource.
- Ops: Verified before the rerun that DEV and PROD already had identical `intake-release` payloads for workflow `21` and `publish/workflow.schema.intake`, so the successful PROD rerun safely used `--skip-schema --skip-data --skip-build` and rolled only the shared/admin/portal app artifacts.
- Docs: Updated the deploy runbooks to stop claiming restore-point snapshots are currently fully covered by `nwac-prod-codex-operator`, and to require explicit checksum proof before using an app-only rerun to bypass a blocked prod schema/data path.
- Ops/Feedback: Reviewed TEST and PROD admin feedback queues again. TEST still contained only the older 27-report set already represented in PROD, so no TEST-only bug/CR rows were merged. The TEST feedback tables were then cleared with guarded script `sql/ops/test-clear-admin-feedback-log-20260424.sql`, leaving `admin_feedback_report`, `admin_feedback_note`, `admin_feedback_status_history`, and `admin_feedback_attachment` empty in TEST.
- Ops/Data: After Amanda Curtis confirmed `kiyaostisondenisehelen@gmail.com` is Denise Chalifoux's real PATH login, the guarded PROD duplicate-client merge in `sql/ops/prod-merge-denise-chalifoux-client-126-into-108-20260424.sql` was executed successfully. Temporary precautions during the live merge were a manual `application_lock` on application `31` plus a short admin service-announcement banner telling staff to avoid Denise's record until the notice cleared. The merge kept activated client `108` / user `115` as the survivor, repointed case `113`, application `31`, submission `31`, and all linked documents away from duplicate client `126` / user `159`, corrected the remaining `submission_snapshot.user_id` in `iset_application.payload_json` to `115`, then cleared the temporary lock/banner. The merged-away client row was intentionally retained without live references for audit/recovery, and PROD feedback report `#51` was resolved as a data fix rather than a code defect.
- Fix: DEV application-document loading now includes only those historical intake uploads that the current application submission payload proves belong to that application, even when older `application_submission` documents were stored with `application_id = NULL`. The same narrow proof rule now feeds both `/api/applicants/:id/documents` and `/api/applicants/:id/document-checklist`, so application workspaces and compliance checklists recover missing legacy intake files without widening document visibility across multiple applications.

## 2026-04-25
- Ops: PROD release `20260425-100201` completed successfully as a normal full deploy with shared/admin/portal app rollout plus `intake-release` data sync for workflow `21`.
- Ops: The reduced prod operator role's restore-point path is confirmed working again. Release `20260425-100201` automatically captured Aurora cluster snapshot `path-prod-20260425-100201-20260425100220` for `nwac-prod-db`, then finished with all three public prod health checks returning `200 {"status":"ok"}`.
- Docs: Updated the deploy runbooks and standing guidance to remove the stale `rds:AddTagsToResource` caveat now that the repaired IAM policy has been validated by a real full prod deploy.
- Security: Added the post-incident privacy/security review note at `docs/planning/privacy-security-systematic-review-2026-04-25.md`, with the current audit method, fixed exposure classes, and remaining review lanes.
- Security: Hardened admin supporting-document routes so applicant/case document listing, checklist computation, presigned file access, uploads, edits, intervention links, duplication, and deletes now validate case/application/action-plan/intervention/client/payment scope instead of trusting staff auth plus document IDs.
- Security: Hardened finance payment-packet routes so casework payment roles are scoped to their authorized cases, while payment batches and full ledger exports require finance/admin payment access.
- Security: Hardened admin secure-message routes by retiring broad legacy list/create endpoints, requiring case access before case-thread reads or sends, and validating message case context before attachment presign/adoption or mailbox mutation.
- Security: Hardened case notes, reminders, and timeline events so narrative client data is returned or mutated only after case/reminder target scope validation.
- Ops/Security: Fixed the public portal TEST deploy installer so the runtime `auth/` helper is copied to `/opt/nwac/portal/auth` alongside `server.js`; the stale helper had caused real TEST applicant-pool users to fail the new applicant-account gate with `applicant_account_required`.
- Ops/Security: Promoted the public-portal auth helper and legacy-fallback hardening to PROD as portal-only release `portal-auth-helper-copy-20260425-prod`, with no schema/data/admin/shared changes and post-deploy health/runtime-file verification passing.

## 2026-04-26
- Security/DB: Started the DEV privacy ERM cleanup with additive migrations for `iset_case.assigned_staff_profile_id`, typed secure-message actors, message attachment scope, secure-message FKs, and document relationship FKs.
- Security: Hardened secure-message applicant/admin access to typed actor plus case/application scope and moved the main admin/portal secure-message UI response contract away from legacy `sender_id` / `recipient_id` authority.
- Security: Retired the unscoped admin `POST /api/applications/ingest-from-submission` endpoint to prevent new applications without client/case ownership.
- Maintenance/Security: Added explicit staff-profile assignment aliases to high-risk admin API responses and updated the application list, home queues, work-queue table, and application overview to compare assignment through staff-profile IDs first.
- Security/DB: Hardened remaining DEV staff-profile actor references with FKs for admin feedback, CFA, applicant-account invite/event, and tutorial-progress tables.
- Security/DB: Added DEV secure-message/document privacy constraints requiring case-scoped typed message actors, exactly one applicant actor, scoped message attachments, source-specific document lineage, and `RESTRICT` delete rules for privacy-sensitive message/document relationships.
- Security/DB: Hardened DEV signing requests with FKs to workflow, case, participant user, and creator user, plus audit coverage for wrong-applicant and message/case mismatch anomalies.
- Security/DB: Hardened DEV escalation routing and case-task audit users with explicit application/case/user FKs, and made escalation creation fail closed when an application has no case scope.
- Docs: Updated older case/work-queue/data notes to describe case ownership as `assigned_staff_profile_id`, with `assigned_to_user_id` documented only as a transitional legacy fallback.

- Maintenance/Security: Completed the DEV assignment response/event naming cleanup for known lower-risk surfaces. Case creation, assignment/reassignment, applicant application-list responses, frontend assignment aliases, portal auto-assignment events, and shared notification routing now prefer explicit staff-profile IDs while retaining legacy `assigned_to_user_id` / `assigned_user_id` aliases for compatibility.
- Security/DB: Completed the DEV case/application pointer cutover. Runtime code no longer reads or writes `iset_case.application_id`, application ownership is derived from `iset_application.case_id`, and migration `20260427_0013` records row-level legacy pointer audit data before dropping the case-side pointer.
- Security/DB: Hardened DEV application ownership. Public portal, admin/manual intake, demo, and seed paths now insert applications only after resolving the owning case, and migration `20260427_0014` makes `iset_application.client_id` and `iset_application.case_id` required after recording row-level scope audit data.
- Security/DB: Retired DEV application-version author shadow `created_by_id` through migration `20260427_0015` after moving version write/read paths to typed staff-profile/local-user author refs.
- Security/DB: Hardened DEV event-entry actors through migration `20260427_0016`; staff/applicant event rows now require typed actor refs, while raw `actor_id` is retained as audit text only.
- Security/DB: Hardened DEV application/CFA relationship FKs through migration `20260427_0017`, including application submission/version lineage and CFA case/version/document/participant links after recording 0 relationship blockers.
- Security/DB: Hardened remaining DEV relationship FKs through migration `20260427_0018` and retired the empty `zzz_legacy_documents` experiment table through migration `20260427_0019`.
- Security: Added DEV privacy smoke checks for the cleaned ERM and route-scope guard markers.
- Security: Hardened admin allocation evidence uploads/downloads/deletes so object keys require finance role access plus allocation/pot metadata provenance or actor-owned pending-upload scope.
- Security: Hardened workflow/component authoring and legacy raw Nunjucks/blockstep debug routes so authoring is step-editor-only and legacy debug routes require explicit unsafe debug enablement plus System Administrator access.
- Security: Hardened application/case raw-ID surfaces in DEV: case watches, application detail/version/lock routes, escalation create/respond/list, case detail/save, case assignment, conflict actions, and ILMP/ready-to-close actions now validate case/application visibility before returning or mutating data.
- Security: Removed remaining checked shared-user-to-staff-profile email fallback paths in DEV. Portal/admin message, signing, and funding-agreement helper paths now resolve staff profiles from local users by Cognito subject only, and the privacy route smoke prevents those email fallbacks from returning.
- Security: Tightened public portal AI support filtering so both the current prompt and recent chat history are scanned for obvious sensitive content before any OpenRouter request.
- Security: Added route-scope smoke coverage for admin feedback attachments, preserving the rule that attachment presign URLs are only returned from System Administrator report-detail reads.
- Security: Hardened admin OpenRouter surfaces in DEV. Admin AI chat now blocks obvious raw applicant/client identifiers before proxying, denial-letter drafts use local templates instead of sending applicant context externally, and AI dummy-data generators require the unsafe debug gate plus System Administrator access.
- Security: Hardened notification configuration APIs in DEV so notification templates, notification routing rows, and shared sender/reply-to settings require System Administrator or NWAC Administrator access server-side.
- Security: Retired legacy generic `/api/users` shared-table endpoints in DEV so the mixed staff/applicant local `user` table is no longer exposed as a broad directory API.
- Security/Ops: Added the live DEV privacy route-denial smoke harness for real-token checks of wrong-role, wrong-surface, wrong-applicant, generated PDF, finance evidence, payment-packet, and explicit out-of-scope case/application/document access before TEST rehearsal.
- Ops: Tightened the privacy ERM grand cleanup TEST rehearsal runbook with exact preflight, cleanup, migration, smoke, maintenance-window, and blocker-decision steps.

## 2026-04-28
- Fix/Homepage/DEV: Denied applications now leave `Pending Completion` once the denial letter is recorded as sent, so sending the denial letter is treated as the terminal follow-up action without changing the denied outcome status.
- Fix/Approvals/DEV: Intervention approvals opened from the Pending Decision queue still land on `Record of decision`, but the queue-provided step intent is now consumed once so approvers can click earlier wizard steps without being snapped back to the decision step.
- Fix: Case manager assessment PDFs generated from new/revised intervention proposals now populate “Previously funded under the ISET Program” and infer the approved-assessment coordinator agreement checkbox from the proposal decision.
- UX: NWAC Administrators are now exempt from the per-case conflict-of-interest declaration gate in the application assessment/approval widget; the gate still applies to case managers.
- UX: Case Workspace quick actions now relabel the intervention action when a draft, changes-requested, or pending intervention proposal/change already exists, and the action opens that current proposal instead of trying to start a new one.
- Fix: Public portal signing-request completion now materializes embedded applicant upload fields, such as the EFT voided-cheque upload, into scoped `iset_document` rows. The generated signed form PDF and the applicant-provided attachment now both appear in Supporting Documents/checklist scope when the signing request is linked to a case/application.
- Ops/Security: Completed the PROD-like TEST rehearsal for the privacy ERM grand-cleanup release. TEST was restored from sanitized PROD data, side-effect guarded, migrated through `20260427_0020_allow_casefile_secure_message_document_scope.sql`, identity-overlaid for TEST Cognito, and deployed as release `prod-like-privacy-erm-test`; both TEST target groups are healthy and SSM DB smoke checks are clean.
- Security/DB: Added rehearsal-discovered migrations for historical pre-materialisation portal uploads (`legacy_intake_upload` quarantine), application-linked manual/system document applicant-scope backfill, unresolved event actor system reclassification, event actor audit reconciliation, and application-less case-file secure-message document scope.
- Fix/Assessment/DEV: Application assessment submission now fails closed if the required generated PDFs cannot be created and recorded before the Pending Decision status transition commits.
- Ops/Data: Repaired Wabanang Polson's PROD pending-decision application by generating the missing system PDFs for case manager assessment, application form, and financial overview, then verified both DB rows and S3 objects.
- Fix/Migrations/DEV: Made the pending document-scope and event-actor audit reconciliation migrations tolerant of the current one-client/one-case schema by removing the retired `iset_case.application_id` dependency and normalizing the audit/event ID collation comparison; local DEV migrations now plan clean with 0 pending.
- UX/Release Notes/DEV: Regenerated the v0.6.0 landing-page release notes from the PROD feedback queue, replaced inferred Known Issues/Coming Next copy with feedback-derived bullets, removed the pre-sign-in summary sentence, and made optional release-note sections hide when empty.

## 2026-04-30
- Fix/TEST: Deployed `regional-manager-doc-scope-test-20260430` to the admin console so Application Workspace `All documents` sends case scope for Regional Managers and the backend includes the case's primary application documents in case-scoped applicant document reads.
- Ops/Data: Backfilled Hailey Lafrance-Chaput's PROD pending-decision assessment PDFs for application `16` / case `98` (`ISET-20260414-53A087`), generating `Case manager assessment v2` and `Case manager assessment redline v2` with Kelly Hyde's original 2026-04-20 submission signature.
- Ops/Data: Submitted Felicia Erickson's PROD amended application assessment for application `10` / case `92` (`MI-MNTBETVR-00DF7C`) as a repair, generating `Case manager assessment v3` and `Case manager assessment redline v3` signed as case manager Amanda Curtis and moving the application back to Pending Decision.
- Ops/Data: Deleted Felicia Erickson's erroneous duplicate PROD case-workspace intervention proposal `13` / intervention `12` with run id `felicia-delete-duplicate-intervention-proposal-prod-20260430` after the earlier withdrawn state still appeared as a resumable draft in Case Workspace; linked document `1384` was archived and the application assessment v3 remains the active Pending Decision item.
