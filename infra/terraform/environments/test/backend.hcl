bucket         = "nwac-test-terraform-state"    # Placeholder – create bucket via bootstrap module
dynamodb_table = "nwac-test-terraform-locks"    # Placeholder – create table via bootstrap module
region         = "ca-central-1"
encrypt        = true
profile        = "default"
